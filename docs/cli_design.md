# CLI Design (legacy)

Este documento descrevia uma CLI anterior (com `queue`, `stats`, `db sync`, etc.) que **não corresponde** à CLI atual do v2.

Referência atual:
- CLI: `docs/cli.md`
- Configuração: `docs/configuration.md`
