---
type: "RunGoal"
id: "run-goals/20260914t132556z-do-the-best-useful-work-availab/goal-audit-cnj-parquets"
run: "runs/20260914T132556Z-do-the-best-useful-work-available-in-this-reposi"
kind: "task-advance"
goal: "Implementar scripts/audit_cnj_parquets.py per issue #1470 (Parquet/CNJ: auditar o catalogo e decidir regeneracao por arquivo), a subissue of the open #1468 epic that PR #1473 (open, unmerged, unifies the Parquet writer per sibling issue #1469) is building toward. Read-only: for each comunicacoes/processos Parquet file in the IA djen-* catalog, read footer/row-group metadata (row group count, per-group numero_processo min/max, KV layout marker) via DuckDB and classify into reorder_candidate / verify_values / conformant / not_applicable / unavailable, matching the issue's explicit acceptance criteria (overlap alone is candidate evidence, non-overlap alone is NOT proof of sortedness, only the certifying KV marker proves conformant)."
rationale: "Issue #1470's acceptance criteria are unimplemented in this repo (confirmed via grep -- no audit_cnj_parquets.py exists on main or any branch) despite the issue body referencing a script 'preparado localmente' that was never published. This is the highest-value unblocked slice of the open #1468 epic: #1471 (pilot validation) and #1472 (rollout) both depend on this audit's classification output, and unlike them it needs no live regeneration/rollback risk -- pure read-only inspection, independently useful and mergeable regardless of PR #1473's fate."
success_signal: "TDD: tests/test_audit_cnj_parquets.py adds unit tests for the classification function (RED against no implementation) covering the issue's exact nuance -- overlapping row-group CNJ ranges -> reorder_candidate, a single row group with no marker -> verify_values (cannot be classified conformant from group count alone), non-overlapping ranges across >1 groups with no marker -> still verify_values (issue explicitly warns absence of overlap does not prove normalization or internal ordering), a file whose KV metadata carries the certifying layout marker -> conformant, a table without a CNJ column (e.g. destinatarios) -> not_applicable, and a read/fetch failure -> unavailable rather than misclassified as absent. GREEN once classify_file() and a DuckDB-backed read_footer_stats() are implemented and a real synthetic Parquet fixture (written via duckdb COPY, mirroring tests/test_exporter.py's pattern) round-trips through read_footer_stats -> classify_file end to end. Full uv run pytest -q and uv run ruff check/format --check stay green; a PR is opened against main."
status: "active"
---

# RunGoal
