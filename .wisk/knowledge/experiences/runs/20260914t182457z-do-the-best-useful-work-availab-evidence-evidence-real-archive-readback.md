---
type: "RunEvidence"
id: "run-evidence/20260914t182457z-do-the-best-useful-work-availab/evidence-real-archive-readback"
run: "runs/20260914T182457Z-do-the-best-useful-work-available-in-this-reposi"
kind: "execution"
reference: "docs/planning/evidence/pilot-tjro-2026-real-archive-readback.json; scripts/benchmarks/pilot_tjro_2026_real_archive_readback.py; scripts/benchmarks/archive_cors_probe.mjs; tests/test_pilot_tjro_2026_real_archive_readback.py; https://github.com/franklinbaldo/causaganha/issues/1482"
summary: "Ran a real HTTPS read-back proof against archive.org for the currently published djen-tjro-2026/comunicacoes.parquet (no simulation, no mock): metadata endpoint 200/62834 bytes/700ms; a 16-byte head Range GET and a 16-byte tail Range GET both returned 206 with correct Content-Range and the PAR1 Parquet magic at both ends (parquet_magic_verified=true); native DuckDB httpfs cold query against the live URL for the busiest day (2026-09-01, 8945 rows) took 2627ms cold vs a median ~1.6ms warm (object-cache hit). New, previously-unverified finding, confirmed with two independent HTTP clients (curl, Python urllib) against three real archive.org endpoints with two as a positive control: archive.org/metadata/... and archive.org/advancedsearch.php both send Access-Control-Allow-Origin, but the file-download endpoint (archive.org/download/{item}/{file}, redirecting to an ia*.us.archive.org datanode) does not -- confirmed again by this script's own live run (download_endpoint_cors_enabled=false). Since DuckDBExplorer.svelte's read_parquet() runs a real cross-origin browser fetch against exactly that download endpoint, this is a potential real production issue independent of the #1471 reorder pilot, filed as issue #1482 rather than folded into #1471/#1472's advance/revise/hold decision. A definitive real-browser confirmation was attempted (scripts/benchmarks/archive_cors_probe.mjs, Playwright/Chromium against the real host) but blocked in this sandboxed session by net::ERR_CERT_AUTHORITY_INVALID -- Playwright's ephemeral browser profile does not trust this session's forced MITM egress proxy's re-terminating CA (confirmed via /root/.ccr/README.md and the proxy's own status endpoint; apt-get install libnss3-tools to fix it via NSS-DB import failed on a stale/404 apt mirror) -- an environment limitation, not evidence about archive.org, and left for a session with normal internet access or a resolved CA trust chain. New tests (20, all green): CORS-header classification, transient-vs-final failure classification (429/500/502/503/504 retried once, a clean first-attempt 404 is not), Parquet magic-byte verification, and the retry-once probe loop against a mocked httpx transport. Publishing the candidate (reordered) file itself remains blocked: no IA_ACCESS_KEY/IA_SECRET_KEY in this environment. Full repo suite (uv run pytest -q, 0 failures) and ruff check/format both clean."
goal: "run-goals/20260914t182457z-do-the-best-useful-work-availab/goal-real-archive-readback"
---

# RunEvidence
