---
type: "RunOutcome"
id: "run-outcomes/20260907t122956z-fa-a-o-melhor-avan-o-poss-vel-n/outcome"
run: "runs/20260907T122956Z-fa-a-o-melhor-avan-o-poss-vel-neste-reposit-rio"
result_state: "partial"
work_status: "complete"
summary: "Repo state scan at round start: 0 open PRs, 17 open issues all independently confirmed still blocked/deprioritized (freshly re-verified same day by run 7gg7l1, unchanged on GitHub). Archived the stale handoff for PR #1272 (already merged before this round started, verified via GitHub and via 33afaf1 in this branch's own git log). Then scanned every Svelte component destructuring $props() for the exact reactivity-bug class the owner's own PR #1275 fixed in AlertBanner (a value derived from props declared 'const' instead of '$derived', so it never updates after a live rerender). Found one real, still-shipped instance: YearSummaryCards.svelte's 'cards' array, rendered by AnnualCoverageMonitor.svelte's persistent (no {#key}) instance across year-button switches and force-refresh refetches — so its stat cards silently froze at first-mount values on every subsequent year change or refresh. Confirmed via TDD: a new rerender-based regression test failed RED against the pre-fix const version, then passed GREEN after wrapping 'cards' in $derived. Three other components the Svelte compiler flagged with the same 'state_referenced_locally' warning (TribunalCoverageExplorer, TribunalDetail, DateDetail) were individually inspected and found to be the correct, intentional pattern (seeding $state once from static Astro-rendered island props at mount, never re-rendered with new props by a parent Svelte component) — left untouched. Full web vitest suite (490/490) and eslint (0 errors) stayed green; python suite/ruff were independently re-verified green before this round's own work (unaffected, no python files touched). Opened PR #1277 and subscribed to its activity; GitHub had not yet reported any CI check runs (state=pending) when this round closed, so the goal is carried_forward via handoff handoffs/handoff-pr-1277-awaiting-ci rather than closed as fully achieved."
next_move: "Re-check PR #1277 (this session is subscribed to its activity): merge once CI is green and mergeable, or diagnose and push a fix if red — see handoffs/handoff-pr-1277-awaiting-ci. After that resolves, re-scan open issues/PRs fresh rather than trusting the backlog cache indefinitely (last independently re-verified by run 7gg7l1 earlier the same day); a human decision (issue #950/#951 hosting choice) or a credentialed session (IAS3 keys for #1011/#1022, TSE egress for #985) could unblock part of it at any time. If the backlog remains fully blocked and no new PR is in flight, the proven fallback pattern from this round (and from #1265/#1267 before it) is to keep scanning shipped-but-verifiable surfaces for real, independently discoverable defects rather than inventing speculative new features."
goals_advanced: ["run-goals/20260907t122956z-fa-a-o-melhor-avan-o-poss-vel-n/goal-year-summary-cards-reactivity"]
evidence: ["run-evidence/20260907t122956z-fa-a-o-melhor-avan-o-poss-vel-n/evidence-pr-1277-opened"]
checks: ["run-checks/20260907t122956z-fa-a-o-melhor-avan-o-poss-vel-n/check-full-suite-and-lint"]
experiences_recorded: ["A Svelte 5 bug just fixed in one component (AlertBanner in #1275: const computed from $props() instead of $derived, frozen after first render) is worth grepping for across the whole component tree — the same anti-pattern recurred once more (YearSummaryCards) and was still shipped. The Svelte compiler's own 'state_referenced_locally' warning at test-run time is a fast, free signal for candidates, but each hit still needs a read of whether the prop is truly reactive over the component's lifetime (a re-rendered live instance) or is legitimately used only to seed $state once (a top-level Astro client:only island mounted with static server-rendered props) — grep the warning list, then judge each site individually rather than blanket-fixing every hit."]
---

# RunOutcome
