---
type: "RunOutcome"
id: "run-outcomes/20260910t112808z-do-the-best-useful-work-availab/outcome-final"
run: "runs/20260910T112808Z-do-the-best-useful-work-available-in-this-reposi"
result_state: "success"
work_status: "complete"
summary: "Audited the scripts/*.py long tail named as next_move by run-outcomes/20260910t102506z-do-the-best-useful-work-availab/outcome-final (the untested production scripts). Found and fixed a real silent-data-loss defect in scripts/generate_homepage_widgets.py: _discover_parquet_urls() filtered the manifest with \"AND date LIKE '{year}-%'\", but generate_catalog.py's parse_filename() sets date=NULL for the current production djen-{tribunal}-{year} consolidated-parquet layout (year lives in ia_item, not per file) -- NULL LIKE '...' is NULL/false in DuckDB, so every current-schema manifest row was silently excluded from comunicacoes/advogados discovery, leaving all three public homepage widgets (activity_summary, top_tribunais_30d, top_advogados_atividade) permanently empty with only a WARNING logged, no crash. Fixed by widening the filter to also match ia_item's year suffix (ia_item LIKE '%-{year}'), safe because _parse_tribunal_year_item enforces a 4-digit year at the end of every tribunal-year item id and legacy djen-YYYY-MM-DD items never end in a bare '-{year}' suffix. RED (1/3 new tests failed on unmodified code) -> GREEN (3/3 pass). Full pytest suite, ruff check, ruff format --check all clean. Opened PR #1419 (https://github.com/franklinbaldo/causaganha/pull/1419), subscribed this session to its activity, created handoffs/handoff-pr-1419-awaiting-ci for a future round to confirm CI/merge."
next_move: "Immediate: drive PR #1419 to green and merge (this session is subscribed and will react to CI/review events; a handoff is also recorded in case this session ends first). Beyond that: this exact defect class (a filter that requires a column the current data layout leaves NULL) may exist in other manifest-querying scripts/.qmd contracts that predate the djen-{tribunal}-{year} consolidated layout migration -- worth a targeted grep for 'date LIKE' / 'WHERE date' across scripts/*.py and web/src/queries/*.qmd in a future round. Separately, the audit agent flagged a secondary, not-yet-fixed issue in the same function: _activity_summary's 'last closed month' query needs the previous month's data, which in January falls in the previous calendar year, but com_urls/adv_urls are only discovered for the single year CLI arg passed to build_widgets() -- so the January case would break independently of this NULL-date bug once it recurs (not urgent until January, but worth fixing proactively). generate_homepage_widgets.py is otherwise now confirmed read end-to-end; validate_manifest.py, daily_benchmark_update.py, snapshot_parquets_for_rollback.py, roundtrip_check.py, and sample_ia_texts.py were also read end-to-end this round (via a background Explore audit) and found clean -- narrowing the remaining untested scripts/*.py surface to: analyze_with_rag.py, annotate_with_llm.py (two uncommented 'except Exception:' blocks at lines 449/489 flagged for closer review), augment_segmenter_data.py, batch_embed_decisions.py, bootstrap_training_corpus.py, build_gold_benchmark.py, classify_from_batch_embeddings.py, evaluate_regex_segmenter.py, ia_practicality_probe.py, stress_test_djen.py, train_decision_segmenter.py, vendor_pje_swagger.py."
goals_advanced: ["run-goals/20260910t112808z-do-the-best-useful-work-availab/goal-audit-scripts-long-tail"]
evidence: ["run-evidence/20260910t112808z-do-the-best-useful-work-availab/evidence-red-green-homepage-widgets"]
checks: ["run-checks/20260910t112808z-do-the-best-useful-work-availab/check-verification"]
experiences_recorded: []
---

# RunOutcome
