---
type: "RunEvidence"
id: "run-evidence/20260914t172602z-do-the-best-useful-work-availab/evidence-query-cost-benchmark"
run: "runs/20260914T172602Z-do-the-best-useful-work-available-in-this-reposi"
kind: "execution"
reference: "docs/planning/evidence/pilot-tjro-2026-query-cost.json; scripts/benchmarks/pilot_tjro_2026_query_cost.py; scripts/benchmarks/wasm_query_bench.mjs; scripts/benchmarks/wasm_query_bench_page.html; tests/test_pilot_tjro_2026_query_cost.py"
summary: "Ran the real end-to-end benchmark against the actual published djen-tjro-2026/comunicacoes.parquet (sha256 fa4a7203...) and its PR #1478-writer candidate rewrite, both served from a local Range-serving HTTP server (not archive.org directly, to isolate ordering effects from network variance -- explicitly labeled 'local-http-range-server-simulation' in the JSON, distinct from the real-Archive read-back proof still owed). Key finding: a date-only COUNT(*) for the busiest single day (2026-09-01, 8945 rows) touches only 2 of 9 row groups in the currently published (chronologically-ordered) file but all 9 of 9 in the numero_processo-first candidate -- confirmed against real footer stats via count_row_groups_touched, not assumed. That translates into a real, measured cost: the candidate needs 11 HTTP requests vs the old file's 4 for the same cold query (native and WASM engines agree on this exactly, since both query the identical local server), and under repeated identical queries on the same warm connection (DuckDB's enable_object_cache does not fully eliminate re-fetching) the candidate re-requests ~8x more bytes per iteration than the old file (about 1.07MB vs 131KB per repeat). DuckDB-WASM (run in real headless Chromium via Playwright against the actual @duckdb/duckdb-wasm browser bundle -- not the Node 'blocking' build, which has no browser Worker and a different, non-functional-here httpfs path) pays a real one-time ~1.9s/35MB engine-init cost fetching the WASM binary, and its cold-query wall time (~730-750ms) is roughly 75x the native engine's loopback-dominated cold-query time (~10-20ms) for the identical request/byte pattern -- a legitimate WASM-vs-native overhead independent of the reordering question. WASM's warm-cache requests were 0/0 for both files (a different, more aggressive in-process cache than native's httpfs object cache showed). New tests (12, all green): count_row_groups_touched's pruning-proxy logic against synthetic sorted/scattered fixtures, and the local Range-HTTP server's byte-range/query-string/reset/stats-endpoint correctness. Full repo suite (uv run pytest -q) and ruff check/format both clean after these changes."
goal: "run-goals/20260914t172602z-do-the-best-useful-work-availab/goal-measure-query-cost"
---

# RunEvidence
