---
type: "RunOutcome"
id: "run-outcomes/20260908t142456z-do-the-best-useful-work-availab/outcome-final"
run: "runs/20260908T142456Z-do-the-best-useful-work-available-in-this-reposi"
result_state: "success"
work_status: "complete"
summary: "Found and fixed a real, currently-live regression introduced by this session-family's own immediately-preceding merged PR (#1325, 'extract shared absent self-consistency rule into one tested module'): that refactor's SQL downgrade in scripts/render_manifest_parquet.py's _normalize_manifest wrote djen_status = NULL for unverifiable-absent rows, while the shared Python source of truth it was built around (djen_backup.absent_consistency.normalize_absent) writes djen_status = '' for the same case -- and every other consumer in the codebase (manifest.py, all four .qmd query contracts) treats '' as the unknown sentinel, never NULL. Because SQL 'djen_status = ' is NULL (not true) when djen_status IS NULL, these downgraded rows silently vanished from every displayed bucket on the public /stats totals card and tribunal coverage table while still counting toward the total, breaking the uploaded+pending+absent+unknown==total invariant. The parity test written specifically to catch this class of drift (test_sql_normalization_agrees_with_python_rule) missed it because it coerced the SQL NULL to '' with 'sql_status = sql_status or \"\"' before comparing -- removed that coercion so the test compares real stored values. Found via a dispatched Explore-agent codebase survey after the issue/PR backlog was re-confirmed exhausted (17 blocked issues, only sibling-session PR #1322 open). RED: the parity test failed (None, '') != ('', ''); two new tests (test_totals_counts_null_djen_status_row_as_unknown, test_tribunal_coverage_counts_null_djen_status_row_as_unknown) failed with unknown=0 while total=1. GREEN: fixed _normalize_manifest to write '' instead of NULL; also hardened totals.qmd/tribunal_coverage.qmd's unknown-bucket filter to COALESCE(djen_status, '') = '' as defense-in-depth for any already-published parquet rows still carrying the old NULL value; fixed the compaction test's own stale 'is None' assertion, which had been encoding the bug as expected behavior. Full suite (uv run pytest -q): 2600+ passed, 1 unrelated skip. ruff check / ruff format --check: clean. scripts/render_queries.py --check: all 19 .qmd contracts valid."
next_move: "PR not yet opened -- next action is to push this commit and open the PR, then drive it to green/merge. If already merged by a future round: re-verify the issue/PR queue fresh (17-issue backlog, .wisk handoffs), and if still empty, dispatch another Explore-agent sweep per this session-family's established fallback pattern. No new lead was deliberately deferred this round."
goals_advanced: ["run-goals/20260908t142456z-do-the-best-useful-work-availab/goal-fix-normalize-manifest-null-vs-empty"]
evidence: ["run-evidence/20260908t142456z-do-the-best-useful-work-availab/evidence-red-tests", "run-evidence/20260908t142456z-do-the-best-useful-work-availab/evidence-green-tests"]
checks: ["run-checks/20260908t142456z-do-the-best-useful-work-availab/check-full-suite-and-lint-green"]
---

# RunOutcome
