---
goal: "Make the download worker in src/djen_backup/engine.py honor SyncConfig.fail_fast (documented in __main__.py as 'Stop on first error', default True) the same way the sibling upload worker already does -- a download failure must set abort_event when fail_fast is True, not just increment summary.errors and keep running to the deadline."
id: "run-goals/20260910t042601z-do-the-best-useful-work-availab/goal-fix-download-fail-fast"
kind: "task-advance"
rationale: "The prior round in this same lineage (run 20260910T035530Z, PR #1404) closed the SyncConfig dead-boolean audit by deleting skip_if_mostly_complete/publish_live_status and stated in its own outcome that 'dry_run and fail_fast were independently verified genuinely read/honored'. Re-auditing fail_fast's actual call sites (grep -n fail_fast src/djen_backup/engine.py) found it is only read inside _process_upload_item (engine.py:211/216, with its own dedicated tests in test_upload_worker.py: test_failed_upload_with_fail_fast_sets_abort / ...without_fail_fast_keeps_abort_unset). download_worker's own except block (engine.py, was lines 612-625) increments summary.inc_error() on the exact same exception classes DJEN download raises (httpx.HTTPError, OSError, ValueError, DJENNotFoundError, DJENRateLimitedError) but never checked config.fail_fast or set abort_event -- so the prior round's claim was only half-verified: it checked whether the field was read at all, not whether every code path documented as governed by it actually enforced it. This is the same 'sibling code path missing enforcement of a just-verified flag' family already named in the wiki's nineteenth pattern (check_only -> upload_only) and structurally identical to the existing upload-side fix, just on the download side which is the more common failure surface (every DJEN network/HTTP error, vs. only IA upload failures)."
run: "runs/20260910T042601Z-do-the-best-useful-work-available-in-this-reposi"
status: "achieved"
success_signal: "New tests/djen_backup/test_download_worker_fail_fast.py: test_failed_download_with_fail_fast_sets_abort (RED on unmodified engine.py: asserts abort_event.is_set() after a monkeypatched _stage_download raises httpx.ConnectError with fail_fast=True; observed False before the fix, pipeline ran to the full deadline instead of stopping) and test_failed_download_without_fail_fast_keeps_abort_unset (already-passing control). GREEN after adding 'if config.fail_fast: abort_event.set()' to download_worker's except block, mirroring _process_upload_item's existing pattern. Full tests/djen_backup/ suite (125 tests) and the full repo suite stay green; ruff check/format stay clean."
type: "RunGoal"
---

# RunGoal
