---
type: "RunEvidence"
id: "run-evidence/20260910t171322z-do-the-best-useful-work-availab/evidence-red-green-confirm-cite"
run: "runs/20260910T171322Z-do-the-best-useful-work-available-in-this-reposi"
kind: "execution"
reference: "tests/test_except_exception_policy.py"
summary: "RED: added build_gold_benchmark.py/daily_benchmark_update.py to _SCRIPTS_CHECKED and batch_embed_decisions.py to _SCRIPTS_NARROWED (via git stash to test against the unmodified originals) -- both new assertions failed against the 3 unmodified files' existing noqa comments. Investigated each site rather than trusting the wiki's carried-forward 'confirm-and-cite' framing: batch_embed_decisions.py's 3 sites (file upload, batch-job creation, one job's status poll inside a while-True retry loop) are each single-shot or poll-the-same-single-resource operations -- none loop over N independent items -- so the noqa's premise ('worth a confirm-and-cite pass') was itself wrong; installed the optional 'lab' dependency group to inspect google.genai.errors's actual exception hierarchy (APIError base, ClientError/ServerError subclasses) and narrowed all 3 to genai_errors.APIError. build_gold_benchmark.py/daily_benchmark_update.py's 1 site each genuinely are per-batch/per-document bulkheads (for batch in track(batches, ...) / for x in track(selected_for_labeling, ...), continuing to the next item on failure) -- but neither file uses structlog, so ruff's .exception(...) BLE001 exemption doesn't apply (confirmed empirically: a minimal repro with console.print_exception() still triggers BLE001, but '# noqa: BLE001 -- ... see docs/adr/0011' combined on one line satisfies both ruff and the citation-scanning test) -- kept the noqa, added the ADR citation, and added console.print_exception() calls so the full traceback is actually captured for postmortem instead of only the exception's string form, genuinely satisfying the ADR's substantive test rather than citing it nominally. GREEN: both tests pass. ruff check + ruff format --check clean (including verifying console.print_exception() alone, without the combined noqa+citation comment, still requires noqa via an isolated repro). Full uv run pytest -q green; no existing tests cover any of these 3 scripts directly."
goal: "run-goals/20260910t171322z-do-the-best-useful-work-availab/goal-except-audit-confirm-cite"
---

# RunEvidence
