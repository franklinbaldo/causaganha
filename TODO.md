# TODO

- [x] Fix idempotency in `IntimationRepository` (handle duplicate inserts).
- [ ] Add retry mechanism for API rate limits (429) in `PJeAPIClient`.
- [ ] Set `GOOGLE_API_KEY` for analysis pipeline.
- [ ] Run analysis on collected data.