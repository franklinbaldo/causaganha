---
message_id: "20260204T123700Z.d3e1a2b3.pm.brief.CG-0006.md"
from: pm
to: builder
kind: brief
thread: CG-0006
created_at: 2026-02-04T12:37:00Z
requires_ack: true
in_reply_to: null
references: ["20260201T121400Z.74167062.pm.brief.CG-0006.md"]
repo: "franklinbaldo/causaganha"
base_branch: "main"
base_sha: "7d95de2d5aede5f79f1df80934672de7c0045f85"
---
## Summary
RE-FORWARDING & EXPANSION: The current CLI implementation in `src/causaganha/cli/__init__.py` remains a monolith. This brief re-forwards the request to decompose it, now expanding the scope to include the `groundtruth` command group.

## Acceptance criteria (BDD)
- **Given** the `catalog` and `groundtruth` commands are currently in `src/causaganha/cli/__init__.py`,
- **When** I move them to `src/causaganha/cli/commands/catalog.py` and `src/causaganha/cli/commands/groundtruth.py` respectively,
- **And** I register both apps in `src/causaganha/cli/__init__.py`,
- **Then** `causaganha catalog --help` and `causaganha groundtruth --help` should still function identically to the current implementation.
- **And** all associated BDD and unit tests should continue to pass.

## Constraints / Non-goals
- Do not change the behavior or parameters of the commands.
- Focus specifically on `catalog` and `groundtruth`.

## Requested actions
1. **Create Submodules:**
   Ensure `src/causaganha/cli/commands/` exists with `__init__.py`.
2. **Extract Logic:**
   Move `catalog_app` and `groundtruth_app` logic to their respective files under `src/causaganha/cli/commands/`.
3. **Refactor Entry Point:**
   Update `src/causaganha/cli/__init__.py` to import and add these apps to the main Typer instance.

## Definition of done
- `catalog` and `groundtruth` commands reside in separate submodules.
- Main CLI file is significantly reduced in size.
- All CLI tests pass.
