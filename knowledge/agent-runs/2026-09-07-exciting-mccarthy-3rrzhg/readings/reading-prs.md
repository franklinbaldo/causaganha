---
type: AgentReading
id: "2026-09-07-exciting-mccarthy-3rrzhg-reading-prs"
run_id: "2026-09-07-exciting-mccarthy-3rrzhg"
subject: "open_prs"
reference: "mcp__github__list_pull_requests(state=open) on franklinbaldo/causaganha, queried live this round; mcp__github__pull_request_read(get/get_diff/get_files/get_check_runs) on PR #1251"
finding: "One open PR: #1251 'feat(agent): migrate hourly loop to WikiSkill', authored directly by the repo owner (franklinbaldo, not this session), not draft, targeting main, created 2026-09-07T02:44:49Z (during 7gg7l1's round, after its own PR-reading finding of 'no open PR to resume' — this PR did not exist yet when 7gg7l1 checked). All 10 CI check runs are conclusion=success (CodeQL, GitGuardian, lint, web, tests (tjro), validate, 4x CodeQL Analyze matrix legs). mergeable_state='unknown' (GitHub had not finished computing it at read time). The PR replaces .claude/hourly-loop.md's entrypoint (which currently tells a session to copy .claude/agent-run-scaffold.md into knowledge/agent-runs/<run-id>/run.md and run okf-parser check) with two `uvx --from git+https://github.com/franklinbaldo/wikiskill.git@<pinned-sha> wikiskill ...` commands, adds five new `.wikiskill/knowledge/{README,experiences,local,skills,wiki}/README.md` placeholder files, and updates knowledge/agent-runs/index.md to mark the whole tree 'legacy histórico'. It explicitly does NOT delete .claude/agent-run-scaffold.md, does NOT touch knowledge/okf.schema.sql's Agent* types, and does NOT touch CLAUDE.md — the AgentRun mechanism this round is following remains fully live on main pending this PR's merge. get_diff/get_files were read in full (11 files, +31/-42): no .gitignore change is included in the diff, despite .wikiskill/knowledge/local/README.md's own prose claiming wikiskill init .'s 'managed bootstrap surface (manifest.json, normative specs, canonical roles and the standard profile)... is intentionally not versioned' and .claude/hourly-loop.md's own new text claiming 'O estado gerenciado reproduzível fica ignorado pelo Git'. `grep -n wikiskill .gitignore` in this session's checkout (main @ bce266e) returns no match. Whether this is a real gap depends on internals of the external franklinbaldo/wikiskill tool this session's GitHub scope cannot inspect (repository access is restricted to franklinbaldo/causaganha only) — if `wikiskill init .` writes its managed bootstrap files under `.wikiskill/` outside `.wikiskill/knowledge/`, a fresh checkout that runs the new hourly-loop.md commands would show those files as untracked (not gitignored), contradicting the PR's own stated design and risking an accidental `git add -A` commit of reproducible bootstrap state in a future round. Raised as decision-review-pr-1251.md and reported as a PR review comment (see evidence-pr-1251-review-comment.md)."
---

# Reading: pull requests

One open PR, #1251, owned by the repo owner: a full migration of the hourly-loop entrypoint from this session's own AgentRun/OKF mechanism to an external WikiSkill runtime. All CI green. Reviewed this round for a concrete, falsifiable gap (missing `.gitignore` entry for a bootstrap surface the PR's own docs claim is gitignored) rather than merged — see `decision-review-pr-1251.md` for why this round reviews rather than merges the owner's own architectural PR.
