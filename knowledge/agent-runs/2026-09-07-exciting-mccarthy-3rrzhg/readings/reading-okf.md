---
type: AgentReading
id: "2026-09-07-exciting-mccarthy-3rrzhg-reading-okf"
run_id: "2026-09-07-exciting-mccarthy-3rrzhg"
subject: "okf_knowledge"
reference: "knowledge/okf.schema.sql; knowledge/backlog/index.md; the three most recent AgentRun reports (kfv7sx, abz39i, 7gg7l1); `uv run okf-parser check knowledge --relational-schema okf.schema.sql`"
finding: "knowledge/ is conformant per okf-parser (664 concepts, 667 markdown files, 3 reserved, 0 diagnostics — see check-okf-parser-baseline.md). The last three AgentRun reports form a clean, verifiable continuity chain: kfv7sx implemented issue #1244's public/operator MCP catalog split (RED->GREEN, merged as PR #1245); abz39i fixed the one stale test on the repo owner's own follow-on PR #1247 (merged as PR #1248, closing #1244 entirely); 7gg7l1 found no new actionable issue/PR and instead live-reverified all 17 knowledge/backlog/issue-*.md entries against primary sources (env grep, GitHub Actions run history, a live curl, and issue/backlog parity), explicitly declining to add a `tcu` Fonte to the OKF model without first designing a DecisionSource<->Fonte alias mapping. Between 7gg7l1's completion and this round's start, two commits landed on main outside the AgentRun mechanism: 9dc676a (7gg7l1's own PR #1250, merged) and bce266e ('release: publicar causaganha no PyPI via Trusted Publishing (#1252)', a workflow-only change adding .github/workflows/publication.yml, authored and merged directly by the repo owner with no Claude-Session trailer — not an agent round, not represented in knowledge/agent-runs/). The OKF/AgentRun model itself is the subject of open PR #1251 (see reading-prs.md): the repo owner is mid-migration to an external WikiSkill runtime, with knowledge/agent-runs/ and knowledge/okf.schema.sql's Agent* tables to be kept as historical/legacy rather than deleted. Given that pending migration, this round does not invest in evolving the Agent* schema further (e.g. no new AgentRun fields or relationships) — the model is not 'poor' for representing this round's work, and speculative schema investment right before a planned deprecation would be wasted effort, not model improvement."
---

# Reading: OKF knowledge

The `knowledge/` OKF bundle is conformant and its recent AgentRun history is coherent and traceable. The one thing the model doesn't yet represent is itself in flux: PR #1251 proposes retiring the very AgentRun mechanism this round is using, in favor of WikiSkill. This round continues using AgentRun (still the live mechanism on `main`), but deliberately does not expand its schema given the pending migration.
