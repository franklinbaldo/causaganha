---
type: AgentReading
id: "2026-09-09-exciting-mccarthy-ktosqx-reading-okf"
run_id: "2026-09-09-exciting-mccarthy-ktosqx"
subject: "okf_knowledge"
reference: "knowledge/.okf/specs/*.schema.sql, knowledge/agent-runs/index.md, .claude/hourly-loop.md, knowledge/backlog/index.md, knowledge/agent-runs/2026-09-09-exciting-mccarthy-{qvqmci,p7xocl,nnysz7}/run.md"
finding: "Same process tension every round today has already recorded, re-verified here: knowledge/agent-runs/index.md now marks the AgentRun/scaffold mechanism as 'Legacy histórico' -- 'Não crie novos AgentRun ... aqui. Consulte .claude/hourly-loop.md para o entrypoint atual' -- and .claude/hourly-loop.md confirms the hourly loop is operated exclusively by a separate tool, Wisk (a dev dependency of this project, driven by `uv run wisk start`, with its own state under .wisk/knowledge/). Confirmed live: this session's own HEAD commit (ec6ff4b) is itself a Wisk-loop commit closing out PR #1383 via a completely separate mechanism (.wisk/knowledge/experiences/, handoff-pr-*.md files), proving both loops are actively running against this same repository in parallel today. Despite the deprecation notice, this specific scheduled task's stored prompt explicitly and unambiguously instructs creating a new AgentRun from .claude/agent-run-scaffold.md -- and every prior round today that followed that instruction produced real merged work (PRs #1356, #1358, #1360, #1362, #1365, #1377, #1379, and others, all squash-merged to main). Decision: proceed with the legacy AgentRun mechanism for this round, for continuity with this specific active schedule and its own track record today, same call every prior round made (see AgentDecision). Not attempting to migrate this schedule to Wisk myself -- that is a scheduling/ops decision for the repo owner, out of scope for a single round, and .claude/hourly-loop.md already says not to recreate a second orchestrator. okf-parser check knowledge --relational-schema okf.schema.sql is conformant at round start: 1048 concepts, 1051 markdown files, 0 diagnostics, reserved_count 3. No schema change requested by the most recent rounds. Most recent next_move signals (nnysz7): two low-value leads declined 6+ consecutive rounds (dead code in web/src/lib/coverageInsights.ts; download_zip()'s 403-vs-DJENRateLimitedError typing gap); processo_documentos ViewSpec flagged as harmless dead registration, not a correctness bug. This round's own survey (see AgentDecision / goal below) independently re-confirmed the processo_documentos ViewSpec is genuinely harmless (CREATE VIEW is lazy DuckDB DDL, not materialized, so no wasted I/O) before ruling it out as this round's goal."
---

# Leitura de conhecimento OKF

Mesma tensão de processo confirmada por todas as rodadas de hoje (scaffold legado vs. migração para Wisk, ambos rodando em paralelo sobre o mesmo repositório) -- decisão de prosseguir com o scaffold por continuidade com este agendamento específico, que segue produzindo trabalho real mesclado. Estado de schema estável (1048 conceitos, 0 diagnósticos). Nenhuma PR pendente para fechar primeiro; trabalho substantivo desta rodada precisa vir de uma nova investigação.
