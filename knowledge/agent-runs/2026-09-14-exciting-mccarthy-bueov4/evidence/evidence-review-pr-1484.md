---
type: AgentEvidence
id: "2026-09-14-exciting-mccarthy-bueov4-evidence-review-pr-1484"
run_id: "2026-09-14-exciting-mccarthy-bueov4"
kind: "review"
reference: "Subagente general-purpose, revisão independente de PR #1484 em worktree /tmp/pr1484-review (origin/claude/exciting-mccarthy-nt97qp)"
summary: "Revisão independente rodou, em worktree próprio: `npm ci`; `npx vitest run src/components/DuckDBExplorer.cors-block-classification.test.ts` (4/4 verde); `npx vitest run src/components/DuckDBExplorer` (16/16 verde, sem regressão nos testes existentes de missing/unavailable); suíte vitest completa (528/528 verde); `npm run lint` (0 erros, 43 warnings pré-existentes só em arquivos gerados styled-system/*.d.ts, não relacionados). Lógica confirmada por leitura de código: `runQuery()` retorna antes de `conn.query` quando `datasetStatus === 'cors-blocked'`, com um segundo guard independente desabilitando o textarea/botão sempre que `status !== 'ready'` -- dois guards independentes tornam `conn.query` inalcançável para um dataset bloqueado por CORS. Nenhuma custom property CSS nova: reaproveita o seletor `[data-tone='error']` já existente em web/src/index.css, pré-existente ao PR -- conforme a fronteira Panda/CSS de CLAUDE.md, já que DuckDBExplorer.svelte não é uma das três ilhas legadas com exceção. `curl` ao vivo confirmou a alegação central do PR: `/metadata/djen-tjro-2026/files` retorna `access-control-allow-origin: *`; `/download/djen-tjro-2026/comunicacoes.parquet` (302) e o alvo CDN resolvido `ia800705.us.archive.org/.../comunicacoes.parquet` (206) não têm esse header em nenhum dos dois. Único achado não bloqueante: `probeDownloadCorsAccess` trata qualquer rejeição de fetch como bloqueio CORS permanente -- no browser, um bloqueio CORS real e uma falha de rede transiente genuína lançam o mesmo `TypeError: Failed to fetch` indistinguível (limitação inerente da API fetch, não um bug do código), então uma falha de rede transiente real nessa segunda sonda seria rotulada como permanente e o botão de retry ficaria omitido nesse caso raro -- vale um esclarecimento de uma linha na descrição do PR, não bloqueia o merge. Veredito do subagente: SAFE TO MERGE."
---

# Evidência: revisão independente de PR #1484

Subagente rodou de forma independente (worktree próprio, `npm ci`) a suíte de testes relevante e completa do PR (528/528 vitest, 0 erros de lint), confirmou os dois guards independentes que impedem `conn.query` para dataset bloqueado por CORS, confirmou ausência de custom properties CSS novas (reaproveita `[data-tone='error']` pré-existente), e reconfirmou ao vivo via `curl` a ausência do header `access-control-allow-origin` no endpoint de download do archive.org. Veredito: SAFE TO MERGE, com um único achado não bloqueante sobre a limitação inerente da API `fetch` em distinguir bloqueio CORS de falha de rede transiente.
