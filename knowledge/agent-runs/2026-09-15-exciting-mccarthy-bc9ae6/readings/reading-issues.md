---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-bc9ae6-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-bc9ae6"
subject: "open_issues"
reference: "mcp__github__list_issues (state=OPEN, 22 issues) + issue_read em #1469, #1470, #1051 + comentários"
finding: "22 issues abertas. O epic #1468 (Parquet nativo por CNJ) está, pelas próprias palavras das rodadas anteriores de hoje, com todo o trabalho não-gated-por-credencial fechado: #1469 (unificar escrita/leitura) teve seu último critério de código fechado nesta manhã (PR #1495 direct-equality em processoCnj.ts, PR #1497 ROW_GROUP_SIZE explícito em reconcile_processos.py, custo de inspeção do rodapé medido e documentado no comentário de #1469); #1470 (auditoria) teve seu último critério de código fechado por PR #1499 (bloom filter/encoding/plano por arquivo) e a questão de contagem 43→24 resolvida por investigação (comentário 2026-09-14). Os únicos itens restantes de todo o cluster #1468-#1472 são operacionais e dependem de IA_ACCESS_KEY/IA_SECRET_KEY (rollout real, #1472) -- confirmado ausente nesta sessão também (ver decision). issue #1482 (CORS) permanece com a mesma classificação de rodadas anteriores (mixed-content bloqueia a alternativa s3.us.archive.org), sem novidade. O cluster com trabalho real, desbloqueado e ativo HOJE é o segmenter: issue #1051 (validation set independente, RFC 0012) teve dois comentários nesta própria manhã -- o primeiro diagnosticou a causa raiz (0 ReviewRecords na store nova, assign-splits produzindo val=0/test=0) e criou `scripts/segmenter_governance_status.py`; o segundo relatou a primeira prova real de progresso (PR #1505, ferramentas `annotate_second_independent.py` + `adjudicate_segmenter_review.py`, review_count 0→2) e definiu o próximo passo explícito: 'scale from 2 to RFC 0012 §5.4's real targets (≥30 val, ≥30 test) repeating the same mechanism over the ~43 documents with exactly one unseeded annotation'. Desde então, 9 PRs consecutivas já escalaram review_count de 2 até 23 (histórico de commits desta manhã: #1507 a #1525), cada uma repetindo o mesmo mecanismo TDD sobre 2 documentos por vez. Verificado ao vivo (`scripts/segmenter_governance_status.py`): review_count=23, ainda 31 documentos elegíveis (exatamente 1 anotação, sem review) restantes -- a issue está longe de fechada e o próximo incremento é o trabalho natural e contínuo desta rodada."
---

# Leitura de issues abertas

22 issues abertas revisadas via `mcp__github__list_issues`, com leitura detalhada de #1469, #1470 (fechamento de código confirmado nos comentários de hoje, restando só o rollout real gated por credenciais) e #1051 (cluster ativo, com progresso real e mensurável nesta mesma manhã via PRs #1505-#1525, escalando `ReviewRecord`s de 0 para 23). Verifiquei ao vivo com `scripts/segmenter_governance_status.py` que ainda restam 31 documentos elegíveis (uma anotação, sem review) para continuar o mesmo mecanismo -- essa é a issue com trabalho real, desbloqueado (não depende de credenciais IA) e imediatamente continuável nesta rodada.
