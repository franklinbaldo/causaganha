---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-bc9ae6-reading-okf"
run_id: "2026-09-15-exciting-mccarthy-bc9ae6"
subject: "okf_knowledge"
reference: ".claude/hourly-loop.md; knowledge/agent-runs/index.md; knowledge/agent-runs/2026-09-15-exciting-mccarthy-yz281l/run.md; docs/rfcs/0012 (RFC 0012 segmenter dataset governance)"
finding: "`.claude/hourly-loop.md` e `knowledge/agent-runs/index.md` seguem declarando o mecanismo AgentRun como legado histórico, migrado para o runtime Wisk (`.wisk/`) -- mesma tensão já identificada e registrada por múltiplas rodadas anteriores (bueov4/to0ars em 09-14, notificação proativa já enviada; 50ns70/yz281l em 09-15, sem nova notificação por falta de novidade). Nada mudou desde a última leitura (yz281l, mesma manhã): sigo a mesma linha -- obedecer ao prompt agendado, que continua instruindo explicitamente o scaffold `AgentRun` sem ressalva, mas escolher trabalho que não compete com o Wisk (`.wisk/knowledge/` não referencia nada do segmenter). Leitura do run.md mais recente (yz281l) confirma que o item 1 do checklist de #1468 está fechado em código; seu next_move aponta para #1469 (agora também fechado, ver reading-issues) e não menciona o segmenter -- ou seja, o trabalho desta rodada (segmenter #1051) é uma frente nova em relação ao handoff mais recente, escolhida porque o cluster Parquet/CNJ chegou ao limite do que não depende de credenciais IA. RFC 0012 (`src/segmenter_dataset/`, §5.3 independência, §5.4 metas de escala ≥30 val/≥30 test, §8 registro de review/adjudicação, §9 dados de validação/teste, §10 elegibilidade de split, §11 validação mecânica) é o contrato que rege o trabalho escolhido: um documento só é elegível para val/test após um ReviewRecord aceito, que por sua vez exige duas anotações genuinamente independentes (nunca uma correção semeada sobre a primeira)."
---

# Leitura de conhecimento OKF

Reconfirmei a tensão AgentRun-vs-Wisk (mesma leitura de yz281l, mesma manhã, sem mudança) e mantenho a mesma linha: seguir o prompt agendado, sem competir com o Wisk. Lida também a leitura factual mais relevante ao trabalho escolhido: o run.md mais recente (yz281l) fecha o item 1 do checklist de #1468 e não deixa PR de continuidade nem trabalho Parquet/CNJ pendente sem gate de credencial — confirmando que a frente natural desta rodada é outra (segmenter, RFC 0012 #1051), já identificada como ativa e com progresso mensurável nesta mesma manhã (ver reading-issues). RFC 0012 (`src/segmenter_dataset/`) é o contrato operacional: elegibilidade de val/test exige `ReviewRecord` aceito, que exige duas anotações independentes (§5.3) adjudicadas (§8) e validadas mecanicamente (§11) — exatamente o mecanismo que `scripts/annotate_second_independent.py` + `scripts/adjudicate_segmenter_review.py` já automatizam desde a PR #1505.
