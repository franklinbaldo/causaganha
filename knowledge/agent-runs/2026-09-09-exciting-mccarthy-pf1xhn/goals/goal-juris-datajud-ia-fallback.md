---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-pf1xhn-goal-juris-datajud-ia-fallback"
run_id: "2026-09-09-exciting-mccarthy-pf1xhn"
goal: "Give scripts/render_queries.py's _register_tjro_juris and _register_datajud_capa the same local-then-IA fallback that _register_acordaos/_register_comunicacoes already have, so juris_totals.json/datajud_totals.json (and the other JURIS/DataJud .qmd contracts) actually get published on the live site instead of permanently rendering as 'cobertura não publicada'."
rationale: "_register_tjro_juris/_register_datajud_capa only glob a local directory (data/tjro_juris/*/tjro-juris-*.parquet, data/datajud/datajud-capa-*.parquet) and return False otherwise. Traced two concrete production paths where that local directory is never populated: (1) deploy-web.yml — the workflow that actually builds and publishes the site — does a fresh checkout and runs render_queries.py --strict without ever running reconcile_processos.py (the only thing that discovers/downloads these files), so the glob is always empty there; (2) even in update-catalog.yml's own job, which does run reconcile_processos.py first, its ensure_juris_parquets()/ensure_datajud_parquets() IA-fallback branch caches downloaded files under data/reconcile-cache/{juris,datajud}/... (per reconcile_processos.py's own _cache_dir()), never under data/tjro_juris/ or data/datajud/ — so the glob in render_queries.py misses those too, per reconcile_processos.py's own module docstring ('this reconciler always takes the IA-fetch branch [in CI]'). Net effect: juris_totals.qmd/datajud_totals.qmd/juris_orgaos.qmd/juris_classes.qmd/datajud_classes.qmd (all optional: true) have silently failed to render in every CI run, in every environment, since these two functions were written — sobre.astro has been showing JURIS/DataJud coverage as unpublished on the real deployed site even though update-catalog.yml's own comment confirms 'All four now publish real IA items (tjro-juris-{year}, datajud-{tribunal})'. Zero test coverage exists for either function today (confirmed by grep), unlike their IA-backed siblings which are exercised by test_register_comunicacoes_prefers_indice_when_available and friends."
success_signal: "New RED tests in tests/test_render_queries.py fail today because _register_tjro_juris/_register_datajud_capa return False whenever the local glob is empty, regardless of IA availability. After delegating both functions to reconcile_processos.ensure_juris_parquets()/ensure_datajud_parquets() (already-tested, already-correct local-then-IA logic used by the reconciler itself), the same tests go GREEN: each function returns True and registers a queryable view when only IA-fallback-shaped paths are available. A third test proves the fix doesn't introduce row-count corruption: when ensure_juris_parquets() reports needs_dedup=True (overlapping monthly IA shards, the same condition reconcile_processos.py's own _register_juris already guards against), the registered tjro_juris view still returns one row per id_documento, not one row per shard occurrence."
status: "achieved"
---

# Goal: fallback de IA para as views JURIS/DataJud em render_queries.py

Corrigir `_register_tjro_juris`/`_register_datajud_capa` para reusarem `reconcile_processos.ensure_juris_parquets()`/`ensure_datajud_parquets()` em vez de só checar um diretório local que nunca é populado nos workflows reais de produção — hoje isso derruba silenciosamente `juris_totals.json`/`datajud_totals.json` em toda execução de CI, em todo ambiente, desde que essas funções foram escritas.
