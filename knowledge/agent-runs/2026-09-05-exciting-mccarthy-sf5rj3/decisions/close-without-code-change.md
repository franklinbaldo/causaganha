---
type: AgentDecision
id: "2026-09-05-exciting-mccarthy-sf5rj3-decision-close-without-code-change"
run_id: "2026-09-05-exciting-mccarthy-sf5rj3"
goal_id: "2026-09-05-exciting-mccarthy-sf5rj3-goal-close-1052-eval-harness-already-built"
question: "Does closing #1052 require any code change first (e.g. an explicit cross-model consistency test proving two different 'models' get identical metric semantics, or an additional run-identity field), or is the existing implementation plus test suite sufficient evidence to close as-is?"
choice: "No code change. Close #1052 as completed based on the existing implementation and its 84 passing tests."
rationale: "Two acceptance-criteria sentences look, at first glance, like they might need a dedicated new test: (1) 'the same prediction fixture produces identical metric semantics for every model' and (2) 'record enough model/run identity to distinguish an upstream-canonical baseline from any locally optimized ablation.' Both are already satisfied structurally rather than by a missing test. (1): DocumentModelPrediction (model_eval.py) is a plain {document_id, gold, model_predicted, baseline_predicted} tuple with zero branching on prediction origin -- every metric function (per_category_metrics, macro_f1_over_target_categories, classify_span_errors, aggregate_region_metrics, etc.) operates purely on that shape, so 'identical semantics for every model' is a property of the type signature, not something a same-model-twice unit test could meaningfully add beyond what the existing 84 tests already exercise generically. Only _run_opf_inference (run_segmenter_test_eval.py) is OPF-specific, and it is explicitly outside the harness itself -- a future challenger (#1054) needs its own analogous adapter function, which is that issue's scope, not #1052's. (2): ExperimentManifest already carries opf_commit, checkpoint_selection, and every optimization hyperparameter explicitly (PR #1104 'classify runs as canonical baseline vs local ablation', PR #1101 pins the exact upstream commit), and ModelCard adds release_id/dataset_release_id/experiment_id/test_result_hash on top -- together these already let a reader distinguish a canonical run from an ablation without any new field. Manufacturing a test or a field to formally close these two bullets would be pure box-checking against already-true structural properties, which the project's own 'don't design for hypothetical future requirements' norm argues against. The correct action is verification and closure, not speculative code."
---

# Decisão: fechar sem mudança de código

As duas cláusulas de aceite que pareciam exigir teste/campo novo já são garantidas estruturalmente pelo desenho atual (tipo agnóstico de modelo; identidade de run já registrada em `ExperimentManifest`/`ModelCard`). Adicionar código só para "marcar a caixinha" seria trabalho artificial contra uma propriedade já verdadeira.
