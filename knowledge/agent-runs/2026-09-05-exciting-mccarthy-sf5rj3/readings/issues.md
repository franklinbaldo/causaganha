---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-sf5rj3-reading-issues"
run_id: "2026-09-05-exciting-mccarthy-sf5rj3"
subject: "open_issues"
reference: "GitHub open issues, franklinbaldo/causaganha (27 total, listed via list_issues)"
finding: "27 open issues fall into four clusters. (1) Web-UX (#1093, #1131-1136, #1138-1139): all gated on a concurrent session's active, green-CI PR stack #1160->#1161->#1164 for #1138/#1139/#1136 -- correctly left untouched, matching every prior round's decision since qvwrkl. (2) Segmenter chain under parent #1047 (#1050-1057): #1050/#1051 are annotation-heavy (real double-annotation rounds), #1053/#1054 need a real GPU training run, #1055-1057 are follow-on experiments gated on #1053's baseline existing -- none is a same-round code-only slice. (3) Ops/data (#1042, #1022, #1011, #985, #950/#951): each requires either a live, hard-to-reverse Internet Archive upload, a live GitHub Actions run with real side effects, or a hosting/deploy decision -- correctly deferred without explicit sign-off, per every prior round's same conclusion. (4) #924 (Ox Alpha automated review, meta-issue): live-verified this round that every one of its SS3.3 'dead code' targets (scripts/db/, deployment/cron+systemd, notebooks/train_privacy_filter.*, web/src/lib/queryData.ts, AUDIT.md) is already gone from the tree (glob/ls returned nothing for all five), and its own comment thread already closes SS3.1 (juris/datajud reconciliation) and points SS3.4 (canary publication-archive alarm) at #924's own suggestion -- confirmed live in scripts/canary_check.py: PENDING_REAL_THRESHOLD and PENDING_REAL_MAX_AGE_HOURS_THRESHOLD are both implemented and unit-tested in tests/test_canary_check.py. Only SS3.2 (annotation, gated) and SS3.5 (layout_revision backfill policy, needs live IA sampling this round did not attempt) remain genuinely open under #924. Outside these four clusters, #1052 ('segmenter: evaluate both anchor spans and reconstructed regions with one common harness') looked at first read like another segmenter chain item gated on real data -- but unlike #1050/#1053/#1054, its entire checklist describes an evaluation *harness* (pure Python metrics/reporting code), not a training run. Reading src/segmenter_dataset/model_eval.py and region_eval.py showed every single checklist item already implemented and unit-tested (84 passing tests), wired into scripts/run_segmenter_test_eval.py by six already-merged PRs (#1090, #1092, #1099, #1101, #1104, #1126) -- none of which referenced #1052 by number in its title, which is why the issue was never auto-closed. This is the round's selected work."
---

# Leitura de issues abertas

27 issues abertas, agrupadas em quatro blocos (web-UX gated no stack concorrente #1160/#1161/#1164; segmenter gated em anotação/GPU; ops/data gated em efeitos colaterais irreversíveis ao vivo; #924 como meta-issue majoritariamente já resolvida por rodadas anteriores, verificado ao vivo). A lacuna real e não reivindicada encontrada foi #1052: um harness de avaliação totalmente implementado e testado por PRs já mergeados, mas nunca fechado porque nenhum desses PRs referenciou `#1052` no título do commit.
