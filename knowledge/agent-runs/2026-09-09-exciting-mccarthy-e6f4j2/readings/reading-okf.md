---
type: AgentReading
id: "2026-09-09-exciting-mccarthy-e6f4j2-reading-okf"
run_id: "2026-09-09-exciting-mccarthy-e6f4j2"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-09-exciting-mccarthy-{pf1xhn,8kw55y,0lpi0s,qvqmci,ez5wkn,8esdwh}/run.md (all six prior rounds today in this AgentRun family); uv run okf-parser check knowledge --relational-schema okf.schema.sql"
finding: "Read all six prior rounds from today in full. This family ran highly concurrently and exhaustively mined scripts/render_queries.py's IA-fallback/join/dedup bug class earlier today (pf1xhn, 8kw55y, 0lpi0s, qvqmci, ez5wkn all fixed distinct instances of it; 8kw55y's and pf1xhn's own follow-up checks -- README optional-flag drift, all VIEW_SPECS sources having IA fallback -- both came back clean, closing that bug class out). The most recent round (8esdwh, merged as 9c5c474) moved one step earlier in the same bug shape: it found and fixed hand-rolled, unescaped CSV row construction (raw f-string comma-joining on write, naive str.split(',') or csv.DictReader mismatch on read) in three sibling manifest modules -- src/datajud/manifest.py, src/tjro_juris/manifest.py, src/stj_acordaos/manifest.py -- via TDD (one RED test per module with a comma-bearing field, then csv.writer/csv.reader). Its next_move explicitly flagged: 'any remaining legacy CSV writer elsewhere in the codebase should be checked the same way.' Checked src/djen_backup/manifest.py (the canonical sync engine's own SyncManifest, per CLAUDE.md the file djen_backup/manifest.py is the persistence layer for the single source of truth) directly: to_csv() (line 461) builds rows via the exact same raw f-string comma-join pattern (f'{e.tribunal},{e.date.isoformat()},{e.ia_status},{e.djen_status},{e.djen_raw},{e.updated_at}'), and load_from_csv()/apply_segment_csv() (lines 471, 743) parse them back via the same naive line.split(',') with zero escaping -- this is the exact same latent bug shape 8esdwh just fixed, in the one manifest module 8esdwh's own search scope explicitly excluded (djen_backup's sync-manifest.parquet was correctly ruled out as unaffected since it's Parquet, but the CSV-format local-disk cache (save_to_disk/load_from_disk) and manifest-log segment format (apply_segment_csv, used by segments.py's append-only event log) inside the same module were not checked). Traced every writer of ManifestEntry fields (engine.py's _classify_djen_status, published.py, service.py) and confirmed tribunal/ia_status/djen_status/djen_raw/updated_at are all always short enum-like tokens or ISO timestamps with no commas today -- same 'real but currently-latent, not yet triggered by live data' shape as every fix in this family so far, not a hypothetical. No existing test (tests/djen_backup/test_ia_contract.py, test_segments.py, test_manifest_counts.py) exercises a comma-bearing field surviving a to_csv/load_from_csv or apply_segment_csv round-trip. Selected this as the round's goal. Ran `uv run okf-parser check knowledge --relational-schema okf.schema.sql` at round start: conformant, 0 diagnostics, 1001 concepts (up from 985 at 8esdwh's own baseline, reflecting its own goal/decision/evidence/check instances recorded before merge)."
---

# Leitura do conhecimento OKF

A rodada anterior (8esdwh) corrigiu o mesmo bug de CSV sem escaping em três módulos de manifesto irmãos e apontou explicitamente, em seu `next_move`, que qualquer outro escritor de CSV artesanal no repositório deveria ser auditado da mesma forma. `src/djen_backup/manifest.py` -- o próprio motor canônico de sincronização -- tem exatamente o mesmo padrão (`to_csv`/`load_from_csv`/`apply_segment_csv`) e não foi verificado por aquela rodada. `okf-parser check`: conformante, 0 diagnósticos, 1001 conceitos.
