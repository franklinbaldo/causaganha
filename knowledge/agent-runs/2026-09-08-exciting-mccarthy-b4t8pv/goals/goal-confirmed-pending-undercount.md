---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-b4t8pv-goal-confirmed-pending-undercount"
run_id: "2026-09-08-exciting-mccarthy-b4t8pv"
goal: "Fix web/src/queries/totals.qmd and tribunal_coverage.qmd so a manifest row with djen_status='confirmed' is counted as 'pending' instead of vanishing from every displayed bucket."
rationale: "totals.qmd:15 and tribunal_coverage.qmd:15 classify 'pending' with `djen_status = 'available' AND ia_status != 'uploaded'`, and their absent/unknown filters are similarly exact-matched. But djen_status='confirmed' is a real, live value written straight into the canonical sync-manifest.parquet: src/djen_backup/probe.py's mark_confirmed() -> segments.py appends a `djen_status='confirmed'` event, and scripts/render_manifest_parquet.py's _apply_deltas (line ~305) merges it into the parquet as-is -- _normalize_manifest (the only function that rewrites djen_status before the parquet is written) never touches 'confirmed'. The codebase's own canonical intent already treats 'confirmed' as a refinement of 'available', not a fourth bucket: drain.py:70 selects `djen_status IN ('available', 'confirmed')` and prioritises confirmed rows; render_manifest_parquet.py's own _print_merge_stats (line 487) computes 'pending' the same way; write_back_csv's docstring (line 506-510) explicitly folds confirmed->available before export specifically because 'the CSV consumers don't know it'. The .qmd files read the parquet directly (not the CSV), so this fold never happens for them. Net effect: a manifest row with djen_status='confirmed' is counted in `total` but in none of uploaded/pending/absent/unknown -- the four displayed buckets silently stop summing to total, and the public /stats page (web/src/pages/stats.astro:185, rendering `t.pending` per tribunal) undercounts pending work for any tribunal with a probe-confirmed-but-not-yet-uploaded row. site_status.qmd is unaffected (it classifies via djen_raw directly, which mark_confirmed always sets to '200', not via djen_status)."
success_signal: "A test rendering the real totals.qmd and tribunal_coverage.qmd against a manifest fixture with one djen_status='confirmed', ia_status='' row demonstrates the bug RED (pending=0, and uploaded+pending+absent+unknown != total), then goes GREEN after each query's pending filter is widened to `djen_status IN ('available', 'confirmed')`, matching the precedent already established in render_manifest_parquet.py's own _print_merge_stats. Full tests/test_render_queries.py suite stays green, plus uv run pytest -q, ruff check, ruff format --check. okf-parser check stays conformant."
status: "achieved"
---

# Goal: count confirmed rows as pending in totals.qmd/tribunal_coverage.qmd

Encontrado por um subagente Explore que investigou o SQL de agregação dos `.qmd` cruzado com a semântica de `djen_status='confirmed'` no runtime Python (`probe.py`/`drain.py`/`render_manifest_parquet.py`). Verificado de forma independente: lido `totals.qmd`/`tribunal_coverage.qmd` (filtro exato `djen_status = 'available'`), `render_manifest_parquet.py` (`_apply_deltas` grava `'confirmed'` no parquet, `_normalize_manifest` não normaliza esse valor, `_print_merge_stats`/`write_back_csv` já tratam `'confirmed'` como refinamento de `'available'`), `manifest.py`'s `_normalize_event` (o runtime em memória do engine já normaliza `'confirmed'`->`'available'`, mas isso não afeta o parquet compactado por `render_manifest_parquet.py`), e `stats.astro` (renderiza `t.pending` publicamente). `court_reliability.qmd`/`tribunal_calendar.qmd` não têm filtro de `'available'`, então não são afetados por este bug específico.
