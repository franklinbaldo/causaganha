---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-b0lycs-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-b0lycs"
subject: "open_issues"
reference: "mcp__github__list_issues franklinbaldo/causaganha state=OPEN (19 results, ordered by updated_at desc); mcp__github__issue_read #1191, #1132"
finding: "19 open issues. #1191 ('perf(web): não hidratar /stats com o tribunal_calendar inteiro'), filed by the owner at 2026-09-06T07:04:36Z — roughly 20 minutes before this round started — is the newest and most urgent: it is an adversarial-review follow-up on the very PR (#1189/#1131) the immediately preceding round merged, explicitly marked 'READY para IMPLEMENTAÇÃO. Prioridade alta por ser regressão introduzida no merge de #1189'. Live read of web/src/pages/stats.astro confirms the regression exactly as described: `tribunalCalendarRows` (the full tribunal_calendar contract — every (tribunal, date) row in the whole archive, ~13.9MB per #1189's own AgentRun measurement) is loaded via loadContract() and passed as a literal prop (`calendarRows={tribunalCalendarRows}`) to `<TribunalCoverageExplorer client:only=\"svelte\">`. Astro islands (including client:only) serialize their props into the rendered page for client hydration, so this ships the entire global calendar to every /stats visitor even though the drill-down UI only ever needs one tribunal's rows at a time. The issue names the exact healthier precedent already in the codebase (web/src/pages/publicacoes/[tribunal].astro, which filters tribunal_calendar to one tribunal at Astro build time before handing the island only that tribunal's arrays) and gives ranked acceptance criteria: (1) prefer a build-time partitioned artifact fetched per tribunal over a global fetch; (2) reuse the same tribunal_calendar contract/semantics, no second source of truth; (3) keep a shareable URL; (4) preserve sem_evidencia≠absent and coveragePct=null-when-unobserved; plus an explicit regression gate requirement ('existe um teste/gate simples que impeça regressão para prop/payload global'). This is a clear, well-specified, high-priority continuation of the prior round's own work and the strongest candidate for this round. Other candidates checked and deprioritized, unchanged from every prior round's assessment: #1132 (explorador recipes — READY but not a regression, medium priority, no continuity pressure), #1022/#1011/#985 (TCU/TSE Internet Archive publication — needs live credentialed-upload sign-off), #950/#951 (MCP remote hosting — live hosting decision), #1047/#1050-1057/#884/#886/#887 (segmenter roadmap — annotation/GPU-heavy), #1093 (explicitly 'NÃO é prioridade imediata')."
---

# Leitura das issues abertas

19 issues abertas. A `#1191` é a mais nova e mais urgente: aberta ~20 minutos antes desta rodada pelo próprio dono, como *follow-up* de revisão adversarial da PR que a rodada anterior acabou de mesclear (#1189/#1131), marcada explicitamente como regressão de prioridade alta. Leitura ao vivo de `web/src/pages/stats.astro` confirma a regressão: o contrato `tribunal_calendar` inteiro (~13,9MB) é passado como prop para a ilha `TribunalCoverageExplorer` com `client:only="svelte"`, que serializa a prop inteira na página para hidratação. Escolhida como o melhor avanço desta rodada por continuidade direta com o trabalho já entregue e por ter critérios de aceite explícitos e um padrão saudável já existente no próprio código (`/publicacoes/[tribunal]`) para seguir.
