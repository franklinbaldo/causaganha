---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-8esdwh-goal-csv-manifest-escaping"
run_id: "2026-09-09-exciting-mccarthy-8esdwh"
goal: "Make ManifestDataJud.save_local, ManifestJuris.save_local, and ManifestSTJ.save/load_text round-trip any field value safely, by writing through csv.writer/csv.reader instead of raw comma-joined f-strings and a naive line.split(\",\")."
rationale: "A background codebase survey (this round's OKF reading) found that all three manifest modules build CSV rows by hand (f'{a},{b},{c}...') while two of the three already read them back through csv.DictReader, and the third (stj_acordaos) also reads them back with a naive line.split(\",\"). Any field value containing a comma silently misaligns columns on the read side with no exception raised -- ManifestDataJud/ManifestJuris's DictReader-based readers put the overflow into a discarded restkey list and truncate the value; stj_acordaos's split-based reader shifts every subsequent column. No current call site feeds a comma-bearing value into any of these fields (cnj/tribunal/status, tipo/mes_ano/ia_status, arquivo/tipo/ia_status are all either digit strings, short enum codes, or pipeline-generated Path.name filenames) -- so this is a real, currently-latent correctness bug in a format contract, not yet triggered in production. It is exactly the kind of gap that render_queries.py's already-fixed IA-fallback bugs (from earlier today's rounds) resembled before their own first trigger. All three modules already import (or can trivially import) the stdlib csv module; the fix is small, symmetric, and testable per-module."
success_signal: "A new RED-then-GREEN test per manifest module: constructing an entry whose one text field contains a comma, saving it, reloading it, and asserting the reloaded value is unchanged. Today (RED) the reloaded value is silently truncated/misaligned instead of raising -- after the fix (GREEN) it round-trips exactly. All pre-existing tests in tests/datajud/test_datajud_manifest.py, tests/tjro_juris/test_juris_manifest.py, and tests/stj_acordaos/test_stj_manifest.py stay green unchanged (including the plain-text format assertions like `lines[1].startswith(\"a.json,json,2024-02-01,,1,\")`, since csv.writer with QUOTE_MINIMAL is byte-identical to the old f-string join for comma-free fields). Full Python suite, ruff check, and ruff format --check stay green."
status: "achieved"
---

# Goal: escapar corretamente os manifestos CSV

Os três manifestos (`datajud`, `tjro_juris`, `stj_acordaos`) escreviam linhas CSV por concatenação de string, sem escapar vírgulas/aspas, enquanto dois deles já liam de volta via `csv.DictReader` (que espera CSV corretamente formatado). Nenhum ponto de chamada atual injeta vírgula em campo controlado, mas o contrato de formato está quebrado de forma silenciosa e sem exceção. Corrigido via `csv.writer`/`csv.reader` nos três módulos.
