---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-wvzu11-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-wvzu11"
subject: "open_issues"
reference: "22 issues abertas em franklinbaldo/causaganha (list_issues, orderBy updated_at DESC); leitura completa de #1468/#1469/#1470/#1471/#1472/#1482/#950/#951/#1093/#1047/#1048/#1050/#1051/#1052/#884 e seus comentários"
finding: "Cluster #1468 (Parquet nativo por CNJ): item 1 do checklist (implementação de produção) está inteiramente fechado por comentários de 2026-09-15 em #1469/#1470 (normalização CNJ, ORDER BY, ZSTD+ROW_GROUP_SIZE 122880, certificação de rodapé, igualdade direta em processoCnj.ts, ordem física do índice, auditoria completa incluindo bloom filters/encodings, gate de índice cobertura desnecessário). Só resta #1472 (rollout real), bloqueado por IA_ACCESS_KEY/IA_SECRET_KEY -- ausentes neste ambiente (confirmado por `env | grep IA_`). #1471 também está com todo o trabalho local/mensurável feito (PR #1478 diff local, PR #1480 custo de consulta DuckDB nativo+WASM); só resta o read-back real do arquivo candidato reordenado, que também depende de #1472. #1482 (CORS do endpoint de download do IA): confirmação real em navegador feita (PR #1491); o workaround (proxy) precisa de deploy real -- sem credenciais Cloudflare neste ambiente (`env | grep CLOUDFLARE` vazio) e o worker existente em deployment/relay-cf/ é um probe autenticado específico para TJRO/STJ JURIS, não uma infra reaproveitável para um proxy público CORS do archive.org. #950 (MCP remoto): 37 comentários confirmam que TODO o código está pronto (transporte HTTP, /health, allowlist read-only, deployment/mcp/, workflow deploy-mcp.yml) -- só falta rollout real com credenciais GCP/Workload Identity, ausentes aqui (`gcloud` não instalado; env CLOUDSDK_* são boilerplate de proxy genérico, não credenciais reais do projeto). #951/#1093 dependem de #950. Cluster #1047 (roadmap do segmentador OPF v8): #1048 e #1052 já fechados; #1050 (escalar corpus real) e #1051 (validation set independente/adjudicado) seguem abertos, sem nenhum comentário desde a criação (03/09) -- nenhuma rodada tocou neles desde os registros Wisk de 09-09. Investigação ao vivo (ver reading-okf/evidence) achou a causa raiz concreta de por que #1051 não avançou: a store RFC 0012 (data/segmenter) tem 0 ReviewRecords, então `assign-splits` não consegue produzir nenhum documento elegível para val/test hoje."
---

# Leitura de issues abertas

Levantamento completo das 22 issues abertas, priorizando os dois clusters ativos mais recentes (#1468 parquet/CNJ, #1047 segmenter/OPF). Conclusão: todo trabalho de código desbloqueado do cluster #1468/#1469/#1470/#1471/#1482/#950 já foi feito por rodadas anteriores -- o que resta em todos eles é rollout/deploy real, que exige credenciais que este ambiente não tem (verificado ao vivo, não assumido). Isso descarta repetir o mesmo domínio pela sexta vez consecutiva. O cluster #1047 tem trabalho real, desbloqueado e ainda não tocado (#1050/#1051), e uma investigação ao vivo da governança RFC 0012 revelou uma causa raiz concreta e não documentada no GitHub para por que #1051 nunca avançou -- ver goal desta rodada.
