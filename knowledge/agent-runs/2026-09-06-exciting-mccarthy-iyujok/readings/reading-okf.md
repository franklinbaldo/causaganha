---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-iyujok-reading-okf"
run_id: "2026-09-06-exciting-mccarthy-iyujok"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-06-exciting-mccarthy-buxwff/run.md (previous round, closed #1219/#1221); web/src/components/McpConfigCard.test.ts (existing behavioral test pattern for this same component)"
finding: "Prior round (buxwff) closed #1219 and merged PR #1221, and its next_move named #951/#1011/#1022/#985 as the next natural product advance — all four remain genuinely blocked per knowledge/backlog/ (infra decision or IAS3 credentials), confirmed again by this round's own reading-issues. With the issue queue exhausted of unblocked work, this round ran the repo's own quality gates end-to-end (uv run okf-parser check — 0 diagnostics; ruff check/format — clean; pytest -q — green; npm run lint/typecheck — clean; npm run test — 451/451) to look for a genuine defect rather than idling. `npm run test`'s own stderr surfaced a real, pre-existing Svelte 5 compiler warning: `[vite-plugin-svelte] src/components/McpConfigCard.svelte:47:2 noninteractive element cannot have nonnegative tabIndex value` (code a11y_no_noninteractive_tabindex) — present since the #1169 Cobogó/Panda reboot commit (2c497c9) that introduced this file, never a regression from a recent round. No OKF type/schema change was needed for this round's selected work; it is a product-code accessibility fix plus one new co-located Vitest contract test (web/src/components/svelteA11y.contract.test.ts), reusing McpConfigCard.test.ts's existing render-and-assert methodology for its GREEN-side confirmation and the Svelte compiler API directly (svelte/compiler's compile()/preprocess(), matching the project's own vitePreprocess() from @astrojs/svelte) for the RED/GREEN contract itself — no new test mechanism invented beyond what the compiler already exposes."
---

# Leitura do conhecimento OKF

A rodada anterior (`buxwff`) fechou #1219/#1221 e apontou #951/#1011/#1022/#985 como próximo avanço — todos seguem bloqueados. Com a fila de issues esgotada, rodei as gates de qualidade do próprio repositório (okf-parser, ruff, pytest, lint/typecheck/test do web) em vez de ficar ociosa, e a própria saída do `npm run test` revelou um warning real e pré-existente do compilador Svelte 5 em `McpConfigCard.svelte` (`a11y_no_noninteractive_tabindex`), presente desde o commit do reboot Cobogó/Panda (#1169) — não uma regressão recente. Nenhuma mudança de schema OKF é necessária; o teste novo reaproveita a API do compilador Svelte (`compile()`/`preprocess()` com o mesmo `vitePreprocess()` já usado pelo projeto) para tornar esse warning um contrato RED→GREEN, em vez de inventar um mecanismo novo.
