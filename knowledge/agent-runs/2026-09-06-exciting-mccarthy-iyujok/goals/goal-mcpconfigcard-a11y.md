---
type: AgentGoal
id: "2026-09-06-exciting-mccarthy-iyujok-goal-mcpconfigcard-a11y"
run_id: "2026-09-06-exciting-mccarthy-iyujok"
goal: "Eliminate the real Svelte 5 compiler accessibility warning on web/src/components/McpConfigCard.svelte's scrollable <pre> (a11y_no_noninteractive_tabindex) with a correct fix, and add a durable regression test so no Svelte component in the tree can reintroduce this class of warning silently again."
rationale: "With all 17 open GitHub issues genuinely blocked (per knowledge/backlog/ and this round's own reading-issues) and zero open PRs to continue, this round's best available advance for CausaGanha was to look beyond the issue queue at the codebase's own quality signals, per this task's explicit instruction that issues are 'uma fila de oportunidades, não um limite'. Running the web test suite surfaced a genuine, pre-existing defect: McpConfigCard.svelte (the copyable MCP config block on the public /agentes page, the same surface two of the last three rounds advanced) puts tabindex=0 directly on a <pre>, which Svelte 5's own a11y analysis flags because a static, non-widget element made focusable without an interactive ARIA role is invisible to assistive technology as a distinct, operable region — a real, if minor, accessibility gap on a public-facing page, not manufactured busywork."
success_signal: "A new Vitest contract test (web/src/components/svelteA11y.contract.test.ts) that compiles every .svelte file under web/src with the project's own vitePreprocess()+svelte/compiler pipeline and asserts zero a11y_* compiler warnings fails (RED) against the current McpConfigCard.svelte (1 warning: a11y_no_noninteractive_tabindex) and passes (GREEN, 0 warnings across all 35 .svelte files) once the component is fixed with role=\"region\" + aria-label + an explicit svelte-ignore comment documenting the deliberate keyboard-scroll pattern. The existing McpConfigCard.test.ts behavioral suite (display/copy/success/failure/no-clipboard/keyboard cases) stays green untouched. A real Chromium browser check against the built /agentes page confirms the <pre> keeps tabindex=\"0\"/role=\"region\"/the correct aria-label, receives keyboard focus, and the copy-to-clipboard button still works end-to-end. npm run lint/typecheck/test, ruff check/format, and pytest -q all stay green (except this round's own report-completeness test, which turns green once this file's required fields are filled in). okf-parser check stays conformant with 0 diagnostics."
status: "achieved"
---

# Goal: corrigir o warning de acessibilidade real em McpConfigCard.svelte

Com a fila de issues esgotada de trabalho desbloqueado e nenhuma PR aberta para retomar, o avanço desta rodada veio de um warning real do compilador Svelte 5 (`a11y_no_noninteractive_tabindex`) já presente em produção na página pública `/agentes`. Corrigi o componente e criei um teste de contrato que trava qualquer componente Svelte da árvore contra essa classe de regressão daqui pra frente.
