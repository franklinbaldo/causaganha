---
type: AgentGoal
id: "2026-09-06-exciting-mccarthy-tp38w3-goal-mostrar-mudancas-desde-ultima-consulta"
run_id: "2026-09-06-exciting-mccarthy-tp38w3"
goal: "Implementar #1133: quando o usuário reabre um processo salvo em /minhas-consultas, o navegador compara o estado atual do dossiê com a última captura local conhecida e indica mudou / sem mudança / não foi possível comparar — nunca inferindo mudança a partir de uma fonte apenas indisponível."
rationale: "#1133 é a issue aberta mais concretamente escopada e testável por TDD nesta rodada: acceptance criteria explícitos cobrem primeira consulta, mudança real, ausência de mudança e fonte indisponível; não depende de decisão arquitetural pendente (diferente de #1093, explicitamente não-prioritária) nem de trabalho ao vivo fora do alcance de uma sessão (segmenter, TCU/TSE, MCP remoto). #1107 e #1136, as duas issues que ocupavam rodadas recentes, já fecharam. A funcionalidade usa infraestrutura já existente e comprovada (buscarProcesso()/DuckDB-WASM, o mesmo client usado por ProcessoLookup.svelte) e o contrato público já consolidado por #1105, sem exigir backend novo nem envio de dado a servidor."
success_signal: "Um novo módulo puro web/src/lib/consultationSnapshot.ts (buildConsultationSnapshot + compareConsultationSnapshots) com testes RED-then-GREEN prova: (1) sem_historico na primeira captura; (2) mudou quando um campo comparável realmente muda OU uma fonte nova aparece no índice; (3) sem_mudanca quando os campos comparáveis são idênticos; (4) uma fonte apenas indisponível na captura atual nunca gera 'mudou' para os campos dela (nunca tratada como remoção), e escala para nao_comparavel só quando NENHuma fonte com baseline anterior sobra comparável. Um segundo módulo (consultationSnapshotStore.ts) persiste/lê/remove o snapshot por consulta salva em localStorage, versionado e migrável (tolera JSON corrompido). SavedConsultations.svelte é ligado a essa lógica: ao montar a página e ao salvar um novo processo, busca o dossiê ao vivo (mesmo client buscarProcesso() de ProcessoLookup), compara com o snapshot salvo e mostra um veredito textual por item; remover a consulta remove também seu snapshot. Toda a suíte web (vitest), typecheck (svelte-check, sem novos erros além da baseline pré-existente), eslint e build estático continuam verdes; nenhum arquivo Python muda, e ruff check/format/pytest -q continuam verdes."
status: "achieved"
---

# Goal: mostrar mudanças desde a última consulta (#1133)

Fecha a issue #1133 com uma primeira fatia completa: motor puro de snapshot/diff (testado exaustivamente, incluindo o caso crítico de não confundir indisponibilidade com remoção), armazenamento local versionado, e integração mínima na UI de `/minhas-consultas` que busca o dossiê ao vivo e mostra o veredito por consulta salva.
