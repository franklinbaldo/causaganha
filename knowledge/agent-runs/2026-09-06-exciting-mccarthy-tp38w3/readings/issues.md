---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-tp38w3-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-tp38w3"
subject: "open_issues"
reference: "mcp__github__list_issues(owner=franklinbaldo, repo=causaganha, state=OPEN) at session start (20 open issues); mcp__github__issue_read on #1107, #1136, #1131, #1132, #1093, #1133"
finding: "Session started exactly at the point the prior round's own next_move predicted: #1107 (contract(processo): eliminar drift do plano de consulta) is now CLOSED as completed, resolved by PR #1159 (merged directly by the repo owner on top of the prior round's ich5gz report, which itself reached result_state 'green' and was waiting on a push). #1136 (web/ux estados padronizados) and #1134 (sobre coverage matrix) are also both closed now — #1134 via owner PR #1182 (fe0aedd), #1136 with zero linked PRs (closed by owner directly, its scope apparently satisfied by the cumulative #1169/#1173/#1174/#1180/#1183/#1185 Cobogó/Panda-reboot work prior rounds already did). Zero open PRs exist (list_pull_requests state=open → []), so there is nothing to babysit/rebase. Of the remaining 20 open issues: #1093 (teor busca direta) is explicitly marked 'ESPECIFICADA, mas NÃO é prioridade imediata' pending #950 or an explicit architecture decision — skip. #1131 (stats drill-down) and #1132 (explorador recipes) are open, well-specified, but larger in scope (drill-down UI with URL-reflected filters; a curated SQL-recipe gallery backed by the catalog). #1133 (minhas-consultas: mostrar mudanças desde a última consulta) is open, has concrete, testable acceptance criteria (first-consultation vs. changed vs. unchanged vs. source-unavailable cases explicitly named), is scoped to a single existing page (web/src/components/SavedConsultations.svelte) with no backend/network dependency beyond the existing buscarProcesso()/DuckDB-WASM client machinery already used by ProcessoLookup.svelte, and does not collide with any in-flight work (no open PR touches it). The segmenter cluster (#884, #886, #887, #1047, #1050-#1057), TCU/TSE publication (#1011, #1022, #985) and MCP remote hosting (#950, #951) remain gated on real annotation/GPU work, live IA-upload sign-off, or hosting decisions respectively — unchanged from every prior round's assessment, still unsuited to a single TDD-shaped session. #1133 was selected as the best continuity-respecting, product-advancing, single-round-sized slice."
---

# Leitura de issues abertas

#1107, #1136 e #1134 fecharam desde a última rodada registrada (ich5gz). Zero PRs abertas. Entre as 20 issues abertas restantes, #1133 (mostrar mudanças desde a última consulta em `/minhas-consultas`) é a mais concretamente escopada e testável por TDD sem depender de decisão arquitetural pendente (diferente de #1093) nem de trabalho ao vivo (segmenter/TCU/TSE/MCP).
