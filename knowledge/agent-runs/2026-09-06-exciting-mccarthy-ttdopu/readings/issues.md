---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-ttdopu-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-ttdopu"
subject: "open_issues"
reference: "mcp__github__list_issues franklinbaldo/causaganha state=OPEN (21 results, ordered by updated_at desc)"
finding: "21 open issues. #1136 ('padronizar estados de carregamento, vazio, indisponível e erro entre superfícies') is the most recently updated (its 4th comment, 00:03:34Z the night before this round, posted by the owner) and its own thread already narrows the remaining scope precisely: the first slice (#1164, then extended to /minhas-consultas by the prior round s5c21a via #1183) covers ProcessoLookup/PublicationSearch/SavedConsultations through web/src/styles/query-states.css; the owner's last comment says new visual work should happen 'dentro das migrações Cobogó/Panda, não como expansão do CSS legado' and that /stats should get the same vocabulary only once migrated. Live read of web/src/pages/stats.astro this round shows /stats is in fact already fully on the Cobogó/Panda shell (styled-system css()/recipes: alert(), badge(), card(), table()) and already implements its own equivalent-but-differently-coded treatment for an unavailable dataset (`<div class={alert({tone:'attention'})}>Dados de cobertura indisponíveis.</div>`) and for pipeline freshness (a badge driven by evaluateSourceFreshness/freshnessDisplayLabel in web/src/lib/data/siteStatus.ts) — a genuinely different, already-migrated code path from query-states.css, not a gap to extend query-states.css into. Checked whether #1136's still-unaddressed acceptance item ('stale' state) generalizes across surfaces: ProcessoLookup's dataset-staleness warning (isDatasetStale, 48h threshold in web/src/lib/processoCnj.ts) is specific to the indice_processual snapshot it queries; PublicationSearch queries DJEN live (no dataset-generation timestamp exists in its data model at all — grepped its component source for generated_at/dataset/snapshot and found none); SavedConsultations is a local bookmark list with no dataset concept; /stats' freshness badge already measures a different thing (DJEN checker pipeline health, not snapshot age). Conclusion: 'stale' is not a cross-surface gap the way empty/error/loading were — forcing it onto surfaces where it doesn't apply would violate #1136's own stated risk ('tratar todos os erros como equivalentes'). This is recorded as this round's second AgentDecision and posted back to #1136 as a narrowing comment (see evidence/1136-narrowing-comment.md) rather than left for a future round to re-derive. No open PR touches #1136, #1131, #1132, #1133, or #1093 right now. Other candidates unchanged from prior rounds' assessment and still rejected: segmenter issues (#1047/#1050-1057/#884/#886/#887) need real annotation/GPU work; #1022/#1011/#985 (TCU/TSE Internet Archive publication) need explicit human sign-off for live credentialed uploads; #950/#951 (MCP remote hosting) are live hosting/deploy decisions unsuited to an unattended round; #1131/#1132/#1133/#1093 have no owner narrowing comment yet (#1093 explicitly says 'NÃO é prioridade imediata')."
---

# Leitura das issues abertas

21 issues abertas. A #1136 continua sendo o item mais recentemente atualizado e mais bem sequenciado pelo próprio dono. Verificação ao vivo mostra que `/stats` já migrou para Cobogó/Panda e já resolve indisponibilidade/frescor à sua própria maneira (recipes Panda), não precisando de `query-states.css`. O item "stale" da issue não generaliza para as outras superfícies (cada uma tem ou não tem o conceito de "snapshot desatualizado" por razões estruturais diferentes) — achado registrado como decisão e devolvido à issue como comentário de descoberta, para não ser re-derivado por outra rodada. Nenhuma PR aberta toca essas issues no momento.
