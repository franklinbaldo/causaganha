---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-ttdopu-reading-claude-md"
run_id: "2026-09-06-exciting-mccarthy-ttdopu"
subject: "claude_md"
reference: "CLAUDE.md"
finding: "Two runtime surfaces: djen-backup sync engine (src/djen_backup) and web/ (Astro 5 + Svelte 5) fed by .qmd query contracts. None of the djen-backup correctness rules (djen_raw vs djen_status, 403≠absent, per-item IA lock) are implicated by this round's candidate work. CLAUDE.md's '### CSS token boundary' section is stale and this round verified exactly how: it still describes 'two token systems' split by page type — Brazilian Modernism (--s-*/--papel-*/--tinta-*) for marketing pages only vs. Semantic (--color-*/--space-*/--pico-*) for data pages — but live grep of web/src shows: (1) --pico-*/--tinta-* do not exist anywhere in the codebase any more; (2) every substantive Astro page (checked all of web/src/pages/**/*.astro; only advogados.astro and comparador.astro are unmigrated, and both are trivial redirect stubs to /stats) now styles via Panda CSS (`css()`/styled-system/recipes from the `cobogo` preset declared in web/panda.config.ts), not via --papel-*/--s-* at all; (3) --s-*/--papel-* now live only as global compatibility aliases defined once in web/src/index.css (e.g. `--papel-00: var(--cg-canvas)`, `--s-6: 1.5rem`) and are consumed, with var() fallbacks to the semantic names, inside four still-unconverted Svelte islands' own <style> blocks (SavedConsultations.svelte, ProcessoLookup.svelte, PublicationSearch.svelte, TribunalCalendar.svelte) — components, not a page-type boundary. This exact staleness was already flagged by four prior rounds (6tcxrn, qnpypw/sf5rj3-era, nao666, s5c21a) without ever being fixed, each time deprioritized as 'lower value than the round's code work'; each round re-derived roughly the same finding from scratch, which is the real cost of leaving it stale. This round fixes it directly (see decision d-fix-css-boundary-now) since the code investigation for this round's other candidate work (issue #1136) already produced the verified facts needed to write it correctly. Before committing: `uv run ruff check`, `uv run ruff format --check`, `uv run pytest -q`, plus `uv run okf-parser check knowledge --relational-schema okf.schema.sql` and `uv run python scripts/check_agent_run_completeness.py knowledge/agent-runs`."
---

# Leitura de CLAUDE.md

A seção `### CSS token boundary` está desatualizada e, pela primeira vez, esta rodada teve o levantamento ao vivo necessário para corrigi-la com precisão: não existe mais fronteira por tipo de página (marketing vs dados) nem os tokens `--pico-*`/`--tinta-*`; praticamente todo o site usa Panda CSS via preset `cobogo`, e `--s-*`/`--papel-*` sobrevivem apenas como aliases de compatibilidade globais consumidos por quatro componentes Svelte ainda não convertidos. Corrigido nesta rodada em vez de adiado outra vez.
