---
type: AgentReading
id: "2026-09-07-exciting-mccarthy-vgrupn-reading-okf"
run_id: "2026-09-07-exciting-mccarthy-vgrupn"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-07-exciting-mccarthy-{kfv7sx,abz39i,7gg7l1,cctnlf}/run.md; knowledge/backlog/*.md; src/djen_backup/{retry,probe,credentials}.py and tests/djen_backup/ live tree"
finding: "Read all four of today's prior round reports in chronological order (kfv7sx 00:24-01:15 -> abz39i 01:15-01:35 -> 7gg7l1 02:29-03:10 -> cctnlf 13:31-14:05) to reconstruct continuity. kfv7sx closed the self-contained slice of #1244 (MCP public/operator profile split) and deferred the http_server.py migration as a future round's primary goal. abz39i fixed a health-check regression on kfv7sx's own PR #1247/#1245 follow-up. 7gg7l1 did the deep same-day backlog re-verification now cached in knowledge/backlog/ (read in reading-issues.md). cctnlf, finding both queues empty, dispatched an Explore subagent and fixed a real bug in tribunais.py's get_tribunal_list(), and named as next_move (item 2): 'src/djen_backup/{retry,probe,credentials}.py have zero direct unit tests ... worth closing before a real regression there goes unnoticed the way tribunais.py's did.' Investigated that claim directly against the live tree rather than trusting it verbatim: retry.py in fact already has tests/djen_backup/test_retry.py (149 lines, 	5+ tests) — added by an earlier same-day commit (1ff5630, 'make request_with_retry actually retry on retriable status codes'), so cctnlf's characterization was already stale by the time it was written. probe.py and credentials.py genuinely have zero direct tests. Reading probe.py's _probe_one line by line against djen.py's exception contract (DJENNotFoundError vs DJENRateLimitedError, the latter raised specifically for HTTP 403 per CLAUDE.md's 'never treat 403 as absent' rule) found a live, demonstrable bug, not just a coverage gap: engine.py's checker (engine.py:253) and scripts/drain_unknowns.py (line 82) both explicitly `except DJENRateLimitedError: return '403'` / `except DJENRateLimitedError: return \"403\"`, treating it as skip-and-retry, but probe.py's _probe_one only catches DJENNotFoundError and (httpx.HTTPError, httpx.RequestError) — DJENRateLimitedError (a plain Exception subclass, not an httpx exception) falls through both except clauses and propagates uncaught out of _probe_worker's per-item loop, silently terminating that worker's asyncio.Task (swallowed without logging by the final `asyncio.gather(*worker_tasks, return_exceptions=True)`). This is a stronger TDD story than a pure coverage-gap fix and directly enforces CLAUDE.md's own correctness rule in a module that had never been checked against it. credentials.py's partial-env-var fallback behavior (access set without secret, or vice versa, silently falling through to the ini file) was also inspected for a demonstrable defect but reads as intentional layered-fallback design, not a bug — left untouched, no test added for it this round to keep the round's TDD story anchored to one real, demonstrated defect rather than padding coverage for its own sake."
---

# Leitura do conhecimento OKF

Lidos os quatro relatórios `AgentRun` de hoje em ordem cronológica. A rodada mais recente (`cctnlf`) apontou como próximo passo testar `retry.py`/`probe.py`/`credentials.py`, hoje sem testes diretos. Verificação direta na árvore mostrou que `retry.py` já ganhou testes num commit anterior no mesmo dia (a observação de `cctnlf` já estava desatualizada). `probe.py` e `credentials.py` seguem sem testes — e a leitura linha a linha de `probe.py` encontrou um bug real: `_probe_one` não trata `DJENRateLimitedError` (HTTP 403), ao contrário de `engine.py` e `scripts/drain_unknowns.py`, que tratam explicitamente. Isso viola diretamente a regra do `CLAUDE.md` "403 nunca é ausência" e mata silenciosamente o worker afetado. `credentials.py` foi inspecionado mas seu fallback parcial parece design intencional, não bug — não selecionado.
