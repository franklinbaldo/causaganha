---
type: AgentDecision
id: "2026-09-07-exciting-mccarthy-vgrupn-decision-probe-over-credentials"
run_id: "2026-09-07-exciting-mccarthy-vgrupn"
goal_id: "2026-09-07-exciting-mccarthy-vgrupn-goal-probe-403-rate-limit"
question: "cctnlf's next_move named all three of retry.py/probe.py/credentials.py as untested. retry.py turned out to already have tests (test_retry.py, added by an earlier same-day commit). Between the two still genuinely untested modules, probe.py and credentials.py, which is this round's goal?"
choice: "probe.py's _probe_one, specifically its missing DJENRateLimitedError (403) handling. Rejected credentials.py: its get_ia_s3_auth() falls back from env vars to ~/.config/internetarchive/ia.ini whenever either IAS3_ACCESS_KEY or IAS3_SECRET_KEY is empty, including a partial case (e.g. access set, secret not). Read closely, this reads as intentional two-tier resolution (env wins only if BOTH halves are present, otherwise try the ini file in full) rather than a defect — there is no comparable cross-module contract it visibly violates the way probe.py visibly violates engine.py's/drain_unknowns.py's established DJENRateLimitedError handling, and this session's environment has no IA credentials to exercise it against a live failure mode anyway. Writing tests for credentials.py without a demonstrated defect would be coverage padding, a weaker TDD story per this project's own stated preference (issue #1244's round already favored a demonstrable bug over 'test-only, no demonstrated behavioral defect' work, and cctnlf's own text called the coverage-only angle 'a slightly weaker TDD story'). probe.py, by contrast, has a provable, currently-live defect with a clear before/after behavioral contract (crashes vs. skips) and an established sibling pattern (engine.py, drain_unknowns.py) to match exactly."
rationale: "Matches this project's recurring TDD selection heuristic across today's rounds (kfv7sx explicitly preferred a demonstrable defect over untested-but-working code in http_server.py; cctnlf explicitly ranked its Explore subagent's coverage-gap runner-up below its actual RED/GREEN bug fix). Picking the option with a real failure scenario (a 403 permanently and silently shrinking probe throughput mid-run, with zero log trace) over the option with no demonstrated failure scenario keeps this round's evidence anchored to an actual behavior change, not a coverage metric."
---

# Decisão: probe.py (403), não credentials.py

Entre os dois módulos ainda sem teste direto (`probe.py`, `credentials.py`), escolhido `probe.py` por ter um defeito real e demonstrável (403 derruba o worker silenciosamente) contra um padrão já estabelecido em `engine.py`/`drain_unknowns.py`. `credentials.py` foi descartado: seu fallback parcial parece design intencional, sem contrato visível violado — testá-lo agora seria só preencher cobertura, história de TDD mais fraca.
