---
type: AgentReading
id: "2026-09-09-exciting-mccarthy-0lpi0s-reading-okf"
run_id: "2026-09-09-exciting-mccarthy-0lpi0s"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-09-exciting-mccarthy-pf1xhn/run.md (most recent report in this AgentRun family); uv run okf-parser check knowledge --relational-schema okf.schema.sql"
finding: "Read pf1xhn's full report. It fixed _register_tjro_juris/_register_datajud_capa's missing IA fallback (PR #1356, merged as 574e5f7) and, in a follow-up commit on the same PR, fixed a real-network leak in scripts/render_contract_fixture.py caused by that same fix (the fixture script only patched renderer.ROOT-style globals, not reconcile_processos's own ROOT/DATA_DIR globals that ensure_juris_parquets()/ensure_datajud_parquets() read from -- so the vitest integration test fell through to a real, slow IA download and timed out at 120s until directly monkeypatched). next_move left three items: (1) spot-check the live /sobre page after the next deploy-web.yml run to confirm juris_totals.json/datajud_totals.json actually populate in production -- verified this round: deploy-web.yml run #277 (head 574e5f7, the merge commit) is the first production build carrying the fix; (2) a structural gap -- the fixture script's network isolation is split across two different patching styles (module-global reassignment for _try_download_parquet-based sources vs. direct function monkeypatch for reconcile_processos-based sources), so a future new IA-fallback source could silently reintroduce the same leak; (3) two low-priority leads already declined by 3+ consecutive rounds (coverageInsights.ts dead code; download_zip's 403 typing gap), still valid but still low-value. Traced render_queries.py/reconcile_processos.py directly: confirmed render_all() (line ~894-896) calls spec.register(con) for every ViewSpec with NO try/except wrapping it -- only the later run_query() call is guarded (against duckdb.CatalogException) -- so a plain, non-OSError/non-httpx.HTTPError exception raised inside a register function propagates uncaught out of render_all(). This makes item (2) directly fixable: a network-blocking guard installed around the real render_all() call in render_contract_fixture.py, raising a plain RuntimeError (not OSError/httpx.HTTPError, both of which existing code already catches-and-degrades) on any attempted urllib.request.urlopen or httpx.Client.send call, converts ANY future unmocked source's real-network fallthrough into an immediate loud crash during fixture rendering -- instead of either a silent 'optional contract skipped' warning or a 120s+ real download, both of which happened for different sources in this exact codebase already. okf-parser check knowledge --relational-schema okf.schema.sql at round start: conformant, 0 diagnostics, 904 concepts (up from 891 at pf1xhn's start, reflecting pf1xhn's own goal/decision/evidence/check instances plus its closing PR #1357)."
---

# Leitura do conhecimento OKF

A rodada anterior (`pf1xhn`) mesclou a PR #1356 (fallback IA para JURIS/DataJud) e corrigiu, na mesma PR, um vazamento de rede real no fixture usado pelo teste de integração do frontend. Deixou como pista estrutural: a lógica de isolamento de rede do fixture está dividida em dois estilos diferentes de patch, arriscando reintroduzir o mesmo vazamento para uma futura fonte com fallback IA. Investigação direta confirma que `render_all()` não captura exceções ao registrar uma view -- uma guarda de rede que levanta `RuntimeError` (não `OSError`/`httpx.HTTPError`, ambos já tratados como degradação silenciosa em outros pontos) converteria qualquer vazamento futuro em falha imediata e clara. `okf-parser check`: conformante, 904 conceitos.
