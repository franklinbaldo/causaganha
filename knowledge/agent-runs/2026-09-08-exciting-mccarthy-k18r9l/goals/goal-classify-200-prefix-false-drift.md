---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-k18r9l-goal-classify-200-prefix-false-drift"
run_id: "2026-09-08-exciting-mccarthy-k18r9l"
goal: "Fix scripts/backfill_probe.py's _classify() to treat djen_raw values of the form '200:<detail>' as 'available', matching the canonical interpret_djen_raw() rule, so the backfill-probe diagnostic stops reporting false DRIFT for rows stored in this legacy-but-live raw format."
rationale: "_classify() (scripts/backfill_probe.py:153-165) is this diagnostic script's own local re-implementation of the canonical djen_raw -> category mapping the whole pipeline agrees on. It matched a bare '200' but not the '200:<url>' variant that src/djen_backup/manifest.py's interpret_djen_raw() (line ~47: `djen_raw == '200' or djen_raw.startswith('200:')`) and src/djen_backup/absent_consistency.py's is_contradictory_200() both already treat as 'available'. This is not a hypothetical format: tests/fixtures/manifest_contract_rows.csv carries a real '200:https://example.test/djen.zip' row, and web/src/queries/site_status.qmd/render_manifest_parquet.py both handle it explicitly. Because _classify() is used both to bucket manifest rows (cat_counts, _sample_by_category) and, critically, by _diff_label() to compare a stored manifest djen_raw against a live re-probe's djen_raw, a manifest row stored in the legacy '200:<url>' format compared against a live re-probe returning bare '200' (the current engine's format) produced two different _classify buckets ('other:200:https://...' vs 'available') -- a false DRIFT on exactly the axis this diagnostic exists to catch. The backfill-probe workflow intentionally exits non-zero on any DRIFT so an operator sees every run's report; this bug meant a legacy-format row could trigger that failure signal for zero actual drift, eroding trust in the diagnostic's real positives."
success_signal: "A new test (tests/test_backfill_probe_classify.py::test_classify_available_on_200_with_detail_suffix) asserts _classify('200:https://example.test/djen.zip') == 'available'. RED before the fix ('other:200:https://example.test/djen.zip' != 'available'), GREEN after widening the '200' check to reuse djen_backup.absent_consistency.BARE_200_RAW/PREFIXED_200_RAW_PREFIX (the same constants src/djen_backup/absent_consistency.py already designates as the single source of truth other runtimes must build from). Full tests/test_backfill_probe_classify.py suite (8 tests, up from 7) green; ruff check and ruff format --check on both changed files clean; full uv run pytest -q shows no new failures beyond the 3 pre-existing draft-state AgentRun-completeness failures the scaffold documents."
status: "achieved"
---

# Goal: fix false DRIFT for the '200:<detail>' djen_raw format

Encontrado por um subagente Explore dedicado a varrer o código em busca de um bug novo, com forma RED/GREEN, fora das áreas já cobertas nesta rodada (README) ou em andamento por outra sessão (PR #1342). Verificado de forma independente lendo `_classify`/`_diff_label` em `scripts/backfill_probe.py`, `interpret_djen_raw` em `src/djen_backup/manifest.py`, `is_contradictory_200`/as constantes em `src/djen_backup/absent_consistency.py` (already designed as a fonte única para essa regra, citando explicitamente PR #1323 como o incidente que motivou sua criação), e confirmando o formato `"200:<url>"` como valor real em `tests/fixtures/manifest_contract_rows.csv`.
