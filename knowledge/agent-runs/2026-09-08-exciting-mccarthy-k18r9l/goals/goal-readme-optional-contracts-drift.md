---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-k18r9l-goal-readme-optional-contracts-drift"
run_id: "2026-09-08-exciting-mccarthy-k18r9l"
goal: "Make web/src/queries/README.md's 'Currently optional' contract list self-enforcing against the .qmd frontmatter it describes, and fix the drift it currently has (missing datajud_totals, datajud_classes)."
rationale: "web/src/queries/README.md:35-37 hand-maintains a prose list of which query contracts declare `optional: true` (contracts whose parquet source may legitimately not exist yet -- see scripts/render_queries.py's own RFC 0007 fail-loud contract: optional sources warn and skip, required sources fail --strict). This list had drifted: datajud_totals.qmd and datajud_classes.qmd both declare `optional: true` in their frontmatter but were never added to the README list when they were introduced (a lead first logged by the prior same-family round, 1c7t6u, as a docs-only, no-behavior-impact fix and never picked up since -- b4t8pv re-confirmed it still unaddressed). Left as hand-maintained prose, this list will drift again the next time an optional contract is added or removed, with nothing catching it -- exactly what happened here. A README describing a machine-checkable property (frontmatter flags scripts/render_queries.py itself reads) should be verified against that property automatically, not just read and trusted."
success_signal: "A new test (tests/test_query_readme_contract.py::test_readme_optional_contracts_list_matches_qmd_frontmatter) parses README.md's 'Currently optional' sentence and cross-checks it, set-equal, against every web/src/queries/*.qmd file's actual `optional: true` frontmatter flag (via scripts/render_queries.py's own parse_qmd()). RED against the unfixed README (missing datajud_totals, datajud_classes), GREEN after adding them to the README's list. The test itself is now a durable regression guard: it will fail again, for either repo, the next time the README list and the actual .qmd flags diverge in either direction (a name added to one side without the other)."
status: "achieved"
---

# Goal: self-enforcing README optional-contracts list

Pista carregada por duas rodadas anteriores (1c7t6u, b4t8pv) como "one-line docs fix, no behavior change" e nunca puxada, porque um fix puramente textual não se encaixa no fluxo TDD padrão da rodada. Resolvido dando-lhe forma RED/GREEN real: um teste que compara a lista de prosa do README com a flag `optional: true` de fato presente em cada `.qmd`, usando o mesmo parser (`scripts.render_queries.parse_qmd`) que o pipeline de renderização já usa em produção. O teste falhou (RED) exatamente nos dois nomes que a rodada anterior já havia identificado (`datajud_totals`, `datajud_classes`), confirmando o diagnóstico anterior; corrigido o README, teste passa (GREEN) e agora impede a mesma categoria de deriva no futuro, em qualquer direção.
