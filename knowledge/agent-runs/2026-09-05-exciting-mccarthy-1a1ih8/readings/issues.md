---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-1a1ih8-reading-issues"
run_id: "2026-09-05-exciting-mccarthy-1a1ih8"
subject: "open_issues"
reference: "mcp__github__list_issues(owner=franklinbaldo, repo=causaganha, state=OPEN) at session start (28 open issues); issue #1139's 5 comments (14:03-20:02:17Z on 2026-09-05), issue #1138's 6 comments (14:03-20:02:26Z), issue #1136's 1 comment (20:02:47Z), issue #950's comment history"
finding: "#1107 (contract(processo) MCP/Web parity) is CLOSED — the prior round (2026-09-05-exciting-mccarthy-ich5gz) merged its availability-parity slice (PR #1159, commit c9d6eca) and it no longer appears in the open list, so that thread of continuity is done for now. A separate, faster-moving automated triage process is actively re-scoping the web/UX issues (#1138, #1139, #1136) with 'Descoberta' comments re-validated against each new main commit, most recently at 20:02:xx — i.e. re-validated against c9d6eca, the exact commit this session started from. #1139's newest comment (5554428819, 20:02:17Z) is explicit and unambiguous: '/publicacoes' still opens with hero+métricas+a full attention-card about coverage gaps BEFORE <PublicationSearch>, contradicting the hierarchy already applied to /processo in #1151; it names this 'READY — prioridade 1 da próxima IMPLEMENTAÇÃO' with a concrete 5-point spec (search immediately after a short header; coverage/freshness reduced to a short line if needed to interpret the query; the long gaps explanation moved after the search/result or to a disclosure; explicitly preserve the 'ausência oficial / backfill pendente / falha temporária' distinction; capture desktop/mobile + a11y + tests/build). #1138's newest comment (5554429562, also 20:02:26Z) is 'prioridade 2' (consolidate /advogados and /comparador into /stats). #1136's only comment (5554431632, 20:02:47Z) is explicitly gated 'READY depois da PR de hierarquia de /publicacoes (#1139)' and is 'prioridade 3'. No open PR existed for any of these at session start. #950/#951 (MCP remote HTTP endpoint) remain open but untouched since #1152 merged; the segmenter/OPF cluster (#1047-1057, #884, #886-887) is an orthogonal ML research track with no TDD-shaped single-round slice. Given the triage's own explicit priority ordering, freshness (re-validated against this exact session's starting commit), and small/reversible scope, #1139's priority-1 item is the clear, unambiguous choice for this round — picking anything else would ignore an owner-authored priority list re-confirmed on the commit this session starts from."
---

# Leitura de issues abertas

A triagem automatizada re-validou #1139/#1138/#1136 no commit exato em que esta sessão começou (`c9d6eca`) e ordenou explicitamente: #1139 (hierarquia de `/publicacoes`) é prioridade 1, #1138 (consolidar `/advogados`/`/comparador` em `/stats`) é prioridade 2, #1136 (padronizar estados) é prioridade 3 e depende de #1139. #1107, o fio de continuidade das últimas rodadas, já fechou com a rodada anterior. Este round segue a prioridade 1: reordenar `/publicacoes` para busca-antes-de-metodologia, no mesmo padrão já aplicado a `/processo` em #1151.
