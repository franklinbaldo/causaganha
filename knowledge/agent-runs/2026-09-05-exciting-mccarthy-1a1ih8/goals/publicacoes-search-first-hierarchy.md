---
type: AgentGoal
id: "2026-09-05-exciting-mccarthy-1a1ih8-goal-publicacoes-search-first-hierarchy"
run_id: "2026-09-05-exciting-mccarthy-1a1ih8"
goal: "Reorder /publicacoes so the search action (PublicationSearch) renders immediately after a short header, with the extensive coverage/gaps explanation (the 'Cobertura e lacunas por tribunal' attention-card) moved below it — applying the same 'ação antes de metodologia/contexto' hierarchy #1151 already established for /processo, per issue #1139's priority-1 slice."
rationale: "Issue #1139's newest comment (5554428819, posted 20:02:17Z against this session's exact starting commit c9d6eca) explicitly names this 'READY — prioridade 1 da próxima IMPLEMENTAÇÃO': /publicacoes still renders hero + global metrics + a full attention-card about coverage gaps BEFORE <PublicationSearch client:load />, which inverts the desired order and makes the primary job (searching) compete with secondary context. The comment gives a concrete 5-point spec: (1) short header immediately followed by the search; (2) coverage/freshness reduced to a short line only if needed to interpret the query; (3) the long gaps explanation moved after the search/result or to disclosure; (4) explicitly preserve the 'ausência oficial / backfill pendente / falha temporária' distinction; (5) desktop/mobile capture + accessibility + tests/build green. This mirrors the already-reviewed and merged /processo precedent (#1151) exactly, so it is a small, reversible, well-scoped slice with no open design question, and it is the freshest, explicitly prioritized item in the whole open-issue backlog."
success_signal: "A new colocated test (web/src/pages/publicacoes/index.order.test.ts) encodes the contract as a structural-order assertion on the page source — no Astro container-render harness exists in this repo yet, and the page's frontmatter depends on a build-time-only data artifact (site-status.json) that doesn't exist outside a real `npm run build`, so a source-marker-order check is the cheapest faithful regression guard available. The test is RED against the original page (search renders after the attention-card) and GREEN after moving <PublicationSearch> immediately after the page-head section, with the attention-card (still containing the full coverage metrics and the 'ausência oficial / backfill pendente / falha temporária' distinction, byte-for-byte) moved below it. Full web vitest suite, `npm run lint`, and `npm run typecheck` (against its established, git-stash-confirmed baseline) all stay green with no new errors."
status: "achieved"
---

# Goal: /publicacoes — busca antes de metodologia (#1139, prioridade 1)

Fecha o item de prioridade 1 da fila mais recente de #1139: mover a ação de busca para logo após o cabeçalho, empurrando a explicação extensa de cobertura/lacunas para depois — a mesma gramática já aplicada e revisada em `/processo` (#1151).
