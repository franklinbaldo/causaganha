---
type: AgentGoal
id: "2026-09-05-exciting-mccarthy-1fxd8b-goal-evidence-matrix"
run_id: "2026-09-05-exciting-mccarthy-1fxd8b"
goal: "Implement issue #1130: a compact per-source evidence-summary strip on /processo, placed right after the existing 'O que este snapshot já responde' snapshot block and before avisos/detail sections, showing for each of the 4 sources (DJEN/JURIS/STJ/DataJud) its papel (Arquivo/Estado/Teor), its per-CNJ status (presente/ausente/indisponível) and a link to that source's existing detail section — reusing the dossier's existing fontes/avisos/cobertura fields with no new inference."
rationale: "Issue #1130 was explicitly blocked ('READY depois do primeiro slice de #1139 em /processo'); that first slice merged minutes before this round via PR #1151. It is a concretely scoped, single-round web slice consistent with this loop's continuity practice (following #1135's two-round precedent), and it closes a real product gap: today /processo shows source presence only as raw badges ('Fontes encontradas') with no distinction between a source having no record for this CNJ (ausente, normal) and a source that could not be queried at all (indisponível, an error) — exactly the distinction #1130's acceptance criteria call out, and exactly what processo.avisos + processo.cobertura already encode but nothing in the UI surfaces (cobertura is fetched by buscarProcesso() but, per exploration, never read by ProcessoLookup.svelte today)."
success_signal: "web/src/lib/processoCnj.test.ts gains a passing 'evidenceMatrixRows' describe block proving: all-4-present all classify 'presente'; a fonte absent from processo.fontes but with no matching aviso/cobertura-unavailable entry classifies 'ausente'; a fonte with either a matching avisos entry ('Fonte 'x' indisponível...') or a cobertura entry with status 'unavailable' classifies 'indisponivel' even if technically also absent from fontes (indisponibilidade takes precedence over ausência). A new ProcessoEvidenceMatrix.svelte component test proves it renders one row per ALL_FONTES with a visible textual status label (not color-only) and an anchor href to that source's existing detail section id (djen-title/datajud-title/documentos-title). ProcessoLookup.svelte renders the matrix, populated by evidenceMatrixRows(processo.fontes, processo.avisos, processo.cobertura), between the existing snapshot section and the avisos block, only when status === 'found'. Full web vitest suite stays green, eslint clean, astro typecheck delta unchanged from baseline, and a PR is opened against main."
status: "achieved"
---

# Goal: matriz de evidências por fonte em /processo (issue #1130)

Fatia única e completa do critério de aceite de #1130, desbloqueada pela fatia de hierarquia de #1139 (PR #1151) que acabou de mesclar.
