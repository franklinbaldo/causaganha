---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-1fxd8b-reading-okf"
run_id: "2026-09-05-exciting-mccarthy-1fxd8b"
subject: "okf_knowledge"
reference: "knowledge/projections/processo-consultar.md, knowledge/contracts/processo.md, knowledge/contracts/processo-ausente.md, knowledge/contracts/fonte-cobertura.md, knowledge/okf.schema.sql (Processo, FonteCobertura, DocumentoProcesso tables)"
finding: "The shared dossier projection ProcessoConsultar (knowledge/projections/processo-consultar.md) already models exactly the three dimensions issue #1130 asks the evidence-matrix strip to display, without any new inference: (1) papel/fonte presence via Processo.djen_id/juris_id/stj_id/datajud_id (nullable FK — null means that source's relation is absent for this CNJ) plus fontes_presentes VARCHAR[]; (2) per-source coverage/availability via FonteCobertura.status (free-text status column, e.g. 'loaded_remote' per the fixture, needs the actual TS union checked in code — likely covers indisponível/não publicado distinctly, not just present/absent); (3) freshness via Processo.dataset_gerado_em, which the processo-ausente.md fixture shows is explicitly nullable and MUST NOT be fabricated when the coverage report itself is unavailable (avisos: [relatorio_indisponivel] is how that absence is signaled, not a fake timestamp). Processo.encontrado is the CNJ-presence flag distinct from any single source's availability — a false encontrado means the CNJ itself was not found in any published source, which per #1130's acceptance criteria ('ausência de CNJ é visualmente distinta de indisponibilidade') must render differently from a source being merely indisponível/não publicado while the CNJ is encontrado=true via other sources. This is the exact contract a new visual component must consume read-only: it must not compute its own 'absent vs unavailable' logic, only branch on encontrado, the four nullable *_id relations, fontes_presentes, and FonteCobertura.status already produced upstream. No existing OKF type represents a UI-only summary-strip artifact (that lives in web/, not knowledge/) — this round's work is expected to be pure web code plus, if the model proves insufficient once web/src/lib/processoCnj.ts is read, a possible refinement of FonteCobertura.status's allowed values or documentation, not a wholesale new OKF concept."
---

# Leitura de conhecimento OKF

Confirma que ProcessoConsultar já contém as três dimensões (papel/presença de fonte, estado de cobertura por fonte, freshness do dataset) que #1130 pede na faixa-resumo, e que `encontrado=false` (CNJ ausente) é semanticamente distinto de uma fonte `indisponível`/`não publicado` com CNJ presente — a UI deve consumir essa distinção já produzida, não recriá-la.
