---
type: AgentGoal
id: "2026-09-06-exciting-mccarthy-3kjpfr-goal-drilldown-cobertura-por-tribunal"
run_id: "2026-09-06-exciting-mccarthy-3kjpfr"
goal: "Implementar #1131: em /stats, permitir selecionar um tribunal e um período e ver o recorte diário que o contrato tribunal_calendar realmente sustenta — dias preservados (uploaded) e dias com ausência confirmada (absent) — sem inventar pending/unknown por dia, com filtros refletidos na URL e paridade provada por teste contra o contrato."
rationale: "#1131 recebeu comentário READY do dono do repositório (franklinbaldo) em 2026-09-06T05:03:07Z, minutos antes desta sessão, com prioridade explícita 1 (acima de #1132, priorizada 2 e condicionada a #1131 ficar pequena). É a issue mais atual, mais bem escopada por slice explícito ('sem inventar novo backend/contrato'; 'não criar API; não adicionar gráfico pesado; não expandir schema diário') e diretamente testável por TDD: a lógica de classificação diária e de paridade contra o contrato é pura e não depende de credencial, GPU ou decisão de hosting pendente (ao contrário do cluster segmenter/TCU-TSE/MCP remoto, gated como toda rodada anterior já avaliou). Reutiliza um contrato já publicado (tribunal_calendar.qmd) e a mesma semântica de /publicacoes/[tribunal] (uploaded/absent), sem duplicar o TribunalCalendar.svelte existente que é um mock decorativo (dayStatus() gera status pseudo-aleatório a partir de hash, não lê dado real) da home page."
success_signal: "Um novo módulo puro web/src/lib/tribunalCoverageDrilldown.ts com testes RED-then-GREEN prova: (1) buildDailyStates() classifica cada dia do período em exatamente uploaded/absent/sem_evidencia, nunca inferindo ausência ou pending a partir da falta de linha no contrato; (2) um teste de paridade mostra que somar uploaded/absent do drill-down para um tribunal, sobre o intervalo mínimo-máximo de datas presentes no tribunal_calendar daquele tribunal, bate exatamente com a contagem de linhas do próprio contrato para esse tribunal (nenhuma linha perdida ou duplicada); (3) parseDrilldownQuery()/buildDrilldownQuery() fazem round-trip correto de tribunal+período pela querystring, com fallback seguro para tribunal desconhecido ou data malformada. Um novo componente Svelte (TribunalCoverageExplorer.svelte, estilizado só com Panda css()/recipes, sem custom properties novas) é integrado em /stats.astro sob a tabela de cobertura por tribunal já existente, atualiza a URL via history.replaceState ao trocar filtro, e mostra claramente 'sem evidência suficiente neste período' quando não há nenhum dia observado no recorte, nunca convertendo isso em 0% ou ausência. Suíte vitest completa, svelte-check (sem novos erros além da baseline pré-existente), eslint e build estático (astro build) continuam verdes; nenhum arquivo Python muda e ruff check/format/pytest -q continuam verdes."
status: "achieved"
---

# Goal: drill-down de cobertura por tribunal e período em /stats (#1131)

Fecha a issue #1131 com o slice exato que o dono priorizou: exploração compacta por tribunal + período sobre os dois estados diários que `tribunal_calendar` realmente prova, com URL compartilhável e paridade comprovada por teste contra o próprio contrato — sem inventar taxonomia nova nem criar backend/API novos.
