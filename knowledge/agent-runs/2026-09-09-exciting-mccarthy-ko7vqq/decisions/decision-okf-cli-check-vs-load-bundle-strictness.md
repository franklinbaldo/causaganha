---
type: AgentDecision
id: "2026-09-09-exciting-mccarthy-ko7vqq-decision-okf-cli-check-vs-load-bundle-strictness"
run_id: "2026-09-09-exciting-mccarthy-ko7vqq"
question: "This round's own edit to knowledge/agent-runs/2026-09-09-exciting-mccarthy-ktosqx/run.md accidentally introduced a duplicated YAML frontmatter key (evidence_ids appeared twice). `uv run okf-parser check knowledge --relational-schema okf.schema.sql` (the CLI command every round's checks rely on) reported conformant=true, 0 diagnostics immediately after that edit -- but `uv run pytest -q` then failed 14 tests in tests/causaganha_mcp/ (test_causaganha_status.py, test_okf_pipeline_catalog.py, test_status_contract.py) with `RuntimeError: knowledge bundle is not OKF-conformant`, traced to `okf_parser.load_bundle(Path('knowledge')).is_conformant` returning False with diagnostic OKF001 ('invalid YAML frontmatter: duplicated key in mapping: evidence_ids') -- a diagnostic the CLI's own check command never surfaced for the identical bundle state. Which signal should this and future rounds trust as the authoritative conformance check before pushing?"
choice: "Trust `okf_parser.load_bundle(...).is_conformant` (the same call src/causaganha_mcp/knowledge.py makes) as authoritative, not just the `okf-parser check` CLI's summary JSON. Fixed the duplicate key (removed the stale first evidence_ids block, kept the complete one) and re-verified both the CLI and load_bundle().is_conformant agree (true) before continuing. Re-ran the three previously-failing MCP test modules directly to confirm all 14 tests pass again."
rationale: "The CLI's `check --relational-schema` summary is what every round's scaffold instructs running as the baseline/final check, but it silently missed a real conformance defect (a duplicated YAML mapping key) that the exact same package's `load_bundle()` API -- the one actually imported and called by production code (src/causaganha_mcp/knowledge.py's load_pipeline_metadata) and exercised by tests/causaganha_mcp/test_causaganha_status.py, test_okf_pipeline_catalog.py, and test_status_contract.py -- correctly flags as OKF001. Whatever the root cause in okf-parser's own CLI-vs-library code paths, the practical lesson for this project is: `uv run pytest -q` (which exercises load_bundle transitively via the MCP status surface) is a strictly more reliable conformance signal than the CLI check alone, and should not be skipped in favor of it. This is now recorded so a future round doesn't re-trust a green CLI check result as sufficient proof the bundle is safe to push, and so a future round investigating a similar false-negative doesn't have to re-diagnose it from scratch. Not filed as an okf-parser upstream bug in this round (no access to that repo from here); a future round with such access could investigate why the CLI's own diagnostics list omitted OKF001 for a bundle load_bundle() flags as non-conformant."
---

# Decisão: confiar em `load_bundle(...).is_conformant`, não só no CLI `check`

Uma edição desta própria rodada introduziu uma chave YAML duplicada (`evidence_ids` duas vezes) em `knowledge/agent-runs/2026-09-09-exciting-mccarthy-ktosqx/run.md`. O comando CLI `okf-parser check --relational-schema` relatou `conformant: true` mesmo assim, mas `uv run pytest -q` falhou 14 testes em `tests/causaganha_mcp/` porque `okf_parser.load_bundle(...).is_conformant` (a mesma chamada usada pelo código de produção) detectou corretamente o diagnóstico `OKF001`. Corrigido removendo a chave duplicada; registrado para que rodadas futuras não confiem apenas no `check` da CLI como prova suficiente antes de fazer push.
