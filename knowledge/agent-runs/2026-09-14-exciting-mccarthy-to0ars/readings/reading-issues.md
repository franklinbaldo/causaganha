---
type: AgentReading
id: "2026-09-14-exciting-mccarthy-to0ars-reading-issues"
run_id: "2026-09-14-exciting-mccarthy-to0ars"
subject: "open_issues"
reference: "mcp__github__list_issues(owner=franklinbaldo, repo=causaganha, state=OPEN), 22 open issues, ordered by updated_at desc"
finding: "#1470 (Parquet/CNJ: auditar o catálogo e decidir regeneração por arquivo, part of epic #1468) is the most recently updated (2026-09-14T22:32:52Z, ~1h before this round started) and the most actionable: PR #1486 (merged as 94c180b, this round's HEAD) just added a NATIONAL_INDEX classification bucket to scripts/audit_cnj_parquets.py and re-ran the audit, but the fresh report (docs/planning/evidence/audit-cnj-parquets-2026-09-14.json, 24 files) still leaves 8 files (djen-cjf-2021, djen-tjam-2021, djen-tjap-2021, djen-tjdft-2021 x2 tables each) in the `verify_values` bucket -- footer-stats-inconclusive (single row group or null bounds), which issue #1470's own acceptance criteria explicitly says is NOT settled by footer stats alone ('Verificar os valores nos casos inconclusivos: ausência de sobreposição entre grupos não prova normalização ou ordenação interna'). No round has yet implemented the actual value-level verification this criterion calls for -- read_footer_stats only ever reads Parquet footer/KV metadata, never row content. This is real, unclaimed, in-scope work for #1470 that does not require IA write credentials (read-only, same as the existing audit). #1471/#1472/#1469/#1468 (the rest of the Parquet/CNJ reorder epic) are the continuation of the pilot already driven by Wisk this same afternoon (PRs #1473/#1474/#1478/#1480/#1483/#1484/#1486); their remaining step (publishing the reordered candidate to IA for a real apples-to-apples read-back, per handoffs/handoff-issue-1471-archive-readback-v2) is blocked in this environment: `env | grep -i IA_` returns nothing, no IA_ACCESS_KEY/IA_SECRET_KEY present, confirming the same credential gap every prior round (going back to at least 2026-09-11) has hit. #1482 (CORS classification) was fixed and merged this same afternoon (PR #1484, c65fd50) -- closed by that PR per its own body, no longer open work. The remaining 15 open issues (#950, #1022, #985, #951, #1093, and 10 segmenter issues #884/#886/#887/#1047/#1050/#1051/#1053/#1054/#1055/#1056/#1057) are all already recorded in `knowledge/backlog/` as blocked (ML/GPU annotation infra or IA credentials) with `last_verified_run_id` pointing at recent rounds; spot-checked knowledge/backlog/issue-1050.md (blocked: GPU training/active-learning annotation an unattended round cannot run) and issue-951.md (blocked: hosting/auth decision for a public MCP endpoint that is the repo owner's call, not a code change), both last verified 2026-09-07 and both still accurate in this environment -- no new investigation needed, citing the cached backlog entries instead of re-deriving them."
---

# Leitura das issues abertas

22 issues abertas. #1470 (auditoria Parquet/CNJ) é a mais recentemente atualizada e tem trabalho real, não reivindicado, dentro do próprio critério de aceite da issue: verificar valores (não só footer) dos 8 arquivos ainda em `verify_values` após a PR #1486 desta tarde. Não exige credenciais de escrita no IA. O resto do épico #1468-#1472 segue bloqueado na etapa de publicação por falta de `IA_ACCESS_KEY`/`IA_SECRET_KEY` neste ambiente -- confirmado de novo, mesmo gap de rodadas anteriores. As demais 15 issues abertas já estão cacheadas em `knowledge/backlog/` como bloqueadas (ML/GPU ou credenciais IA); não há necessidade de reinvestigar do zero.
