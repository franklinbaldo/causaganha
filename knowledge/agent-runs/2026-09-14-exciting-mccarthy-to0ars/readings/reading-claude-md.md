---
type: AgentReading
id: "2026-09-14-exciting-mccarthy-to0ars-reading-claude-md"
run_id: "2026-09-14-exciting-mccarthy-to0ars"
subject: "claude_md"
reference: "CLAUDE.md"
finding: "Read in full against the current working tree (HEAD 94c180b). Two runtime surfaces: Python backend (src/causaganha, src/djen_backup) and web frontend (web/, Astro 7 + Svelte 5). Load-bearing correctness rules for this lineage: djen_raw is the raw DJEN HTTP transport code, never a verdict on availability; genuine absent is 404, 400 (holiday), or HTTP 200 with body 'Sem comunicações' (no download URL) -- a bare djen_raw='200' is NOT proof of djen_status='available'; sync-manifest.parquet on IA is the single compacted source of truth (manifest-log/*.csv segments feed it, sync-manifest.csv is a derived export only); never treat 403 as absent (CloudFront/WAF rate-limiting); per-item lock + token bucket required for IA uploads, ItemBusyError re-queues rather than blocking. Style: no blind `except Exception` outside the ADR-0011 per-item worker-loop bulkhead (must call .exception(...) and cite the ADR); TRY300/TRY301/TRY401 enforced; ruff strict; Python 3.12+, `from __future__ import annotations`. CSS token boundary: Panda CSS via the cobogo preset is the one design system; three legacy Svelte islands (ProcessoLookup, PublicationSearch, SavedConsultations) keep --papel-*/--s-* aliases, Panda's `include` never scans .svelte so raw css() calls there are unreliable. Before committing: `uv run ruff check`, `uv run ruff format --check`, `uv run pytest -q`. Distinct from CLAUDE.md but load-bearing for THIS round's scaffold instructions: `.claude/hourly-loop.md` (introduced in the same commit as `.claude/agent-run-scaffold.md`, bc627c7/#1429) explicitly states the hourly loop is now operated exclusively by Wisk, and that new rounds must NOT create new AgentRuns (`knowledge/agent-runs/`, this very scaffold, and the AgentRun/AgentReading/AgentGoal/AgentDecision/AgentEvidence/AgentCheck types are 'legado histórico ... preserve para auditoria, mas não crie novos AgentRuns no loop horário'). This round's own scheduled prompt still instructs creating a new AgentRun report as its first action -- a direct conflict with the repo's own committed automation policy, already flagged without resolution by the two immediately preceding AgentRun rounds (njkncp/vd5dfq on 2026-09-11, bueov4 on 2026-09-14). Recorded here as a fact this round must act on, not just repeat."
---

# Leitura de CLAUDE.md

Leitura completa contra o HEAD atual (`94c180b`). Regras de domínio inalteradas em relação a rodadas anteriores (djen_raw/djen_status, sync-manifest.parquet como fonte única, estilo ruff estrito, fronteira Panda/cobogó). Achado novo e mais forte que "tensão observada": `.claude/hourly-loop.md`, no repositório desde a mesma PR que introduziu este scaffold, já determina explicitamente que o loop horário deve usar exclusivamente o Wisk e que **novos `AgentRun` não devem mais ser criados**. O prompt agendado desta sessão continua pedindo exatamente isso, sem ter sido atualizado.
