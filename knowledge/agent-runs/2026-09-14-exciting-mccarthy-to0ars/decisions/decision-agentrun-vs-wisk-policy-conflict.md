---
type: AgentDecision
id: "2026-09-14-exciting-mccarthy-to0ars-decision-agentrun-vs-wisk-policy-conflict"
run_id: "2026-09-14-exciting-mccarthy-to0ars"
goal_id: null
question: "This round's scheduled prompt explicitly instructs creating a new AgentRun report as its first action, following `.claude/agent-run-scaffold.md`. But `.claude/hourly-loop.md` -- committed to this same repository in the same PR (bc627c7/#1429) that introduced the scaffold -- states in the repo's own words that the hourly loop is now operated exclusively by Wisk and that new AgentRuns must NOT be created (`knowledge/agent-runs/`, the scaffold, and the six Agent* types are 'legado histórico ... preserve para auditoria, mas não crie novos AgentRuns'). Two immediately preceding AgentRun rounds (njkncp/vd5dfq on 2026-09-11, bueov4 on 2026-09-14) already noticed a version of this tension and asked a human to decide, without the scheduled prompt being updated since. Given the repo's own committed policy already answers the question those rounds left open, should this round refuse to create a new AgentRun and instead run `uv run wisk start`, or comply with the scheduled prompt as written?"
choice: "Comply with the scheduled prompt as written for this round -- create the AgentRun report, do the readings/goal/decision/evidence/check cycle it describes, and use it to drive one genuinely valuable, non-duplicative piece of work (see goal-verify-values-bucket) -- rather than unilaterally switching this session to `wisk start`. Escalate the conflict itself via a proactive notification to the repo owner, since two prior silent flags (buried in run.md prose merged days apart) have not produced a schedule update, and the conflict is now sharper: it is not an inferred tension anymore, it is a literal contradiction between this session's own stored instructions and the repository's own committed automation policy."
rationale: "A single autonomous, unattended round is the wrong place to unilaterally decide to stop honoring its own explicit scheduled instructions based on its own reading of a repo doc, however clear that doc is -- the scheduled prompt is itself an artifact the owner controls and may simply not have gotten around to updating yet (this is exactly what bueov4's own next_move predicted might happen). Silently switching mechanisms this round would also produce a confusing, undocumented one-off: a `wisk start` invocation from inside what looks like (and was invoked as) an AgentRun session, with no AgentRun record of the round happening at all -- worse for auditability than either finishing this AgentRun cleanly or being told explicitly to stop making them. Escalating via notification (rather than a third silent run.md paragraph) is the correct response to a scheduled, unattended routine hitting a real, actionable, repo-documented policy contradiction it cannot resolve on its own authority: exactly the case this session's own operating instructions describe as worth interrupting the owner for. The safest concrete action available without overstepping is to keep this round's real engineering work fully decoupled from the scaffold question -- goal-verify-values-bucket's tests/PR stand on their own regardless of how the AgentRun-vs-Wisk question is ultimately resolved -- so nothing here is wasted if the owner disables this schedule immediately after reading the notification."
---

# Decisão: manter o scaffold nesta rodada, escalar o conflito ao invés de decidir sozinho

O prompt agendado desta sessão continua pedindo a criação de um novo `AgentRun`; `.claude/hourly-loop.md`, já commitado no repositório, diz o oposto. Como uma rodada autônoma não deve decidir sozinha desligar seu próprio mecanismo de agendamento com base na sua própria leitura de um documento do repo, esta rodada cumpre o scaffold como instruído, usa-o para produzir um avanço real e desacoplado da questão (ver `goal-verify-values-bucket`), e escala o conflito via notificação proativa ao dono do repositório -- as duas tentativas anteriores de sinalizar isso em prosa de `run.md` não mudaram o agendamento.
