---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-uwm65t-reading-okf"
run_id: "2026-09-06-exciting-mccarthy-uwm65t"
subject: "okf_knowledge"
reference: "knowledge/backlog/index.md and its 17 issue files; knowledge/agent-runs/2026-09-06-exciting-mccarthy-usm2ot/run.md (previous round); tests/causaganha_mcp/test_web_agents_contract.py; tests/causaganha_mcp/test_arquivo_estado_teor_contract.py"
finding: "Prior round (usm2ot) closed a knowledge-model gap (BacklogItem) so future rounds stop re-deriving the same 17 blocked-issue rejections; its next_move explicitly hands off: read knowledge/backlog/ instead of re-investigating, and spend the saved effort on 'a new issue, new PR, or another loop-tooling deficiency'. This round found exactly the first case (#1217). Separately, tests/causaganha_mcp/test_web_agents_contract.py already implements the pattern issue #1217 asks for at the tool/fonte level (an HTML-parsing contract test comparing web/src/pages/agentes.astro's data-mcp-tool/data-mcp-fonte attributes against causaganha_mcp.server.build_server()'s live catalog and causaganha.decisoes.published.unpublished_fontes()) — the same authority-not-duplicated pattern should extend to the new per-job example questions #1217 asks for, rather than inventing a second contract mechanism. tests/causaganha_mcp/test_arquivo_estado_teor_contract.py already treats CNJ = \"00000010220248220001\" (masked 0000001-02.2024.8.22.0001, tribunal tjro) plus texto=\"dano moral\"/fonte=\"stj\" as this project's de facto golden fixture for composing processo_consultar/processo_estado/decisoes_buscar — reusing it satisfies #1217's 'golden case/fixture determinístico já conhecido pelo projeto' criterion without inventing a new one. No OKF type/schema change is needed for this round's selected work; it is pure product code (web + its MCP-catalog contract test), unlike the previous round's knowledge-model work."
---

# Leitura do conhecimento OKF

A rodada anterior (usm2ot) fechou a lacuna dos 17 issues bloqueados via `BacklogItem` e explicitamente indicou: usar o cache, e gastar o esforço poupado numa issue nova, PR nova ou outra lacuna do próprio loop. Esta rodada encontrou exatamente o primeiro caso (#1217). O padrão de teste de contrato já usado em `test_web_agents_contract.py` (parsear `agentes.astro` e comparar contra `build_server()`/`unpublished_fontes()` ao vivo) e o CNJ já tratado como fixture dourada em `test_arquivo_estado_teor_contract.py` (`00000010220248220001` / `dano moral` + `stj`) são a autoridade certa para reutilizar — nenhuma mudança de schema OKF é necessária para o trabalho desta rodada, que é código de produto (web + seu teste de contrato Python), diferente da rodada anterior.
