---
type: AgentDecision
id: "2026-09-06-exciting-mccarthy-yigsua-decision-conform-agentrun-field-names"
run_id: "2026-09-06-exciting-mccarthy-yigsua"
question: "This round's first-drafted AgentGoal/AgentDecision/AgentCheck records used field names (title/motivation for AgentGoal; decision/reason for AgentDecision; procedure for AgentCheck) that differ from every prior round's records. `uv run okf-parser check` passed anyway (conformant, 0 diagnostics). Should these records be conformed to the established field names, or are the invented names an acceptable evolution?"
choice: "Conform to the field names declared in knowledge/.okf/specs/agentgoal.schema.sql (goal, rationale, success_signal, status), agentdecision.schema.sql (question, choice, rationale, goal_id) and agentcheck.schema.sql (command, result, evidence_id, summary, goal_id) — the same names every prior round's records already use — rather than keep the invented alternatives."
rationale: "`uv run pytest -q` caught what `okf-parser check` did not: regenerating src/causaganha_mcp/_generated/domain_models.py and web/src/lib/processoConsultar.gen.ts against the knowledge bundle with the invented field names produced a real diff, flipping several of these types' fields from required to optional (AgentCheckConcept.command/summary, AgentDecisionConcept.choice/question/rationale, AgentGoalConcept.goal) and adding new never-before-seen optional fields (procedure, decision, reason) — silent contract drift that only a full pytest run surfaces, not the OKF conformance check itself, since AgentCheck/AgentGoal/AgentDecision Pydantic models are generated with `extra=\"allow\"` and okf-parser's own `check` command does not flag a concept file for using columns absent from its declared schema.sql (only required-field and foreign-key violations are diagnostics). Rewriting these three records to the schema-declared field names, then regenerating, produced a byte-for-byte zero diff against the committed generated files — restoring the intended architecture (schema.sql as the single source of truth for these types' shape) instead of letting this round's own vocabulary silently widen it. Recorded here as a note for a future round: `scripts/check_agent_run_completeness.py` (or a new companion check) could flag Agent*-family concept files using field names absent from their own knowledge/.okf/specs/*.schema.sql, catching this class of drift at `okf-parser check` time instead of at the final `pytest -q` gate."
---

# Decisão — conformar nomes de campo ao schema já estabelecido

Os primeiros rascunhos de `AgentGoal`/`AgentDecision`/`AgentCheck` desta rodada usaram nomes de campo diferentes dos já estabelecidos por todas as rodadas anteriores. `okf-parser check` não acusou isso (só valida obrigatoriedade/FK, não colunas extras), mas `pytest -q` acusou via diff nos arquivos gerados (`domain_models.py`/`processoConsultar.gen.ts`), que passaram a divergir do bundle. Corrigido conformando aos nomes de `knowledge/.okf/specs/agent*.schema.sql` — diff zero após a correção.
