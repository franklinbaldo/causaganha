---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-izm703-goal-fix-absent-uploaded-double-count"
run_id: "2026-09-08-exciting-mccarthy-izm703"
goal: "Make the public dashboard's absent/reliability metrics (web/src/queries/totals.qmd, tribunal_coverage.qmd, court_reliability.qmd) exclude manifest rows already marked ia_status='uploaded' from their 'absent' bucket, matching the same 'uploaded wins' priority already enforced by SyncManifest._categorize() in Python, by render_manifest_parquet.py's delta-merge, and by the pending/unknown filters in these exact same .qmd files."
rationale: "src/djen_backup/manifest.py's SyncManifest.mark_ia_uploaded() (called from engine.py's checker_worker IA-metadata-sync path) flips ia_status to 'uploaded' for any manifest row IA reports as archived, but never touches or clears that row's djen_status -- so a row that was previously checked and recorded djen_status='absent' (e.g. a transient/stale check before the item was later discovered on IA) can end up with BOTH ia_status='uploaded' AND djen_status='absent' simultaneously. This is a reachable, live state in SyncManifest, not theoretical. Every other place in the codebase that has to resolve this same ambiguity already treats 'uploaded' as authoritative: SyncManifest._categorize() (manifest.py:169-177) checks ia_status=='uploaded' first and returns before ever looking at djen_status; scripts/render_manifest_parquet.py's _apply_deltas explicitly guards its 'absent' UPDATE with 'AND (ia_status IS NULL OR ia_status != uploaded)' and its own docstring states outright that 'applying uploaded first means an archived item is never also flagged absent'; and the already-shipped tribunal_calendar.qmd/tests/test_render_queries.py's _TRIBUNAL_CALENDAR_SQL uses 'CASE WHEN ia_status=uploaded THEN uploaded WHEN djen_status=absent THEN absent END' (uploaded branch first, short-circuiting absent). But totals.qmd, tribunal_coverage.qmd, and court_reliability.qmd's own 'absent' COUNT(*) FILTER lacks the 'AND ia_status != uploaded' guard that their own 'pending' and 'unknown' filters two lines above already have -- so any row in this uploaded+stale-absent state is counted in BOTH the uploaded bucket and the absent bucket. This double-counts the same row on the public /stats totals card, the per-tribunal coverage table, and the court-reliability ranking's denominator (collected+absent), silently inflating totals and deflating displayed reliability rates for any affected tribunal."
success_signal: "A new test that renders the real totals.qmd, tribunal_coverage.qmd, and court_reliability.qmd (copied verbatim from web/src/queries/) against a manifest fixture containing one row with ia_status='uploaded' and djen_status='absent' (djen_raw='404', simulating a stale pre-upload check) fails before the fix -- RED, observing absent=1 alongside uploaded=1 so uploaded+pending+absent+unknown (2) != total (1), and court_reliability's rate denominator includes the row twice -- and passes after adding 'AND ia_status != 'uploaded'' to each file's 'absent' filter -- GREEN, absent=0, bucket sums equal total, reliability denominator counts the row once. Full Python suite (uv run pytest -q) stays green with the new test(s) added. ruff check and ruff format --check stay clean. No web/ files touched, so the web suite, eslint, and astro check are unaffected (still expected green at their current baseline)."
status: "achieved"
---

# Goal: corrigir dupla contagem absent+uploaded nas métricas públicas

`mark_ia_uploaded()` marca `ia_status='uploaded'` sem nunca limpar um `djen_status='absent'` obsoleto da mesma linha -- estado alcançável no `SyncManifest` real. `_categorize()` (Python), o compactor de deltas e o próprio `tribunal_calendar.qmd` já tratam `uploaded` como prioritário sobre `absent`; mas os filtros `absent` de `totals.qmd`, `tribunal_coverage.qmd` e `court_reliability.qmd` não têm a mesma guarda `AND ia_status != 'uploaded'` que seus próprios filtros `pending`/`unknown` já têm nas linhas ao lado. Corrigido adicionando a guarda faltante, com teste RED reproduzindo a dupla contagem e GREEN após a correção.
