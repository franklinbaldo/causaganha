---
type: AgentRun
id: "2026-09-08-exciting-mccarthy-izm703"
started_at: "2026-09-08T13:25:51Z"
completed_at: "2026-09-08T14:10:00Z"
branch_at_start: "claude/exciting-mccarthy-izm703"
commit_at_start: "2e2f32480683f2cf09ddf44eaf72d6c5788cf07c"
claude_md_reading_id: "2026-09-08-exciting-mccarthy-izm703-reading-claude-md"
issues_reading_id: "2026-09-08-exciting-mccarthy-izm703-reading-issues"
prs_reading_id: "2026-09-08-exciting-mccarthy-izm703-reading-prs"
okf_reading_id: "2026-09-08-exciting-mccarthy-izm703-reading-okf"
goal_ids:
  - "2026-09-08-exciting-mccarthy-izm703-goal-fix-absent-uploaded-double-count"
primary_goal_id: "2026-09-08-exciting-mccarthy-izm703-goal-fix-absent-uploaded-double-count"
considered_work:
  - "17 open GitHub issues, the same set every same-day round in this family has found, all still blocked for the same reasons re-verified this round (segmenter needs GPU/annotation; #950/#951 need an infra hosting decision from the repo owner; #1011/#1022 need IAS3 credentials absent from this environment; #985 blocked on TSE 403; #1093 explicitly deprioritized by its own body) -- not actionable."
  - "One open PR (#1322), but it belongs to a separate 'Wisk-loop' tracking family (records .wisk/knowledge, not this repo's knowledge/agent-runs/ AgentRun contract) -- not this family's to resume or drive, all 9 checks already green, left untouched."
  - "The prior round in this family (obl3ux) left two concrete, lower-priority leads in its next_move: (a) ~55% dead code in web/src/lib/coverageInsights.ts (5 exported functions, zero call sites outside the file, zero test coverage, orphaned by TribunalCalendar.svelte's deletion) -- re-verified still present and still dead this round (grep across web/src for each export name); (b) src/djen_backup/djen.py's download_zip() not explicitly raising DJENRateLimitedError on 403 like get_caderno_url does -- re-traced this round through both callers (engine.py's download_worker and drain.py's _drain_one, the latter fixed for the sibling issue in PR #1305 by the parallel Wisk-loop family) and confirmed both already catch DJENRateLimitedError and generic httpx.HTTPError identically (same log-and-skip behavior, neither marks the pair absent) -- still a typing/log-message inconsistency only, not a live behavioral bug. Neither selected as this round's primary goal; (a) remains available as a non-TDD-shaped cleanup fallback."
  - "Dispatched an Explore subagent to survey areas not recently touched by either family today (src/causaganha_mcp/, src/causaganha/processos/, src/segmenter_dataset/, scripts/, web/src/queries/*.qmd, contracts.ts, remaining djen_backup modules), explicitly told what was already fixed/rejected today to avoid duplicate proposals. It returned one well-evidenced, high-value candidate (below) plus a note that causaganha_mcp/, processos/cnj.py's CNJ check-digit formula, dedup.py, retry.py, and archive.py were all checked and found sound, and no TODO/FIXME markers exist in the target directories."
  - "Independently re-verified the Explore agent's candidate by reading the three .qmd files directly (web/src/queries/totals.qmd:16, tribunal_coverage.qmd:16, court_reliability.qmd:17), src/djen_backup/manifest.py's _categorize() (169-177) and mark_ia_uploaded() (226-243), scripts/render_manifest_parquet.py's _apply_deltas docstring, and this repo's own tests/test_render_queries.py::_TRIBUNAL_CALENDAR_SQL (already correctly prioritizing uploaded over absent) -- confirmed a real, reachable, live bug: totals.qmd, tribunal_coverage.qmd, and court_reliability.qmd's 'absent' COUNT(*) FILTER lacks the 'AND ia_status != uploaded' guard that their own neighboring 'pending'/'unknown' filters already have, so a manifest row that is both ia_status='uploaded' and has a stale djen_status='absent' (reachable because mark_ia_uploaded() never clears djen_status) is double-counted on the public /stats totals card, per-tribunal coverage table, and court-reliability ranking."
selected_work: "Fixed a real double-counting bug in three public reporting queries (web/src/queries/totals.qmd, tribunal_coverage.qmd, court_reliability.qmd): their 'absent' COUNT(*) FILTER counted a manifest row even when that same row was already ia_status='uploaded', because unlike the 'pending'/'unknown' filters in the exact same files, the 'absent' filter was missing 'AND ia_status != uploaded'. Root cause: SyncManifest.mark_ia_uploaded() (manifest.py) flips ia_status to 'uploaded' for any date IA reports as archived without ever clearing a previously-recorded, now-stale djen_status='absent' -- so a row with both fields set simultaneously is a reachable live state, not theoretical. Every other place in the codebase that resolves this same ambiguity (SyncManifest._categorize(), render_manifest_parquet.py's _apply_deltas, and this repo's own tribunal_calendar.qmd/its mirrored test SQL) already treats 'uploaded' as authoritative over a stale 'absent' -- these three reporting queries were the one place that forgot to apply the same rule internally. Added a fixture (manifest_parquet_uploaded_and_stale_absent) and three RED tests (rendering the real .qmd files against it, asserting the affected row's absent bucket must be 0) to tests/test_render_queries.py, confirmed each failed exactly as predicted (absent=1 instead of 0, so uploaded+pending+absent+unknown != total), then fixed each file's absent filter with the missing 'AND ia_status != uploaded' guard and confirmed GREEN."
expected_behavior: "A manifest row with ia_status='uploaded' and a stale djen_status='absent' contributes to the uploaded bucket only, never also to absent, in totals.json, tribunal_coverage.json, and court_reliability.json -- this failed before the fix (RED, absent=1 alongside uploaded=1, bucket sums != total) and passes after (GREEN, absent=0, bucket sums == total, reliability rate=1.0 instead of 0.5). tests/test_render_queries.py grows from 31 to 34 tests, all green. Full uv run pytest -q stays green except the one report-completeness gate expected while this run.md was mid-draft. ruff check and ruff format --check stay clean. No web/ TypeScript/Svelte files touched (pure SQL + Python test change), so the web suite, eslint, and astro check are unaffected. A PR is opened and driven to a green, mergeable state."
entry_state: "new"
target_state: "merged"
decision_ids:
  - "2026-09-08-exciting-mccarthy-izm703-decision-sql-guard-not-python-clear"
evidence_ids:
  - "2026-09-08-exciting-mccarthy-izm703-evidence-red-tests"
  - "2026-09-08-exciting-mccarthy-izm703-evidence-green-tests"
  - "2026-09-08-exciting-mccarthy-izm703-evidence-diff-fix"
  - "2026-09-08-exciting-mccarthy-izm703-evidence-pr-1326-opened"
check_ids:
  - "2026-09-08-exciting-mccarthy-izm703-check-okf-parser-baseline"
  - "2026-09-08-exciting-mccarthy-izm703-check-python-suite"
  - "2026-09-08-exciting-mccarthy-izm703-check-okf-parser-mid-round"
result_state: "review"
result_summary: "Fixed a real, live, currently-reachable double-counting bug in three public reporting queries. src/djen_backup/manifest.py's SyncManifest.mark_ia_uploaded() (called from engine.py's checker_worker IA-metadata-sync path) flips ia_status to 'uploaded' for any date IA reports as archived, without ever clearing a stale djen_status='absent' left over from an earlier check -- so a manifest row can genuinely carry both ia_status='uploaded' and djen_status='absent' at once. Every other place in the codebase that resolves this exact ambiguity (SyncManifest._categorize(), render_manifest_parquet.py's _apply_deltas, and this repo's own tribunal_calendar.qmd/its test-mirrored SQL) already treats 'uploaded' as authoritative and short-circuits before 'absent' -- but web/src/queries/totals.qmd, tribunal_coverage.qmd, and court_reliability.qmd's own 'absent' COUNT(*) FILTER lacked the 'AND ia_status != uploaded' guard their neighboring 'pending'/'unknown' filters in the same files already had, so any affected row was counted in both the uploaded bucket and the absent bucket -- inflating totals on the public /stats totals card and per-tribunal coverage table, and deflating the court-reliability ranking's displayed rate for any tribunal with such a row. Wrote a fixture and 3 RED tests in tests/test_render_queries.py rendering the real .qmd files against a manifest with exactly this row shape, confirmed each failed as predicted (absent=1, bucket sums off by one), then added the missing 'AND ia_status != uploaded' guard to each file's absent filter (matching the already-precedented pattern) and confirmed GREEN. tests/test_render_queries.py: 34/34 passed (up from 31). Full uv run pytest -q: green except the report-completeness gate expected while run.md was mid-draft; the two generated-schema drift tests the scaffold warns about were independently confirmed still green in isolation. ruff check and ruff format --check: clean. Diff scope: 3 one-line .qmd changes + the new test file, no web/TS/Svelte files touched. okf-parser check knowledge --relational-schema okf.schema.sql run at baseline (769 concepts, 3 expected OKF022 diagnostics for this round's own not-yet-filled AgentRun id, matching every prior round's same-stage result) and again after run.md was filled in (conformant, 0 diagnostics, 778 concepts). PR #1326 opened onto main at commit c520fe7; this session subscribed to its activity."
next_move: "PR #1326 opened onto main at commit c520fe7 (https://github.com/franklinbaldo/causaganha/pull/1326); this session subscribed to its activity to drive it to a green, mergeable state and merge per this repo's continuity convention -- a follow-up commit will update result_state/result_summary here once CI resolves, without erasing completed_at. If issue/PR queue is again exhausted next round, two leads remain available and unclaimed: (a) coverageInsights.ts's ~55% dead code (cleanup-shaped, no RED test possible); (b) download_zip()'s missing explicit DJENRateLimitedError on 403 (cosmetic typing gap, confirmed three times now to have no live behavioral effect since both callers already catch DJENRateLimitedError and httpx.HTTPError identically). A new lead surfaced but not pursued this round: SyncManifest.mark_ia_uploaded() itself could also clear/reset a stale djen_status when flipping ia_status to 'uploaded', which would fix the same ambiguity at its source rather than at each reporting query -- deliberately not done this round (see decision-sql-guard-not-python-clear.md) because it touches the live runtime's incremental counts-cache invariant for no additional observable benefit once the reporting-layer fix lands; worth reconsidering only if a new downstream consumer of djen_status is found that doesn't already prioritize ia_status the way _categorize()/render_manifest_parquet.py do."
---

# Agent run

Corrigida dupla contagem real em três queries públicas (`totals.qmd`, `tribunal_coverage.qmd`, `court_reliability.qmd`): uma linha do manifesto com `ia_status='uploaded'` e `djen_status='absent'` obsoleto (estado alcançável porque `mark_ia_uploaded()` nunca limpa `djen_status`) era contada tanto como `uploaded` quanto como `absent`, ao contrário de todo o resto do código, que já trata `uploaded` como prioritário. TDD: fixture + 3 testes RED reproduzindo a dupla contagem em `tests/test_render_queries.py`, correção mínima (`AND ia_status != 'uploaded'` nos três filtros `absent`, replicando a guarda já presente em `pending`/`unknown` nos mesmos arquivos), GREEN confirmado. Suíte completa e `ruff` verdes (exceto o gate de completude do próprio relatório, esperado em rascunho). PR ainda não aberta neste ponto do relatório — próxima ação.
