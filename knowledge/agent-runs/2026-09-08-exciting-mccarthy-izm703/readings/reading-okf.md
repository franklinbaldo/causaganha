---
type: AgentReading
id: "2026-09-08-exciting-mccarthy-izm703-reading-okf"
run_id: "2026-09-08-exciting-mccarthy-izm703"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-08-exciting-mccarthy-obl3ux/run.md (most recent report in this AgentRun family); uv run okf-parser check knowledge --relational-schema okf.schema.sql"
finding: "Read obl3ux's report in full. Its next_move left two concrete, unacted leads: (a) ~55% dead code in web/src/lib/coverageInsights.ts (5 exported functions, zero call sites, zero test coverage, orphaned by TribunalCalendar.svelte's deletion two rounds ago); (b) src/djen_backup/djen.py's download_zip() not explicitly raising DJENRateLimitedError on 403 like get_caderno_url does. Independently re-verified both directly: (a) confirmed still present and still fully dead (grep for every export name across web/src turns up zero call sites outside the file and its own test, which only exercises buildTribunalAttentionCards); (b) confirmed still present, and re-traced through both real callers (engine.py's download_worker and drain.py's _drain_one, the latter fixed for the sibling DJENRateLimitedError-swallowing gap by the parallel 'Wisk-loop' family's PR #1305 since obl3ux's close) -- both already catch DJENRateLimitedError and generic httpx.HTTPError identically, so this remains a typing/log-message inconsistency only, not a live behavioral bug, same conclusion as the last two rounds that considered it. Neither (a) nor (b) was strong enough to be this round's primary goal, so dispatched an Explore subagent (told explicitly what was already fixed or rejected today by both families, to avoid duplicate proposals) to survey less-recently-touched areas (causaganha_mcp/, processos/, segmenter_dataset/, scripts/, web/src/queries/*.qmd, contracts.ts, remaining djen_backup modules). It returned a well-evidenced, high-value candidate: totals.qmd/tribunal_coverage.qmd/court_reliability.qmd's 'absent' COUNT(*) FILTER lacks the 'AND ia_status != uploaded' guard its own 'pending'/'unknown' filters already have, so a manifest row with a stale djen_status='absent' left over from before mark_ia_uploaded() later flipped ia_status to 'uploaded' is double-counted on three public dashboard endpoints. Independently re-verified by reading all three .qmd files, manifest.py's _categorize()/mark_ia_uploaded(), render_manifest_parquet.py's _apply_deltas docstring, and this repo's own tribunal_calendar.qmd (which already resolves the same ambiguity correctly) -- confirmed real and selected as this round's goal. Ran uv run okf-parser check knowledge --relational-schema okf.schema.sql at round start (right after the scaffold + first three readings, before this round's own run.md had its id filled in): conformant=false, 3 expected OKF022 diagnostics (AgentReading_run_id_id_fkey referencing an AgentRun.id not yet present), concept_count=769 (up from obl3ux's close-time count via #1319/#1323/#1325 landing on main since). Re-ran mid-round after run.md's own id/goal_ids/decision_ids/evidence_ids/check_ids were filled in: conformant=true, 0 diagnostics, concept_count=777."
---

# Leitura do conhecimento OKF

A rodada anterior desta família (`obl3ux`) deixou duas pistas no `next_move`, ambas reverificadas e ainda de baixo valor (limpeza de código morto sem forma RED/GREEN; inconsistência de tipagem em `download_zip` sem efeito comportamental, confirmado pela terceira vez). Um subagente Explore, orientado sobre o que já foi corrigido/descartado hoje por ambas as famílias, encontrou um candidato forte e verificado de forma independente: dupla contagem de linhas `uploaded`+`absent` obsoleto em três queries públicas. `okf-parser check` no início: 3 diagnósticos esperados (FK do próprio `run.md` ainda vazio); no meio da rodada, após preencher `run.md`: conformante, 0 diagnósticos, 777 conceitos.
