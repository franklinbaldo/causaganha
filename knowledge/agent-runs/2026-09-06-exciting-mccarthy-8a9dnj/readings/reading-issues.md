---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-8a9dnj-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-8a9dnj"
subject: "open_issues"
reference: "17 open issues in franklinbaldo/causaganha as of 2026-09-06T13:26Z (mcp__github__list_issues, state=OPEN); knowledge/backlog/index.md and its 17 issue-<n>.md BacklogItem files, last verified by round usm2ot at 2026-09-06T12:00Z"
finding: "The set of open issue numbers (985, 1022, 950, 1093, 951, 1011, 1057, 1056, 1055, 1054, 1051, 1047, 1053, 1050, 884, 887, 886) matches knowledge/backlog/ exactly (diffed the two lists, zero delta), and last_verified_at for all 17 is under 90 minutes old — per knowledge/backlog/index.md's own stated staleness bar ('só reabra a investigação se... last_verified_at está muito antigo'), no re-investigation was warranted and none was performed beyond confirming the set is unchanged. Read #1022's own comment thread in full regardless (15 comments, oscillating discovery notes from many prior rounds) to independently verify the backlog's 'credentials' classification still holds: confirmed live that `src/tcu_acordaos/publish.py` and `scripts/tcu_acordaos_publish_teor.py` already exist on `main` (git ls-tree origin/main), and that this session's env has no IAS3_ACCESS_KEY/IAS3_SECRET_KEY — consistent with the cache. Also independently investigated a candidate from round nao666's next_move (benchmark scripts/benchmarks/row_group_size.py against a real production djen-{tribunal}-{year} Parquet, to inform docs/planning/parquet-storage-optimization-plan.md's still-[speculative] A1b row-group-size question) and found it is not currently executable: the causaganha-catalog manifest.parquet (downloaded and queried live) only lists per-day legacy items (djen-YYYY-MM-DD, discontinued per CLAUDE.md's own IA-naming note) with tiny row counts (27 rows, 1 row group, confirmed by downloading djen-2026-02-16/comunicacoes.parquet) — no djen-{tribunal}-{year} consolidated Parquet has actually been uploaded to IA yet, so there is no real 'large tribunal-year' production file to benchmark against. This is a dead end, not a live candidate, until the consolidation pipeline's new-format items are actually published."
---

# Leitura das issues abertas

O conjunto das 17 issues abertas é idêntico ao cache `knowledge/backlog/`, verificado há menos de 90 minutos pela rodada usm2ot — dentro do próprio critério de frescor do cache, então nenhuma reinvestigação era necessária. Reli a `#1022` inteira mesmo assim e confirmei ao vivo (git ls-tree + env) que a classificação `credentials` segue válida. Também investiguei o candidato de rodada anterior (benchmark A1b contra Parquet real de produção) e descobri que é um beco sem saída agora: o `manifest.parquet` do `causaganha-catalog` só lista itens legados por-dia, minúsculos (27 linhas), sem nenhum item `djen-{tribunal}-{year}` consolidado publicado ainda no IA para servir de amostra "grande".
