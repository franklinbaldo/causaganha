---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-yz281l-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-yz281l"
subject: "open_issues"
reference: "mcp__github__list_issues (state=OPEN, 22 issues) + issue_read em #1468, #1470, #1472, #1482"
finding: "22 issues abertas. O cluster mais ativo e recentemente tocado é o epic #1468 (Parquet nativo: otimizar consulta por CNJ), com sub-issues #1469 (unificar escrita/leitura -- ainda não iniciado), #1470 (auditoria -- entregue por PR #1487, evidência em docs/planning/evidence/audit-cnj-parquets-2026-09-14.json, mas o checklist da própria issue segue sem marcar as caixas), #1471 (piloto TJRO 2026 -- validado localmente, PR #1478/#1480 mergeados) e #1472 (regeneração/publicação real no IA -- bloqueado: `env | grep -i 'IA_\\|ARCHIVE'` vazio nesta sessão, igual a toda rodada desde 11/09). O checklist do próprio #1468 tem 4 itens, nenhum marcado; investigação desta rodada (ver decision) mostrou que o item 1 ('Implementação compatível incorporada e validada') está de fato quase todo implementado em produção (`src/causaganha/consolidate/exporter.py`, commit 4c13ef1 / PR #1473): normalização CNJ 20 dígitos, ORDER BY numero_processo/data/id, ZSTD e marcador de certificação `causaganha.layout=cnj-text-sorted-v1` em KV metadata -- mas o ROW_GROUP_SIZE explícito (A1b do plano) segue '[speculative]', nunca medido contra dado real de produção, só benchmark sintético (`scripts/benchmarks/row_group_size.py`). issue #1482 (CORS no download direto do archive.org) teve seu follow-up de CI fechado pela rodada anterior (PR #1491, probe Python + workflow agendado), mas o bug em si (DuckDBExplorer.svelte não consegue ler Parquet via read_parquet() contra archive.org/download) segue aberto -- confirmei ao vivo nesta rodada que a alternativa `s3.us.archive.org` também manda CORS (`access-control-allow-origin: *`, inclusive no preflight OPTIONS), mas seu redirect final aponta para um host `http://...s3dns.us.archive.org` sem HTTPS válido (cert mismatch confirmado via curl) -- inutilizável a partir de uma página HTTPS por mixed-content, então a classificação atual (cors-blocked, não corrigível sem proxy) permanece correta; construir um proxy público segue fora de escopo (decisão de infra de rounds anteriores, não revisitada aqui). Demais issues (#950, #951, #1093, #1022, #985, cluster segmenter #1047-#1057/#884/#886/#887) seguem sem novidade -- bloqueadas por infra de GPU/anotação ou por credenciais de escrita no IA, confirmado sem mudança desde rodadas anteriores."
---

# Leitura de issues abertas

22 issues abertas revisadas via `mcp__github__list_issues` + leitura detalhada de #1468, #1470, #1472 e #1482 via `issue_read`. O epic Parquet/CNJ (#1468-#1472) é o cluster mais maduro e mais próximo de fechar: auditoria (#1470) e piloto (#1471) entregues, publicação real (#1472) bloqueada por falta de credenciais IA_ACCESS_KEY/IA_SECRET_KEY nesta sandbox (confirmado, sem mudança). O item 1 do checklist de #1468 (implementação de produção) está quase inteiramente feito, exceto a decisão A1b (ROW_GROUP_SIZE) do plano em `docs/planning/parquet-storage-optimization-plan.md`, que segue `[speculative]` por nunca ter sido medida contra um arquivo Parquet real de produção -- só contra dados sintéticos. Essa é a lacuna concreta e desbloqueada (não depende de credenciais de escrita) escolhida como trabalho desta rodada.

Investiguei também, ao vivo, se `s3.us.archive.org` resolveria o CORS de #1482 sem precisar de proxy novo: manda CORS corretamente, mas seu redirect final é HTTP puro para um host sem certificado válido -- inutilizável de uma página HTTPS (mixed content). Confirma que a classificação atual do dashboard está correta e que um proxy dedicado continua sendo a única correção real, fora do escopo desta rodada (decisão de infra que exige dono explícito, como já registrado por rounds anteriores).
