---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-yz281l-reading-okf"
run_id: "2026-09-15-exciting-mccarthy-yz281l"
subject: "okf_knowledge"
reference: ".claude/hourly-loop.md; knowledge/agent-runs/index.md; knowledge/agent-runs/2026-09-14-exciting-mccarthy-bueov4/run.md; knowledge/agent-runs/2026-09-15-exciting-mccarthy-50ns70/run.md; docs/planning/parquet-storage-optimization-plan.md"
finding: "`.claude/hourly-loop.md` e `knowledge/agent-runs/index.md` declaram, sem ressalva, que o mecanismo AgentRun é legado histórico e que o loop horário migrou inteiramente para o runtime Wisk (`.wisk/`) -- 'não crie novos AgentRun aqui'. Essa é a mesma tensão já identificada e registrada como AgentDecision por três rodadas consecutivas (bueov4 e to0ars em 2026-09-14, que já enviaram notificação proativa ao usuário sobre o conflito de configuração; 50ns70 em 2026-09-15 optou por não repetir a notificação por não haver informação nova). O runtime que efetivamente disparou esta sessão é o mesmo scaffold `AgentRun` -- o prompt agendado não mudou. Rodadas anteriores (bueov4, to0ars, 50ns70) resolveram essa tensão seguindo a instrução explícita do prompt agendado (criar o AgentRun) mas escolhendo trabalho que não compete com o Wisk. Mantenho a mesma linha nesta rodada (ver decision). Leitura de `docs/planning/parquet-storage-optimization-plan.md`: documenta a estratégia aprovada do epic #1468 -- ORDER BY por CNJ já habilita pruning de row groups sem depender de ROW_GROUP_SIZE explícito (A1, `[verified]`); o ajuste explícito do tamanho do row group (A1b) é uma otimização independente, marcada `[speculative]`, com instrução explícita de 'rodar benchmark contra arquivos de produção antes de fixar um valor não-default' -- só existe hoje como benchmark sintético (`scripts/benchmarks/row_group_size.py`), nunca rodado contra um arquivo Parquet real. Essa é a lacuna concreta escolhida como goal desta rodada."
---

# Leitura de conhecimento OKF

Reconfirmei a tensão AgentRun-vs-Wisk (`.claude/hourly-loop.md`, `knowledge/agent-runs/index.md`): nenhuma mudança desde a última vez que foi lida e escalada (rounds bueov4/to0ars, 2026-09-14; reconfirmada sem nova notificação por 50ns70, 2026-09-15). Sigo a mesma linha: obedecer ao prompt agendado (que continua instruindo o scaffold legado, sem ressalva), mas escolher trabalho que não duplica nem compete com o Wisk.

Lida também a leitura factual mais relevante para o trabalho de domínio desta rodada: `docs/planning/parquet-storage-optimization-plan.md`. O plano é explícito sobre o que falta para fechar o item 1 do checklist de #1468 -- o ajuste de `ROW_GROUP_SIZE` (A1b) segue `[speculative]` porque só foi testado com dados sintéticos (`scripts/benchmarks/row_group_size.py`), nunca contra um Parquet real já publicado no Internet Archive. Essa lacuna não depende de credenciais de escrita (é leitura pública + benchmark local) e tem sinal de sucesso observável: medir de fato e atualizar o plano com uma decisão fundamentada em dado real, não mais especulação.
