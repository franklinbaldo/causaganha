---
type: AgentGoal
id: "2026-09-15-exciting-mccarthy-yz281l-goal-row-group-size-a1b"
run_id: "2026-09-15-exciting-mccarthy-yz281l"
goal: "Resolver o flag `[speculative]` do item A1b (ROW_GROUP_SIZE explícito) em docs/planning/parquet-storage-optimization-plan.md, medindo o efeito de diferentes ROW_GROUP_SIZE contra um arquivo Parquet REAL de produção (djen-tjro-2026/comunicacoes.parquet, 1.041.723 linhas, já no layout novo `cnj-text-sorted-v1`), não apenas dados sintéticos -- e, a partir da medição, decidir e registrar se `src/causaganha/consolidate/exporter.py` deve fixar um ROW_GROUP_SIZE explícito ou manter o default do DuckDB, fechando item 1 do checklist de #1468 de forma auditável."
rationale: "O epic #1468 (Parquet nativo por CNJ) é o cluster de issues mais maduro e ativo do repositório. Publicação real (#1472) está bloqueada por falta de credenciais IA_ACCESS_KEY/IA_SECRET_KEY (confirmado sem mudança desde 11/09). Mas o item 1 do checklist de #1468 -- a implementação de produção em si -- está quase pronto: CNJ normalizado, ORDER BY, ZSTD e marcador de certificação já estão em exporter.py (PR #1473/4c13ef1). A única peça que falta é uma decisão fundamentada sobre ROW_GROUP_SIZE, que o próprio plano marca como '[speculative] até medir em produção' e que só foi benchmarcada com dados sintéticos (scripts/benchmarks/row_group_size.py). Essa lacuna é real, está desbloqueada (leitura pública do IA + benchmark local, sem precisar de credenciais de escrita) e fecha um item de arquitetura pendente com evidência real, em vez de mais uma rodada bloqueada pelo mesmo motivo de sempre."
success_signal: "docs/planning/parquet-storage-optimization-plan.md deixa de marcar A1b como '[speculative]' e passa a citar uma medição real (row groups tocados por CNJ point-lookup e por consulta de 1 dia, tamanho de arquivo, taxa de compressão) contra o arquivo de produção djen-tjro-2026/comunicacoes.parquet, com a decisão resultante (manter default ou fixar valor explícito) implementada em exporter.py com teste cobrindo o comportamento decidido (RED antes, GREEN depois) e `uv run pytest -q`/`ruff` verdes."
status: "achieved"
---

# Goal: decidir ROW_GROUP_SIZE (A1b) com dado real de produção

O plano de otimização de Parquet (`docs/planning/parquet-storage-optimization-plan.md`) trata `ROW_GROUP_SIZE` explícito como uma otimização independente do `ORDER BY` já implementado, mas proíbe fixar um valor não-default "sem benchmarkar contra arquivos de produção". O benchmark existente (`scripts/benchmarks/row_group_size.py`) só cobre dados sintéticos. Esta rodada baixa um arquivo real já publicado (djen-tjro-2026/comunicacoes.parquet, já com o layout novo `cnj-text-sorted-v1`), mede o efeito real de diferentes ROW_GROUP_SIZE em point-lookup por CNJ (o caso de uso central do epic #1468) e em consulta por data (para confirmar/quantificar a regressão que o plano já previa), e registra a decisão resultante — implementando-a em `exporter.py` via TDD se a medição justificar um valor explícito, ou documentando por que o default permanece correto.
