---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-ez5wkn-goal-datajud-join-key-normalization"
run_id: "2026-09-09-exciting-mccarthy-ez5wkn"
goal: "Make scripts/render_queries.py's _DATAJUD_AGG_SQL emit the same punctuation-stripped CNJ join key every sibling aggregate (_DJEN_AGG_SQL, _JURIS_AGG_SQL, _STJ_AGG_SQL) already emits, so processos_unificados's FULL OUTER JOIN USING (nr_processo) can never silently fail to match a DataJud row against its DJEN/JURIS/STJ counterpart."
rationale: "Every one of _DATAJUD_AGG_SQL's three siblings computes regexp_replace(<col>, '[^0-9]', '', 'g') AS nr_processo directly in its SELECT (render_queries.py lines 203, 216 -> 393, 269 -> 407) before the join. _DATAJUD_AGG_SQL alone emitted the raw numero_processo unchanged (line 288) -- its own WHERE clause on the very next line computes the normalized length to filter malformed rows, but never applies that normalization to the column it actually joins on, and its GROUP BY grouped by the raw column too. This worked only because ProcessoCapa.capa_row() (src/datajud/models.py) already writes a digit-only CNJ at ingest time today -- an invariant enforced far away, in a different package, with zero assertion at the SQL layer that builds the join. A future or alternate DataJud ingest path (a manual backfill, an older engine version, a different capa producer) that emits a punctuated numero_processo would silently produce an unmatched extra row instead of merging into the existing DJEN/JURIS/STJ row for that CNJ -- undercounting processos_multi_fonte.qmd's cross-source hits with no error, no warning, nothing in CI to catch it, since processos_unificados/_UNIFICADOS_SQL had zero existing test coverage before this round (confirmed via grep across tests/ -- no test referenced _UNIFICADOS_SQL, _DATAJUD_AGG_SQL, or processos_unificados at all). Found via this round's Explore-agent code survey (rated PLAUSIBLE, not CONFIRMED, by the agent itself since it found no live dirty data today); independently confirmed CONFIRMED by direct side-by-side comparison against all three sibling aggregate queries in the same file and against scripts/reconcile_processos.py's own _INDICE_DATAJUD_SQL (line 519-532), which already normalizes correctly -- render_queries.py's _DATAJUD_AGG_SQL is the sole remaining outlier."
success_signal: "A new test in tests/test_render_queries.py (test_processos_unificados_datajud_join_key_normalizes_punctuation) creates a datajud_capa row with a punctuated CNJ and a comunicacoes/indice_processual row with the same CNJ in digit-only form, then calls _register_processos_unificados and queries processos_unificados. Before the fix: two rows (the punctuated CNJ unmatched, tem_datajud=False, n_fontes=1; and the digit-only CNJ with no DataJud fields, tem_datajud=False) -- FAILS RED. After the fix: exactly one merged row (digit-only nr_processo, tem_datajud=True, n_fontes=2, classe_oficial populated) -- PASSES GREEN. All other tests/test_render_queries.py tests, the full Python suite, ruff check/format, the --check static contract validator (all 19 .qmd files including processos_multi_fonte.qmd), and the full web/vitest suite stay green."
status: "achieved"
---

# Goal: normalizar a chave de join do DataJud em processos_unificados

`_DATAJUD_AGG_SQL` era a única das quatro fontes agregadas (DJEN/JURIS/STJ/DataJud) que não normalizava sua própria chave de join (`nr_processo`) por `regexp_replace`, dependendo silenciosamente de um invariante garantido em outro pacote (`ProcessoCapa.capa_row()`). Corrigido para seguir exatamente o mesmo padrão das três fontes irmãs, com um teste novo cobrindo `processos_unificados` — que antes não tinha nenhuma cobertura de teste.
