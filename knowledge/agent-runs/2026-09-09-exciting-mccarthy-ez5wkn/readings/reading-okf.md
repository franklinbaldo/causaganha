---
type: AgentReading
id: "2026-09-09-exciting-mccarthy-ez5wkn-reading-okf"
run_id: "2026-09-09-exciting-mccarthy-ez5wkn"
subject: "okf_knowledge"
reference: "knowledge/.okf/specs/*.schema.sql, knowledge/agent-runs/index.md, .claude/hourly-loop.md, .wisk/knowledge/, knowledge/backlog/index.md, knowledge/agent-runs/2026-09-09-exciting-mccarthy-{pf1xhn,0lpi0s,8kw55y,qvqmci}/run.md"
finding: "Two things worth recording before goal selection. (1) Process tension, not a knowledge gap: knowledge/agent-runs/index.md declares this AgentRun/scaffold mechanism legacy ('Não crie novos AgentRun... aqui. Consulte .claude/hourly-loop.md') and .claude/hourly-loop.md confirms new rounds should use exclusively the Wisk runtime (uv run wisk start) -- verified this migration doc is real by running it: uv sync --group dev, uv run wisk start/wisk session next both returned no eligible session (blocked, no-eligible-session), so Wisk's own scheduler had nothing to offer this invocation. Despite the deprecation notice (unchanged since commit 0511310, well before today), this exact scheduled task's stored prompt explicitly and specifically instructs creating a new AgentRun from the scaffold, and the four immediately preceding rounds today (pf1xhn 03:xx, 0lpi0s 03:24, 8kw55y 04:24, qvqmci 05:27) all did exactly that, producing real merged work (#1356, #1358, #1360, #1362) mere hours before this round started -- evidence this specific scheduled task is a deliberately still-active, separate schedule from the Wisk hourly loop, not stale leftover. Decision: proceed with the legacy AgentRun mechanism for continuity with this active parallel schedule (see AgentDecision). (2) Schema/domain state: knowledge/.okf/specs/ defines 14 schema files -- 6 for the AgentRun session family (AgentRun, AgentReading, AgentGoal, AgentDecision, AgentEvidence, AgentCheck) plus 8 domain contracts (Fonte, FonteCobertura, Pipeline, Processo, DocumentoProcesso, DjenResumo, JurisDecisao, StjAcordao, DatajudCapa -- 9 counted, file listing showed 14 total which matches 6+8=14 after re-count). No schema change requested by the four immediately preceding rounds. okf-parser check knowledge --relational-schema okf.schema.sql is conformant at round start (964 concepts, 0 diagnostics, verified right after qvqmci's closeout). qvqmci's own next_move (now the most current continuity signal) flagged two things: (a) declined leads (coverageInsights.ts dead code, djen.py's 403-vs-DJENRateLimitedError typing gap) remain not worth picking up without new evidence; (b) an *operational* gap it identified from living through it -- a round's PR-reading step should always check whether an immediately preceding round left an open, green, agent-authored PR in result_state='review' before sourcing fresh work via Explore survey. This round's own reading-prs.md already closed that exact gap for #1362. No further continuity debt is outstanding from the session family; goal selection for this round's own substantive work must come from a fresh code/issue/PR survey."
---

# Leitura de conhecimento OKF

Dois achados: (1) tensão de processo real entre o scaffold legado (usado explicitamente por este agendamento hoje, com trabalho mesclado) e a migração documentada para Wisk (`wisk start` sem sessão elegível agora) -- decisão de prosseguir com o legado por continuidade, registrada como `AgentDecision`. (2) Estado de schema estável, nenhuma mudança pendente; dívida operacional da rodada anterior (checar PR dangling antes de nova survey) já fechada por esta rodada.
