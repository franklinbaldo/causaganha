---
type: AgentDecision
id: "2026-09-09-exciting-mccarthy-ez5wkn-decision-use-legacy-scaffold-despite-wisk-migration"
run_id: "2026-09-09-exciting-mccarthy-ez5wkn"
question: "knowledge/agent-runs/index.md and .claude/hourly-loop.md both say new rounds should use exclusively the Wisk runtime (uv run wisk start) and explicitly say not to create new AgentRun/AgentReading/AgentGoal/AgentDecision/AgentEvidence/AgentCheck instances. This round's own scheduled task prompt, however, explicitly and specifically instructs copying .claude/agent-run-scaffold.md into knowledge/agent-runs/ and building a full AgentRun report. Which instruction governs this round?"
choice: "Follow the scheduled task's explicit instruction: create a new AgentRun report using the legacy scaffold, exactly as the four immediately preceding rounds today (pf1xhn, 0lpi0s, 8kw55y, qvqmci) did."
rationale: "First, verified this isn't a stale prompt overtaken by migration: ran the actual documented Wisk golden path (uv sync --group dev; uv run wisk start; uv run wisk session next) and it returned state=blocked/no-eligible-session -- Wisk's own scheduler had no work queued for this invocation, so falling back to Wisk would have produced nothing, not a better-aligned alternative. Second, the four immediately preceding rounds today all used this exact scaffold-based mechanism and produced real, live-merged work (PRs #1356, #1358, #1360, #1362) within the last three hours -- strong evidence this specific scheduled task is a deliberately still-active, separate schedule running in parallel with the Wisk hourly loop (which has its own extensive, independent .wisk/knowledge/ history from the same days), not an artifact nobody noticed was deprecated. Refusing to do the assigned work because a different, adjacent mechanism has its own deprecation notice would break continuity with an actively-running family of rounds for no benefit -- the deprecation notice targets a different consumer (the hourly loop) and says nothing about this specific scheduled task. Recorded here rather than silently followed so a future round (of either family) has the reasoning on record if the two schedules are ever reconciled or one is intentionally retired."
---

# Decisão: manter o mecanismo AgentRun legado nesta rodada

Apesar do aviso de depreciação em `knowledge/agent-runs/index.md`/`.claude/hourly-loop.md`, `uv run wisk start` não tinha nenhuma sessão elegível no momento desta rodada, e as quatro rodadas imediatamente anteriores de hoje usaram este mesmo mecanismo legado com sucesso (4 PRs mescladas em ~3h). A tarefa agendada desta rodada instrui explicitamente o scaffold legado. Decisão: seguir o scaffold, registrando a tensão para uma futura reconciliação.
