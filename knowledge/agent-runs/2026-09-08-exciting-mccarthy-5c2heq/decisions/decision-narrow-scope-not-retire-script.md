---
type: AgentDecision
id: "2026-09-08-exciting-mccarthy-5c2heq-decision-narrow-scope-not-retire-script"
run_id: "2026-09-08-exciting-mccarthy-5c2heq"
goal_id: "2026-09-08-exciting-mccarthy-5c2heq-goal-calendar-json-status-vocabulary"
question: "Investigation found scripts/generate_cache_from_manifest.py's own header flagging unresolved migration debt ('RFC: which of these JSONs are still consumed by the live frontend, and can this be retired'), and traced that cache/backfill.json, cache/today.json, and cache/calendar.json all appear unconsumed by the live frontend today. Should this round also resolve that RFC (retire the unused outputs / narrow the script) alongside the status-vocabulary fix, or leave it for a later round?"
choice: "Fix only generate_calendar_json()'s status-bucket vocabulary this round; do not attempt to resolve the script's own retirement RFC in the same round."
rationale: "Investigation traced every consumer reachable from web/src: cache/backfill.json was explicitly superseded by the tribunal_coverage .qmd contract (per advogadosCoverage.ts's own docstring); readJson.ts declares a CacheTodayFile type for cache/today.json that nothing imports; and no file under web/src references cache/calendar.json at all (the live calendar heatmap reads /data/tribunal_calendar.json from the .qmd pipeline instead). That's strong evidence the whole script's output category could be retired, but two things argue against doing that in this same round: (1) the script's --upload path still runs on every catalog update and publishes to the public causaganha-dashboard Internet Archive item -- removing it changes what a public, externally-referenceable archive item contains, which is a higher-blast-radius, externally-visible change that deserves its own round with a clear before/after audit, not a bundled side effect of a status-vocabulary fix; (2) confirming 'zero consumers' with certainty would require also checking whether any external tooling or documentation references these IA-published JSONs directly (outside this repo's own web/src), which this round did not attempt. The narrower fix is self-contained, TDD-able, low-risk, and directly closes the one remaining place a bug class already fixed twice today (commits efa595a, b85fcf8) could still recur -- a smaller, certain improvement now over a larger, uncertain one attempted in the same round."
---

# Decisao: escopo estreito, nao decidir a retirada do script

Corrigir apenas o vocabulario de status em `generate_calendar_json`. Nao tentar resolver a RFC do proprio cabecalho do script (retirar ou nao os JSONs legados) nesta rodada -- essa e uma decisao de blast radius maior (afeta um item publico do Internet Archive atualizado a cada catalogacao) que merece sua propria rodada com auditoria completa de consumidores externos.
