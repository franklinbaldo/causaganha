---
type: AgentReading
id: "2026-09-08-exciting-mccarthy-5c2heq-reading-okf"
run_id: "2026-09-08-exciting-mccarthy-5c2heq"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-08-exciting-mccarthy-obl3ux/run.md (most recent report in this AgentRun family); git log (commits efa595a, b85fcf8 from a parallel 'Wisk-loop' family, both merged same-day); uv run okf-parser check knowledge --relational-schema okf.schema.sql"
finding: "Read obl3ux's report in full: it fixed weekend-inflated completion metrics (PR #1313, merged) and left two low-value leads (dead code in coverageInsights.ts; a typing-only 403 inconsistency in djen.py's download_zip that engine.py's download_worker already handles identically to DJENNotFoundError). Independently re-verified both are still exactly as described and still low-value (no RED/GREEN shape or no live behavioral effect) -- not selected. Dispatched an Explore subagent to survey less-covered areas (scripts/render_queries.py, .qmd contracts, causaganha_mcp/, manifest.py) for a better candidate; it returned scripts/generate_cache_from_manifest.py's generate_calendar_json() (line 255: `status = row.get('ia_status') or row.get('djen_status') or ''`) as its top pick, on the theory that djen_status='confirmed' rows fall into neither the uploaded nor absent bucket. I independently verified this by reading the function directly: the coverage_pct number itself (uploaded/total) is NOT mathematically wrong (it matches totals.qmd's own coverage_pct definition, which is also uploaded/total, confirmed-inclusive-in-denominator by design) -- the agent's 'silently deflating coverage_pct' framing overstates it. The real, verified bug is narrower but still real: the per-day breakdown only exposes uploaded/absent/total fields, so confirmed/available/unknown rows are counted in total but in neither of the two exposed buckets, meaning uploaded+absent structurally cannot sum to total for any day with pending entries -- unlike totals.qmd and tribunal_coverage.qmd, which since commit efa595a (merged today, 2026-09-08T15:49 -04:00, by a separate same-day 'Wisk-loop' session) expose all four categories (uploaded/pending/absent/unknown) that do sum to total. Confirmed via render_manifest_parquet.py:512-518 that djen_status='confirmed' is a real, distinct value that survives into the canonical sync-manifest.parquet unmodified (the confirmed->available normalization only happens in the *derived* legacy-CSV write-back path, never in the parquet itself) -- so this is not a hypothetical value. Traced consumers: web/src/lib/readJson.ts declares a CacheTodayFile type for cache/today.json but nothing in web/src imports/uses it, and web/src/lib/advogadosCoverage.ts's own docstring confirms cache/backfill.json was already replaced by the tribunal_coverage .qmd contract -- so this script's JSON outputs are legacy/unconsumed by the live frontend today, but still executed on every catalog update (update-catalog.yml:113, with --upload) and published to the public causaganha-dashboard IA item, and the script's own header explicitly flags it as unresolved migration debt ('RFC: which of these JSONs are still consumed... can this be retired'). Chose the narrower, well-scoped fix (bring generate_calendar_json's bucketing in line with the vocabulary already established in totals.qmd/tribunal_coverage.qmd) over the larger RFC (auditing/retiring the whole script), since the latter is an architectural decision affecting a live IA-published item and deserves its own round, not a same-round bundling. okf-parser check at round start (after readings 1-3): conformant, 0 diagnostics, 819 concepts, 822 markdown files."
---

# Leitura do conhecimento OKF

A rodada anterior desta familia (`obl3ux`) fechou com a PR #1313 mesclada e deixou duas pistas de baixo valor (ja reavaliadas e descartadas). Um subagente Explore encontrou um candidato mais forte em `scripts/generate_cache_from_manifest.py`: `generate_calendar_json` ainda usa o vocabulario antigo de `djen_status` (so `uploaded`/`absent`), o mesmo bug de classe ja corrigido hoje, na mesma data, por uma familia paralela ("Wisk-loop", commits `efa595a`/`b85fcf8`) em `totals.qmd`/`tribunal_coverage.qmd`. Verifiquei diretamente: `coverage_pct` em si nao esta matematicamente errado, mas o breakdown por dia (`uploaded`/`absent`/`total`) nao soma `total` quando ha linhas `confirmed`/`available` pendentes -- inconsistente com o resto do codebase. Esse e o objetivo desta rodada.
