---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-5c2heq-goal-calendar-json-status-vocabulary"
run_id: "2026-09-08-exciting-mccarthy-5c2heq"
goal: "Make scripts/generate_cache_from_manifest.py's generate_calendar_json() classify every manifest row into one of four buckets (uploaded/pending/absent/unknown) that sum exactly to the day's total, matching the djen_status vocabulary (available/confirmed/absent/'') already canonicalized in web/src/queries/totals.qmd and tribunal_coverage.qmd, instead of the current two-bucket (uploaded/absent) classification that silently drops confirmed/available/unknown rows from both buckets."
rationale: "generate_calendar_json's status line (`status = row.get('ia_status') or row.get('djen_status') or ''`; then only 'uploaded' and 'absent' are counted) predates the four-way pending/absent/unknown/uploaded vocabulary that totals.qmd and tribunal_coverage.qmd adopted. That vocabulary needed a fix as recently as today: commit efa595a (merged 2026-09-08T15:49-04:00 by a separate same-day agent session) fixed totals.qmd/tribunal_coverage.qmd because their own 'pending' filter exact-matched djen_status='available' only, silently excluding djen_status='confirmed' rows -- a real, distinct value confirmed via render_manifest_parquet.py:512-518 to survive unmodified into the canonical sync-manifest.parquet (the confirmed->available normalization only happens in the derived legacy-CSV export, never in the parquet). generate_cache_from_manifest.py reads that same canonical parquet but was never updated to the same vocabulary: any day with a djen_status='confirmed' or 'available' row (probe-verified as having a caderno, not yet uploaded) or an unknown row (djen_status='') has uploaded+absent < total in its cache/calendar.json output, with no field accounting for the gap. This script is legacy (its own header calls it out as unresolved migration debt) and its outputs are not read by the live frontend today, but it still runs on every catalog update (update-catalog.yml, with --upload) and publishes cache/calendar.json to the public causaganha-dashboard Internet Archive item -- so the inconsistency is live in the sense that it is continuously re-published as public data. Bringing it into the same vocabulary as the rest of the manifest-consuming pipeline removes one more place this status-bucketing bug class can recur, and keeps the publicly-archived JSON internally consistent (uploaded+pending+absent+unknown == total) the way every other consumer of the same manifest already is."
success_signal: "A new pytest test (tests/test_generate_cache_from_manifest.py) builds a small rows fixture including a djen_status='confirmed' row, a djen_status='available' row, an ia_status='uploaded' row, a djen_status='absent' row, and a fully-unknown row (djen_status='', ia_status='') all on the same date, calls generate_calendar_json(rows), and asserts the resulting day has uploaded=1, pending=2, absent=1, unknown=1, and uploaded+pending+absent+unknown==total(5) -- fails before the fix (RED: today only exposes uploaded/absent/total, with pending/unknown missing as keys entirely and uploaded+absent=2 != total=5) and passes after adding the pending/unknown buckets with the same djen_status IN ('available','confirmed') / ='absent' / ='' classification totals.qmd uses (GREEN). ruff check and ruff format --check stay green. Full python test suite (uv run pytest -q) stays green with one more test file / at least one more passing test than this round's baseline."
status: "achieved"
---

# Goal: alinhar vocabulario de status em generate_calendar_json

`generate_calendar_json` em `scripts/generate_cache_from_manifest.py` ainda usa apenas dois buckets (`uploaded`/`absent`), deixando linhas `djen_status='confirmed'`/`'available'`/desconhecidas fora de ambos -- o mesmo bug de classe corrigido hoje em `totals.qmd`/`tribunal_coverage.qmd` (commit `efa595a`). Alinhar ao vocabulario canonico de quatro buckets (`uploaded`/`pending`/`absent`/`unknown`) para que a soma sempre bata com `total`, mantendo consistencia com o resto do pipeline que le o mesmo `sync-manifest.parquet`.
