---
type: AgentEvidence
id: "2026-09-06-exciting-mccarthy-fpldz1-evidence-runtime-browser-verification"
run_id: "2026-09-06-exciting-mccarthy-fpldz1"
goal_id: "2026-09-06-exciting-mccarthy-fpldz1-goal-continue-with-agent"
kind: "runtime"
reference: "npm run build + `python3 -m http.server` serving dist/ under /causaganha/, driven by playwright@1.55.0 (chromium at /opt/pw-browsers/chromium) against http://127.0.0.1:4511/causaganha/processo.html"
summary: "Built the site (npm run build, with CI's own stubbed web/public/data/*.json) and served dist/ locally under the site's real /causaganha base path, driven by real Chromium (playwright@1.55.0, installed temporarily via `npm install --no-save`, browser at /opt/pw-browsers/chromium — no new binary downloaded). Loading /causaganha/processo.html in a real browser confirmed: the ProcessoLookup Svelte island hydrates successfully (no astro-island hydration error, unlike an earlier attempt that used the wrong base path and failed to fetch the chunk), the page renders the expected idle-state copy, and the compiled JS bundle (dist/_astro/ProcessoLookup.*.js) contains the new strings ('Continuar com um agente', the /agentes onboarding link, and the exact wording from buildAgentContinuationQuestion). Reaching the 'found' state itself requires DuckDB-WASM to fetch its worker/wasm binaries and then query the real indice_processual.parquet from archive.org; both fetches failed in this sandboxed environment ('Falha ao inicializar DuckDB-WASM: Failed to fetch', net::ERR_CONNECTION_RESET) — a pre-existing environment/network-policy limitation unrelated to this change (the same DuckDB init path every /processo visit depends on, untouched by this diff), not a defect in the new code. The interactive behavior this environment cannot exercise live (clicking the new button in a real 'found' DOM, clipboard write, feedback text swap, the three copy actions producing distinct text) is exactly what ProcessoLookup.agentQuestion.test.ts already exercises against the real compiled Svelte component via @testing-library/svelte, using the identical clipboard-mocking technique already proven correct by the pre-existing ProcessoLookup.reference.test.ts for the sibling 'Copiar link'/'Copiar referência' actions. Temporary playwright install used --no-save; web/package.json and web/package-lock.json are untouched (git diff confirmed empty for both), and the scratch script, dist/, and stubbed web/public/data/ were deleted after the check."
---

# Evidência runtime: verificação em navegador real

Chromium real confirma que a página hidrata sem erro e que o bundle compilado contém a nova UI. O estado `found` ao vivo depende de DuckDB-WASM buscar binários e o parquet real no archive.org, ambos bloqueados pela política de rede deste sandbox — uma limitação de ambiente pré-existente, não um defeito da mudança. O comportamento interativo do novo botão está coberto por `ProcessoLookup.agentQuestion.test.ts` contra o componente Svelte real compilado.
