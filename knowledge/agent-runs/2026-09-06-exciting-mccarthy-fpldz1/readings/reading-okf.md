---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-fpldz1-reading-okf"
run_id: "2026-09-06-exciting-mccarthy-fpldz1"
subject: "okf_knowledge"
reference: "uv run okf-parser check knowledge --relational-schema okf.schema.sql (baseline, before this round's own report existed); knowledge/projections/processo-consultar.md; knowledge/agent-runs/2026-09-06-exciting-mccarthy-uwm65t (PR #1217, the example-question authority this round's target issue asks to reuse)"
finding: "Baseline: 525 concepts, 0 diagnostics, conformant. knowledge/projections/processo-consultar.md confirms Processo is the single shared domain root behind both /processo (web) and the MCP tools (processo_consultar/publicacoes_buscar/processo_estado/decisoes_buscar) — 'não reinlina uma segunda ontologia para MCP ou Web', consistent with issue #1225's request to bridge the two surfaces rather than duplicate one. Round uwm65t's report (PR #1217) shows the established authority pattern for agent-facing example wording: causaganha_mcp/agents_examples.py is the single source of the natural-language question text, and a contract test (tests/causaganha_mcp/test_agents_page_examples_contract.py) asserts the static /agentes page reproduces it verbatim plus stays inside the live MCP tool catalog and never names an unpublished fonte. That contract only works because agentes.astro's example text is static (fixed golden CNJ); /processo's case is different — the CNJ is a runtime value the user typed, so verbatim cross-file duplication is not applicable. The correct analog for #1225 is a single TypeScript authority function (mirroring processoReference.ts's buildProcessoReferenceText/buildDocumentoReferenceText pattern already used by ProcessoLookup.svelte for 'Copiar referência') that both the component and its own tests exercise, plus tests asserting the wording stays task-language-only (no MCP tool names, no JSON) — the same constraint #1217's contract enforces server-side."
---

# Leitura do conhecimento OKF

Bundle conformante (525 conceitos, 0 diagnósticos). A projeção `ProcessoConsultar` confirma que `Processo` já é a ontologia única compartilhada entre `/processo` e o MCP — a issue #1225 pede exatamente uma ponte sobre essa base já unificada, não uma nova ontologia. O padrão de autoridade a reaproveitar não é a duplicação verbatim de `agents_examples.py` (que só funciona porque a página `/agentes` é estática com um CNJ fixo), e sim o padrão já usado por `ProcessoLookup.svelte` para "Copiar referência": uma função TypeScript única em `web/src/lib/`, testada isoladamente, consumida pelo componente.
