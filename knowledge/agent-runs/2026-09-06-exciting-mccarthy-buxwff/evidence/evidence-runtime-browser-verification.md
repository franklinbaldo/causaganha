---
type: AgentEvidence
id: "2026-09-06-exciting-mccarthy-buxwff-evidence-runtime-browser-verification"
run_id: "2026-09-06-exciting-mccarthy-buxwff"
goal_id: "2026-09-06-exciting-mccarthy-buxwff-goal-agents-home-discovery"
kind: "runtime"
reference: "npm run build (with web/public/data/*.json generated by uv run python scripts/render_queries.py); served under /causaganha/ via python3 -m http.server; a local Playwright 1.55.0 script launched against /opt/pw-browsers/chromium (the pre-installed browser, since npx-fetched playwright 1.55.0 expects a browser revision this environment doesn't ship) at 1280x900 and 390x844"
summary: "First pass (button visual: 'outline' for the third hero CTA) revealed a real rendering bug via screenshot: the button was invisible against the hero's dark background (cobogo's outline variant is dark text on transparent background, confirmed by reading node_modules/cobogo/preset/index.mjs's button recipe CSS). Replaced with a text link (see decision-text-link-not-button); re-verified: document.documentElement.scrollWidth - clientWidth === 0 at both 1280x900 and 390x844 on both / and /agentes (no horizontal overflow anywhere); the hero link and footer /agentes link are both .isVisible() === true; the /agentes primary-nav link carries aria-current=\"page\" when on that route (same mechanism already used by Processo/Publicações/Salvos, since Agentes now shares their array-driven rendering loop). Screenshots (home-desktop.png, home-mobile.png, agentes-desktop.png, agentes-mobile.png) show: top nav reads PROCESSO / PUBLICAÇÕES / SALVOS / AGENTES / MAIS with no wrapping/clipping issues on desktop, and wraps cleanly to a second line under 'MAIS' on mobile without overflow; the footer reads PROCESSO / PUBLICAÇÕES / AGENTES / SOBRE; the hero shows the two original buttons plus 'Um agente consulta o mesmo acervo por MCP — não uma API separada. Usar com um agente →' as a subordinate underlined link."
---

# Evidência de runtime: verificação em navegador real

Build real servido localmente e inspecionado com Chromium via Playwright em 1280x900 e 390x844: overflow horizontal zero em `/` e `/agentes` nos dois viewports; bug real de contraste encontrado e corrigido (botão `outline` invisível sobre o hero escuro) antes de reportar a rodada como concluída.
