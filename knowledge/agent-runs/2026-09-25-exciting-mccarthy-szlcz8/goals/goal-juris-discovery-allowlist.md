---
type: AgentGoal
id: "2026-09-25-exciting-mccarthy-szlcz8-goal-juris-discovery-allowlist"
run_id: "2026-09-25-exciting-mccarthy-szlcz8"
goal: "Fechar o item (2) do critério de conclusão de #1652/TM-16: scripts/reconcile_processos.py::_discover_juris_items deve parar de tratar uma busca livre `identifier:tjro-juris-*` no namespace público do IA como fonte de verdade e passar a derivar os anos tjro-juris-{ano} confiáveis exclusivamente do manifesto CSV que o próprio projeto publica e controla (tjro_juris.archive.MANIFEST_DOWNLOAD_URL, o mesmo que causaganha.decisoes.published.discover_published_juris_datasets já usa como allowlist para o mesmo dado) — só anos com pelo menos uma janela (tipo, mes_ano) marcada ia_status='uploaded' nesse manifesto viram item_id candidato; nenhum item sintaticamente parecido mas fora do manifesto é aceito, mesmo que exista de fato no IA."
rationale: "#1652 documenta a ameaça: um item IA de terceiro cujo identifier bate com o padrão tjro-juris-{ano} pode ser descoberto pela busca global do advancedsearch.php e aceito como parte do acervo canônico — catalog/data poisoning sem comprometer nenhuma credencial do projeto. O item (1) da mesma issue (scripts/generate_catalog.py) já foi corrigido numa rodada anterior (230b86) trocando list_ia_items() por get_items_from_sync_manifest() quando --verified-inventory é pedido — mesmo padrão arquitetural: usar um artefato que o próprio projeto publica e controla como allowlist, nunca o resultado de uma busca no namespace público. Para JURIS esse artefato já existe e já é lido por outro consumidor (causaganha.decisoes.published.discover_published_juris_datasets, que projeta o mesmo manifesto em URLs públicas) — não é preciso inventar nenhum schema novo nem RFC, só parar de ignorar essa fonte de verdade já disponível em _discover_juris_items. Duas PRs externas (codex #1644/#1645) já tentaram diagnosticar itens (2)/(3) desta issue, mas #1652 registra que estavam desatualizadas/com checks vermelhos no momento em que foram lidas, e esta leitura confirma que continuam sem nenhum check reportado — reimplementar via TDD própria, seguindo o padrão já estabelecido por toda a série de rodadas de hoje, é mais confiável do que tentar reaproveitar um diagnóstico externo não revalidado."
success_signal: "tests/test_reconcile_processos.py ganha um novo teste RED confirmado (AttributeError ou item indevido no resultado contra a implementação anterior) provando que _discover_juris_items: (a) nunca aceita um item cujo ano não aparece com ia_status='uploaded' no manifesto do projeto, mesmo que uma busca IA simulada o devolvesse; (b) devolve exatamente os anos com pelo menos uma janela 'uploaded' no manifesto; (c) devolve [] quando o manifesto não existe (404), sem lançar exceção. Os testes existentes que hoje mockam identifier:tjro-juris-* via advancedsearch.php (_mock_juris_remote e os mocks inline em ~7 testes) são migrados para mockar o manifesto em vez da busca livre — sem regressão de comportamento no restante do pipeline (fetch_juris_from_ia, ensure_juris_parquets, reconcile completo). GREEN depois da implementação: toda a suíte de tests/test_reconcile_processos.py passa, e a função não faz mais nenhuma chamada HTTP a advancedsearch.php. #1652 ganha o checkbox do item (2) marcado e docs/SECURITY_THREAT_MODEL.md (linha TM-16) atualizado. ruff check/format --check e uv run pytest -q (suíte completa) verdes. PR aberta contra main a partir de claude/exciting-mccarthy-szlcz8."
status: "achieved"
---

# Goal: allowlist de descoberta JURIS via manifesto do projeto (#1652/TM-16, item 2)
