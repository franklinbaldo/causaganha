---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-ich5gz-reading-issues"
run_id: "2026-09-05-exciting-mccarthy-ich5gz"
subject: "open_issues"
reference: "https://github.com/franklinbaldo/causaganha/issues?q=is%3Aissue+is%3Aopen (29 open issues at session start); issue #1107 body + its 8 comments (2026-09-05T02:04 through 19:02, the last one posted right after PR #1157 merged, 8 minutes before this session started)"
finding: "#1107 (contract(processo): eliminar drift do plano de consulta entre MCP e Web) is OPEN and READY, and is the clear continuation candidate: the prior round (2026-09-05-exciting-mccarthy-e9r0mj) just merged PR #1157, fixing the DataJud timestamp-truncation drift and reinstating the mapping-layer PRESENT/ABSENT parity harness (processoQueryPlanParity.test.ts + scripts/processo_query_plan_compare.py). The issue owner's own post-merge review comment (5554099924, 19:02:59Z) explicitly re-scopes the remaining work in order: (1) prove 'fonte registrada mas parquet indisponível' as a state distinct from 'CNJ ausente' on both runtimes; (2) prove documentos_truncados (MCP) / pagination (Web) observable equivalence with stable ordering; (3) confirm these proofs run over the real public paths, not just isolated builders/mappers. Inspecting service.py and processoCnj.ts shows both runtimes ALREADY implement this distinction correctly at the real-function level (Python: _build_djen/etc. catch duckdb.Error into an aviso via _fonte_indisponivel_aviso, exercised by test_service.py's test_one_source_parquet_unreachable_is_partial_not_fatal vs test_not_found_still_carries_cobertura; Web: queryRowSafe does the same inline, exercised by processoCnj.test.ts's 'isolates a single source failure' vs 'returns encontrado=false...') — but each proof lives in its OWN runtime's unit-test suite against its OWN fixture, never cross-checked against the other, which is exactly the drift risk #1107 exists to close (a unilateral wording/shape change on one side would not be caught). No open PR exists for this issue (list_pull_requests: state=open → []). Other candidates considered: #950/#951 (MCP remote HTTP endpoint, gated on 'stack MCP corrente reconciliada' — #1152 already merged per prior round's evidence, worth a future look but a different theme); #1131-#1134/#1136/#1138/#1139 (web/UX, no freshly-unblocked dependency, less concretely scoped); #1042 (ops(catalog), needs live IA-upload side effects, unsuited to this session); the segmenter/OPF cluster (#884, #886-887, #1047-1057, orthogonal ML research track, no TDD-shaped single-round slice). #1107 remains the most concretely scoped, already-diagnosed, single-round-sized, continuity-respecting candidate."
---

# Leitura de issues abertas

#1107 continua a issue certa: seu comentário mais recente (pós-merge de #1157) já enumera o próximo passo em ordem, e a inspeção do código confirma que a distinção "fonte indisponível vs CNJ ausente" já existe nos dois runtimes isoladamente, mas nunca foi provada em conjunto — exatamente o gap que a issue nomeia.
