---
type: AgentGoal
id: "2026-09-05-exciting-mccarthy-ich5gz-goal-fonte-indisponivel-vs-ausente-parity"
run_id: "2026-09-05-exciting-mccarthy-ich5gz"
goal: "Prove, with a single cross-runtime test using the shared #1107 fixture/bridge harness, that 'fonte registrada mas parquet indisponível' (a source the índice registers but whose parquet fails to read) is a state distinct from 'CNJ ausente' (no índice registration at all) on BOTH the Python service and the Web SQL/mapper plans — not just within each runtime's own separate unit-test suite, which already covers each half in isolation but never cross-checks them."
rationale: "Issue #1107's latest comment (5554099924, posted right after this session's predecessor merged PR #1157) explicitly queues this as the next item: 'provar fonte registrada mas parquet indisponível como estado distinto de CNJ ausente nas duas superfícies' plus 'confirmar que essas provas atravessam os caminhos públicos completos, e não apenas builders/mapeadores isolados'. Reading service.py and processoCnj.ts shows this distinction is already correctly implemented on each side (Python's _build_djen/_fonte_indisponivel_aviso, Web's queryRowSafe with an identically-worded aviso template) and already unit-tested on each side separately (test_service.py's test_one_source_parquet_unreachable_is_partial_not_fatal / test_not_found_still_carries_cobertura; processoCnj.test.ts's 'isolates a single source failure' / 'returns encontrado=false'), but never proven to agree with each other against the SAME fixture the way processoQueryPlanParity.test.ts already does for the PRESENT/ABSENT and DataJud-timestamp cases. That is precisely the drift #1107 exists to catch: a unilateral wording or shape change to either side's 'fonte indisponível' handling would not be caught by either runtime's own tests alone."
success_signal: "A new test case in web/src/lib/processoQueryPlanParity.test.ts, run through the existing scripts/processo_query_plan_compare.py bridge extended with a new CNJ_SOURCE_UNAVAILABLE fixture (a djen entry registered in the shared índice but pointing at a parquet path that does not exist), asserts: (1) raw-SQL execution of the broken source raises duckdb.Error identically on both the Python-plan and Web-plan SQL text (python_raised === web_raised === true), while a CNJ absent from a healthy source parquet never raises on either side; (2) the real production _build_djen() mapper (not the raw SQL) never propagates that error — it degrades to present:false plus exactly one fonte-specific aviso, while the CNJ-absent case degrades to the same present:false shape with zero avisos, proving the two states are observably distinct in the SAME mapped view; (3) the aviso text Python actually produced, once its variable exception detail is extracted, reproduces byte-for-byte under the Web side's own aviso-formatting function (a new exported formatFonteIndisponivelAviso in processoCnj.ts, extracted from queryRowSafe's inline template), proving the two runtimes' user-facing wording has not drifted apart. The test is RED before the fixture/bridge/export changes exist (missing manifest fields / import), GREEN after. Full web vitest suite and `uv run pytest -q` stay green; ruff check/format clean."
status: "achieved"
---

# Goal: fonte indisponível ≠ CNJ ausente, provado nas duas superfícies (#1107)

Fecha o item 1 da fila mais recente de #1107: a distinção "fonte registrada mas parquet indisponível" vs "CNJ ausente" já existe em cada runtime isoladamente, mas nunca foi comparada lado a lado contra a mesma fixture. Esta rodada estende o harness de paridade já existente (fixture + bridge script + teste Vitest) para provar isso formalmente, sem introduzir DSL, backend novo ou dependência do site no MCP.
