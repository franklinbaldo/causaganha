---
type: AgentGoal
id: "2026-09-05-exciting-mccarthy-qnpypw-goal-close-1042-catalog-parity-proof"
run_id: "2026-09-05-exciting-mccarthy-qnpypw"
goal: "Close out issue #1042's last remaining acceptance criterion — proving processo_consultar (MCP) and the /processo web contract agree, for the same real CNJ, on data read back from a real post-#1040 update-catalog.yml run — with live, non-fixture evidence."
rationale: "#1042 had accumulated 12 comments across at least 5 prior rounds, each confirming the pipeline/publish/read-back half of the proof (run #776 on 2026-09-03) but leaving exactly one criterion open: comparing the MCP surface against the web surface for the same real CNJ, never a fixture or frozen fallback. This is real, unclaimed, high-value work — a long-standing product-correctness gap (#924 SS3.1) that keeps almost-closing but never quite does — and it required no code change, only verification, matching the round's own OKF reading finding that many 'open' items are actually done and just need evidence. Attempting a live browser render of the deployed /processo page failed due to this session's sandboxed headless-Chromium being unable to complete TLS handshakes through the egress proxy to any external host (confirmed via /__agentproxy/status showing systemic ws_closed_mid_exchange failures, including to unrelated hosts) — an environment limitation, not a product defect, and the same wall implicit in why this criterion sat open across so many prior rounds. The goal was met instead by extracting the literal SQL query-builder functions from web/src/lib/processoCnj.ts and executing that exact SQL (not a reimplementation) via native DuckDB against the same published parquet files, which shares DuckDB's engine/dialect with duckdb-wasm — a genuine contract-parity proof rather than an approximation."
success_signal: "A real, multi-source CNJ present in the newest post-#1040 update-catalog.yml run's published indice_processual.parquet is queried both via causaganha.processos.service.buscar_processo (the function behind the MCP tool processo_consultar) and via the literal SQL strings from web/src/lib/processoCnj.ts, both against the live https://archive.org/download/causaganha-dashboard/ artifacts with no fixture/fallback, and both produce identical fontes_presentes/field values; the evidence is posted on #1042 and the issue is closed with state_reason=completed."
status: "achieved"
---

# Goal: fechar a última prova de paridade da #1042

A #1042 estava a um único critério de fechar havia várias rodadas: comparar `processo_consultar` (MCP) com `/processo` (web) para o mesmo CNJ real, sem fixture. Renderizar o site publicado num browser real não foi possível neste sandbox (limitação de rede do Chromium headless via proxy), então a prova foi feita executando literalmente o SQL do frontend (`processoCnj.ts`) via DuckDB nativo contra o mesmo artefato publicado, e comparando campo a campo com a saída do MCP — paridade de contrato, não aproximação.
