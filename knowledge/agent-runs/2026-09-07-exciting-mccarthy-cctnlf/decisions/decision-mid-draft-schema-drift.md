---
type: AgentDecision
id: "2026-09-07-exciting-mccarthy-cctnlf-decision-mid-draft-schema-drift"
run_id: "2026-09-07-exciting-mccarthy-cctnlf"
goal_id: "2026-09-07-exciting-mccarthy-cctnlf-goal-tribunal-list-merge"
question: "Running the full pytest suite mid-round showed 3 failures, not the 1 documented in .claude/agent-run-scaffold.md ('this round's own report-completeness test'): tests/test_check_agent_run_completeness.py's own-tree check (expected), plus tests/web/test_generate_okf_zod_schemas.py and tests/causaganha_mcp/test_okf_domain_models.py's generated-file drift checks (not previously documented as expected-while-drafting). Is this a real regression this round introduced, or the same known mid-draft artifact under a different test?"
choice: "Same artifact, verified directly rather than assumed: temporarily moved this round's own run directory out of knowledge/agent-runs/ and re-ran both generated-schema tests — both passed clean with the directory absent, confirming the drift is caused solely by this round's own run.md/goal files carrying intentionally-empty fields (completed_at, primary_goal_id, result_summary, next_move) while still mid-draft, not by any change to tribunais.py or its test. okf-parser's schema/model export infers field shape (optional vs required) from the full knowledge/ bundle's actual instances, so one in-flight AgentRun with empty required-looking fields transiently changes the inferred shape for every AgentRun/AgentGoal-family instance until this round's own report is filled in like every other historical instance. Filling in completed_at/primary_goal_id/result_summary/next_move at the end of this round (not deleting or faking them earlier) is the correct fix, matching the scaffold's own existing rule for the completeness test. Also updated .claude/agent-run-scaffold.md's note to name all three tests instead of just one, so a future round doesn't have to re-derive this from scratch."
rationale: "The scaffold already establishes the right mental model (completed_at empty is fine locally, must be filled before push) for one test; it just didn't know the same cause has two more effects once the OKF code-generation gates (#1105-era Zod/domain-model exports) started deriving shape from bundle content. Documenting this is a small, low-risk improvement to the OKF model's own self-description — exactly the kind of 'melhore o modelo quando ele estiver pobre para representar o trabalho' this round's instructions call for — and prevents a future round from spending investigation time re-confirming the same non-regression."
---

# Decisão: falha dos testes de schema gerado é o mesmo artefato de rascunho, não uma regressão

Confirmado isolando o diretório da própria rodada: sem ele, os dois testes de geração (Zod/domain models) passam limpos. A causa é a mesma já documentada para o teste de completude — campos vazios de propósito (`completed_at` etc.) enquanto o relatório está em rascunho mudam a forma inferida do schema. Atualizado `.claude/agent-run-scaffold.md` para nomear os três testes, não só um.
