---
type: AgentReading
id: "2026-09-07-exciting-mccarthy-cctnlf-reading-okf"
run_id: "2026-09-07-exciting-mccarthy-cctnlf"
subject: "okf_knowledge"
reference: "knowledge/backlog/*.md; knowledge/agent-runs/2026-09-07-exciting-mccarthy-{kfv7sx,7gg7l1,abz39i}/run.md; docs/planning/{oportunidades-melhoria-2026-07.md,manifest-source-of-truth.md,okf-integration.md,infrastructure-review-2026-07.md}; live spot-checks against current tree"
finding: "With the issue queue and PR queue both empty of actionable work (see reading-issues.md/reading-prs.md), this round audited whether older planning docs still name live, unblocked opportunities rather than assuming the July 2026 audit (oportunidades-melhoria-2026-07.md) is current. Verified against the actual tree, most of it is now stale/resolved: web/src/lib/queryData.ts (flagged as dead+buggy) no longer exists; the 15 named orphan scripts (analyze_pipeline_performance.py, probe_tribunal_start_dates.py, validate_system_health.py, laptop_service.py, monitor_github_backfill.py, etc.) no longer exist; root junk files (server.log, dev_output.json, run_stats.json, ia_data.json) are not in git ls-files; scripts/render_queries.py --check now runs in test.yml's PR CI and --strict runs in deploy-web.yml (item 3 of that doc, previously flagged as a silent-failure risk, is closed); tests/stj_acordaos/ and tests/tjro_juris/ each now have 5-7 test files (2725 lines) covering client/dedup/manifest/archive/main, and both stj-sync.yml/tjro-sync.yml run them on cron (item 4, 'zero testes' + 'não operacionalizado', is closed). manifest-source-of-truth.md's own status line confirms Fase 0/1/2/3 all ✅, Fase 3 closed by PR #800 on 2026-07-08 — the CSV-fallback risk that doc's item 1 called 'prioridade máxima' is fully resolved, not just planned. okf-integration.md's 'próximas fatias' 1-3 (pin a stable okf-parser release, have causaganha_mcp read the Pipeline relation for status metadata, use typed Ibis relations in architecture tests) are all done: pyproject.toml pins okf-parser>=0.45.4,<0.46 (a real release, no commit-SHA pin left); src/causaganha_mcp/knowledge.py loads the Pipeline relation and src/causaganha_mcp/tools/status.py dispatches from it with a hard RuntimeError on drift; tests/causaganha_mcp/test_okf_pipeline_catalog.py asserts every declared Pipeline has a real importable package and status tool. Only genuinely open, non-blocked items found in these docs: (a) infrastructure-review-2026-07.md's item 4 — the publish-to-archive 24h delay alarm on `pending_real` in site-status.json — is explicitly deferred pending the maintainer confirming the threshold, i.e. an infra_decision like #950, not something to implement unilaterally; (b) okf-integration.md's item 4 (an Artefato/Dataset OKF relation) is explicitly gated on 'só promover o que tiver consumidor real' — no consumer exists yet, so inventing the relation now would be decorative per the doc's own criterion. Dispatched an Explore subagent in parallel to search fresh, not-yet-documented ground (retry.py/probe.py/credentials.py test coverage, remaining Svelte reactivity bugs, orphaned MCP code, loose ends in recent merged PRs) since the known planning docs are exhausted of unblocked work."
---

# Leitura do conhecimento OKF

Com fila de issues e PRs esgotada (ver outras leituras), esta rodada auditou os planos antigos (`docs/planning/`) contra o estado real da árvore em vez de presumir que ainda valem. Resultado: quase tudo já foi resolvido — `queryData.ts` deletado, scripts órfãos removidos, `render_queries.py --check`/`--strict` já no CI, `stj_acordaos`/`tjro_juris` já testados e operacionalizados via cron, migração do manifesto com Fase 3 concluída (PR #800), e as três primeiras "próximas fatias" de `okf-integration.md` (pin de release, leitura da relação `Pipeline`, testes de arquitetura Ibis) já implementadas. Os dois únicos itens realmente abertos exigem decisão do mantenedor (limiar de alarme de atraso) ou consumidor real ainda inexistente (relação `Artefato`) — nenhum é executável nesta rodada sem input externo. Um subagente Explore foi despachado em paralelo para procurar terreno genuinamente novo.
