---
type: AgentGoal
id: "2026-09-07-exciting-mccarthy-cctnlf-goal-tribunal-list-merge"
run_id: "2026-09-07-exciting-mccarthy-cctnlf"
goal: "Make src/djen_backup/tribunais.py's get_tribunal_list() actually merge the live DJEN API tribunal list with the hardcoded TRIBUNAIS baseline, instead of silently discarding the entire 96-entry hardcoded list whenever the API returns any non-empty response."
rationale: "The module's own docstring says 'hardcoded fallback + live API merge' and engine.py calls get_tribunal_list() once per run (src/djen_backup/engine.py:733) to decide which tribunals to back up this run — it is the single source of truth for run scope in the canonical sync engine. The current implementation is `result = sorted(set(api_codes))` whenever api_codes is non-empty: any transient, partial, or filtered DJEN proxy API response (rate limiting, pagination bug, format drift) silently drops every hardcoded tribunal not present in that response, for that entire run, with only a log.info line and no error/alert. That directly contradicts CausaGanha's core mission (complete, verifiable archiving — CLAUDE.md's 'falhar cedo e alto' and the archival project's completeness guarantee) and is a currently-live reliability gap: zero tests exist for tribunais.py today (confirmed: no file under tests/ references it), so this has never been caught. The fix (`set(api_codes) | set(TRIBUNAIS)`) is a pure, credential-free, one-line correctness fix with no product/infra decision required — unlike the 17 backlog issues, which are all blocked on exactly that kind of decision."
success_signal: "A new tests/djen_backup/test_tribunais.py, using the existing respx `mock_api` fixture pattern from test_ia_contract.py, is written FIRST and fails RED against today's code: it mocks the DJEN tribunal API endpoint to return exactly one tribunal code absent from TRIBUNAIS (e.g. 'TJXX'), calls get_tribunal_list(), and asserts the result contains BOTH 'TJXX' AND a known hardcoded code (e.g. 'TJRO') — RED today because the current implementation returns only ['TJXX']. After the fix (get_tribunal_list unions api_codes with TRIBUNAIS instead of replacing), that test passes GREEN, plus additional tests locking in: API-failure/empty-response still falls back to the full hardcoded list; API returning exactly the hardcoded set changes nothing; the API-fetch exception path (existing fetch_tribunal_list_from_api behavior) is unaffected. Docstrings for the module and get_tribunal_list are corrected to describe the real (now-true) union behavior. Full `TRIBUNAL=tjro uv run pytest -q`, `uv run ruff check`, and `uv run ruff format --check` stay green. A PR is opened and driven to a green, mergeable state."
status: "achieved"
---

# Goal: get_tribunal_list realmente faz merge, não substituição

`get_tribunal_list()` em `src/djen_backup/tribunais.py` descarta os 96 tribunais hardcoded sempre que a API ao vivo devolve qualquer lista não vazia — mesmo parcial. Corrige para união (`api_codes | TRIBUNAIS`), com teste RED→GREEN via `respx`, sem exigir credencial ou decisão de infraestrutura.
