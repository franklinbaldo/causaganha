---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-rvjn2w-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-rvjn2w"
subject: "open_issues"
reference: "mcp__github__list_issues(owner=franklinbaldo, repo=causaganha, state=OPEN) as of 2026-09-06T23:26Z; knowledge/backlog/index.md and its 18 issue-<n>.md files; mcp__github__issue_read(get, #1241) and (get_comments, #1022)"
finding: "18 open issues. #1217 (this repo's previous round's work) is now closed, as expected. All 17 issues catalogued in knowledge/backlog/ (884, 886, 887, 950, 951, 985, 1011, 1022, 1047, 1050, 1051, 1053, 1054, 1055, 1056, 1057, 1093) are unchanged blocked/deprioritized items per the backlog mechanism's own instructions — none show a GitHub state change since their last_verified_run_id (the most recent, buxwff, verified #1022 as still blocked on missing IAS3_ACCESS_KEY/IAS3_SECRET_KEY at 2026-09-06T15:24:49Z; #1022's own comment thread since then is a long chain of the repo owner's own automated 'descoberta'/reconciliation comments restating that the code path is already merged and only a live IA-credentialed run is missing — no new blocker, no new fact). One genuinely new issue exists outside the backlog cache: #1241 'mcp(decisoes): não voltar ao scan de 1.000+ partições quando o índice CNJ estiver indisponível', filed by the repo owner (franklinbaldo, author_association OWNER) at 2026-09-06T23:04:36Z — 28 minutes before this round started — explicitly marked 'READY para IMPLEMENTAÇÃO' in its own body, and an explicit, scoped follow-up to #1238/#1239 (merged just before this round, per git log: commit 970235b). It names a concrete regression risk in code already read this round (src/causaganha_mcp/tools/decisoes.py:_narrow_juris_datasets_for_cnj and src/causaganha/decisoes/published.py:resolve_juris_urls_for_cnj/IndiceProcessualUnavailableError): the current fallback on IndiceProcessualUnavailableError returns the full unnarrowed dataset list (1000+ JURIS partitions in production), exactly the unbounded-scan behavior #1238/#1239 were merged to eliminate for the normal path. It has concrete, testable acceptance criteria (explicit distinct states for 'index unavailable' vs 'CNJ absent from index'; fonte=juris fails bounded and clearly; fonte=todas preserves other sources without inventing a false zero; a realistic-scale (1000+) regression test proving zero JURIS parquets are opened on the failure path) and zero open PRs to compete for attention. This is the strongest available new work this round: unlike all 17 backlog items it has no external blocker (pure code fix, no credentials/infra/product decision needed), and unlike re-verifying the backlog it is real, undone, owner-prioritized product risk with a live regression already latent in main."
---

# Leitura das issues abertas

17 das 18 issues abertas seguem cobertas por `knowledge/backlog/` sem mudança de estado (incluindo #1022, cujos comentários recentes são só reconciliação do próprio dono, sem fato novo). A novidade é #1241, aberta pelo dono minutos antes desta rodada, follow-up direto de #1238/#1239 (mergeada na rodada anterior): o fallback atual de `_narrow_juris_datasets_for_cnj` ainda devolve a lista JURIS inteira (1000+ partições em produção) quando `indice_processual` está indisponível — exatamente o scan sem bound que a correção anterior eliminou no caminho normal. Sem bloqueio externo, critérios de aceite concretos e testáveis, e nenhuma PR aberta concorrendo. Esta é a issue escolhida para o trabalho da rodada.
