---
type: AgentGoal
id: "2026-09-25-exciting-mccarthy-o3ubcj-goal-tm04-web-parity"
run_id: "2026-09-25-exciting-mccarthy-o3ubcj"
goal: "Fechar o gap Web remanescente de TM-04/#1610: portar para web/src/lib/processoCnj.ts as duas checagens de proveniência que o lado Python (service.py) já faz antes de compor read_parquet(arquivo_ia_url) -- coerência de tribunal (linha do índice vs. tribunal que a própria URL do item IA nomeia) e coerência do rodapé Parquet (causaganha.schema_version/item_id do artefato djen vs. o item_id que a URL do índice nomeia) -- descartando (aviso, nunca exceção) qualquer artefato djen/datajud que reprove qualquer uma das duas."
rationale: "docs/SECURITY_THREAT_MODEL.md (TM-04) e o próprio comentário de código em processoCnj.ts (linha ~152-155) marcavam explicitamente as duas checagens como pendentes do lado Web, apesar de já implementadas e testadas do lado Python (service._validar_tribunal_coerente/_validar_metadata_djen, mescladas por rodadas anteriores hoje: e0vvbh e qn6gvy). O next_move de qn6gvy nomeou esse gap por nome. processoCnj.ts é o único lugar onde o dossiê unificado é montado no browser (ProcessoLookup.svelte usa buscarProcesso()) -- sem essa paridade, um indice_processual.parquet comprometido continuava podendo enganar o visitante do site mesmo depois da mesma classe de ataque já estar fechada no MCP/backend Python, quebrando a garantia de TM-04 de 'identidade verificável' para a superfície pública mais visitada do produto."
success_signal: "web/src/lib/processoCnj.test.ts ganha testes novos (RED confirmado contra o processoCnj.ts anterior: ReferenceError por função/classe inexistente para as funções puras, e falha de asserção real -- não erro de execução -- para os dois testes de integração em buscarProcesso) cobrindo: tribunalDaUrl/validarTribunalCoerente/ArtifactProvenanceError (espelho de _tribunal_da_url/_validar_tribunal_coerente); itemIdDaUrl/validarMetadataDjen (espelho de _item_id_da_url/_validar_metadata_djen); validarMetadataDjenUrls assíncrono (espelho de _validar_metadata_djen_urls, uma query por URL para não degradar todos os artefatos djen por uma falha de rede pontual); e dois testes de integração em buscarProcesso() mostrando que um índice com tribunal incoerente ou um artefato djen com rodapé incoerente degrada a fonte para ausente + aviso (nunca lança, nunca chega a compor a query final). GREEN depois da implementação, sem regressão nas fixtures pré-existentes de fonteUrls/buscarProcesso (nenhuma delas usa uma URL com a forma real de item IA djen/datajud, então passam pelo mesmo bypass 'URL não verificável' que já valia para _tribunal_da_url do lado Python). docs/SECURITY_THREAT_MODEL.md (TM-04) atualizado para refletir que as duas checagens agora têm paridade nas duas linguagens. npx vitest run (suite web completa), npx eslint ., npx astro check, uv run ruff check/format --check e uv run pytest -q (suite completa do repositório) verdes. PR aberta contra main a partir de claude/exciting-mccarthy-o3ubcj."
status: "achieved"
---

# Goal: paridade Web de TM-04/#1610 (tribunal + rodapé djen)
