---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-qvqmci-goal-stats-coverage-exclude-in-flight"
run_id: "2026-09-09-exciting-mccarthy-qvqmci"
goal: "Stop web/src/queries/stats_coverage.qmd's 'Pior dia'/'Melhor dia' (best_day/worst_day/best_count/worst_count in stats_coverage.json, rendered verbatim on web/src/pages/stats.astro) from picking a day that is still mid-archival as the worst coverage day, by excluding not-yet-settled days from the best/worst selection -- mirroring the pending_real/absent_confirmed distinction site_status.qmd already applies."
rationale: "stats_coverage.qmd defined 'collected' purely as COUNT(DISTINCT tribunal) FILTER (ia_status='uploaded') per date, then picked MIN/ARG_MIN over the whole 30-day window as 'worst_count'/'worst_day' with zero distinction between a genuine coverage failure and a day that simply hasn't finished processing yet. Confirmed real, not hypothetical: src/djen_backup/__main__.py's default sync end_date is 'yesterday' (deliberately excludes 'today') precisely because a partial day looks artificially bad; docs/SERVICE_OBJECTIVES.md declares an explicit 24h publication-to-archive SLO, meaning even 'yesterday' can legitimately still have pending_real pairs (DJEN confirmed the caderno exists, upload hasn't landed yet) when the dashboard renders. site_status.qmd already solved exactly this problem for its own metrics -- it classifies every gap as absent_confirmed/pending_real/errors_transient/never_checked and its 'Lacuna' honesta explicitly excludes absent_confirmed, with pending_real_max_age_hours tracking the SLO directly -- but stats_coverage.qmd, added separately, never adopted that distinction. Net effect on the live site: any day within the 30-day window that's still catching up (near-certain for at least the most recent day, every render) is eligible to win 'Pior dia', showing users a misleadingly low count (e.g. 1/27) as if it were a real outage, when it is normal in-SLO lag -- the exact 'don't conflate a transient/incomplete state with a genuine failure verdict' pattern CLAUDE.md is explicit about for djen_raw/403 elsewhere in this codebase, just not yet applied here. No existing test covered this: the only stats_coverage test (test_stats_coverage_last_30_days_excludes_the_31st_boundary_day) exercises the window boundary, not settledness."
success_signal: "A new RED test in tests/test_render_queries.py (test_stats_coverage_worst_day_excludes_still_in_flight_day) fails today because the current query's worst_day picks 'today' (collected=1, still pending_real on 2 of 3 tribunals) over the true worst *settled* day (collected=2, one genuinely djen-confirmed-absent tribunal). After adding an 'unsettled' classification to stats_coverage.qmd's SQL (NOT uploaded AND NOT raw_absent, same raw_absent vocabulary as site_status.qmd: djen_raw IN ('404','400','no_publications')) and filtering best/worst day selection to unsettled=0 days only, the same test goes GREEN, and the pre-existing 30-day-window boundary test keeps passing unchanged. Full Python suite, ruff check, ruff format --check, --check static contract validation, and the full web/vitest suite (506 tests, including the contract-render integration test) all stay green."
status: "achieved"
---

# Goal: excluir dias ainda em processamento de "Pior dia" em stats_coverage.qmd

`stats_coverage.qmd` escolhia o dia com menor `collected` em toda a janela de 30 dias sem distinguir "falha real de cobertura" de "dia que ainda não terminou de subir" (`pending_real`, dentro do SLO de 24h já documentado). Isso deixava "Pior dia" no dashboard público sistematicamente vulnerável a mostrar o dia mais recente — quase sempre ainda em voo — como se fosse uma queda real, exatamente a confusão que `site_status.qmd` já evita para suas próprias métricas.
