---
type: AgentReading
id: "2026-09-09-exciting-mccarthy-qvqmci-reading-okf"
run_id: "2026-09-09-exciting-mccarthy-qvqmci"
subject: "okf_knowledge"
reference: "knowledge/.okf/specs/*.schema.sql, knowledge/backlog/index.md, knowledge/agent-runs/2026-09-09-exciting-mccarthy-{pf1xhn,0lpi0s,8kw55y}/run.md"
finding: "knowledge/.okf/specs/ defines 15 schema files: domain contracts (Fonte, FonteCobertura, Pipeline, Processo, DocumentoProcesso, DjenResumo, JurisDecisao, StjAcordao, DatajudCapa) plus the AgentRun session family (AgentRun, AgentReading, AgentGoal, AgentDecision, AgentEvidence, AgentCheck). This session family is stable -- no schema change requested by any of the three immediately preceding rounds today. All three closed 'merged' with no open next_move debt beyond two long-declined low-value leads (coverageInsights.ts dead code; djen.py 403 typing gap) plus 8kw55y's own two new leads: (c) cross-check web/src/queries/README.md's 'Currently optional' list against actual `optional: true` .qmd flags, and (d) audit every scripts/render_queries.py VIEW_SPECS source for the IA-fallback pattern to close that bug class for good. Both leads were run down THIS round before goal selection: (c) is clean -- the README's ten-name list (lawyer_leaderboard, stj_totals, stj_temas, stj_relatores, juris_totals, juris_classes, juris_orgaos, processos_multi_fonte, datajud_totals, datajud_classes) matches exactly the ten .qmd files with `optional: true` set, verified by grepping every .qmd frontmatter directly -- no drift, nothing to fix. (d) is also now closed -- read scripts/render_queries.py in full: all 8 registered ViewSpecs (manifest, lawyer_ratings, ratings_history, acordaos, tjro_juris, datajud_capa, processos_unificados, processo_documentos) either call _try_download_parquet directly (manifest, acordaos, lawyer/ratings via _register_ratings_table, comunicacoes via indice_processual/catalog-manifest fallback) or delegate to reconcile_processos.ensure_juris_parquets/ensure_datajud_parquets (tjro_juris, datajud_capa) or aggregate exclusively from already-covered sources via _ensure_view (processos_unificados, processo_documentos) -- no source left with a local-only glob and no fallback. This closes the bug class the last three rounds worked incrementally; the entire lead set from 8kw55y's next_move is exhausted, so this round sources work fresh (see AgentReading on Explore survey once it lands) rather than continuing a specific thread. No unresolved OKF-model gap identified so far; the model is not obviously poor for representing this round's work, so no type/schema change is anticipated up front."
---

# Leitura de conhecimento OKF

As duas pistas deixadas pela rodada anterior (8kw55y) — drift do README de optional flags e auditoria do padrão IA-fallback em todo VIEW_SPECS — foram checadas nesta rodada e ambas estão limpas/fechadas. A fila de continuidade está esgotada; a rodada precisa buscar trabalho novo via survey de código.
