---
type: AgentReading
id: "2026-09-08-exciting-mccarthy-obl3ux-reading-claude-md"
run_id: "2026-09-08-exciting-mccarthy-obl3ux"
subject: "claude_md"
reference: "CLAUDE.md"
finding: "Re-read in full at round start. Correctness rules for djen_backup (djen_raw is a transport code, never a verdict; 403 must never be treated as absent; genuine absent is 404, 400, or 200-with-'Sem comunicações'-body; the historical ~79K legacy-row false-positive issue already resolved via the promoted sync-manifest.parquet) are unchanged since the last read (ful6xk, 2026-09-08T~03:00Z). The CSS token boundary section still correctly names three (not four) live legacy Svelte islands (ProcessoLookup, PublicationSearch, SavedConsultations) after TribunalCalendar.svelte's deletion two rounds ago. No stale claim found. Note: main advanced significantly since ful6xk's close (#1303) via a separate 'Wisk-loop' tracking family (PRs #1305 fix(djen-backup) DJENRateLimitedError in drain worker, #1307 removed abandoned dashboard-fetch architecture, #1309 deleted orphaned TribunalCoverageGrid.astro, #1311 rewrote FRONTEND.md's Pico CSS section) — none of these touch CLAUDE.md's own content, so its accuracy claim stands unaffected."
---

# Leitura de CLAUDE.md

Releitura completa. Regras de correção do djen_backup inalteradas e ainda vigentes. Seção CSS token boundary segue correta (três ilhas legadas). Desde o fechamento da última rodada desta família (`ful6xk`), a `main` avançou via uma família de rastreamento separada ("Wisk-loop": PRs #1305, #1307, #1309, #1311) que não altera o conteúdo do CLAUDE.md.
