---
type: AgentReading
id: "2026-09-08-exciting-mccarthy-obl3ux-reading-okf"
run_id: "2026-09-08-exciting-mccarthy-obl3ux"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-08-exciting-mccarthy-ful6xk/run.md (most recent report in this AgentRun family); uv run okf-parser check knowledge --relational-schema okf.schema.sql"
finding: "Read ful6xk's report in full. Its next_move left two concrete, unacted leads from its own investigation: (a) web/src/lib/coverageInsights.ts has ~55% dead code (summarizeCatalogDay, filterCoverageDays, countCoverageFilters, getDayStatusLabel, buildCatalogAttentionCards and supporting types) orphaned by TribunalCalendar.svelte's deletion two rounds ago; (b) src/djen_backup/djen.py's download_zip() does not explicitly handle HTTP 403 the way get_caderno_url does (raises DJENRateLimitedError), instead falling through to a bare httpx.HTTPStatusError. Independently re-verified both directly by reading the files: (a) confirmed still present, zero call sites outside the file itself (grep across web/src), and the file's own test suite (coverageInsights.test.ts, 44 lines) only exercises buildTribunalAttentionCards -- the other five exports have zero test coverage, reinforcing they are dead; (b) confirmed still present -- download_zip's probe path (checks only HTTP_NOT_FOUND) and its _download_simple fallback (checks HTTP_NOT_FOUND and >=500) both skip HTTP_FORBIDDEN and fall through to resp.raise_for_status(), a plain httpx.HTTPStatusError, unlike get_caderno_url's explicit DJENRateLimitedError. However, tracing the only caller (engine.py's download_worker) shows both DJENRateLimitedError and httpx.HTTPError are caught by the exact same except clause and handled identically (log + summary.inc_error()) -- so today the inconsistency is a typing/readability gap, not a live functional bug (no breaker distinction or retry-count difference depends on it, unlike the checker path where _classify_djen_status does branch on DJENRateLimitedError specifically). Ran `uv run okf-parser check knowledge --relational-schema okf.schema.sql` at round start: conformant, 0 diagnostics, 747 concepts (up from 728 -- the four Wisk-loop PRs merged since ful6xk's close). Dispatched an Explore subagent to scan less-covered areas (scripts/render_queries.py, .qmd contracts, causaganha_mcp/, manifest.py, TODO/FIXME) for a higher-value candidate before committing to (a) or (b) as this round's goal."
---

# Leitura do conhecimento OKF

A rodada anterior desta família (`ful6xk`) deixou duas pistas concretas no `next_move`. Verifiquei ambas diretamente: a limpeza de código morto em `coverageInsights.ts` segue válida e sem cobertura de teste; a inconsistência de tratamento de 403 em `download_zip` também segue presente, mas hoje é apenas uma lacuna de tipagem/legibilidade (o `download_worker` já trata `DJENRateLimitedError` e `httpx.HTTPError` de forma idêntica). `okf-parser check` no início: conformante, 0 diagnósticos, 747 conceitos. Disparei um subagente Explore para varrer áreas menos cobertas antes de escolher o goal desta rodada.
