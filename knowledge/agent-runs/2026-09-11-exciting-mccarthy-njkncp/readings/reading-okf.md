---
type: AgentReading
id: "2026-09-11-exciting-mccarthy-njkncp-reading-okf"
run_id: "2026-09-11-exciting-mccarthy-njkncp"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-10-exciting-mccarthy-{r3erpr,aezdb9,yd5lu0,41w39p,25og4b,8042ey}/run.md; knowledge/agent-runs/index.md; knowledge/backlog/index.md; knowledge/contracts/stj-acordao.md; knowledge/projections/processo-consultar.md"
finding: "Read all six of today's prior same-lineage AgentRun reports (HEAD e5fee06 is 8042ey's own closing commit) plus knowledge/agent-runs/index.md and knowledge/backlog/index.md. Confirmed continuity: 8042ey's next_move offered two leads -- (a) a broader grep across djen_backup/ and scripts/ for other 'single source of truth' constant modules whose consumers re-type literals instead of importing (following the write_back_csv/absent_consistency.py fix pattern), and (b) nothing else concrete. This round checked (a) directly: grepped every 'single source of truth' docstring in the repo (absent_consistency.py -- already fixed by 8042ey; circuit_breaker.py -- every consumer in archive.py/ia_s3.py/engine.py/exporter.py/consolidate.py imports CircuitState/CircuitBreaker properly, no re-typed literal found; manifest.py's ABSENT_CODES/TRANSIENT_CODES -- engine.py imports and uses ABSENT_CODES correctly at line 587, djen.py's own 403/404/timeout classification is the origin of the raw codes, not a second copy; render_queries.py's ViewSpec registry -- single registration point, no duplicate). Lead (a) is exhausted: no new instance of the re-typed-constant bug class found. Also directly re-verified yd5lu0's still-open engine.py orphaned-Task audit lead was already closed by 8042ey (confirmed via 8042ey's own considered_work, cross-checked live in probe.py/drain.py/engine.py: worker_tasks/background_tasks are tracked and gathered in finally blocks). Since both standing leads from today's lineage were exhausted, dispatched a fresh Explore-agent survey of previously-untouched modules (src/causaganha/processos/, src/causaganha_mcp/, src/tjro_juris/, src/stj_acordaos/, src/tcu_acordaos/, src/tse_processual/, src/datajud/, src/causaganha/analysis/, web/src/lib/) for a concrete correctness bug. It reported the codebase in those areas as unusually well-hardened, but surfaced one confirmed real bug: causaganha.processos.service._carregar_cobertura (service.py:220-231) builds each FonteCobertura via fonte['status']/fonte['rows'] outside its try/except (which only catches OSError/httpx.HTTPError/json.JSONDecodeError) -- so a syntactically-valid indice_processual.report.json whose 'sources' entries are missing 'status'/'rows' raises an uncaught KeyError/TypeError that propagates out of buscar_processo, contradicting both the module's own docstring ('Não conseguir carregar ... o relatório de cobertura ... é parcial ... nunca uma exceção') and its Web twin (web/src/lib/processoCnj.ts::fetchCobertura), which already defaults missing fields to 'unknown'/0 inside the same try/except that guards fetch/JSON errors. Verified this finding directly against the working tree (read service.py lines 195-260, processoCnj.ts lines 600-630, and the existing test_report_fetch_follows_archive_org_redirect regression test documenting a prior instance of this exact Python/TS-parity bug class from #1042) before committing to it as this round's goal -- not taken on the sub-agent's word alone."
---

# Leitura de conhecimento OKF

Lidos os seis relatórios `AgentRun` de hoje e os índices de `knowledge/agent-runs/`/`knowledge/backlog/`. Os dois leads em aberto de `8042ey` (varredura de outros módulos "single source of truth" e a auditoria de `asyncio.create_task` órfão) foram verificados e esgotados nesta rodada -- nenhuma nova instância encontrada. Uma varredura fresca por sub-agente em módulos ainda não tocados hoje (`causaganha.processos`, `causaganha_mcp`, `tjro_juris`, `stj_acordaos`, `tcu_acordaos`, `tse_processual`, `datajud`, `web/src/lib`) encontrou um bug real confirmado independentemente: `_carregar_cobertura` deixa `KeyError`/`TypeError` escapar de `buscar_processo` quando `indice_processual.report.json` é JSON válido mas tem forma errada, contradizendo o próprio docstring do módulo e divergindo do gêmeo TypeScript `fetchCobertura` -- mesma classe de bug de paridade Python/TS já documentada no repositório (#1042). Escolhido como goal desta rodada.
