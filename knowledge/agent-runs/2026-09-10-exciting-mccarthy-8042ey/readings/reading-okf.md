---
type: AgentReading
id: "2026-09-10-exciting-mccarthy-8042ey-reading-okf"
run_id: "2026-09-10-exciting-mccarthy-8042ey"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-10-exciting-mccarthy-{r3erpr,aezdb9,yd5lu0,41w39p,25og4b}/run.md; knowledge/agent-runs/index.md; .claude/hourly-loop.md"
finding: "Read all five of today's prior same-lineage AgentRun reports plus the two files governing the mechanism itself. index.md and hourly-loop.md (added by PR #1401, merged 2026-09-09) say the CausaGanha *hourly loop* has migrated to the Wisk runtime (uv run wisk start) and that new AgentRuns should not be created there -- but this session's own scheduled-task prompt explicitly instructs creating a new AgentRun from .claude/agent-run-scaffold.md, and every one of today's five prior exciting-mccarthy-* rounds (a separate scheduled routine from the hourly loop, sharing the branch-prefix convention) did exactly that, each landing a merged PR. Treated as no conflict: this is a distinct scheduled routine from 'the hourly loop' hourly-loop.md describes, so the scaffold-based AgentRun mechanism is still this routine's live, working contract -- confirmed by index.md itself calling the legacy mechanism still auditable and by five consecutive successful rounds today. 25og4b's next_move offered two forward leads: (a) sweep remaining asyncio.create_task call sites (drain.py, archive.py's CircuitBreaker probe scheduling, CLI entry points) for the 'concurrency primitive not tracked to completion' pattern found repeatedly earlier today, and (b) audit whether any other DATE-typed fixture column outside query_plan_fixtures.py backs a production ::DATE cast that could hide a ::VARCHAR regression, mirroring the STJ fixture fix 25og4b just merged (PR #1450). This round checked both directly: (a) probe.py and drain.py already track every worker_tasks list and asyncio.gather it in a finally block; engine.py's monitor_task is cancelled+awaited and background_tasks is gathered in a finally block at shutdown -- no untracked create_task found in any of the three files. (b) grep for DATE-typed fixture columns outside query_plan_fixtures.py found none; the two remaining DATE '...' literal columns inside query_plan_fixtures.py itself (data_disponibilizacao for djen/comunicacoes, data_julgamento for juris) are genuinely date-only in production -- src/causaganha/storage/djen_schema.py declares data_disponibilizacao as a 'yyyy-mm-dd' string and src/tjro_juris/service.py's PyArrow schema declares data_julgamento as pa.date32() -- so unlike the STJ case there is no hidden timestamp being truncated away; both leads from 25og4b's next_move are exhausted with no actionable follow-up. Investigating a fresh angle from CLAUDE.md's own djen_raw/absent-consistency rule (the historical ~79K-row bug it documents) surfaced a real, previously-unaddressed gap: src/djen_backup/absent_consistency.py is the documented single source of truth for the 'absent status with a 200 raw is contradictory' rule, created after PR #1323 caught two independent runtimes (SyncManifest._normalize_event in manifest.py, _normalize_manifest in scripts/render_manifest_parquet.py) re-typing the same literals and drifting apart; its docstring says 'the SQL literals below must be interpolated from these same constants rather than re-typed'. render_manifest_parquet.py imports ABSENT/BARE_200_RAW/PREFIXED_200_RAW_PREFIX/NO_PUBLICATIONS_SENTINEL at module level and _normalize_manifest (line 418) does interpolate them correctly -- but write_back_csv (line 494), in the very same file, re-types the same contract as bare string literals ('absent', '200', '200:', 'no_publications') inside its ibis.cases() expression instead of referencing the already-imported constants. tests/test_render_manifest_writeback.py::test_write_back_makes_absent_200_rows_self_consistent proves write_back_csv is called standalone (without _normalize_manifest running first) in at least one real path, so this is not dead code -- it is a live second re-typed copy of exactly the contract PR #1323 already had to fix once, sitting unfixed in the same file that fixed the first copy. Chosen as this round's goal: fix write_back_csv to reference the imported constants, with a new test that mutates the module-level constant names to prove the values are actually derived from them (not hardcoded) -- a test that is RED against the current code and GREEN after the fix."
---

# Leitura de conhecimento OKF

Lidos os cinco relatórios `AgentRun` de hoje, `knowledge/agent-runs/index.md` e `.claude/hourly-loop.md`. Confirmado que a migração para Wisk documentada nesses dois arquivos descreve o "loop horário", uma rotina distinta desta (`exciting-mccarthy-*`, que segue usando com sucesso o scaffold `AgentRun` em cinco rodadas consecutivas hoje) -- sem conflito real. Os dois leads deixados por `25og4b` (varredura de `asyncio.create_task` e auditoria de outras colunas `DATE`-tipadas em fixtures) foram verificados diretamente e esgotados: ambos já estão corretos na base atual. Investigação independente a partir da própria regra de `CLAUDE.md` sobre `djen_raw`/auto-consistência encontrou uma segunda cópia rechaçada da mesma constante em `scripts/render_manifest_parquet.py`: `_normalize_manifest` usa as constantes importadas de `absent_consistency.py`, mas `write_back_csv`, no mesmo arquivo, reescreve os mesmos literais à mão -- exatamente o padrão de duplicação que a PR #1323 já corrigiu uma vez nesse módulo. Escolhido como goal desta rodada.
