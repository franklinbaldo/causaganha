---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-6d5vnd-reading-okf"
run_id: "2026-09-15-exciting-mccarthy-6d5vnd"
subject: "okf_knowledge"
reference: ".claude/hourly-loop.md; knowledge/agent-runs/index.md; knowledge/agent-runs/2026-09-15-exciting-mccarthy-yz281l/run.md; .wisk/knowledge/experiences/runs/20260915t052720z-*-outcome-outcome-final.md; docs/planning/parquet-storage-optimization-plan.md"
finding: "`.claude/hourly-loop.md` continua declarando, sem ressalva, que o loop horário é operado exclusivamente pelo Wisk e que AgentRun é legado histórico -- mesmo texto de todas as rodadas anteriores. A diferença concreta desta rodada: pela primeira vez há EVIDÊNCIA OBSERVADA (não só a declaração de política) de que o Wisk está de fato rodando em paralelo -- `.wisk/knowledge/experiences/runs/20260915T052720Z-...` é um RunOutcome real, mesclado como PR #1499/#1500 em main (commits `251be79`/`87cf5f0`, rotulados `feat(audit)`/`docs(wisk)`), fechando os critérios não-gated-por-IA de #1470. O próprio outcome desse round Wisk registra: 'an 8th consecutive round to hit this same conflict, so no new escalation needed beyond what round 20260915T002743Z already flagged to the owner' -- ou seja, o lado Wisk já decidiu, de forma simétrica a este mecanismo, não reescalar sem informação nova. Não há PR Wisk aberta nem handoff Wisk que compita com o trabalho escolhido nesta rodada (handoff ativo do Wisk, handoff-issue-1471-ia-publish-pending, é sobre publicação real no IA -- mesmo bloqueio de credenciais que #1472 já tinha para o mecanismo AgentRun). Releitura de `docs/planning/parquet-storage-optimization-plan.md`: A1 (ordenação CNJ-first) e A1b (ROW_GROUP_SIZE 122880) já `[verified]`/`[feito]`; A1c (gate do índice covering, §1c) segue `[speculative até medir em produção]` -- pede escrever os layouts candidatos em dado real e inspecionar `bloom_filter_offset`/encoding por row group, substituindo a extrapolação do benchmark sintético `bloom_cardinality.py`. Essa é a lacuna concreta escolhida como goal desta rodada."
---

# Leitura de conhecimento OKF

A tensão AgentRun-vs-Wisk é a mesma identificada e já escalada duas vezes (bueov4, to0ars) e reconfirmada sem nova notificação por rounds seguintes (50ns70, yz281l). Nesta rodada, o fato novo é que a coexistência deixou de ser só uma leitura de política (`.claude/hourly-loop.md`) e passou a ter evidência observável em `git log origin/main`: um round Wisk completo, com PR própria mesclada, convivendo com a sequência de rounds AgentRun também mescladas na mesma janela. Isso confirma a predição das rodadas anteriores (dois mecanismos rodando em paralelo sobre o mesmo repositório), mas não é, por si só, um incidente -- os dois lados verificam o estado real do GitHub antes de escolher trabalho e, nesta janela, não há conflito nem duplicação (o round Wisk mais recente fechou #1470; esta rodada mira A1c, uma lacuna diferente e ainda aberta em `docs/planning/parquet-storage-optimization-plan.md`). Sigo a mesma linha das rodadas anteriores: obedecer ao prompt agendado explícito desta sessão (criar o AgentRun), sem nova notificação proativa (nenhuma decisão nova é exigida do usuário além da já escalada), e escolher trabalho de domínio real que não compete com nada em voo do lado Wisk.
