---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-6d5vnd-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-6d5vnd"
subject: "open_issues"
reference: "mcp__github__list_issues (state=OPEN, 22 issues) + issue_read em #1093, #1469, #1470"
finding: "22 issues abertas. O epic #1468 (Parquet nativo por CNJ) segue o cluster mais maduro: #1470 (auditoria) teve nesta última rodada Wisk seus dois critérios não-gated por IA fechados (Bloom filter/encoding detail + plano de regeneração por arquivo, PR #1499) e recebeu comentário de fechamento; único item restante de #1470 é 're-executar antes do rollout', que é operacional (#1472), não código. #1469 (unificar escrita/leitura) tem a maioria dos ~10 critérios de aceite fechados por rodadas anteriores (normalização, ORDER BY, ZSTD+ROW_GROUP_SIZE 122880, certificação de rodapé, igualdade direta em processoCnj.ts, ordem física em reconcile_processos.py, testes de rodapé ausente/misto/erro de rede em processoCnj.test.ts, custo de inspeção do rodapé medido em scripts/benchmarks/djen_certification_probe.py) -- mas o plano de otimização (docs/planning/parquet-storage-optimization-plan.md) ainda marca o item A1c como aberto: 'Gate da decisão de índice covering (§1c) -- em dados reais, escrever os layouts candidatos e inspecionar encoding + bloom_filter_offset por row group, confirmando com produção (não só o benchmark sintético scripts/benchmarks/bloom_cardinality.py) se a ordenação por data realmente não rende bloom filter para numero_processo.' Isso é trabalho real, desbloqueado (leitura pública de arquivo já publicado no IA, sem precisar de credenciais de escrita) e ainda não feito -- candidato natural desta rodada. #1472/#1471 seguem bloqueados por IA_ACCESS_KEY/IA_SECRET_KEY (`env | grep -i 'IA_\\|ARCHIVE'` vazio, confirmado de novo, inalterado desde 11/09). Demais issues (#1093 web/teor, #950/#951 MCP remoto, #1022/#985 datasets TCU/TSE, cluster segmenter #1047-1057/#884/#886/#887) seguem sem novidade -- #1093 está explicitamente marcada pelo próprio dono como 'ESPECIFICADA, mas NÃO é prioridade imediata', gated por #950; os demais seguem gated por infra de GPU/anotação ou credenciais de escrita, sem mudança."
---

# Leitura de issues abertas

Revisão de `mcp__github__list_issues` (22 abertas) + leitura detalhada de #1469, #1470 e #1093 via `issue_read`. Confirmei em `docs/planning/parquet-storage-optimization-plan.md` que o item A1c (gate do índice covering, §1c) segue `[speculative até medir em produção]`: o benchmark existente (`scripts/benchmarks/bloom_cardinality.py`) é sintético, e o plano pede explicitamente confirmar em dados reais antes de decidir se o índice aditivo covering é necessário. Nenhuma evidência em `docs/planning/evidence/` contém a palavra "bloom" -- confirma que essa medição real nunca foi feita, apesar de todo o resto de #1469 já estar fechado. Esse é o candidato de trabalho: real, desbloqueado, testável, e fecha uma lacuna que o próprio plano classifica como gate de uma decisão de arquitetura (índice covering aditivo, bump 3.1.0) que senão ficaria sem base empírica de produção.
