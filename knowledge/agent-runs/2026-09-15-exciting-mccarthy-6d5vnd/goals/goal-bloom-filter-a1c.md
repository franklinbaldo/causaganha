---
type: AgentGoal
id: "2026-09-15-exciting-mccarthy-6d5vnd-goal-bloom-filter-a1c"
run_id: "2026-09-15-exciting-mccarthy-6d5vnd"
goal: "Resolver o flag `[speculative até medir em produção]` do item A1c (gate da decisão do índice covering, §1c) em docs/planning/parquet-storage-optimization-plan.md: reescrever um arquivo Parquet REAL de produção (djen-tjro-2026/comunicacoes.parquet) sob o layout candidato já decidido para `comunicacoes` (ORDER BY numero_processo, data_disponibilizacao, id; ROW_GROUP_SIZE 122880; WRITE_BLOOM_FILTER true) e inspecionar `parquet_metadata()` por `bloom_filter_offset`/encoding por row group para `numero_processo`, substituindo a extrapolação do benchmark sintético `scripts/benchmarks/bloom_cardinality.py` por uma confirmação real -- e registrar a decisão resultante sobre a necessidade (ou não) do índice covering aditivo."
rationale: "O epic #1468/#1469 está com quase todos os critérios de aceite fechados por rodadas anteriores (normalização, ORDER BY, ROW_GROUP_SIZE, certificação de rodapé, leitura direta em processoCnj.ts, ordem física em reconcile_processos.py, custo de inspeção do rodapé medido). O único item de arquitetura do plano de storage que segue aberto e não depende de credenciais IA é A1c: o plano proíbe explicitamente decidir sobre o índice covering aditivo (§1c, passo 2 -- um dataset novo com bump 3.1.0) a partir só do benchmark sintético, e nenhuma evidência em docs/planning/evidence/ contém a palavra 'bloom' -- confirma que a medição real nunca foi feita. Fechar essa lacuna evita prescrever (ou deixar de prescrever) um índice covering sem base empírica real, e reusa a mesma técnica já validada por A1b (baixar arquivo publicado, reescrever localmente sob o layout candidato de exporter.py, medir)."
success_signal: "docs/planning/parquet-storage-optimization-plan.md deixa de marcar A1c como '[speculative até medir em produção]' e passa a citar uma medição real (bloom_filter_offset por row group, encoding da coluna numero_processo) contra o arquivo de produção djen-tjro-2026/comunicacoes.parquet reescrito sob o layout de exporter.py, com a decisão resultante (índice covering necessário, ou ordenação+bloom já suficientes) registrada -- evidência JSON reproduzível em docs/planning/evidence/, script de benchmark testado, e `uv run pytest -q`/`ruff check`/`ruff format --check` verdes."
status: "achieved"
---

# Goal: confirmar bloom filter em dado real (A1c, gate do índice covering)

O plano de otimização de Parquet marca A1c como o gate da decisão do índice covering aditivo (§1c): antes de prescrever (ou descartar) esse índice, é preciso confirmar em dados reais se o layout já decidido para `comunicacoes` (CNJ-first) de fato produz bloom filter para `numero_processo`, e com que taxa por row group -- o benchmark existente (`bloom_cardinality.py`) é sintético e o próprio plano avisa que a conclusão "tem que ser confirmada em dados reais por A1c... não cravada a partir do sintético". Esta rodada baixa/usa o mesmo arquivo real já usado por A1b (djen-tjro-2026/comunicacoes.parquet), reescreve-o localmente sob o ORDER BY/ROW_GROUP_SIZE reais de `exporter.py` com `WRITE_BLOOM_FILTER true`, inspeciona `parquet_metadata()` por `bloom_filter_offset`/encoding por row group, e mede o ganho de I/O real de um point-lookup por CNJ via range bytes (como pilot_tjro_2026_real_archive_readback.py já fez para outro caso), fechando a lacuna com evidência real.
