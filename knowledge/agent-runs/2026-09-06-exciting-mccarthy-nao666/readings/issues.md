---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-nao666-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-nao666"
subject: "open_issues"
reference: "franklinbaldo/causaganha list_issues(state=OPEN, 26 total) + issue_read on #1168, #1173, #1174, #1136, #924 + live code/data verification of #924's five sub-items, checked 2026-09-06T00:3x"
finding: "New since the last round (qnpypw, ended 00:20Z): the repo owner (author_association=OWNER, login=franklinbaldo, non-claude/* branch names reboot/cobogo-web and reboot/cobogo-panda-home — these read as the human owner's own branches, not an automated session's) opened issue #1168 (reboot the public web experience onto Cobogó/Panda CSS) plus two competing PRs against it: #1169 (a single big-bang PR migrating all 12 surfaces at once, 39 files, +3368/-4854, mergeable_state=unknown, no CI status yet) and #1170 ('first slice' — shell + home only, 7 files, +2764/-500, mergeable_state=behind, needs rebase), together with two follow-up issues (#1173, #1174) that assume #1170's incremental strategy and plan /processo then /publicacoes as the next two slices. #1170's own scope matches #1168's explicitly-defined 'primeira fatia' acceptance criteria exactly; #1169 overshoots that defined first slice by migrating every route in one PR. This is a genuine architectural fork authored by the repo owner across two of their own PRs, not a race between automated sessions — picking a winner is the owner's call, continuing the qnpypw round's 'avoid-web-reboot-collision' decision (see this round's AgentDecision). Consequence: #1136 (padronizar estados de loading/empty/unavailable/error), which PR #1164 explicitly left half-done ('intentionally out of scope for this first slice: /stats, /minhas-consultas'), and #1131-1134/#1093 (other web/UX issues) all sit on a page shell that is about to be replaced wholesale by whichever reboot PR wins — building more legacy-CSS UI now would likely be discarded. #950/#951 (MCP remote hosting) remain live deploy/hosting decisions unsuited to an unattended round. #1022/#1011/#985 (TCU/TSE Internet Archive publication) remain hard-to-reverse public uploads needing explicit sign-off. #1047/#1050-1057/#884/#886/#887 (segmenter) remain annotation- or GPU-heavy. #924 (an unverified model-authored review, explicitly 'não é auditoria' in its own body) was re-verified live item by item: §3.1 (JURIS/DataJud→catalog) is done — `RECONCILE_EXPECTED_SOURCES: djen,juris,stj,datajud` on main, with a code comment citing this exact issue number; §3.3 (dead-code purge) is done — the five named targets are all gone from the tree; §3.4 (canary pending_real alarm) is done — `PENDING_REAL_THRESHOLD`/`PENDING_REAL_MAX_AGE_HOURS_THRESHOLD` live in scripts/canary_check.py with matching tests; the 'risco silencioso' staleness-on-/processo suggestion is done — `ProcessoLookup.svelte` already derives `datasetStale` via `isDatasetStale(...)` and renders it conditionally. §3.5 (layout_revision backfill policy) has its mechanism already shipped and tested (PR #785: `CURRENT_LAYOUT_REVISION` in schema_registry.py, `dates_needing_reconsolidation()` in candidates.py correctly treats missing/empty layout_revision as stale) but had never been checked against the real published archive — this round fetched the live `consolidation-manifest.json` from `https://archive.org/download/causaganha-dashboard/consolidation-manifest.json` (a prior round's attempt 404'd because it looked at a GitHub Pages path that was never where this file is published) and found all 23 currently-tracked items still carry `layout_revision=\"\"`: expected, not a hidden bug, because the physical row-group layout change (A1 in docs/planning/parquet-storage-optimization-plan.md) that would justify bumping past revision 1 is still gated on an unrun production benchmark (A1b/A1c, 'speculative até medir em produção'), so no reconsolidation pass has run yet. §3.2 (segmenter double-annotation for val/test) is still the one genuinely open item — a tiny 3+3 doc seed exists under `data/segmenter_splits/{val,test}.jsonl`, ensemble-adjudicated but explicitly noted as a single-tribunal seed to 'scale to more tribunals/volume next', not the scaled independent annotation RFC 0012 §9.1 requires. Every other line of #924's body (§3.1/§3.3/§3.4/risco-silencioso) is now resolved with live evidence, and the two remaining items (§3.2, §3.5) are already correctly tracked by their own dedicated issues/docs (#1047's segmenter roadmap; docs/planning/parquet-storage-optimization-plan.md's A1/A0e path) — #924 itself, as a one-shot triage document, has served its purpose and its surviving claims have already spawned or been absorbed into proper tracking, matching its own stated intent ('o que sobreviver à verificação merece issue própria; esta aqui é o ponto de partida, não o registro final')."
---

# Leitura de issues abertas

Achado central: a issue #924 (revisão automatizada de estado) está, na prática, totalmente triada — 4 de 5 subitens (e o "risco silencioso") já resolvidos e verificados ao vivo nesta rodada, incluindo uma amostra nunca antes obtida do `consolidation-manifest.json` real publicado no IA (confirma o mecanismo do §3.5 funciona e mede o backlog real: 23/23 itens, esperado, gated no benchmark A1 ainda não rodado em produção). O único subitem genuinamente pendente (§3.2, anotação dupla do segmentador) já tem rastreamento próprio em #1047 e não precisa que #924 continue aberta. Achado secundário: o dono do repositório abriu uma bifurcação arquitetural real (#1168 com PRs concorrentes #1169/#1170) que continua fora do escopo desta rodada — decisão de não interferir, registrada como AgentDecision.
