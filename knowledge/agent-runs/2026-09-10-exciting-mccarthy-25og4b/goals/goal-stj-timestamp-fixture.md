---
type: AgentGoal
id: "2026-09-10-exciting-mccarthy-25og4b-goal-stj-timestamp-fixture"
run_id: "2026-09-10-exciting-mccarthy-25og4b"
goal: "src/causaganha/processos/query_plan_fixtures.py's STJ fixture rows (dataDecisao/dataPublicacao) must be TIMESTAMP-typed with a non-midnight time-of-day, so that the ::DATE casts in service.py's _stj_sql/_documentos_sql and processoCnj.ts's buildStjSql are genuinely exercised against a regression to ::VARCHAR -- matching the analogous datajud fixture column (ultima_atualizacao), which is already TIMESTAMP-typed for exactly this reason."
rationale: "The STJ fixture's dataDecisao/dataPublicacao columns are currently DATE '...' literals. DATE::VARCHAR and DATE::DATE produce the identical ISO string ('YYYY-MM-DD'), so no existing test (tests/causaganha/processos/test_service.py::test_multi_fonte_dossier's `assert result.stj.data_decisao == \"2024-05-01\"`, the mirrored web/src/lib/processoCnj.test.ts assertions, or processoQueryPlanParity.test.ts's cross-runtime parity check) can currently tell a correct ::DATE cast apart from an accidental ::VARCHAR one for this specific column family -- a real regression there would go undetected. This is a three-times-deferred lead from today's lineage (r3erpr's, aezdb9's and yd5lu0's next_move fields all named it as a known gap; none picked it up because each found a stronger concrete bug first, and 41w39p judged the stronger concurrency-bug lineage it was pursuing possibly exhausted). Fixing it closes a genuine, if narrow, test-coverage blind spot without touching production code paths, and does so with the same TDD discipline (RED proving the gap exists today, GREEN proving the fixture change closes it) the task's process requires."
success_signal: "A new test file demonstrates, executed against the CURRENT (DATE-typed) fixture, that DuckDB's ::DATE and ::VARCHAR casts of STJ dataDecisao/dataPublicacao are indistinguishable (RED: an assertion expecting them to differ fails). After changing query_plan_fixtures.py's STJ VALUES to TIMESTAMP '... HH:MM:SS' (non-midnight) for all three rows, the same test proves the two casts now genuinely diverge (GREEN), while every existing consumer of the fixture (tests/causaganha/processos/test_service.py, tests/causaganha_mcp/test_processo_consultar.py, tests/causaganha_mcp/test_arquivo_estado_teor_contract.py, web/src/lib/processoCnj.test.ts, web/src/lib/processoQueryPlanParity.test.ts) stays green unmodified, since the correct ::DATE cast still truncates to the same 'YYYY-MM-DD' string. Full `uv run pytest -q` and the web vitest suite (`cd web && npm test` / the relevant vitest run) both pass; ruff check/format stay clean."
status: "achieved"
---

# Goal: fixture STJ deve ser TIMESTAMP-tipado, não DATE

O fixture `query_plan_fixtures.py` usa `DATE '...'` para `dataDecisao`/`dataPublicacao` do STJ. Como `DATE::VARCHAR` e `DATE::DATE` produzem a mesma string, nenhum teste hoje consegue distinguir um cast `::DATE` correto de um `::VARCHAR` defeituoso nessas colunas -- ao contrário do fixture análogo do `datajud`, que já é `TIMESTAMP`-tipado por essa razão exata. Lead identificado e adiado por três rodadas seguidas (`r3erpr`, `aezdb9`, `yd5lu0`).
