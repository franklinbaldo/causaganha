---
type: AgentReading
id: "2026-09-10-exciting-mccarthy-25og4b-reading-okf"
run_id: "2026-09-10-exciting-mccarthy-25og4b"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-10-exciting-mccarthy-{r3erpr,aezdb9,yd5lu0,41w39p}/run.md"
finding: "Read all four of today's prior same-lineage AgentRun reports. r3erpr/aezdb9/yd5lu0/41w39p form a same-day lineage of 'a concurrency primitive spawned by src/djen_backup/{archive,djen,engine}.py is not defensively tracked/cleaned up when the operation it guards aborts or ends' (circuit-breaker probe vs. lock ordering; IA_UPLOAD_RATE_LIMIT env parsing; orphaned sibling segment-download Tasks; untracked background manifest-upload Task). 41w39p's own next_move judged that lineage likely exhausted within engine.py/djen.py/archive.py and offered two forward options: (a) sweep remaining asyncio.create_task call sites repo-wide (drain.py, CircuitBreaker probe scheduling, CLI entry points), or (b) pick a new lens/module entirely. Separately, a second, weaker lead has been carried unaddressed across three consecutive rounds (r3erpr -> aezdb9 -> yd5lu0, and still unaddressed as of 41w39p, which pursued a different fresh lead instead): src/causaganha/processos/query_plan_fixtures.py's STJ fixture rows (dataDecisao/dataPublicacao) are DATE-typed, so the ::DATE casts in service.py/_stj_sql and processoCnj.ts's buildStjSql cannot be distinguished from a buggy ::VARCHAR cast by any existing test -- a DATE column casts to the same string either way, whereas the analogous datajud fixture column (ultima_atualizacao) is already TIMESTAMP-typed for exactly this reason (its ::VARCHAR cast is genuinely tested via toIsoTimestamp's space-to-'T' normalization). Verified directly by reading query_plan_fixtures.py, service.py, processoCnj.ts, decisoes/search.py (TRY_CAST used defensively there, suggesting the real STJ column type is not guaranteed DATE), and the consuming assertions in tests/causaganha/processos/test_service.py, tests/causaganha_mcp/test_processo_consultar.py, tests/causaganha_mcp/test_arquivo_estado_teor_contract.py and web/src/lib/processoCnj.test.ts -- all of them hardcode the date-only string ('2024-05-01'/'2024-05-10'), which would stay valid unchanged if the fixture's TIMESTAMP were given a non-midnight time-of-day, since the correct ::DATE cast truncates it away regardless. This is a concrete, well-scoped, three-times-deferred test-coverage gap that no round has yet picked up -- chosen as this round's goal in preference to re-opening the asyncio.create_task sweep, which 41w39p already partially covered and flagged as possibly running out of genuinely new instances."
---

# Leitura de conhecimento OKF

Lidos os quatro relatórios `AgentRun` de hoje na mesma linhagem. A linhagem de bugs de concorrência (`archive.py`/`djen.py`/`engine.py`) foi julgada possivelmente esgotada pela própria `41w39p`. Um segundo lead, mais fraco mas nunca endereçado, foi carregado por três rodadas consecutivas (`r3erpr` -> `aezdb9` -> `yd5lu0`): o fixture STJ de `query_plan_fixtures.py` é `DATE`-tipado, então nenhum teste hoje distingue um cast `::DATE` correto de um `::VARCHAR` defeituoso -- ao contrário do fixture análogo do `datajud`, que já é `TIMESTAMP`-tipado por essa razão exata. Escolhido como goal desta rodada.
