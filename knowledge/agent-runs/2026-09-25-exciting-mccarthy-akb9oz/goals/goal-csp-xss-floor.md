---
type: AgentGoal
id: "2026-09-25-exciting-mccarthy-akb9oz-goal-csp-xss-floor"
run_id: "2026-09-25-exciting-mccarthy-akb9oz"
goal: "Fechar #1613 (security/web, TM-08): adicionar uma Content-Security-Policy efetiva a web/src/layouts/Layout.astro (todas as paginas usam este layout) e um piso de regressao automatizado que (a) inventaria e trava os sinks {@html} do app, (b) prova que sanitizeHtml() em web/src/lib/djen.ts neutraliza um corpus de payloads XSS comuns, e (c) limita a origem do worker/dependencia remota do DuckDB-WASM (cdn.jsdelivr.net) em vez de deixar CSP ausente permitir qualquer origem."
rationale: "TM-08 da matriz de threat model (docs/SECURITY_THREAT_MODEL.md) descreve o gap exato: Layout.astro nao tem CSP alguma, entao um bypass do DOMPurify (regressao de configuracao, dependencia comprometida) ou um script malicioso injetado por qualquer outro vetor tem via livre para executar, exfiltrar localStorage (que guarda pesquisas/snapshots salvos, TM-12) ou redirecionar o worker do DuckDB-WASM para uma origem arbitraria. E o proximo item tratavel da ordem de execucao do threat model (Sec.5, item 7), ja recomendado por 2 rodadas anteriores no mesmo dia (r2xele, 3zkmxg) como mais self-contained que #1609 (infra de deploy) e #1614 (supply chain/build). O app e 100% estatico (GitHub Pages, sem controle de headers HTTP), entao a politica so pode ser entregue via <meta http-equiv> -- o que exclui frame-ancestors/sandbox/report-uri do escopo tecnico (ignorados pelo spec quando entregues via meta), uma limitacao a documentar, nao a fingir resolvida."
success_signal: "(1) web/src/layouts/Layout.csp.test.ts (novo) le a fonte de Layout.astro e confirma uma <meta http-equiv=\"Content-Security-Policy\"> com script-src 'self' (sem 'unsafe-inline'/'unsafe-eval'/'*'), object-src 'none', base-uri 'self', form-action 'self', connect-src e worker-src restritos a uma lista fechada de hosts (self + archive.org + comunicaapi.pje.jus.br + o proxy DJEN + cdn.jsdelivr.net, este ultimo so em connect-src/worker-src), e nenhuma diretiva contendo um wildcard '*' cru. (2) web/src/components/htmlSinks.inventory.test.ts (novo) varre src/components e src/pages por {@html} e falha se aparecer em qualquer arquivo alem de PublicationReader.svelte e PublicationDetailPanel.svelte -- trava contra novo sink nao revisado. (3) web/src/lib/djenXssCorpus.test.ts (novo) roda um corpus de >=8 payloads XSS classicos (script tag, onerror/onload de img/svg, javascript: href, data: uri disfarcado de imagem com script embutido, style expression, iframe/object/embed, formaction/autofocus, HTML entity-encoded script) atraves de normalizePublication()/sanitizeHtml() e confirma que o HTML resultante nunca contem tag executavel nem atributo on*/style, preservando texto/HTML benigno em paralelo (nao regressao de falso-positivo). (4) web/src/pages/advogados.astro e comparador.astro perdem o <script define:vars> redundante (o <meta http-equiv=\"refresh\"> ja redireciona) -- unico inline script do app, removido para nao violar script-src 'self' sem 'unsafe-inline'; um teste estatico confirma a ausencia. RED confirmado contra o codigo/paginas atuais antes de qualquer mudanca de producao; GREEN apos a implementacao; npx vitest run (suite web completa) e npm run typecheck/lint ficam verdes. (5) Descoberto so depois, por CI real (compare-product-surfaces, Chromium real): script-src 'self' sem unsafe-inline bloqueava tambem os scripts inline que o proprio Astro injeta para hidratar ilhas Svelte, quebrando ProcessoLookup.svelte de verdade em qualquer navegador real -- nenhum teste Vitest pegou isso, pois nenhum carrega uma pagina real sob a CSP real. Corrigido com hash allowlisting via novo passo postbuild (web/scripts/injectCspHashes.mjs, TDD proprio), nunca com unsafe-inline; ver decision-csp-hash-not-unsafe-inline e evidence-fix-csp-blocks-astro-hydration."
status: "achieved"
---

# Objetivo: CSP + piso de regressao XSS (#1613)

Trabalho principal desta rodada. `docs/SECURITY_THREAT_MODEL.md` (TM-08)
descreve o gap: nenhuma CSP existe em `Layout.astro`, o sanitizer DOMPurify
em `web/src/lib/djen.ts` e os dois sinks `{@html}` (`PublicationReader.svelte`,
`PublicationDetailPanel.svelte`) nao tem regressao automatizada, e o worker
do DuckDB-WASM carrega de `cdn.jsdelivr.net` sem limite de origem
declarado. Ver `decision_ids`/`evidence_ids`/`check_ids` para o processo TDD
completo (RED com os 3 arquivos de teste novos contra o `Layout.astro` e
`djen.ts` atuais, GREEN apos a CSP + remocao do script inline redundante).
