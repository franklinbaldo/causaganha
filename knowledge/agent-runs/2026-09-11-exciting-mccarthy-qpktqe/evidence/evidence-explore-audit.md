---
type: AgentEvidence
id: "2026-09-11-exciting-mccarthy-qpktqe-evidence-explore-audit"
run_id: "2026-09-11-exciting-mccarthy-qpktqe"
goal_id: "2026-09-11-exciting-mccarthy-qpktqe-goal-juris-url-percent-encoding-mismatch"
kind: "review"
reference: "Explore-agent deep line-by-line audit of scripts/ (reconcile_processos.py, render_queries.py, generate_cache_from_manifest.py, check_agent_run_completeness.py) and src/causaganha_mcp/ tool handlers"
summary: "Dispatched after this round's original goal was independently completed by a concurrent session (PR #1458) before this round's own PR #1459 could merge. The agent found one high-confidence, previously-undiscovered bug: src/causaganha_mcp/tools/decisoes.py::_narrow_juris_datasets_for_cnj (line 135) compares item.url (from causaganha.decisoes.published._juris_url, always percent-encoded via quote()) against resolved_urls (from indice_processual.parquet's arquivo_ia_url, populated by scripts/reconcile_processos.py::fetch_juris_from_ia with the RAW un-encoded IA filename) by strict string equality. Every real TJRO JURIS tipo (ACÓRDÃO, SENTENÇA, DECISÃO, DECISÃO DA PRESIDÊNCIA -- confirmed in src/tjro_juris/client.py) contains diacritics, so the two representations never match for any real shard; the CI sync workflow never runs the consolidation step that would produce an ASCII-safe filename, confirming this is the steady-state path, not a rare fallback. Practical effect: decisoes_buscar(cnj=..., fonte='juris'|'todas') silently returns zero JURIS results for essentially every real CNJ with a JURIS decision. Independently verified: (1) live Python repro showing quote('2024-01-ACÓRDÃO.parquet') != the raw string; (2) httpx.URL() auto-encodes the raw string when constructing an HTTP request, confirming the wire-level request already uses the percent-encoded form while the stored arquivo_ia_url does not; (3) the existing test fixture in tests/test_reconcile_processos.py::_mock_juris_remote uses ASCII placeholder filenames ('2024-01-ACORDAO.parquet', no diacritics), which is exactly why this bug was never caught -- it never exercises the encoding path. The agent's runner-up finding (scripts/generate_cache_from_manifest.py::generate_backfill_json missing the try/except ValueError guard that generate_ia_snapshot has around date.fromisoformat) was not adopted: no concrete path producing a NULL/unparseable date for an uploaded manifest row was found (ManifestEntry.date is a required, non-nullable field), so it is a defensive-parity nice-to-have rather than a proven-reproducible bug -- left for a future round if nothing higher-value is available."
---

# Auditoria do agente Explore

Bug real de alta confiança encontrado: mismatch de percent-encoding entre `published._juris_url` (sempre `quote()`) e `arquivo_ia_url` armazenado por `fetch_juris_from_ia` (nome bruto). Todo `tipo` real do TJRO tem acentuação, então a comparação estrita em `_narrow_juris_datasets_for_cnj` nunca bate para nenhum shard real -- efeito prático: `decisoes_buscar` retorna zero resultados JURIS silenciosamente para praticamente qualquer CNJ real. Achado runner-up (guard de data ausente em `generate_backfill_json`) não adotado por falta de caminho de dado real que o dispare.
