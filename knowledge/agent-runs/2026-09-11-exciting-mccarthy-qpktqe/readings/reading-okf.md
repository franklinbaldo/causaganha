---
type: AgentReading
id: "2026-09-11-exciting-mccarthy-qpktqe-reading-okf"
run_id: "2026-09-11-exciting-mccarthy-qpktqe"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-1{0,1}-exciting-mccarthy-*/run.md (all 8 rounds from 2026-09-10/11); knowledge/agent-runs/index.md; knowledge/backlog/index.md; .claude/hourly-loop.md; .claude/agent-run-scaffold.md; git log for the two 'docs(wisk)' merge commits from 2026-09-08 (1a611c5, 63428b0)"
finding: "Read every 2026-09-10/09-11 same-lineage ('exciting-mccarthy') AgentRun report (r3erpr, aezdb9, yd5lu0, 41w39p, 25og4b, 8042ey, njkncp, vd5dfq), each result_state='merged' (vd5dfq's fully closed out this round after resolving its own closing-report PR #1457's merge conflict, see reading-prs.md), each fixing exactly one previously-undiscovered correctness bug via RED-then-GREEN TDD: query_plan_fixtures.py STJ date typing, engine.py orphaned periodic-upload asyncio.create_task, render_manifest_parquet.py write_back_csv re-typed absent/200 constants, archive.py IA_UPLOAD_RATE_LIMIT parse fallback, archive.py circuit-breaker-vs-lock ordering, djen.py parallel segment-download gather, causaganha.processos.service._carregar_cobertura malformed-report KeyError/TypeError, and web/src/lib/processoCnj.ts::toIsoDate's naive-datetime timezone shift (plus three Codex-review follow-ups on that last fix). Coverage is now heavy in src/djen_backup/ and scripts/ (6 of 8 rounds) and web/src/lib/processoCnj.ts is now closed for this exact bug class per vd5dfq's own next_move, which flags a targeted follow-up: grep other TypeScript files that parse a DuckDB-sourced date/timestamp string outside processoCnj.ts's shared helper for the same 'naive datetime reinterpreted via new Date() in non-UTC code' pattern. vd5dfq's next_move also flags a real, still-open tooling gap: `uv run okf-parser check knowledge --relational-schema okf.schema.sql` (the CLI command this scaffold instructs every round to run) reported conformant=true on a bundle that `okf_parser.load_bundle(...).is_conformant` -- the API causaganha_mcp.knowledge.load_pipeline_metadata actually depends on -- correctly rejected as invalid YAML, letting a bad AgentReading file sit undetected long enough to fail 14 unrelated tests in tests/causaganha_mcp/ before being root-caused; no repo-side guard against this CLI/API divergence exists yet. Separately, git log shows two 'docs(wisk)' merge commits from 2026-09-08 (1a611c5, 63428b0, both authored by the same account two days before this lineage started), confirming the Wisk runtime described in knowledge/agent-runs/index.md and .claude/hourly-loop.md as the current hourly-loop entrypoint was genuinely used on this repo, not merely documented. Both index.md and hourly-loop.md still declare the AgentRun/AgentReading/AgentGoal/AgentDecision/AgentEvidence/AgentCheck mechanism 'legacy histórico' and instruct 'não crie novos AgentRun aqui'. This is the same standing tension njkncp and vd5dfq already surfaced and evaluated (a Codex PR comment on vd5dfq's own PR proposed recording through Wisk instead); both chose not to switch, reasoning that the scheduled task prompt driving each of these sessions explicitly requires this exact scaffold as its operational format. This round reconfirms and adopts that same resolution rather than re-litigating it a third time: the task prompt that invoked this session is the authoritative instruction for how this session's work must be recorded, and the 2026-09-08 Wisk commits confirm the tension is real (two different automations of the same repo, on different cadences, using different formats) rather than a stale doc nobody acted on -- but resolving that duplication is a repo-governance decision for the human maintainer, not something this scheduled routine should decide unilaterally by abandoning its own explicit instructions mid-lineage."
---

# Leitura de conhecimento OKF

Lidos os oito relatórios `AgentRun` da lineage `exciting-mccarthy` (2026-09-10/11), todos `merged`. `djen_backup/` e `scripts/` já receberam 6 das 8 correções; `web/src/lib/processoCnj.ts` está fechado para a classe de bug de fuso horário (`vd5dfq`), que recomenda como próximo passo (a) uma varredura por outros arquivos TypeScript com o mesmo padrão de `new Date()` sobre string ingênua, e (b) investigar a divergência entre `okf-parser check` (CLI) e `okf_parser.load_bundle(...).is_conformant` (API), que já deixou um YAML inválido passar despercebido nesta lineage.

Também confirmado via `git log` que o runtime Wisk (`docs(wisk)` commits de 08/09) foi de fato usado neste mesmo repositório dois dias antes desta lineage começar -- a tensão entre os dois mecanismos é real, não apenas documental. `njkncp` e `vd5dfq` já haviam avaliado essa tensão e decidido não trocar de mecanismo, porque o prompt desta rotina agendada exige exatamente este scaffold. Esta rodada reconfirma a mesma decisão em vez de reabri-la.
