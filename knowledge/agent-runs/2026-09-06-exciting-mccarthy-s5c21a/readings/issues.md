---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-s5c21a-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-s5c21a"
subject: "open_issues"
reference: "mcp__github__list_issues franklinbaldo/causaganha state=OPEN (22 results)"
finding: "22 open issues. The post-reboot fork that gated prior rounds is fully resolved: #1168 (reboot) closed, and both of its named route-migration children — #1173 (/processo → Cobogó/Panda) and #1174 (/publicacoes → Cobogó/Panda) — are now closed as completed too (closed_at ~02:02-02:03Z, before this round started). Live read of web/src/pages/stats.astro and minhas-consultas.astro confirms both are already on the new Cobogó/Panda shell (styled-system css()/recipes), not just /processo and /publicacoes. #1136 ('padronizar estados de carregamento, vazio, indisponível e erro entre superfícies') has its own comment history explicitly sequencing this: its first slice (#1164, merged) covered only ProcessoLookup+PublicationSearch via web/src/styles/query-states.css, and its own last comment (00:03:34Z) said '/stats e /minhas-consultas só devem receber o mesmo vocabulário quando forem migradas ao novo shell em slices posteriores' — which has now happened. Live inspection of SavedConsultations.svelte (rendered on /minhas-consultas) shows it already reuses the exact same semantic markers query-states.css keys off of (class=\"empty-state\", role=\"alert\", aria-busy=\"true\") but its own top-level class (.saved-consultations) is absent from every :where() selector in query-states.css, so it gets only the generic global box treatment (web/src/index.css:137) and none of the layout-stability guarantees (min-block-size on empty/alert/loading, narrow-viewport padding) #1136 already proved for the other two surfaces — a concrete, narrow, unblocked slice of #1136 selected as this round's goal. Other candidates: #1134 ('sobre' source coverage) already has an active owner PR (#1182, feat/1134-source-coverage-matrix, CI failing on both test.yml and the visual-capture workflow) — not this session's PR and not one I'm subscribed to watch, so left untouched and noted rather than acted on, per this project's established pattern of not colliding with the owner's own in-flight work. #1131/#1132/#1133/#1093 remain open with no owner narrowing comments yet (#1093 explicitly says 'NÃO é prioridade imediata'). Segmenter issues (#1047/#1050-1057/#884/#886/#887) remain annotation/GPU-heavy; #1022/#1011/#985 (TCU/TSE IA publication) still need explicit human sign-off for live credentialed uploads; #950/#951 (MCP remote hosting) remain live deploy/hosting decisions — all unchanged from prior rounds' assessment."
---

# Leitura das issues abertas

22 issues abertas. A bifurcação do reboot (#1168) e suas duas migrações de rota primária (#1173 `/processo`, #1174 `/publicacoes`) estão fechadas como completed. Isso libera exatamente o próximo passo que a própria #1136 havia sequenciado em seus comentários: estender o vocabulário compartilhado de estados (`query-states.css`) para `/minhas-consultas`, agora que essa página já está no novo shell Cobogó/Panda. Inspeção ao vivo de `SavedConsultations.svelte` confirma que o componente já usa os mesmos marcadores semânticos (`empty-state`, `role="alert"`, `aria-busy="true"`) das duas superfícies já cobertas, mas sua classe de topo (`.saved-consultations`) não está em nenhum seletor de `query-states.css` — logo não recebe a garantia de estabilidade de layout já provada para `ProcessoLookup`/`PublicationSearch`. Selecionado como objetivo desta rodada. A #1134 já tem PR do próprio dono em andamento (#1182, CI vermelho) — não é desta sessão nem estou inscrito para observá-la, então só registrada, não tocada.
