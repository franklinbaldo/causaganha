---
type: AgentReading
id: "2026-09-07-exciting-mccarthy-kfv7sx-reading-okf"
run_id: "2026-09-07-exciting-mccarthy-kfv7sx"
subject: "okf_knowledge"
reference: "knowledge/backlog/index.md and issue-950.md; knowledge/agent-runs/2026-09-06-exciting-mccarthy-uwm65t/run.md (previous round); src/causaganha_mcp/server.py, src/causaganha_mcp/tools/datajud.py, src/causaganha_mcp/http_server.py, tests/causaganha_mcp/test_web_agents_contract.py, tests/causaganha_mcp/test_http_readonly_allowlist.py, tests/causaganha_mcp/test_readme_catalog_contract.py"
finding: "Prior round (uwm65t) closed #1217 and explicitly handed off: re-read open issues fresh, since a new one may appear. #1244 is that new issue. Investigated the actual code #1244 targets: causaganha_mcp.server.build_server() registers 10 tools total via 9 tool modules; datajud.py's single register(mcp) call registers BOTH datajud_status (accepts fonte='local'/diretorio_dados=<arbitrary path>) and datajud_facetas (tribunal/por/limite only, no path) — this is the one module that mixes a path-accepting operator tool with a remote-safe product aggregation in a single register() call, so it must be split to compose independently. causaganha_mcp.http_server already has a *runtime* defense (_PATH_ARGUMENT_TOOLS + PathArgumentGuardMiddleware rejects the four path-arg tools' arguments if a remote caller supplies them, plus _READ_ONLY_TOOL_NAMES + _assert_only_read_only_tools_exposed as a fail-closed allowlist) — this is exactly the 'blacklist ad hoc no deploy' #1244's proposal section names as what the future HTTP entrypoint should stop depending on. That existing mechanism has 3 dedicated test files (test_http_path_argument_guard.py, test_http_readonly_allowlist.py, test_http_transport.py) and #950 (the HTTP deploy this feeds) is independently blocked on a hosting decision (issue-950.md) — so this round scopes to #1244's own self-contained acceptance criteria (composition + tests + web parity gate) and leaves touching http_server.py's already-tested runtime guard to a future round explicitly, rather than risk a large, only-partially-necessary rewrite of tested security machinery in one unattended pass. test_web_agents_contract.py's build_server()-backed parity gate (test_agents_page_only_names_registered_mcp_tools, _decisoes_buscar_fonte_enum) is the concrete 'gate de paridade' #1244 asks to point at the public profile instead. test_readme_catalog_contract.py and test_agent_catalog_contract.py assert against build_server()'s full 10-tool catalog matching README — since this round's plan keeps build_server()'s catalog byte-identical (it becomes a thin wrapper over the new operator profile), those stay green untouched, satisfying #1244's 'stdio existente não sofre quebra silenciosa' criterion structurally, not by exception. No OKF schema/type change is needed: this is product code plus its own pytest contract, like #1217 last round."
---

# Leitura do conhecimento OKF

A rodada anterior fechou #1217 e recomendou reler issues no início — encontramos #1244. Investigação do código: `datajud.py` é o único módulo que mistura, num único `register(mcp)`, uma tool seguro para remoto (`datajud_facetas`) com uma que aceita caminho arbitrário (`datajud_status`) — precisa ser dividido. `http_server.py` já tem uma defesa em runtime (allowlist + middleware) que é exatamente a "blacklist ad hoc" que #1244 quer substituir por composição, mas essa camada tem 3 arquivos de teste dedicados e alimenta #950, que está bloqueado por decisão de deploy — esta rodada limita o escopo à composição + testes + gate de paridade web (auto-contido), deixando a reescrita de `http_server.py` para rodada futura, registrada como `next_move`. `build_server()` permanece byte-idêntico (vira wrapper fino sobre o novo perfil operador), preservando os testes README/catálogo existentes sem exceção.
