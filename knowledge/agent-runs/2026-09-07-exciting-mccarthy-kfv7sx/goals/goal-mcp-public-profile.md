---
type: AgentGoal
id: "2026-09-07-exciting-mccarthy-kfv7sx-goal-mcp-public-profile"
run_id: "2026-09-07-exciting-mccarthy-kfv7sx"
goal: "Compose an explicit public/product MCP tool profile, separate from the operator/local profile, so no filesystem-path-accepting tool is even registered on a server built for remote/product use — closing the self-contained (deploy-independent) slice of issue #1244."
rationale: "causaganha_mcp.server.build_server() currently registers all 10 tools through a single flat composition, including datajud_status(fonte='local', diretorio_dados=<arbitrary path>) alongside the four canonical product tools. #1244, filed by the repo owner minutes before this round and marked READY, asks for an explicit, tested, programmatic split so a future remote endpoint (#950, independently blocked on a hosting decision) can build the safe profile directly instead of relying on a runtime blacklist. This is the only open issue with no external blocker: all other 17 are cached in knowledge/backlog/ as blocked, and there is no open PR to continue."
success_signal: "A new src/causaganha_mcp/profiles.py exposes PUBLIC_TOOL_NAMES/OPERATOR_ONLY_TOOL_NAMES frozensets and build_public_server()/build_operator_server() functions, reusing each tool module's existing register()/register_status()/register_facetas() calls with no business logic duplicated. src/causaganha_mcp/tools/datajud.py's single register(mcp) is split into register_status(mcp) (the path-accepting tool) and register_facetas(mcp) (remote-safe), with register(mcp) kept as calling both for compatibility. causaganha_mcp.server.build_server() becomes a thin wrapper over build_operator_server() and its resulting tool catalog stays byte-identical to today's (verified by a regression test), so every existing test that depends on it (test_readme_catalog_contract.py, test_agent_catalog_contract.py, test_tool_schema.py, stdio) stays green untouched. A new pytest module (tests/causaganha_mcp/test_mcp_profiles.py) is written FIRST and fails RED (ModuleNotFoundError: causaganha_mcp.profiles) before profiles.py exists, then passes GREEN once implemented, asserting: (a) build_public_server()'s tool names equal exactly {processo_consultar, processo_estado, publicacoes_buscar, decisoes_buscar, datajud_facetas, causaganha_status}; (b) none of the public profile's tool JSON schemas exposes a property named diretorio_dados/arquivo_manifesto/caminho_manifesto (the three known local-path escape hatches); (c) datajud_status is present only in the operator profile, never the public one; (d) build_operator_server()'s tool names equal build_server()'s tool names exactly (no silent stdio catalog drift). tests/causaganha_mcp/test_web_agents_contract.py's parity gate (test_agents_page_only_names_registered_mcp_tools and _decisoes_buscar_fonte_enum) is switched from build_server() to build_public_server(), so the /agentes page is now literally checked against the public profile, not an accidentally-wider catalog. ruff check, ruff format --check and the full pytest -q suite stay green; a PR is opened and driven to a mergeable, green state."
status: "achieved"
---

# Goal: perfil MCP público explícito (#1244, fatia autocontida)

Compor `build_public_server()`/`build_operator_server()` em `causaganha_mcp.profiles`, dividindo `datajud.py` para que `datajud_status` (aceita caminho local) nunca seja registrado no perfil público, mantendo o catálogo do `build_server()` (stdio) byte-idêntico e apontando o gate de paridade de `/agentes` para o novo perfil público. Escopo autocontido: não mexe em `http_server.py` (alimenta #950, que segue bloqueado por decisão de deploy).
