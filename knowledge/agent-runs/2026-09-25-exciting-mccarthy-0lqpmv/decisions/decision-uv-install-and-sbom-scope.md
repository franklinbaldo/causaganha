---
type: AgentDecision
id: "2026-09-25-exciting-mccarthy-0lqpmv-decision-uv-install-and-sbom-scope"
run_id: "2026-09-25-exciting-mccarthy-0lqpmv"
goal_id: "2026-09-25-exciting-mccarthy-0lqpmv-goal-supply-chain-lock-nonroot"
question: "Como instalar `uv` dentro de deployment/mcp/Dockerfile (copiar o binário de uma segunda imagem ghcr.io/astral-sh/uv, ou instalar via pip na própria imagem base já pinada), e o critério de conclusão completo de #1614 (lock, digest, non-root, SBOM+scan) cabe inteiro nesta rodada?"
choice: "`RUN pip install --no-cache-dir uv==0.7.22` na própria imagem `python:3.12-slim@sha256:...` já pinada, em vez de `COPY --from=ghcr.io/astral-sh/uv:...`. Versão pinada igual à já usada em .github/actions/setup/action.yml (astral-sh/setup-uv, version: 0.7.22), para as duas superfícies (CI e build de produção) resolverem com a mesma ferramenta. Escopo desta rodada limitado a lock/frozen + digest pin + non-root; SBOM+scanner explicitamente NÃO implementados aqui, registrados como next_move."
rationale: "Copiar o binário `uv` de uma segunda imagem multi-stage (`ghcr.io/astral-sh/uv`) introduziria uma segunda fonte de imagem externa para pinar/auditar, contrariando o espírito de 'mínimo' do próprio invariante de #1614 (menos superfícies de confiança, não mais). Instalar via `pip install uv==<versão exata>` a partir do PyPI (já confiável e usado no resto do projeto para todas as outras dependências) mantém uma única imagem base pinada por digest e reaproveita a mesma versão de uv já fixada em CI, evitando que build de produção e CI usem versões divergentes da própria ferramenta de lock -- o tipo exato de drift que #1614 quer eliminar. SBOM + scanner (o 4º item do critério de conclusão) foi deliberadamente deixado de fora: exige escolher uma ferramenta concreta (syft/grype/trivy para a imagem, ou pip-audit para as dependências Python) e decidir em qual gate de CI ela roda e o que faz falhar o build -- uma decisão de política de segurança nova, não uma mudança mecânica sobre uma política já decidida (diferente de lock/digest/non-root, que são a aplicação direta de uma prática já padrão no restante do projeto: JS já usa lockfiles, GitHub Actions já são SHA-pinned). Fechar 3 dos 4 itens do critério de conclusão com evidência real, deixando o 4º explicitamente registrado como gap e não escondido, segue o mesmo padrão de fatia tratável usado por rodadas anteriores em #1609/#1610 (KV_METADATA sem RFC prévia; egress relay sem tocar em djen_proxy.go na mesma rodada)."
---

# Decisão: uv via pip na imagem já pinada; SBOM/scan fora de escopo desta rodada
