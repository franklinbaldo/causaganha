---
type: AgentReading
id: "2026-09-25-exciting-mccarthy-0lqpmv-reading-issues"
run_id: "2026-09-25-exciting-mccarthy-0lqpmv"
subject: "open_issues"
reference: "GitHub issues abertas (franklinbaldo/causaganha, mcp__github__list_issues, state=OPEN, 25 abertas)"
finding: "Cluster de segurança #1608-#1616 (docs/SECURITY_THREAT_MODEL.md) quase todo fechado por 8+ rodadas OKF anteriores hoje (2026-09-25): #1608/#1611/#1612/#1613/#1615/#1616 fechadas por PR mesclada; #1610 fechado nas fatias tribunal-coerência e schema-fingerprint/item_id djen (KV_METADATA, rodada qn6gvy 10:39Z), com generation-id/hash/row-count completo explicitamente fora de alcance por decisão registrada (decision-kv-metadata-not-full-hash); #1609 com a fatia Go (#1623) e agora também Python+CF (PR #1634, aberta minutos antes desta rodada por outra sessão, claude/exciting-mccarthy-5pnpmt) endereçadas. #1614 (security(supply-chain): tornar build Python e container reproduzíveis e mínimos) segue INTEIRAMENTE aberta, sem nenhuma PR associada (closed_by_pull_requests.total_count=0) e sem nenhum commit relacionado no histórico do repositório -- confirmado por grep em .github/workflows e deployment/ (nenhuma menção a uv.lock/frozen/pip-audit/sbom/syft/grype/trivy/USER não-root em nenhum Dockerfile ou workflow). Verificação direta do estado do repositório (não apenas da issue) confirma cada item do critério de conclusão como um gap real e não hipotético: uv.lock está listado em .gitignore e não é rastreado por git (git ls-files não retorna nada), embora `.github/workflows/deploy-mcp.yml` e `.github/actions/setup/action.yml` já dependam implicitamente de reprodutibilidade (deploy-mcp.yml roda `uv sync --frozen` sem nunca ter um uv.lock committado para ancorar isso -- um `--frozen` sem lock é uma inconsistência latente, não apenas uma lacuna deste issue); `deployment/mcp/Dockerfile` usa `FROM python:3.12-slim` sem pin de digest e `RUN pip install --no-cache-dir .` (resolução livre de versões, não frozen); não há `USER` não-root em nenhum Dockerfile do repositório; não há SBOM nem scanner de dependências/imagem em nenhum workflow. Fora do cluster de segurança: #1605 (segmenter batch27, branch claude/exciting-mccarthy-034xwb) segue bloqueado por conflito de merge em branch sem permissão de push desta sessão, reconfirmado por 6+ rodadas hoje sem nenhum progresso -- não selecionado. #1470/#1469/#1482/#1471/#1472/#1468/#1022/#985 seguem bloqueados por credenciais IA/Cloudflare ausentes neste tipo de sessão, fato já estabelecido por dezenas de rodadas anteriores. #1050/#1051/#1047/#1053-1057/#884/#886/#887 (segmenter RFC0012) são a frente historicamente mais ativa mas já reconfirmada exaustivamente hoje por outras sessões concorrentes (ver commits 4709458..a861568). #1614 é a única frente do cluster de segurança com trabalho zero e escopo claramente tratável sem credenciais de deploy (a parte de lock/digest/non-root é pura mudança de repositório; só SBOM+scan em produção real exigiria decidir uma ferramenta, mas pode ser adicionado ao CI existente sem credenciais externas)."
---

# Leitura: issues abertas
