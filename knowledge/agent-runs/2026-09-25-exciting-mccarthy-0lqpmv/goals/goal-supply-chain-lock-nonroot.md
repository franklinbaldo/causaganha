---
type: AgentGoal
id: "2026-09-25-exciting-mccarthy-0lqpmv-goal-supply-chain-lock-nonroot"
run_id: "2026-09-25-exciting-mccarthy-0lqpmv"
goal: "Fechar a fatia tratável (sem credenciais de deploy) do critério de conclusão de #1614 (security(supply-chain)): commitar uv.lock e exigir resolução frozen tanto em CI (.github/actions/setup) quanto no build de produção (deploy-mcp.yml já assume --frozen sem lock ancorado); reescrever deployment/mcp/Dockerfile para instalar via `uv sync --frozen` a partir desse lock em vez de `pip install .` com resolução livre; pinar a imagem base por digest; e rodar o processo MCP como usuário não-root."
rationale: "Issue #1614 lista quatro itens no critério de conclusão: lock Python versionado e frozen no build; base image por digest; runtime não-root; SBOM+scan ligados ao build/deploy. Verificação direta (reading-issues) confirmou que nenhum dos quatro existe hoje -- uv.lock está gitignored e não rastreado, apesar de deploy-mcp.yml já invocar `uv sync --frozen` (uma inconsistência latente: esse --frozen falharia hoje contra um checkout limpo sem lock); deployment/mcp/Dockerfile usa `FROM python:3.12-slim` sem digest e `pip install .` sem lock; nenhum Dockerfile do repositório tem USER não-root; nenhum workflow gera SBOM ou roda scanner. Os três primeiros itens (lock/frozen, digest, non-root) são mudança pura de repositório, testável estaticamente sem depender de credenciais de deploy nem de decidir uma ferramenta de scan -- mesmo padrão de fatia self-contained que rodadas anteriores usaram para #1609/#1610. SBOM+scan (4º item) fica fora de escopo desta rodada: exige escolher e integrar uma ferramenta (syft/grype/trivy/pip-audit) num job de CI novo, decisão maior que não cabe com segurança junto do resto -- registrado como next_move, não escondido."
success_signal: "tests/deployment/test_mcp_deployment.py ganha testes novos (RED confirmado contra o Dockerfile/repositório anteriores): (1) uv.lock existe na raiz do repositório e não está mais listado em .gitignore; (2) deployment/mcp/Dockerfile referencia uv.lock e roda `uv sync --frozen` (não mais `pip install .` livre); (3) a linha FROM do Dockerfile pina a imagem base por digest (`python:3.12-slim@sha256:...`); (4) o Dockerfile define um usuário não-root (USER != root) antes do CMD final. GREEN depois de: rodar `uv lock` e commitar uv.lock; remover a entrada de .gitignore; reescrever deployment/mcp/Dockerfile com uv+lock+frozen, digest pin e usuário não-root; atualizar .github/actions/setup/action.yml para `uv sync --frozen` (fail-closed se pyproject.toml e uv.lock divergirem) em vez de resolução livre a cada run de CI. uv run ruff check/format --check e uv run pytest -q (suite completa) verdes, sem regressão. PR aberta contra main."
status: "achieved"
---

# Goal: lock + frozen build + digest pin + non-root para o MCP (#1614, fatia sem credenciais)
