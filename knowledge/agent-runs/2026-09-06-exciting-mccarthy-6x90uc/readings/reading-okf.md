---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-6x90uc-reading-okf"
run_id: "2026-09-06-exciting-mccarthy-6x90uc"
subject: "okf_knowledge"
reference: "uv run okf-parser check knowledge --relational-schema okf.schema.sql; knowledge/agent-runs/2026-09-06-exciting-mccarthy-yigsua/run.md (most recent completed AgentRun by completed_at)"
finding: "Bundle conformant at session start: {concept_count: 387, conformant: true, diagnostics: [], markdown_count: 389, reserved_count: 2} — 19 prior completed AgentRun reports under knowledge/agent-runs/, most recent by completed_at being yigsua (2026-09-06T09:46:11Z, fixed #1197 via PR #1200, merged). yigsua's own next_move flagged a concrete, still-unaddressed gap in this loop's own tooling: `okf-parser check` validates only PRIMARY KEY/FOREIGN KEY catalog metadata (per scripts/check_agent_run_completeness.py's own docstring), and even that project's completeness checker (`missing_fields_for_type`) only ever verifies that a *declared* required field is present and non-blank — neither checks whether a frontmatter key is one `knowledge/okf.schema.sql` actually declares for that type. yigsua's own round hit exactly this: it first drafted AgentGoal/AgentDecision/AgentCheck records with renamed fields (title/motivation/procedure instead of goal/rationale/command) that `okf-parser check` reported as conformant, and the drift only surfaced later as an unrelated `pytest -q` failure (a diff in the generated src/causaganha_mcp/_generated/domain_models.py and web/src/lib/processoConsultar.gen.ts). Cross-checked knowledge/okf.schema.sql directly against scripts/check_agent_run_completeness.py's REQUIRED_*_BY_TYPE/ENUM_FIELDS_BY_TYPE dicts and confirmed every currently-shipped Agent*-family document across all 19 prior rounds already uses exactly the schema-declared column names (verified: no false positives would result from adding a strict key check) — this round's own reading and goal follow directly from this finding, with the non-issue-tracked opportunity explicitly authorized by this project's own instructions ('SE O MODELO OKF ESTIVER POBRE PARA REPRESENTAR O TRABALHO, MELHORE O MODELO')."
---

# Leitura do conhecimento OKF

Bundle conformante no início (387 conceitos, 0 diagnósticos). A rodada mais recente concluída (`yigsua`) apontou uma lacuna concreta: nem `okf-parser check` nem o próprio `check_agent_run_completeness.py` detectam um campo de frontmatter com nome divergente do declarado em `knowledge/okf.schema.sql` — só a presença dos campos exigidos é checada, não se as chaves presentes são as certas. A própria rodada `yigsua` caiu nisso e só descobriu via diff inesperado em `pytest -q`. Verificado que todos os 19 relatórios anteriores já usam os nomes corretos, então reforçar o check não quebra nada existente.
