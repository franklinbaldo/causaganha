---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-v38h6d-goal-check-only-no-io"
run_id: "2026-09-09-exciting-mccarthy-v38h6d"
goal: "Make `djen-backup check` actually honor its documented 'no I/O' contract: when SyncConfig.check_only is True, run_pipeline must never feed the existing available/not-yet-uploaded backlog into the download/upload workers."
rationale: "CLAUDE.md documents `djen-backup check --workers 8` as '# Only check DJEN availability (no I/O)', and the check subcommand's own docstring in __main__.py says 'Check DJEN availability without downloading/uploading.' Grepping src/djen_backup/engine.py showed SyncConfig.check_only is set (__main__.py:405) and threaded through service.py into SyncConfig, but never read anywhere in run_pipeline: `backlog = manifest.entries_needing_upload()`, `feeder_task = asyncio.create_task(feed_available())`, `dl_tasks`, and `upload_tasks` were all created unconditionally regardless of check_only. `upload_only` only gates Phase 0 IA discovery (engine.py:393), nothing else. Net effect: `djen-backup check` behaves identically to a full sync whenever there is an existing upload backlog -- it downloads ZIPs and uploads them to IA, directly contradicting its documented contract. This is exactly the kind of 'documented CLAUDE.md rule with no corresponding test' gap this round's dispatched Explore-agent survey was asked to find, and I independently re-traced the full code path (backlog/feeder_task/dl_tasks/upload_tasks wiring, entries_needing_upload semantics, ManifestEntry fields) before accepting it as this round's goal rather than trusting the survey's summary alone."
success_signal: "tests/djen_backup/test_check_only_no_io.py::test_check_only_never_downloads_or_uploads_backlog: seed a manifest with one entry (djen_status='available', djen_raw='200', ia_status='') -- an existing backlog entry -- then run engine.run_pipeline with check_only=True and monkeypatched _stage_download/upload_zip that record a call and raise if invoked. Fails RED on unmodified engine.py (download gets called); passes GREEN after gating backlog/feeder_task/dl_tasks/upload_tasks on `not config.check_only`. Full tests/djen_backup/ suite (118 tests) and the full Python suite stay green; ruff check and ruff format --check stay clean."
status: "achieved"
---

# Goal: `djen-backup check` deve ser genuinamente "no I/O"

Fechar o gap entre o contrato documentado (`# Only check DJEN availability (no I/O)` no CLAUDE.md, e o docstring do subcomando `check`) e o comportamento real de `run_pipeline`: `check_only` era gravado no `SyncConfig` mas nunca lido depois — a fase de download/upload rodava incondicionalmente sempre que já existisse backlog (`djen_status="available", ia_status!="uploaded"`), o que faz `djen-backup check` baixar e subir ZIPs para o IA exatamente como um sync completo.
