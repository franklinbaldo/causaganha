---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-virf8r-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-virf8r"
subject: "open_issues"
reference: "GitHub franklinbaldo/causaganha, mcp__github__list_issues(state=OPEN), scripts/segmenter_governance_status.py"
finding: "22 issues abertas. Cluster Parquet/CNJ (#1468/#1469/#1470/#1471/#1472) teve quase todos os critérios de aceite fechados pelas 6 rodadas anteriores do dia (normalização CNJ, ORDER BY, ZSTD+ROW_GROUP_SIZE 122880 em exporter.py E reconcile_processos.py, certificação de rodapé, leitura direta condicional em processoCnj.ts, bloom filter/covering index descartado com dado real) -- o único item restante (#1472, publicação real no IA do acervo reordenado) segue bloqueado por IA_ACCESS_KEY/IA_SECRET_KEY ausentes neste ambiente (`env | grep -i 'IA_\\|ARCHIVE'` vazio), inalterado desde 11/09. #1482 (CORS archive.org) foi fechado em CI pela rodada 50ns70 (workflow dedicado). #1051 (validation set independente do segmentador) é a issue mais recentemente tocada (atualizada 2026-09-15T08:56, pelas duas últimas rodadas, wvzu11 e 5crg57): `scripts/segmenter_governance_status.py` mostra ao vivo review_count=2/evaluation_eligible_count=2 sobre 61 documentos, subindo de 0 nesta mesma manhã -- 5crg57 registrou explicitamente em seu next_move que a meta de RFC 0012 §5.4 (>=30 val, >=30 test adjudicados) segue muito distante e que o próximo passo natural, sem depender de credenciais IA, é repetir o mesmo mecanismo (subagente Técnica 1 + scripts/adjudicate_segmenter_review.py) sobre os 43 documentos restantes que já têm exatamente 1 anotação unseeded -- reconfirmado nesta rodada por um script de inventário ad hoc (43 candidatos, distribuição de model_family igual à contada por 5crg57). Demais issues abertas (#950/#951/#1093/#1022/#985, cluster segmenter #884/#886/#887/#1047/#1053-1057) seguem gated por infraestrutura ausente (credenciais de deploy, GPU/anotação em escala) ou explicitamente não-prioritárias pelo dono -- sem mudança de estado desde a última verificação."
---

# Leitura de issues abertas

Lidas via `mcp__github__list_issues` (22 abertas) e cruzadas com o estado real do repositório (`env`, `scripts/segmenter_governance_status.py`, inventário ad hoc de candidatos unseeded). O cluster Parquet/CNJ está, em código, praticamente esgotado sem credenciais de escrita no IA -- não há trabalho de domínio novo ali para esta rodada sem essas credenciais. #1051 é a única frente com trabalho real, incremental e desbloqueado disponível: o mecanismo (duas ferramentas testadas, TDD, RFC 0012 §8/§9) já existe e foi provado ponta a ponta em 2 documentos; escalar é repetir o mesmo processo, documento a documento, sobre o pool de 43 candidatos restantes.
