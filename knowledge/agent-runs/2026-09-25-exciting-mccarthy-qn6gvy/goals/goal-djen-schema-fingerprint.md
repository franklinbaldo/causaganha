---
type: AgentGoal
id: "2026-09-25-exciting-mccarthy-qn6gvy-goal-djen-schema-fingerprint"
run_id: "2026-09-25-exciting-mccarthy-qn6gvy"
goal: "Fechar a fatia tratável (sem RFC prévia) do critério de conclusão remanescente de TM-04/#1610 — 'schema fingerprint'/'generation id' coerentes com o manifesto — para a fonte djen: antes de compor read_parquet(arquivo_ia_url) em service.buscar_processo, ler o KV_METADATA do rodapé Parquet já emitido por schema_registry.kv_metadata_for_export (causaganha.schema_version, causaganha.item_id) via parquet_kv_metadata() e rejeitar/degradar (aviso, não exceção fatal) quando o schema_version for desconhecido ou o item_id do artefato divergir do item_id que a própria URL do índice nomeia."
rationale: "Issue #1610/TM-04 exige, além da checagem de tribunal já fechada por e0vvbh, que 'generation id/hash/schema fingerprint/row count' sejam coerentes com o manifesto antes da composição. Duas rodadas anteriores (e0vvbh, akb9oz) concluíram independentemente que essa parte 'não existe em nenhum gerador do repositório' e cogitaram uma RFC. Investigação ao vivo nesta rodada mostrou que isso está parcialmente errado para a fonte djen especificamente: o exportador já grava causaganha.schema_version e causaganha.item_id no rodapé Parquet (schema_registry.py, ativo desde a v3.0.0/2026-05-01) — só nunca foi lido/verificado por nenhum consumidor. parquet_kv_metadata() é uma leitura de rodapé (barata, via httpfs range-read, confirmada ao vivo contra um artefato real do IA), compatível com o padrão de consumo seletivo (DuckDB-WASM/httpfs) que tornava um hash de arquivo inteiro impraticável — motivo real pelo qual rodadas anteriores hesitaram. Isso fecha uma fatia real e verificável do critério de conclusão sem inventar schema novo em nenhum gerador, deixando explicitamente documentado (não escondido) que juris/stj/datajud não emitem esse metadado hoje e que row-count/hash de conteúdo completo continuam fora de alcance sem uma decisão de design maior — não finge fechar TM-04 inteiro."
success_signal: "tests/causaganha/processos/test_service.py ganha testes novos (RED confirmado contra o service.py anterior: AttributeError por função inexistente) cobrindo: extração de item_id a partir da URL djen; _validar_metadata_djen pura (schema_version ausente/desconhecido rejeita, item_id divergente rejeita, item_id/schema coerentes passa, URL não verificável — path local sem shape djen-*-AAAA — não levanta); um teste de integração real com parquet local gravado via COPY...KV_METADATA (schema_registry.kv_metadata_sql_fragment) provando que buscar_processo degrada a fonte djen com aviso (não exceção) quando o rodapé está ausente/incoerente, e continua populando djen normalmente quando o rodapé é coerente — sem regressão nos fixtures existentes (que usam paths locais sem shape djen-*, portanto sem rodapé KV_METADATA, e devem continuar passando pelo bypass 'URL não verificável', igual ao precedente já estabelecido por _tribunal_da_url). GREEN depois da implementação. docs/SECURITY_THREAT_MODEL.md (linha TM-04) e o comentário-guia de #1610 em service.py atualizados para refletir exatamente o que ficou coberto e o que continua em aberto. ruff check/format --check e a suite completa (uv run pytest -q) verdes. PR aberta contra main a partir de claude/exciting-mccarthy-qn6gvy."
status: "achieved"
---

# Goal: schema fingerprint djen (TM-04/#1610)
