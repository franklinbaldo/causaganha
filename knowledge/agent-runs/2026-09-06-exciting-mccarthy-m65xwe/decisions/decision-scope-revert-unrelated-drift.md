---
type: AgentDecision
id: "2026-09-06-exciting-mccarthy-m65xwe-decision-scope-revert-unrelated-drift"
run_id: "2026-09-06-exciting-mccarthy-m65xwe"
goal_id: "2026-09-06-exciting-mccarthy-m65xwe-goal-typecheck-debt-and-ci-gate"
question: "Running `npm run typecheck` (and its `pretypecheck` codegen hook, which regenerates web/src/lib/djen-zod.gen.ts via orval) produced an incidental diff in that generated file — the checked-in version was generated by an older orval release (v8.21.0) than the one package.json now pins (^8.28.1 — confirmed via node_modules/orval/package.json and package-lock.json, unchanged by this round). Keep that regenerated diff in this round's PR, or revert it?"
choice: "Reverted web/src/lib/djen-zod.gen.ts with `git checkout --`, keeping this round's diff confined to the two typing-pattern fixes and the CI workflow addition."
rationale: "The orval-version drift in that generated file is real but predates this round and is orthogonal to the typecheck-debt goal — it reproduces for any session that reruns codegen with the currently pinned orval version, regardless of who runs it or why, and fixing it would require its own verification (comparing the regenerated OpenAPI-derived types against the live API contract) that this round did not do. Bundling an unrelated, unverified generated-file change into a PR about fixing type-annotation debt would widen the review surface for no benefit; it is a legitimate candidate for a future round's own goal, not a side effect of this one."
---

# Decisão: não incluir o diff de orval não relacionado

Rodar `npm run typecheck` regenerou `djen-zod.gen.ts` com uma saída diferente do commit atual porque a versão do `orval` instalada (8.28.1) já diverge da versão que gerou o arquivo hoje versionado (8.21.0) — um drift preexistente, alheio ao objetivo desta rodada. Revertido para manter o escopo da PR restrito à correção de tipagem.
