---
type: AgentGoal
id: "2026-09-06-exciting-mccarthy-m65xwe-goal-typecheck-debt-and-ci-gate"
run_id: "2026-09-06-exciting-mccarthy-m65xwe"
goal: "`npm run typecheck` (astro check) in web/ reports zero errors, and .github/workflows/test.yml's `web` job runs it so a future type regression fails CI instead of accumulating silently."
rationale: "With all 17 open issues blocked or deprioritized (verified live this round, including confirming #1022's IA upload prerequisite #1020 is merged but its actual credentials — IAS3_ACCESS_KEY/IAS3_SECRET_KEY — are absent from this session), this round looked past the issue queue per CLAUDE.md's own instruction that issues are an opportunity queue, not a ceiling. `npm run typecheck` currently reports 19 pre-existing errors — the same count round yigsua's evidence logged in passing ('astro check: identical 19 pre-existing errors, no new ones') without anyone investigating why they persist. Investigating found the real reason: .github/workflows/test.yml's `web` job runs lint/test/build but never `npm run typecheck`, so this whole class of type error is invisible to CI and free to grow unnoticed — a genuine, previously-undocumented reliability gap in the project's own quality gates, not a cosmetic one. All 19 errors trace to two root causes confined to test-only code (`web/src/components/__steps__/shared.ts`'s typed `render` wrapper and three `ProcessoLookup.*.test.ts` files' local `submit()` helper both parameter-type a Svelte Testing Library render result as `ReturnType<typeof render>`, which loses the library's default `Queries` type parameter when taken from an uninstantiated generic function and collapses to an incompatible union type; plus two narrower type-narrowing gaps in `renderedContracts.integration.test.ts`), so fixing them is type-annotation-only and touches zero production Svelte/Astro/Python code — a safe, self-contained, real advancement without needing a product-owner call."
success_signal: "`npm run typecheck` exits 0 with 0 errors (down from 19), `npm test` (vitest) still passes in full with no test logic changed, `npm run lint` and `npm run build` stay green, and .github/workflows/test.yml's `web` job gains a typecheck step positioned so a reintroduced type error fails that job. A PR containing only these test-file type-annotation fixes, the CI workflow change, and this round's OKF report is opened and merged."
status: "achieved"
---

# Goal: eliminar débito de typecheck no web/ e fechar o gate de CI que faltava

19 erros de `astro check` já eram conhecidos e citados como "pré-existentes" por uma rodada anterior sem ninguém investigar a causa: `npm run typecheck` nunca roda em CI. Corrigir as anotações de tipo (sem tocar produção) e adicionar o passo de typecheck ao workflow fecha a lacuna de verificação, não só o sintoma.
