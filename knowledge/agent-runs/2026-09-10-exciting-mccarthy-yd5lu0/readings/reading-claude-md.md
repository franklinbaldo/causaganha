---
type: AgentReading
id: "2026-09-10-exciting-mccarthy-yd5lu0-reading-claude-md"
run_id: "2026-09-10-exciting-mccarthy-yd5lu0"
subject: "claude_md"
reference: "CLAUDE.md"
finding: "Read in full via the system-provided project instructions. Correctness rules unchanged from prior rounds: djen_raw is the raw HTTP transport code (never a verdict on availability); 403 is never absent (CloudFront/WAF rate-limiting); genuine absent is 404, 400 (holidays), or 200-with-'Sem comunicações'-body; sync-manifest.parquet is the sole source of truth (the ~79K legacy-row false-positive issue is already closed per docs/planning/manifest-source-of-truth.md Fase 1-3). 'Rules of the road' documents the per-item lock + circuit-breaker interaction in src/djen_backup/archive.py (fixed in an earlier round, PR #1433) and the IA_UPLOAD_RATE_LIMIT parsing rule (fixed in PR #1441). Style section: no blind except Exception (BLE001 enforced by ruff.toml, extend-select TRY+BLE), TRY300/TRY301/TRY401 enforced (extract raises to inner functions), Python 3.12+. Also separately confirmed (not one of the four mandated readings, but load-bearing for how this round should proceed): .claude/hourly-loop.md states the hourly loop is now operated exclusively by a Wisk-based runtime and marks knowledge/agent-runs/, .claude/agent-run-scaffold.md and the AgentRun/AgentReading/AgentGoal/AgentDecision/AgentEvidence/AgentCheck types as legacy -- 'preserve for audit, but do not create new AgentRuns in the hourly loop'. This session's own scheduled-task prompt, however, explicitly and specifically directs creating this run.md from the legacy scaffold. Two same-day prior rounds (r3erpr, aezdb9) already made this exact judgment call and both successfully completed full scaffold-based cycles (PR #1433, PR #1441, both merged) alongside a visibly separate, interleaved lineage of Wisk-authored commits ('wisk(wiki): ...') in the same repo history -- confirming these are two genuinely parallel, still-active automation tracks rather than one superseding the other. Followed the same precedent: use the legacy AgentRun scaffold for this specific scheduled-task track, as r3erpr's and aezdb9's own AgentDecision/reading records already established and left as the standing interpretation for this track."
---

# Leitura de CLAUDE.md

Releitura completa via as instruções de projeto fornecidas pelo sistema. Nenhuma divergência de correção em relação às rodadas anteriores. Confirmado que `.claude/hourly-loop.md` marca o mecanismo `AgentRun`/scaffold como legado do loop horário baseado em Wisk, mas o prompt desta sessão pede explicitamente o fluxo legado -- tratado, como em `r3erpr` e `aezdb9`, como uma trilha de automação separada e ainda ativa.
