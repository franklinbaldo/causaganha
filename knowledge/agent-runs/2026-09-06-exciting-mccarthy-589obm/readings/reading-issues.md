---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-589obm-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-589obm"
subject: "open_issues"
reference: "mcp__github__list_issues(owner=franklinbaldo, repo=causaganha, state=OPEN) as of 2026-09-06T19:20Z; knowledge/backlog/index.md and its 17 issue-<n>.md files; issue #985's full comment thread"
finding: "17 open issues, all already catalogued in knowledge/backlog/ (884, 886, 887, 950, 951, 985, 1011, 1022, 1047, 1050, 1051, 1053, 1054, 1055, 1056, 1057, 1093). Re-verified this round rather than trusted blindly, because this session's runtime turned out to differ materially from the last verifying round (buxwff, 15:24Z) on the one point that matters for #985: DNS resolves and TCP connects to both github.com and cdn.tse.jus.br in this session (buxwff and the round before it hit 'Could not resolve host' / 'Temporary failure in name resolution' on both). Live curl against the three official ZIP URLs in src/tse_processual/catalog.py, the bare /estatistica/sead/ directory, and the CKAN API at dadosabertos.tse.jus.br/api/3/action/package_show all returned HTTP 403 with an Akamai edgesuite.net 'Access Denied' body (not a TSE application response), reproducible with and without browser-like User-Agent/Referer/Range headers, and reproducible even via a real Chromium (Playwright) navigation (net::ERR_CONNECTION_RESET). This is new, more precise evidence than the DNS-failure story every prior round recorded: this runtime's egress can reach tse.jus.br's Akamai front but is rejected network-wide, consistent with an IP/geo-level block rather than a per-file or bot-fingerprint block a different header set could route around. Separately, discovered that knowledge/backlog/issue-985.md itself has been carrying the wrong blocking_reason since its creation in #1211: it was copy-pasted from the Internet-Archive-upload-credentials template that correctly describes #1011/#1022 (publishing TCU data to IA) but has never matched #985's actual blocker, which per the issue's own thread (comments from 2026-09-03 through 2026-09-06T19:00Z) has always been read/network access to TSE's official ZIPs, not IA upload credentials — #985 has not reached the IA-upload step yet. Every round since #1211 that 're-verified' #985 (including this round's own initial env-var grep) mechanically confirmed 'IAS3 keys still absent' without going back to what #985 itself needs, silently perpetuating the wrong record. Remaining 16 items unchanged: same credential/infra-decision/owner-deprioritization blockers, no GitHub state change since buxwff's verification."
---

# Leitura das issues abertas

17 issues abertas, todas já no backlog. A novidade real desta rodada é técnica, não do GitHub: o runtime desta sessão resolve DNS e conecta via TCP a `cdn.tse.jus.br` (as rodadas anteriores não conseguiam), mas todo caminho testado devolve HTTP 403 "Access Denied" da própria Akamai — evidência mais precisa que a história de "sem DNS" registrada até aqui. Também descobri que `knowledge/backlog/issue-985.md` carrega, desde a criação em #1211, a `blocking_reason` errada (copiada do template de credenciais de upload para IA, que descreve #1011/#1022, não #985). Essa é a lacuna que esta rodada corrige.
