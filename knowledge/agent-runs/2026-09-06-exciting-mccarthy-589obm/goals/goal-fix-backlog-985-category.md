---
type: AgentGoal
id: "2026-09-06-exciting-mccarthy-589obm-goal-fix-backlog-985-category"
run_id: "2026-09-06-exciting-mccarthy-589obm"
goal: "Correct knowledge/backlog/issue-985.md's category and blocking_reason to describe #985's real blocker (network access to TSE's Akamai-fronted infrastructure), add a 'network_access' BacklogItem category to the OKF schema so this class of blocker has a proper home, and add a regression test that pins this so the wrong reason cannot silently drift back in."
rationale: "All 17 open issues are confirmed still blocked this round (see reading-issues.md) — there is no unblocked issue-queue work available, and both the Python and web suites are already fully green with no lint/TODO debt found. Per this round's own read of the OKF knowledge (reading-okf.md), knowledge/backlog/issue-985.md has carried the wrong blocking_reason since its creation in #1211: it describes Internet Archive upload credentials (#1011/#1022's real blocker), not #985's actual blocker (read access to TSE's official ZIPs), and this error survived two subsequent 're-verification' rounds because they only re-checked the recorded reason's own premise (IAS3 keys absent) rather than the issue's actual scope. This round's own live network investigation produced a more precise, previously-undocumented fact — DNS/TCP now succeed against tse.jus.br, but every path is rejected with an Akamai-level 403 'Access Denied', not a TSE application response — so this is genuine, evidence-first advancement (correcting a durable, cross-round knowledge artifact) rather than busywork, and it prevents future rounds from repeating the same misdiagnosis."
success_signal: "tests/knowledge/test_backlog.py gains a new test asserting issue-985.md's category is 'network_access' and its blocking_reason mentions tse.jus.br/Akamai rather than IAS3/Internet Archive; that test fails (RED) against the current file before any edit, and passes (GREEN) after knowledge/okf.schema.sql's BacklogItem.category CHECK constraint gains 'network_access' and knowledge/backlog/issue-985.md is rewritten with the accurate blocking_reason, unblock_condition, and this round's last_verified_run_id/last_verified_at. The full existing test_backlog.py suite, uv run ruff check/format --check/pytest -q, and uv run okf-parser check knowledge --relational-schema okf.schema.sql all stay green after the change."
status: "achieved"
---

# Goal: corrigir a categoria/motivo de bloqueio da #985 no backlog OKF

Nenhuma das 17 issues abertas está desbloqueada nesta rodada, e o repositório está limpo (lint/testes/OKF verdes, sem débito óbvio). O avanço real disponível é corrigir uma inconsistência real entre o conhecimento OKF durável (`knowledge/backlog/issue-985.md`) e o estado real da issue #985: o arquivo descreve o bloqueio de credenciais de upload para o Internet Archive (que é o bloqueio de #1011/#1022), não o bloqueio real de #985 (acesso de leitura aos ZIPs oficiais do TSE). Corrijo isso com TDD: teste novo RED → schema com nova categoria `network_access` → conteúdo do `BacklogItem` corrigido → GREEN.
