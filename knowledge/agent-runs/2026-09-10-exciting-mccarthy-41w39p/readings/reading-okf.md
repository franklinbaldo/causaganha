---
type: AgentReading
id: "2026-09-10-exciting-mccarthy-41w39p-reading-okf"
run_id: "2026-09-10-exciting-mccarthy-41w39p"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-10-exciting-mccarthy-yd5lu0/run.md"
finding: "Read the most recent same-day AgentRun report in full. yd5lu0 fixed src/djen_backup/djen.py's download_zip, which left sibling segment-download Tasks orphaned in the background on partial failure (asyncio.gather does not cancel siblings on the first exception) -- the third bug in a same-file-family lineage of 'a stateful resource / concurrency primitive is not defensively cleaned up when a guarded operation aborts partway' (r3erpr: circuit-breaker probe vs. per-item lock ordering in archive.py; aezdb9: IA_UPLOAD_RATE_LIMIT env parsing constructing a broken AsyncLimiter in archive.py; yd5lu0: orphaned sibling Tasks in djen.py). yd5lu0's own next_move explicitly named the natural continuation of this exact lens: 'src/djen_backup/engine.py's own worker loops (checker_worker, upload_worker, download_worker) run several concurrent coroutines via asyncio.gather/create_task patterns that have not been read with this same lens (does a sibling task get orphaned when one fails?) -- ... the natural continuation of the pattern this round and the two before it have now found three times in this same module family.' Read engine.py directly (see AgentGoal) applying exactly that lens to every asyncio.create_task/gather call site: found a fourth instance -- run_pipeline's periodic manifest-segment upload (engine.py:589, checker_worker) is fired via a bare `asyncio.create_task(_upload_manifest_background())` with no reference kept anywhere, and run_pipeline's shutdown (`finally` block) never awaits or tracks it before returning. This is a distinct sub-pattern from yd5lu0's fix (that was sibling Tasks left running past a failure the caller already knows about; this is a background Task never joined at all, so it either finishes racing against process/event-loop teardown or -- per the asyncio docs' explicit warning that an unreferenced Task may be garbage-collected before completion since the loop holds only a weak reference -- can be silently dropped), but it is the same root family: a concurrency primitive spawned by this module is not defensively tracked to completion. CLAUDE.md documents this exact background upload as 'protects against crashes' -- an untracked, possibly-abandoned instance of it undermines its own stated purpose."
---

# Leitura de conhecimento OKF

Leitura do `AgentRun` mais recente do mesmo dia (`yd5lu0`), cujo `next_move` apontou explicitamente `engine.py`'s worker loops como a continuação natural da linhagem de bugs "recurso concorrente não limpo/rastreado ao falhar/encerrar" (já encontrada 3x em `archive.py`/`djen.py`). Aplicada a mesma lente a `engine.py` -- usada para orientar o goal desta rodada: o upload periódico do manifest é disparado via `asyncio.create_task` sem referência guardada nem aguardado no shutdown.
