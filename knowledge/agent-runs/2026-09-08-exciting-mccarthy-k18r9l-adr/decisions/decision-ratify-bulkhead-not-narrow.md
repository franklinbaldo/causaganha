---
type: AgentDecision
id: "2026-09-08-exciting-mccarthy-k18r9l-adr-decision-ratify-bulkhead-not-narrow"
run_id: "2026-09-08-exciting-mccarthy-k18r9l-adr"
goal_id: "2026-09-08-exciting-mccarthy-k18r9l-adr-goal-except-exception-adr"
question: "For the 4 except-Exception sites, choose between: (a) narrow each to specific exception types per CLAUDE.md's literal rule, (b) ratify the broad catch as an intentional 'bulkhead' pattern via ADR and fix the documentation instead, or (c) something else (e.g. suppress via ruff ignore only)."
choice: "(b) Ratify all 4 sites as an accepted bulkhead pattern via docs/adr/0011, amend CLAUDE.md's rule text to state the carve-out explicitly, add a one-line ADR-citing comment at each site, and back the policy with an automated test (tests/test_except_exception_policy.py) rather than relying on prose alone."
rationale: "Narrowing (option a) was checked concretely per site, not assumed: archive.py's ia_s3.upload_to_ia() already catches its own known failure modes (httpx.HTTPError, httpx.RequestError, OSError) internally and returns False, so anything still reaching the caller's except block is by construction a bug in code that runs before that internal try (e.g. header-building) -- narrowing the caller would let that bug propagate out of the uploader worker pool's loop and crash the whole pool instead of failing one item, directly contradicting CLAUDE.md's own stated purpose for this subsystem ('Periodic IA upload every 10 min protects against crashes'). llm_analyzer.py's litellm.acompletion() multiplexes multiple provider SDKs; inspecting litellm.exceptions' actual class MROs shows most failures share litellm.exceptions.APIError as a common base, but litellm's own guardrail/budget exceptions (BudgetExceededError, GuardrailRaisedException, BlockedPiiEntityError, SensitiveDataRouteException, ModifyResponseException) inherit directly from Exception with no narrower shared ancestor -- narrowing to APIError would silently stop retrying/falling-back on those with no evidence that's the desired behavior, and the existing _is_retryable() already does real discrimination by message content across whatever type comes back, re-raising anything classified as non-retryable. cli.py's export_table_sync runs arbitrary ibis/DuckDB/PyArrow code per table with no common exception ancestor narrower than Exception. Option (c), a bare ruff ignore, was rejected because ruff's BLE001 already silently exempts these sites via their `.exception(...)` calls -- adding a ruff-level ignore would add nothing and wouldn't fix the actual problem, which is that CLAUDE.md's prose contradicts what the codebase already (correctly) does. Backing the ADR with a test rather than trusting the prose/comment alone follows this session's own established pattern from earlier in the day (the README optional-contracts drift was also converted from a bare docs edit into a regression test) -- a written rule with no enforcement is exactly how this gap survived 3 rounds unaddressed in the first place."
---

# Decisao: ratificar o padrao bulkhead, nao estreitar os tipos

Ver `run.md`/`goals/goal-except-exception-adr.md` para o contexto completo. Verificado por site, nao por suposicao: estreitar quebraria a resiliencia que cada subsistema foi desenhado para ter. Decisao apoiada por um teste automatizado, nao apenas por prosa/comentario.
