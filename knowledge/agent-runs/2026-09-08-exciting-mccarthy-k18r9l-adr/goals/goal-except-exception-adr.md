---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-k18r9l-adr-goal-except-exception-adr"
run_id: "2026-09-08-exciting-mccarthy-k18r9l-adr"
goal: "Resolve the except-Exception/BLE001 architectural gap that 3 prior rounds (1c7t6u, b4t8pv, k18r9l) logged as 'needs a human decision' but never decided: write an ADR ratifying or rejecting the current 4 broad-except sites, update CLAUDE.md's rule to match reality, and add an automated test enforcing the decision going forward."
rationale: "CLAUDE.md states an absolute rule ('No blind except Exception') that 4 real call sites already violate (src/djen_backup/archive.py:264, src/causaganha/analysis/llm_analyzer.py:387/499, src/causaganha/consolidate/cli.py:160), invisibly to ruff's BLE001 because their handlers call `.exception(...)` (confirmed directly: a minimal repro shows `.exception(...)` suppresses BLE001, `.warning(...)` does not). This left the codebase and its own style rule silently disagreeing, and three consecutive agent rounds identified this exact gap and each deferred it as needing human judgment rather than deciding. The live user explicitly instructed this round to stop deferring: decide it now, using an ADR and industry best practice, no human needed. Analysis of all 4 sites shows they share one shape -- a per-item bulkhead inside a worker-pool loop over independent units of work (one IA upload, one LLM call, one table export), each already logging the full exception via `.exception()` before recording a per-item failure or re-raising for known-fatal cases. This is the standard 'bulkhead' resilience pattern for batch/worker systems (isolate one item's failure from the rest of the batch), not an oversight: narrowing any of the 4 sites to specific types would let an unanticipated bug in one item crash the entire worker pool instead of failing just that item -- verified per-site (archive.py's ia_s3.upload_to_ia already catches its own known httpx/OSError cases internally and returns False, so anything else reaching the caller is by definition a bug that would otherwise crash the uploader pool; llm_analyzer.py's litellm.acompletion() multiplexes over multiple provider SDKs whose guardrail/budget exceptions -- confirmed via litellm.exceptions' actual class MROs -- don't share a narrower common base than Exception, and the existing _is_retryable() already discriminates by message content and re-raises non-retryable cases; cli.py's export_table_sync runs arbitrary ibis/DuckDB/PyArrow code with no common exception ancestor narrower than Exception, and one table's failure must not abort exporting the rest)."
success_signal: "docs/adr/0011-broad-except-in-worker-loop-bulkheads.md exists in standard Nygard format (Title/Date/Status/Context/Decision/Consequences) ratifying the 4 sites as an accepted bulkhead pattern. CLAUDE.md's 'No blind except Exception' rule is amended to state the carve-out and cite the ADR. All 4 call sites carry a one-line comment citing docs/adr/0011. A new test (tests/test_except_exception_policy.py) scans src/ for every `except Exception` and asserts each cites the ADR (or is in the pre-existing ruff.toml per-file-ignore) -- confirmed RED when a citation is stripped from one site (temporarily verified on archive.py:264, then restored), GREEN with all 4 sites correctly cited. Full uv run pytest -q, ruff check, and ruff format --check stay green. okf-parser check stays conformant."
status: "achieved"
---

# Goal: decidir e documentar o gap except-Exception/BLE001 via ADR

Pista carregada por 3 rodadas anteriores (`1c7t6u`, `b4t8pv`, `k18r9l`) como "precisa de decisao arquitetural" e nunca decidida. Usuario ao vivo instruiu explicitamente: decidir agora, sem esperar humano, usando ADR e melhores praticas da industria. Analise dos 4 sites confirma um padrao coerente de "bulkhead" por item dentro de loops de worker pool -- decisao: manter o catch amplo, documentar via ADR, e transformar a politica num teste automatizado que impede um novo `except Exception` nao justificado no futuro.
