---
type: AgentReading
id: "2026-09-10-exciting-mccarthy-aezdb9-reading-okf"
run_id: "2026-09-10-exciting-mccarthy-aezdb9"
subject: "okf_knowledge"
reference: "knowledge/index.md, knowledge/backlog/index.md + issue-*.md, knowledge/agent-runs/2026-09-10-exciting-mccarthy-r3erpr/run.md, .claude/hourly-loop.md"
finding: "knowledge/index.md describes the OKF bundle's current scope (Fonte/Pipeline domain concepts + this AgentRun mechanism). The most recent prior round (r3erpr, same day, completed 17:35Z) fixed a circuit-breaker/per-item-lock ordering bug in src/djen_backup/archive.py's upload_zip and merged PR #1433; its next_move explicitly flagged two concrete unexplored leads plus a suggested systematic audit: (a) a TIMESTAMP-typed STJ query-plan fixture gap in src/causaganha/processos/query_plan_fixtures.py, (b) zero test coverage for IA_UPLOAD_RATE_LIMIT's env-var override/fallback parsing in archive.py, and (c) a general sweep of archive.py/djen.py/engine.py for other places a stateful/single-use resource is consumed before a cheaper guard that could abort first. Investigated lead (b) directly this round (see AgentGoal below) and found it is not just an untested code path but an actual unguarded-crash bug: archive.py's `_ia_max_rate = int(os.environ.get('IA_UPLOAD_RATE_LIMIT', '4'))` only catches ValueError (non-numeric strings) and falls back to 4, but does not reject numerically-valid non-positive values (0 or negative) -- and AsyncLimiter.acquire() unconditionally raises ValueError when `0 <= amount <= max_rate` is false, which is always true when max_rate <= 0. That acquire() call at archive.py:268 sits outside upload_zip's own try/except (which only wraps ia_s3.upload_to_ia), and the caller chain (_process_upload_item's except clause in engine.py only catches httpx.HTTPError/OSError, and upload_worker's try/finally doesn't catch at all) never catches ValueError either -- so IA_UPLOAD_RATE_LIMIT=0 or a negative value crashes every single upload attempt with an unhandled exception instead of falling back to a sane default, unlike the non-numeric-string case which is already handled gracefully. This is a genuine, previously-unflagged correctness bug (a misconfiguration that should degrade gracefully, per the module's own existing fallback-to-4 precedent for parse failures, instead silently breaks all uploads), not just a coverage gap -- chosen as this round's primary goal over leads (a) and (c) since it is concrete, testable, and immediately actionable without needing new infra."
---

# Leitura de conhecimento OKF

Retomando o `next_move` da rodada anterior (r3erpr): a lacuna de cobertura de teste do `IA_UPLOAD_RATE_LIMIT` não é só falta de teste -- é um bug real de crash não tratado quando o valor é `0` ou negativo (`AsyncLimiter.acquire()` levanta `ValueError` sem handler em toda a cadeia de chamadas). Escolhido como goal principal desta rodada.
