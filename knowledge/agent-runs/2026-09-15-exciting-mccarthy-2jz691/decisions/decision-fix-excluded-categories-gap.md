---
type: AgentDecision
id: "2026-09-15-exciting-mccarthy-2jz691-decision-fix-excluded-categories-gap"
run_id: "2026-09-15-exciting-mccarthy-2jz691"
goal_id: "2026-09-15-exciting-mccarthy-2jz691-goal-scale-segmenter-reviews"
question: "7drjlg registrou como AgentDecision (não corrigida) um gap estrutural: annotate_second_independent.py descarta automaticamente EXCLUDED_CATEGORIES={ref_normativa} antes de persistir uma anotação, mas adjudicate_segmenter_review.py não replica esse descarte no resolution-file, então um resolution-file copiado de uma anotação com ref_normativa falha a validação mecânica ('category not in ontology'). 7drjlg escolheu um band-aid manual e adiar o fix estrutural para 'uma rodada futura ... provavelmente quando um documento repetir o mesmo padrão'. Esta rodada bateu no MESMO gap em 2 dos 3 documentos adjudicados (doc_f22271af, doc_bed363d9) -- o padrão se repetiu exatamente como previsto. Corrigir agora com um módulo compartilhado (fix estrutural) ou aplicar band-aid manual pela terceira vez consecutiva?"
choice: "Corrigir estruturalmente agora: extraído EXCLUDED_CATEGORIES/_drop_excluded_categories de annotate_second_independent.py para um novo `drop_excluded_categories`/`EXCLUDED_CATEGORIES` em src/segmenter_dataset/ontology.py (módulo já compartilhado por ambos os scripts para ALLOW_MULTIPLE_SINGLE_ANCHOR/ONTOLOGY_V8/load_categories), e adjudicate_segmenter_review.py passou a aplicar o mesmo drop em build_review antes de validate_record."
rationale: "O próprio 7drjlg já apontou o gatilho exato para esse fix ('provavelmente quando um documento repetir o mesmo padrão') e ele se repetiu nesta mesma rodada, não numa futura -- adiar de novo seria ignorar o próprio critério que a decisão anterior definiu. O fix é pequeno, mecânico e de baixo risco (mover uma função pura + uma constante para um módulo já importado por ambos os scripts, sem mudar comportamento em nenhum outro caminho), coberto por um teste RED->GREEN novo (test_build_review_drops_ref_normativa_before_validation) que reproduz exatamente a falha ao vivo encontrada nesta rodada antes do fix. A suíte completa de segmenter_dataset permanece verde (365/365, +1 do teste novo) e ruff/format ficam limpos. Não estendido a scripts/ingest_juris_technique1_batch.py nem scripts/ingest_synthetic_segmenter_corpus.py (que têm sua própria cópia local do mesmo padrão) -- fora do escopo desta rodada (nenhum dos dois falhou ao vivo aqui, e mudá-los sem um teste que force a mudança seria refatoração especulativa); registrado como observação para quem tocar esses dois scripts de novo."
---

# Decisão: corrigir o gap EXCLUDED_CATEGORIES entre os dois scripts de adjudicação

Ao adjudicar doc_f22271af e doc_bed363d9 nesta rodada, o mesmo `MechanicalValidationError: category 'ref_normativa' not in ontology` que 7drjlg encontrou (e corrigiu manualmente, uma vez) apareceu de novo -- desta vez duas vezes na mesma rodada. Em vez de aplicar o band-aid manual pela terceira vez, TDD: escrito `test_build_review_drops_ref_normativa_before_validation` (RED, reproduzindo a falha), depois `drop_excluded_categories`/`EXCLUDED_CATEGORIES` movidos para `src/segmenter_dataset/ontology.py` e usados por `annotate_second_independent.py` (substituindo sua cópia local) e por `adjudicate_segmenter_review.py::build_review` (novo uso). Teste GREEN, suíte completa de `tests/segmenter_dataset` 365/365, `ruff check`/`ruff format --check` limpos em todo o repositório.
