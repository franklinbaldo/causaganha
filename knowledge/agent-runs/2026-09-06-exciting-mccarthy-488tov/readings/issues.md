---
type: AgentReading
id: "2026-09-06-exciting-mccarthy-488tov-reading-issues"
run_id: "2026-09-06-exciting-mccarthy-488tov"
subject: "open_issues"
reference: "mcp__github__list_issues(owner=franklinbaldo, repo=causaganha, state=OPEN) as of 2026-09-06T~21:10Z; knowledge/backlog/index.md and its 17 issue-<n>.md files"
finding: "18 open issues. 17 are already catalogued in knowledge/backlog/ (884, 886, 887, 950, 951, 985, 1011, 1022, 1047, 1050, 1051, 1053, 1054, 1055, 1056, 1057, 1093), each last_verified earlier the same day with no GitHub state change since — trusted per the backlog mechanism's own instructions, not re-derived. The genuinely new one is #1235 'web(minhas-consultas): permitir exportar e importar os salvos sem criar conta', filed by the repo owner (franklinbaldo, author_association OWNER) at 2026-09-06T21:01:11Z, explicitly marked 'READY para IMPLEMENTAÇÃO' in its own body, with a concrete implementation direction ('Começar por funções puras de export/parse/merge com round-trip e falha atômica; só depois ligar download/file input à UI') and ten falsifiable acceptance criteria (download without account/network, explicit schema version, round-trip into empty storage, merge/dedup rule covered by test, atomic failure on invalid/unknown-version input, no URL/SQL/HTML injection via imported data, keyboard+mobile usability, explicit non-sync framing in the UI copy, tests covering round-trip/merge/duplicate/corruption/unknown-version, and an explicit tested decision on whether snapshots/baselines are included). It is a direct follow-up to already-merged #908 (local-first storage) and #1232 (change-tracking semantics, closed by the immediately preceding round 1na8o6) and explicitly says it must not reopen #1232's comparison semantics. No external blocker (no credentials, no infra/deploy decision, no GPU/annotation work) — matches exactly the shape of every other round's selected work today (#1217, #1228, #1232). Zero open PRs exist to compete for attention (confirmed via mcp__github__list_pull_requests state=open -> [])."
---

# Leitura das issues abertas

17 das 18 issues abertas seguem cobertas por `knowledge/backlog/`, sem mudança de estado desde a última verificação de hoje. A novidade é #1235, aberta pelo dono do repositório minutos após a rodada anterior (1na8o6) terminar — exatamente como o `next_move` daquela rodada previu. É "READY para IMPLEMENTAÇÃO", com direção de implementação e critérios de aceite concretos e testáveis, follow-up de #908/#1232 sem reabrir a semântica de comparação já corrigida. Nenhuma PR aberta concorre por atenção. Esta é a issue escolhida para o trabalho da rodada.
