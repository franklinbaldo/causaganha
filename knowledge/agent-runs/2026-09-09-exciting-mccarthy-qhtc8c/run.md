---
type: AgentRun
id: "2026-09-09-exciting-mccarthy-qhtc8c"
started_at: "2026-09-09T22:24:01Z"
completed_at: "2026-09-09T22:51:04Z"
branch_at_start: "claude/exciting-mccarthy-qhtc8c"
commit_at_start: "362e068a0ac8b138e22e8bcafbce19c0bfc28767"
claude_md_reading_id: "2026-09-09-exciting-mccarthy-qhtc8c-reading-claude-md"
issues_reading_id: "2026-09-09-exciting-mccarthy-qhtc8c-reading-issues"
prs_reading_id: "2026-09-09-exciting-mccarthy-qhtc8c-reading-prs"
okf_reading_id: "2026-09-09-exciting-mccarthy-qhtc8c-reading-okf"
goal_ids:
  - "2026-09-09-exciting-mccarthy-qhtc8c-goal-stj-date-cast-web-sql-twin"
primary_goal_id: "2026-09-09-exciting-mccarthy-qhtc8c-goal-stj-date-cast-web-sql-twin"
considered_work:
  - "17 open issues (segmenter roadmap, TCU/TSE/MCP endpoint) -- all re-verified blocked, same as every round today (GPU/annotation infra absent, product/infra decisions pending, live 403 blocking #985)."
  - "The single open PR (#1353, Dependabot) -- healthy automated dependency bump, not agent work to resume."
  - "Own investigation (this round) of scripts/render_manifest_parquet.py's urllib-based IA segment downloads, following up on round 02ggxp's own next_move: confirmed no gap exists -- those are whole-file GETs (no Range header), and urllib.request.urlopen() already raises on any non-2xx status, caught by the existing except (urllib.error.URLError, OSError) handlers. Does not share djen.py's Range-GET reassembly risk shape. No fix needed; independently confirmed by the dispatched Explore-agent."
  - "Own manual review of remaining web/src/queries/*.qmd contracts not yet touched today (consolidation_status, court_reliability, daily_uploads, datajud_*, juris_*, lawyer_leaderboard, processos_multi_fonte, site_status, stj_relatores/temas/totals, tribunal_calendar/coverage) -- found no new bug; site_status.qmd's coverage_pct/pending_real_max_age_hours already use correct NULLIF/epoch-of-NULL patterns, court_reliability.qmd's dead ELSE 0 branch is unreachable-but-harmless, tribunal_coverage.qmd's dead 'confirmed' IN-clause branch is defensive-but-harmless (interpret_djen_raw() never emits it). Not selected."
  - "Dispatched an Explore-subagent survey of areas not yet mined by today's 12 prior AgentRun rounds and the parallel Wisk-loop's own extensive same-day sweep (both already exhaustively covered .qmd contracts, CSV escaping, segmenter_dataset gates, render_queries.py error isolation, velocityCalc.ts, consolidate/cli.py). It independently confirmed the render_manifest_parquet.py negative finding above and surfaced one concrete, plausible bug: processoCnj.ts's buildStjSql()/buildDocumentosSql() cast STJ dataDecisao/dataPublicacao to ::VARCHAR instead of ::DATE, diverging from service.py's already-correct ::DATE cast -- a timezone-dependent off-by-one-day risk for non-UTC viewers if the real STJ parquet column is TIMESTAMP-typed (plausible: STJ ingestion has no explicit schema pin, unlike JURIS' pa.date32())."
selected_work: "2026-09-09-exciting-mccarthy-qhtc8c-goal-stj-date-cast-web-sql-twin"
expected_behavior: "buildStjSql() and buildDocumentosSql()'s stj branch cast dataDecisao/dataPublicacao to DATE (matching service.py's _stj_sql/_documentos_sql), instead of VARCHAR -- so the value reaching toIsoDate() is always a bare calendar date, never a datetime-with-time string that new Date(...) could reinterpret as local time in a non-UTC viewer's browser."
entry_state: "red"
target_state: "green"
decision_ids:
  - "2026-09-09-exciting-mccarthy-qhtc8c-decision-cast-not-toisodate-rewrite"
evidence_ids:
  - "2026-09-09-exciting-mccarthy-qhtc8c-evidence-red-test"
  - "2026-09-09-exciting-mccarthy-qhtc8c-evidence-green-and-diff"
  - "2026-09-09-exciting-mccarthy-qhtc8c-evidence-pr-1395-opened"
  - "2026-09-09-exciting-mccarthy-qhtc8c-evidence-pr-1395-merged"
check_ids:
  - "2026-09-09-exciting-mccarthy-qhtc8c-check-red-test"
  - "2026-09-09-exciting-mccarthy-qhtc8c-check-green-test"
  - "2026-09-09-exciting-mccarthy-qhtc8c-check-parity-and-full-suites"
  - "2026-09-09-exciting-mccarthy-qhtc8c-check-okf-final"
result_state: "merged"
result_summary: "Issue/PR queue exhausted again (17 pre-verified-blocked issues; one healthy Dependabot PR); no dangling agent PR from the immediately preceding round (02ggxp merged clean as PR #1391/5f6be2d). Ruled out round 02ggxp's own next_move lead (render_manifest_parquet.py's urllib segment downloads) by direct reading: those are whole-file GETs with no Range header, so djen.py's Range-GET-reassembly risk shape doesn't apply -- urllib already raises on any non-2xx status. Manually reviewed all remaining web/src/queries/*.qmd contracts not yet touched by today's 12 prior rounds; found no fresh bug there. Dispatched an Explore-agent survey which independently confirmed the render_manifest_parquet.py negative finding and surfaced a real one: web/src/lib/processoCnj.ts's buildStjSql()/buildDocumentosSql() -- the JS twin of src/causaganha/processos/service.py's DuckDB SQL, kept in sync only by the #1107 cross-runtime parity harness -- cast STJ's dataDecisao/dataPublicacao fields to ::VARCHAR, while service.py's _stj_sql/_documentos_sql already cast the same fields to ::DATE. Unlike JURIS' data_julgamento (schema-pinned to pa.date32() in tjro_juris/service.py), STJ's dedup_acordaos() ingests via DuckDB's read_json(auto_detect=true) with no explicit type pin, so a time-bearing upstream JSON value could make DuckDB infer TIMESTAMP for that column; ::VARCHAR on a TIMESTAMP yields 'YYYY-MM-DD HH:MM:SS', which the row-mapper's toIsoDate() reinterprets via new Date(...) -- parsed as *local* time per the ECMAScript spec for a non-ISO datetime string (unlike a bare 'YYYY-MM-DD', parsed as UTC) -- risking a day-shifted STJ decision/publication date on a /processo dossie for any viewer outside UTC. The file's own toIsoTimestamp() docstring already documents this exact hazard class (built to protect datajud's ultima_atualizacao) but STJ's dates never used the safe path. Fixed via TDD: added two SQL-text RED tests to web/src/lib/processoCnj.test.ts (matching the file's existing 'SQL builders never use SELECT *' idiom) asserting ::DATE casts on both call sites; confirmed both failed against unmodified code (found ::VARCHAR); fixed by changing the two casts from ::VARCHAR to ::DATE (4 lines); confirmed GREEN (86/86 in processoCnj.test.ts). Verified no behavior regression: processoQueryPlanParity.test.ts (4/4, real DuckDB execution of both Python and JS SQL against shared Python-built fixtures) stayed green -- though this also confirms the fixture's own known blind spot (its STJ dates are hardcoded as DATE literals, so it cannot distinguish the old VARCHAR cast from the new DATE cast; the Explore-agent's finding was PLAUSIBLE, not fully confirmed against real production data, precisely because of this gap). Full web vitest (71 files/510 tests) green; tsc --noEmit showed only 3 pre-existing unrelated errors (import.meta.env typing, confirmed untouched by this diff); ruff check/format clean (no Python file touched); pytest -q showed only the expected single failure from this run.md being in draft at check time. okf-parser check: conformant throughout the round. PR #1395 opened (https://github.com/franklinbaldo/causaganha/pull/1395); all 11 CI checks completed green (CodeQL, GitGuardian, lint, web, validate, tests (tjro), compare-product-surfaces) on the pushed head, mergeable_state 'clean', zero pending reviews -- squash-merged via mcp__github__merge_pull_request as commit e23dc65645f1821cc6c5cc1d2e294a79b1df93a1 onto main. Session unsubscribed from PR activity afterward, and this branch was restarted from the post-merge main (per the fresh-change convention) to record this closing report without duplicating already-merged content."
next_move: "This round's own work is fully merged (PR #1395). This round's fix closes the textual divergence but not the underlying test-fixture blind spot both this round and the dispatched Explore-agent flagged: scripts/processo_query_plan_fixture.py hardcodes STJ's dataDecisao/dataPublicacao as DATE literals, so processoQueryPlanParity.test.ts cannot currently distinguish a correct ::DATE cast from the old buggy ::VARCHAR one -- a future round could add a TIMESTAMP-typed STJ fixture variant (a new CNJ constant, mirroring how CNJ_TIEBREAK/CNJ_SOURCE_UNAVAILABLE were each added for a specific parity gap) to make this exact regression class detectable end-to-end rather than only at the SQL-text level. Separately, the same JS/Python SQL-twin file has one more matching-pattern mismatch this round deliberately left alone as out of scope: buildJurisSql()'s data_julgamento::VARCHAR (vs. service.py's ::DATE) -- currently harmless in practice since tjro_juris/service.py pins that column to pa.date32() at ingestion, so no live bug exists there today, but worth revisiting if that pin is ever relaxed. If a future round finds the issue/PR queue still exhausted, continue the established pattern: dispatch a fresh Explore-agent audit of areas not yet swept by either this AgentRun lineage or the parallel Wisk-loop (both now cover .qmd contracts, CSV escaping, segmenter_dataset gates, render_queries.py, velocityCalc.ts, consolidate/cli.py, djen_backup's Range-GET path, and render_manifest_parquet.py exhaustively) -- src/djen_backup/archive.py's token-bucket/circuit-breaker interaction under real concurrent load (named by the Wisk loop's own next_move as not yet fuzzed) and the remaining web/src/lib/*.ts modules are the clearest still-open candidates."
---

# Agent run

Rodada iniciada a partir do scaffold, 13a do dia nesta mesma mecânica OKF. Issues e PR abertos re-verificados como sem trabalho novo (17 issues bloqueadas, 1 PR Dependabot saudável). Verificado e descartado o `next_move` da rodada anterior (02ggxp): `render_manifest_parquet.py` não compartilha o risco do bug de Range-GET corrigido naquela rodada, pois faz apenas downloads de arquivo inteiro via `urllib` (sem header `Range`), que já levanta exceção em qualquer status não-2xx. Revisão manual de todos os `.qmd` restantes não tocados hoje não encontrou bug novo. Um subagente Explore, disparado para varrer áreas ainda não mineradas por 12 rodadas AgentRun e pelo loop paralelo Wisk (ambos já exaustivos em contratos `.qmd`, escaping de CSV, gates do `segmenter_dataset`, isolamento de erro em `render_queries.py`, `velocityCalc.ts`, `consolidate/cli.py`), confirmou o descarte acima e achou um bug real: `web/src/lib/processoCnj.ts` — o gêmeo em JS do SQL DuckDB de `service.py`, mantido em paridade só pelo harness cross-runtime do #1107 — fazia cast `::VARCHAR` nos campos de data do STJ (`dataDecisao`/`dataPublicacao`), enquanto `service.py` já fazia `::DATE`. Como o STJ não tem pin de schema na ingestão (ao contrário do JURIS, fixado em `pa.date32()`), uma coluna real do tipo TIMESTAMP produziria uma string "YYYY-MM-DD HH:MM:SS" que `toIsoDate()` reinterpreta via `new Date(...)` como hora local — deslocando o dia exibido para visitantes fora de UTC. Corrigido via TDD: dois testes RED de asserção textual do SQL gerado (seguindo o idioma já existente no arquivo de teste), fix mínimo trocando `::VARCHAR` por `::DATE` nos dois pontos, GREEN confirmado (86/86 em `processoCnj.test.ts`, 510/510 na suite completa do frontend, 4/4 no harness de paridade cross-runtime real via DuckDB). `next_move` documenta a lacuna remanescente: a fixture Python do harness de paridade fixa as datas STJ como literais `DATE`, então não consegue hoje distinguir o cast antigo do novo — útil para uma rodada futura endurecer.
