---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-qhtc8c-goal-stj-date-cast-web-sql-twin"
run_id: "2026-09-09-exciting-mccarthy-qhtc8c"
goal: "Make web/src/lib/processoCnj.ts's buildStjSql()/buildDocumentosSql() cast STJ dataDecisao/dataPublicacao to DATE, not VARCHAR, matching src/causaganha/processos/service.py's already-correct ::DATE cast."
rationale: "processoCnj.ts is a hand-maintained JS twin of service.py's DuckDB SQL, kept in sync only by the cross-runtime query-plan parity harness (#1107) -- nothing forces the two to agree by construction. service.py's _stj_sql/_documentos_sql cast dataDecisao/dataPublicacao to ::DATE before returning them; the JS twin cast the same fields to ::VARCHAR instead. Unlike JURIS' data_julgamento (schema-pinned to pa.date32() in tjro_juris/service.py), STJ's dedup_acordaos() ingests via DuckDB's read_json(auto_detect=true) with no explicit type pin, so if the upstream JSON date carries a time component, DuckDB infers TIMESTAMP for that column -- and ::VARCHAR on a TIMESTAMP yields 'YYYY-MM-DD HH:MM:SS' (space-separated, no timezone marker). The JS row-mapper's toIsoDate() reinterprets that shape via `new Date(...)`, which the ECMAScript spec parses as *local* time for a non-ISO datetime string (unlike a bare 'YYYY-MM-DD', which is parsed as UTC) -- so a viewer outside UTC could see the STJ decision/publication date on a /processo dossie shifted by a day from the canonical Python-computed value. The file's own toIsoTimestamp() docstring already documents this exact hazard class (written to protect datajud's ultima_atualizacao field) but STJ's dates never used it. Found by a dispatched Explore-agent audit of areas not yet mined by 12 same-day AgentRun rounds plus the parallel Wisk-loop's own extensive same-day sweep (both had already exhaustively covered .qmd contracts, CSV escaping, segmenter gates, and djen_backup's Range-GET path)."
success_signal: "A RED test (SQL-text assertion against buildStjSql()/buildDocumentosSql()'s generated SQL) fails against unmodified code (asserts ::DATE, finds ::VARCHAR) and passes after the fix; the full web/vitest suite (510 tests) and the cross-runtime query-plan parity harness (processoQueryPlanParity.test.ts, 4 tests, real DuckDB execution against Python-built fixtures) both stay green, proving the DATE cast doesn't change any Python/JS row-equality outcome; ruff/pytest on the Python side stay unaffected (no Python file touched)."
status: "achieved"
---

# Goal: alinhar o cast de datas STJ entre service.py e processoCnj.ts

Fechar uma divergencia textual entre duas implementacoes que devem ficar em paridade: o SQL Python (`_stj_sql`/`_documentos_sql`) ja fazia `::DATE` nos campos `dataDecisao`/`dataPublicacao` do STJ, mas o gemeo em JS (`buildStjSql`/`buildDocumentosSql`) fazia `::VARCHAR` — um gap que, se a coluna real ingerida via `read_json(auto_detect=true)` (sem pin de schema, ao contrario do JURIS) for inferida como TIMESTAMP em vez de DATE, produziria uma data deslocada por fuso horario para visitantes fora de UTC.
