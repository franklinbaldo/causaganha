---
type: AgentDecision
id: "2026-09-08-exciting-mccarthy-2xmp5l-decision-delete-tribunal-calendar"
run_id: "2026-09-08-exciting-mccarthy-2xmp5l"
goal_id: "2026-09-08-exciting-mccarthy-2xmp5l-goal-delete-tribunal-calendar"
question: "The prior round (14x3v7) left an explicit open decision: should web/src/components/TribunalCalendar.svelte be wired to the real sync-manifest data (like TribunalDetail.svelte on /publicacoes/[tribunal] already does) or deleted?"
choice: "Delete the file outright, and correct CLAUDE.md's CSS section to stop naming it as a maintained legacy island (three islands, not four)."
rationale: "Wiring it up would mean building a second, redundant coverage-calendar surface: web/src/components/TribunalCoverageExplorer.svelte already exists, already ships on web/src/pages/stats.astro, and already reads the real per-tribunal partition data (web/src/lib/tribunalCalendarPartition.ts's loadTribunalCalendarPartition(), backed by the same TribunalCalendarRow contract in web/src/lib/data/contracts.ts that tribunal_calendar.qmd renders from the manifest) with its own passing tests. There is no unmet product need TribunalCalendar.svelte would fill if wired up — it would duplicate an already-shipped, already-correct component. Wiring it up would also require writing real CSS for its markup from scratch, since none exists anywhere in the codebase for its cal-control/mini-month/mini-day/cal-stat classes (checked via grep across web/src, index.css, and panda.config.ts) — so 'wire it up' is not a small patch, it is rebuilding a component that already has a correct twin. Deleting is strictly lower-risk (it is unreferenced by any page, confirmed via grep, so removing it changes zero rendered output) and directly closes the landmine 14x3v7 flagged: CLAUDE.md documenting it as one of the 'four legacy Svelte islands' worth 'maintaining' actively invites a future contributor to treat its fabricated hash-based data as real and extend it."
---

# Decisão: apagar em vez de religar

A rodada anterior deixou em aberto se `TribunalCalendar.svelte` deveria ser religado ao dado real ou apagado. Escolhido apagar: já existe um componente irmão (`TribunalCoverageExplorer.svelte`, em `/stats`) que cobre exatamente a mesma necessidade de produto com dado real do manifesto, com testes próprios passando. Religar duplicaria esse componente já correto, e ainda exigiria escrever CSS do zero (nenhuma classe usada pelo markup existe em lugar nenhum do código). Apagar é estritamente de menor risco — o componente não é renderizado por nenhuma página hoje — e fecha o risco de "landmine" que a rodada anterior apontou: o CLAUDE.md o documentava como uma ilha legada "mantida", convidando um futuro contribuinte a tratar seus dados fabricados como reais.
