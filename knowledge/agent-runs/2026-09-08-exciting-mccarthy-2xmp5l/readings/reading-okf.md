---
type: AgentReading
id: "2026-09-08-exciting-mccarthy-2xmp5l-reading-okf"
run_id: "2026-09-08-exciting-mccarthy-2xmp5l"
subject: "okf_knowledge"
reference: "knowledge/agent-runs/2026-09-07-exciting-mccarthy-14x3v7/run.md (most recent same-family report); web/src/components/TribunalCalendar.svelte, TribunalCoverageExplorer.svelte, lib/tribunalCalendarPartition.ts live tree; CLAUDE.md lines 67-76"
finding: "Read 14x3v7's report, the most recent AgentRun in this family (completed 2026-09-07T23:45:43Z). Its next_move item 2 flagged an unresolved decision from its own Explore-subagent pass: web/src/components/TribunalCalendar.svelte fabricates every calendar cell's uploaded/absent/partial/error status from a deterministic hash of `tribunal:date` instead of reading the real sync-manifest, was confirmed unreferenced by any .astro page (dead code, not live), and 14x3v7 explicitly deferred the wire-up-or-delete call to 'a future round' rather than making it inline. Investigated directly this round rather than trusting the deferred framing: grepped web/src for every calendar-specific class name the component's own markup uses (cal-control, mini-month, mini-day, cal-stat, etc.) and found none defined anywhere outside the component's own file — not even in its own scoped <style> block (which only styles .cal-control__label/.cal-summary/.cal-summary__value) — meaning the component would render functionally unstyled even if wired up, on top of using fabricated (not real) data. Separately confirmed a correct, live, already-shipped replacement exists for the same product need: web/src/components/TribunalCoverageExplorer.svelte, imported by web/src/pages/stats.astro, backed by web/src/lib/tribunalCalendarPartition.ts's loadTribunalCalendarPartition() which reads the real per-tribunal partition data via the same TribunalCalendarRow contract type (web/src/lib/data/contracts.ts) that tribunal_calendar.qmd renders from the manifest — with its own passing test suite (TribunalCoverageExplorer.test.ts, tribunalCalendarPartition.test.ts). This flips the decision from 'a genuine build-vs-delete product question' (14x3v7's framing) to a clear-cut deletion: the real, correct, data-backed version of this exact feature already exists and is live; TribunalCalendar.svelte is not a stub awaiting wiring, it is an orphaned predecessor made fully redundant by a component that already shipped. CLAUDE.md's CSS section (lines 67-76) still lists it as one of 'four legacy Svelte islands' that are 'maintained' and worth preserving `--papel-*`/`--s-*` naming for — that claim needed correcting as part of this round's diff, per 14x3v7's own warning that leaving it as 'complete-looking dead code with fabricated data is the landmine risk.'"
---

# Leitura do conhecimento OKF

A rodada anterior (`14x3v7`) identificou `TribunalCalendar.svelte` como código morto (dados fabricados por hash, não referenciado por nenhuma página) mas deixou a decisão "religar ou apagar" para uma rodada futura. Esta rodada investigou diretamente e encontrou dois fatos que resolvem a decisão: (1) o componente nem tem CSS próprio para suas classes — ficaria sem estilo mesmo se religado; (2) já existe um substituto correto e ativo (`TribunalCoverageExplorer.svelte`, usado em `/stats`) que lê os dados reais do manifesto via o mesmo contrato `TribunalCalendarRow`. A decisão correta é apagar o órfão e corrigir o CLAUDE.md, que ainda o lista como uma das "quatro ilhas legadas mantidas".
