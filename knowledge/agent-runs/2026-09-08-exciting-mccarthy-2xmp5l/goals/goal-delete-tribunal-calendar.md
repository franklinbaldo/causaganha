---
type: AgentGoal
id: "2026-09-08-exciting-mccarthy-2xmp5l-goal-delete-tribunal-calendar"
run_id: "2026-09-08-exciting-mccarthy-2xmp5l"
goal: "Delete the orphaned TribunalCalendar.svelte (fabricated data, zero CSS, zero references) and correct CLAUDE.md's stale 'four legacy islands' claim"
rationale: "web/src/components/TribunalCalendar.svelte is unreferenced by any .astro page (confirmed via grep across web/), so it renders nowhere in the shipped site. Its own dayStatus() function fabricates every cell's status from a deterministic hash of `tribunal:date` — never reading the real sync-manifest — and its markup's own CSS classes (cal-control, mini-month, mini-day, cal-stat, ...) have no definition anywhere in the codebase, not even in the component's own scoped <style> block, so it would render unstyled even if some future page imported it. A correct, live replacement for the same product need already exists and ships today: web/src/components/TribunalCoverageExplorer.svelte, imported by web/src/pages/stats.astro, reads real per-tribunal data via lib/tribunalCalendarPartition.ts's loadTribunalCalendarPartition() against the same TribunalCalendarRow contract the manifest renders. CLAUDE.md's CSS token boundary section (lines 67-76) still lists TribunalCalendar.svelte as one of 'four legacy Svelte islands' worth 'maintaining' with --papel-*/--s-* names — that documentation is now actively misleading (the prior round's own next_move called this exact gap 'the landmine risk: whoever re-wires it later will assume it's real'). Deleting the dead file and correcting the doc removes both the landmine and a maintenance-signal false positive, with zero product regression since nothing renders it today."
success_signal: "web/src/components/TribunalCalendar.svelte no longer exists in the tree. `grep -rn TribunalCalendar web/src` (excluding TribunalCalendarRow/TribunalCoverageExplorer/tribunalCalendarPartition, which are the unrelated real feature) returns nothing. CLAUDE.md's CSS section names three legacy islands, not four, and no longer mentions TribunalCalendar.svelte. The full web suite (npx vitest run), eslint, and astro check all stay green with the same or fewer test files than before (no test referenced the deleted component, confirmed before deletion). A PR is opened and driven to a green, mergeable state."
status: "achieved"
---

# Goal: apagar TribunalCalendar.svelte órfão e corrigir CLAUDE.md

`TribunalCalendar.svelte` não é importado por nenhuma página, fabrica status por hash em vez de ler o manifesto real, e nem tem CSS próprio para as classes que usa — ficaria sem estilo mesmo se religado. Já existe um substituto correto e ativo (`TribunalCoverageExplorer.svelte`, em `/stats`) usando o contrato real `TribunalCalendarRow`. O CLAUDE.md ainda lista o componente morto como uma das "quatro ilhas legadas mantidas" — essa documentação está desatualizada e é o próprio risco de "landmine" apontado pela rodada anterior. Apagar o arquivo e corrigir a documentação remove o risco sem regressão de produto, já que nada o renderiza hoje.
