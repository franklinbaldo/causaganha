---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-5crg57-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-5crg57"
subject: "open_issues"
reference: "22 issues abertas em franklinbaldo/causaganha (list_issues, orderBy updated_at DESC); leitura focada em #1047/#1050/#1051 (segmenter) e reconfirmação do estado de #1468/#1469/#1470/#1471/#1472/#1482/#950"
finding: "Cluster #1468 (Parquet/CNJ) e #950/#1482 seguem exatamente como a rodada anterior (wvzu11) confirmou: todo trabalho de código local está feito, resta apenas rollout/deploy real (IA_ACCESS_KEY/IA_SECRET_KEY, Cloudflare, GCP) -- reconfirmado ao vivo nesta rodada (`env | grep -i 'IA_\\|ARCHIVE\\|CLOUDFLARE\\|GCP'` vazio), sem mudança desde 11/09. Cluster #1047 (roadmap do segmentador OPF v8): #1051 ('segmenter: build an independently annotated validation set for model selection') é a issue aberta mais recentemente tocada (comentário de wvzu11 às 07:40 diagnosticando a causa raiz: 0 ReviewRecords na store RFC 0012, `assign-splits` produz val=0/test=0). #1050 ('repair and scale the real training corpus with agent annotation') nomeia explicitamente anotação por agente como o caminho -- confirma que produzir uma segunda anotação independente via subagente (Técnica 1, RFC 0012 §8/§9) é trabalho pretendido pelo próprio roadmap, não um desvio de escopo. Investigação ao vivo nesta rodada (antes deste registro) confirmou meu próprio achado adicional: dos 61 documentos com pelo menos 1 anotação, 13 têm 2+ anotações, mas NENHUM par é independente pela definição operacional de mechanical.annotations_are_independent (RFC 0012 §5.3) -- todo par existente é 'anotação real + correção seedada' (seeded_with != 'none'), nunca duas anotações genuinamente unseeded de famílias distintas. 45 documentos têm exatamente 1 anotação unseeded (22 historical_migration_unspecified, 16 prompt_subagents:general-purpose, 7 prompt_subagents:haiku) -- candidatos diretos para receber uma segunda anotação independente e virar o primeiro ReviewRecord real da store."
---

# Leitura de issues abertas (foco no cluster do segmentador)

Confirmação ao vivo de que o cluster #1468/#950/#1482 permanece esgotado (rollout real bloqueado por credenciais ausentes neste ambiente, sem mudança desde 11/09) e que #1051/#1050 são o próximo trabalho de domínio real e desbloqueado. Fui além do comentário de wvzu11 (que só mediu a store agregada) e inspecionei os pares de anotação existentes documento a documento: confirmei que a causa dos "0 ReviewRecords" não é falta de tentativa de segunda anotação -- é que as tentativas existentes (correções assistidas por modelo, `claude_agent_repair` sobre rascunho, etc.) são todas *seeded*, o que a própria RFC 0012 §5.3 desqualifica deliberadamente como não-independente. O caminho real para o primeiro ReviewRecord é produzir uma segunda anotação genuinamente unseeded para um dos 45 documentos que já têm exatamente uma.
