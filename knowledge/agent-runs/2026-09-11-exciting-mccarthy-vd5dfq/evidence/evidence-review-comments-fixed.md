---
type: AgentEvidence
id: "2026-09-11-exciting-mccarthy-vd5dfq-evidence-review-comments-fixed"
run_id: "2026-09-11-exciting-mccarthy-vd5dfq"
goal_id: "2026-09-11-exciting-mccarthy-vd5dfq-goal-datajud-timezone-date-shift"
kind: "review"
reference: "PR #1456 Codex automated Code Review (2 review comments on web/src/lib/processoCnj.ts:326 and web/src/lib/processoCnj.test.ts:325)"
summary: "Codex's Code Review on PR #1456 found two real P2 bugs in this round's own fix, both independently verified live before being fixed: (1) BARE_ISO_DATE_RE's fast path returned the extracted 'YYYY-MM-DD' digits unconditionally, so a digit-shaped but calendrically invalid naive string (e.g. '2024-13-01T12:00:00', which normalizar_data14 can emit unchanged since it only extracts digit groups without validating ranges) now silently 'succeeded' instead of returning null the way the pre-existing new Date(...) fallback did (verified: `new Date('2024-13-01T12:00:00').toISOString()` throws RangeError: Invalid time value) -- fixed by adding isValidCalendarDate(), which round-trips the extracted (year, month, day) through Date.UTC and rejects (returns null) whenever the round-trip doesn't match (catching month/day overflow the same way Invalid Date did, and additionally catching day-of-month overflow like day=31-in-April that the old new Date(...) fallback had actually silently rolled over to the next month -- verified live: `new Date('2024-04-31T00:00:00').toString()` produced 'Wed May 01 2024', which the new stricter round-trip validation correctly rejects as null instead of returning a fabricated date). (2) both new #datajud-tz tests restored TZ via `process.env.TZ = originalTz`, which coerces `undefined` to the literal string \"undefined\" instead of deleting the variable when TZ was originally unset -- verified live that this makes Node fall back to a UTC/GMT default instead of the host's real original timezone, which could leak into and corrupt later timezone-sensitive tests in the same worker. Fixed by extracting both call sites into a shared `withTz(tz, fn)` test helper that deletes `process.env.TZ` when the saved value was undefined. New regression test (test_report_rejects... -- named 'rejects a digit-shaped but calendrically invalid naive date instead of returning it unchanged') confirmed RED against the pre-review-fix code (returned '2024-13-01' instead of null) and GREEN after. Full web/src/lib/processoCnj.test.ts (89/89) and the full web vitest suite (516/516, one transient flake on the unrelated query-plan-parity test reproduced as a flake by re-running clean twice and confirmed to pass standalone) are green; `npm run lint` reports 0 errors."
---

# Review: dois bugs P2 reais corrigidos

Codex encontrou dois bugs reais na correção desta rodada: (1) `toIsoDate` aceitava datas com mês/dia fora de faixa sem validar (`normalizar_data14` só extrai dígitos, não valida), corrigido com `isValidCalendarDate` (round-trip via `Date.UTC`); (2) os testes restauravam `TZ` atribuindo `undefined` (vira a string `"undefined"` em vez de apagar a variável), corrigido com um helper `withTz` que usa `delete`. Ambos corrigidos com TDD RED→GREEN.
