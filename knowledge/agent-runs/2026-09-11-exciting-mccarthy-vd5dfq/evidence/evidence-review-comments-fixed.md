---
type: AgentEvidence
id: "2026-09-11-exciting-mccarthy-vd5dfq-evidence-review-comments-fixed"
run_id: "2026-09-11-exciting-mccarthy-vd5dfq"
goal_id: "2026-09-11-exciting-mccarthy-vd5dfq-goal-datajud-timezone-date-shift"
kind: "review"
reference: "PR #1456 Codex automated Code Review, two rounds (3 review comments across web/src/lib/processoCnj.ts:311,326 and web/src/lib/processoCnj.test.ts:325); PR #1457 Codex automated Code Review, one round (5 review comments, 2 real: web/src/lib/processoCnj.ts:321, knowledge/agent-runs/.../checks/check-green-and-suite.md:6,14, knowledge/agent-runs/.../checks/check-okf-parser-cli-vs-api-yaml-gap.md:6)"
summary: "Codex's Code Review on PR #1456 ran twice (once per pushed commit) and found three real P2 bugs across the two rounds, all independently verified live before being fixed. Round 1 (on commit d6abf5b): (1) BARE_ISO_DATE_RE's fast path returned the extracted 'YYYY-MM-DD' digits unconditionally, so a digit-shaped but calendrically invalid naive string (e.g. '2024-13-01T12:00:00', which normalizar_data14 can emit unchanged since it only extracts digit groups without validating ranges) now silently 'succeeded' instead of returning null the way the pre-existing new Date(...) fallback did (verified: new Date('2024-13-01T12:00:00').toISOString() throws RangeError: Invalid time value) -- fixed by adding isValidCalendarDate(), which round-trips the extracted (year, month, day) through Date.UTC and rejects whenever the round-trip doesn't match (catching month/day overflow the same way Invalid Date did, and additionally catching day-of-month overflow like day=31-in-April that the old new Date(...) fallback had actually silently rolled over to the next month -- verified live: new Date('2024-04-31T00:00:00').toString() produced 'Wed May 01 2024', which the new stricter round-trip validation correctly rejects as null instead of returning a fabricated date). (2) both new #datajud-tz tests restored TZ via process.env.TZ = originalTz, which coerces undefined to the literal string 'undefined' instead of deleting the variable when TZ was originally unset -- verified live that this makes Node fall back to a UTC/GMT default instead of the host's real original timezone, which could leak into and corrupt later timezone-sensitive tests in the same worker. Fixed by extracting both call sites into a shared withTz(tz, fn) test helper that deletes process.env.TZ when the saved value was undefined. Round 2 (on commit eb1772e, re-reviewing round 1's own fix): (3) BARE_ISO_DATE_RE's time-component groups (HH:MM:SS) were matched by digit shape only, and isValidCalendarDate only checked year/month/day -- so a calendrically valid date with out-of-range time digits (e.g. normalizar_data14('20240101999999') emitting '2024-01-01T99:99:99') was returned as '2024-01-01' instead of null, again diverging from the pre-existing new Date(...) fallback (verified live: new Date('2024-01-01T99:99:99').toISOString() throws the same Invalid time value error). Fixed by capturing hour/minute/second in BARE_ISO_DATE_RE and rejecting when hour>23, minute>59, or second>59. Each of the three fixes has its own regression test, each confirmed RED against the code as it stood immediately before that fix and GREEN after. Full web/src/lib/processoCnj.test.ts (90/90) and the full web vitest suite (517/517, one transient flake on the unrelated query-plan-parity test observed once mid-round and reproduced as a flake by re-running clean twice plus a standalone pass) are green; npm run lint reports 0 errors. A fourth Codex comment on PR #1456 (proposing to record this run through Wisk instead of the legacy AgentRun scaffold) was read and evaluated separately -- see decision reconfirmed in reading-okf.md. PR #1457 (the round-closing docs-only follow-up) then hit its own Code Review pass with 5 comments, 2 real: (4) isValidCalendarDate's round-trip used Date.UTC(year, ...), which applies JS's legacy rule mapping a 0-99 year argument onto 1900-1999 -- so a perfectly valid ISO year below 100 (e.g. '0001-01-01', which the pre-existing new Date(isoString) fallback always accepted, since that legacy offset does not apply to ISO string parsing) was incorrectly rejected as invalid (verified live: Date.UTC(1, 0, 1) produces 1901-01-01, while new Date('0001-01-01T00:00:00') correctly produces 0001-01-01). Fixed by switching isValidCalendarDate to construct a Date and call setUTCFullYear(year, month-1, day) instead of Date.UTC(...)/new Date(year, ...) -- setUTCFullYear does not apply the legacy two-digit-year rule. (5) Two of this round's own AgentCheck records had accuracy bugs Codex caught on re-reading: check-green-and-suite.md's markdown body still narrated the initial (wrong) 'draft-AgentRun cascade' theory for the 14 tests/causaganha_mcp/ failures after the frontmatter summary had already been corrected to the real YAML root cause, and its recorded command joined the web and Python halves with `;` instead of `&&`, so a web/lint failure would not propagate to the overall exit status if replayed; and check-okf-parser-cli-vs-api-yaml-gap.md's recorded command used the literal text ' vs. ' as a pseudo-separator (not valid shell) and omitted `from pathlib import Path` in the Python one-liner, so the record could not actually be replayed as written. Fixed by rewriting the check-green-and-suite.md body to match its own frontmatter and joining its command with `&&`, and by rewriting check-okf-parser-cli-vs-api-yaml-gap.md's command into two independently valid statements (verified executable, live, after the fix). New regression test for (4): toIsoDate > accepts a valid ISO year below 100 instead of rejecting it via the legacy two-digit-year offset. Full web/src/lib/processoCnj.test.ts (91/91) and the full web vitest suite (518/518) are green; npm run lint reports 0 errors."
---

# Review: cinco bugs P2 reais corrigidos em três rodadas de Code Review

Codex encontrou cinco bugs reais nas correções desta rodada, em três rodadas de review (PR #1456 duas vezes, PR #1457 uma vez): (1) `toIsoDate` aceitava datas com mês/dia fora de faixa sem validar, corrigido com `isValidCalendarDate`; (2) os testes restauravam `TZ` atribuindo `undefined` (vira a string `"undefined"` em vez de apagar a variável), corrigido com um helper `withTz` que usa `delete`; (3) a mesma validação não cobria hora/minuto/segundo fora de faixa, corrigido capturando esses grupos na regex e rejeitando valores >23/>59/>59; (4) `isValidCalendarDate` usava `Date.UTC(year, ...)`, que aplica a regra legada de mapear anos 0-99 para 1900-1999, rejeitando incorretamente anos ISO válidos abaixo de 100 -- corrigido trocando para `setUTCFullYear(year, ...)`, que não aplica essa regra; (5) dois dos próprios registros `AgentCheck` desta rodada tinham comandos não reproduzíveis ou prosa desatualizada, corrigidos para bater com seu próprio frontmatter e serem executáveis como gravados. Todos corrigidos com TDD RED→GREEN, um teste de regressão por bug de código.
