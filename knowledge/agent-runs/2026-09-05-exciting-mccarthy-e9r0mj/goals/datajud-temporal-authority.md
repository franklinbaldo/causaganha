---
type: AgentGoal
id: "2026-09-05-exciting-mccarthy-e9r0mj-goal-datajud-temporal-authority"
run_id: "2026-09-05-exciting-mccarthy-e9r0mj"
goal: "Fix issue #1107's diagnosed DataJud temporal-authority drift: make web/src/lib/processoCnj.ts's mapDatajudRow() preserve the full timestamp semantics of DatajudCapa.ultima_atualizacao (time-of-day included) instead of truncating it to a bare date via toIsoDate(), matching the Python/MCP side's _iso() behavior; add a regression test proving this; then reintroduce the mapping-layer PRESENT/ABSENT parity proof across all 4 sources (DJEN/JURIS/STJ/DataJud) that the prior round's PR #1125 attempted and correctly left unmerged because it caught this exact drift."
rationale: "#1107's comment thread (issue #1107, comment 5551321571) already root-caused this precisely: a prior PR (#1125) added a mapping-layer parity test that went RED against real production code, proving ultima_atualizacao truncates to date-only on Web while Python/MCP preserves the full ISO timestamp. The author's own verdict was explicit: fix the Web mapper first (do not touch Python, do not mask the drift in the test), then reintroduce the parity proof. This is the single most concretely scoped, already-diagnosed READY item across all open issues at session start — a real, previously-undetected regression with a known root cause and a known fix location, not a fresh design question requiring new architecture."
success_signal: "web/src/lib/processoCnj.test.ts's mapDatajudRow describe block gains a passing test asserting that a raw ultima_atualizacao value carrying a time-of-day component (e.g. '2024-06-01 14:23:05', DuckDB's own space-separated VARCHAR-cast format) is NOT truncated to '2024-06-01' by the view, and instead preserves the full instant (ISO 'T'-separated, matching Python's datetime.isoformat()); a bare-date input ('2024-06-01', matching a DATE column) still round-trips unchanged, and null still maps to null. This test is RED under the current mapDatajudRow (toIsoDate truncates), then GREEN after mapDatajudRow is changed to use a new timestamp-preserving mapper instead of toIsoDate for this one field. Full web vitest suite stays green, eslint clean, and a mapping-layer parity test (using the real Python _build_datajud/service.buscar_processo path and the real Web mapDatajudRow/buscarProcesso path against the shared query_plan_fixtures.py fixture, per #1107's queued next step) passes GREEN for PRESENT and ABSENT DataJud rows without any bridge/normalization that would mask a future real drift. PR opened against main."
status: "achieved"
---

# Goal: DataJud temporal authority + reintroduce mapping-layer parity (#1107)

Corrige o drift diagnosticado no comentário mais recente de #1107 (truncamento de `ultima_atualizacao` no mapeador Web) e, uma vez corrigido, reintroduz a prova de paridade de camada de mapeamento que a PR #1125 tentou e corretamente deixou vermelha.
