---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-e9r0mj-reading-okf"
run_id: "2026-09-05-exciting-mccarthy-e9r0mj"
subject: "okf_knowledge"
reference: "knowledge/contracts/datajud-capa.md; src/causaganha/processos/models.py (DatajudCapa); src/causaganha/processos/service.py (_iso, _build_datajud, buildDatajudSql equivalent _datajud_sql); web/src/lib/processoCnj.ts (toIsoDate, mapDatajudRow, buildDatajudSql); web/src/lib/processoContract.ts (serializeSharedCore); src/causaganha/processos/query_plan_fixtures.py"
finding: "DatajudCapa.ultima_atualizacao is typed str | None in both the OKF contract (knowledge/contracts/datajud-capa.md, deliberately null-by-example since every field except id is nullable) and the Python dataclass — it is NOT typed as a date-only field anywhere in the contract; the contract only says 'string or absent', leaving the actual temporal precision to whatever the underlying DataJud column carries. Cross-checking the two runtimes' SQL: _datajud_sql/buildDatajudSql both read a genuine TIMESTAMP column (query_plan_fixtures.py's fixture uses TIMESTAMP '2024-06-01 00:00:00', not DATE, unlike data_ajuizamento which IS a DATE column in the same fixture) and MAX() it. Python's _build_datajud calls _iso(ultima_atualizacao) on the raw duckdb TIMESTAMP object -> full 'YYYY-MM-DDTHH:MM:SS' isoformat, no truncation. Web's buildDatajudSql already casts MAX(ultima_atualizacao)::VARCHAR in SQL (so DuckDB itself, not JS, produces the string — DuckDB's VARCHAR cast of a TIMESTAMP is space-separated 'YYYY-MM-DD HH:MM:SS', not 'T'-separated), but mapDatajudRow then re-parses that VARCHAR through toIsoDate(), which round-trips it through `new Date(...).toISOString().slice(0,10)` — losing the time-of-day entirely and only coincidentally not corrupting the date component (because it re-interprets a naive space-separated string as local time before reformatting to UTC, which is safe near local midday but is itself a latent timezone bug for edge times). serializeSharedCore/processoContract.ts pass ultima_atualizacao straight through to ProcessoConsultarSchema.parse without any further transform, so the fix belongs entirely in mapDatajudRow's read of the raw SQL row, not in the shared-core adapter. Existing processoCnj.test.ts coverage for mapDatajudRow only exercises a bare-date ultima_atualizacao ('2024-06-01'), so no existing test protects the time-of-day component — confirming this is a genuine, previously-undetected regression surface, exactly as #1107's comment thread (via the closed PR #1125) already diagnosed. No existing OKF type needs to change: this is purely a mapping-function bug, not a contract/schema gap."
---

# Leitura de conhecimento OKF

Confirma que `ultima_atualizacao` é a única data-com-hora real do dossiê DataJud (as demais são `DATE`), que o contrato OKF já a tipa como string livre (sem impor truncamento), e que o bug está isolado em `mapDatajudRow` — nenhuma mudança de schema/contrato é necessária, apenas corrigir a função de mapeamento e adicionar a regressão que falta.
