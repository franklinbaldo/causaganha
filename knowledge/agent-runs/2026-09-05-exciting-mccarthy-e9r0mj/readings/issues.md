---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-e9r0mj-reading-issues"
run_id: "2026-09-05-exciting-mccarthy-e9r0mj"
subject: "open_issues"
reference: "https://github.com/franklinbaldo/causaganha/issues?q=is%3Aissue+is%3Aopen (29 open issues at session start); issue #1107 body + its 6 comments (2026-09-05T02:04 through 11:00)"
finding: "#1105 (contract(product): projeção ProcessoConsultar do OKF) is now CLOSED/completed — MCP and Web both consume generated bindings from the shared OKF contract, unblocking #1107. #1107 (contract(processo): eliminar drift do plano de consulta entre MCP e Web) is OPEN and READY, with its comment thread documenting a precise, already-diagnosed next step: a prior round's PR #1125 (test(processo): prove mapping-layer parity for source views) was correctly closed WITHOUT merging because its new mapping-layer parity test went RED against real production code — it found that DatajudCapa.ultima_atualizacao preserves a full ISO timestamp on the Python/MCP side (service.py's _iso() calls .isoformat() on the raw duckdb TIMESTAMP value) while web/src/lib/processoCnj.ts's mapDatajudRow() passes the same field through toIsoDate(), which truncates to YYYY-MM-DD. The issue's last comment (5551321571) explicitly queues, in order: (1) DataJud temporal authority — fix the Web mapper to preserve the same timestamp semantics, with a unit regression covering time-of-day and nullability; (2) reintroduce the mapping-layer PRESENT/ABSENT parity proof across all 4 sources using the real _build_*/mapper functions (not just raw SQL row comparison, which processoQueryPlanParity.test.ts already covers and would NOT have caught this, since it compares raw SQL execution output before either side's _iso()/toIsoDate() runs); (3) unavailable-vs-absent; (4) documentos/pagination equivalence. This is the most concretely scoped, already-diagnosed, single-round-sized candidate in the backlog — a real bug with a known root cause, not a fresh design question. Other open candidates: #1131-#1134/#1136 (web/UX, no freshly-unblocked dependency), #1042 (ops(catalog), needs live IA-upload side effects, unsuited to autonomous verification), #950/#951 (MCP remote endpoint, explicitly gated on 'stack MCP corrente reconciliada/mergeada' — #1152 merged, worth re-checking in a future round but not chosen this round to keep to one coherent theme), the segmenter/OPF ML cluster (#884, #886-887, #1047-1057, orthogonal research track)."
---

# Leitura de issues abertas

Confirma que #1107 é o próximo passo natural: seu comentário mais recente já diagnosticou a causa raiz exata (truncamento de timestamp em `mapDatajudRow`) a partir de uma PR anterior (#1125) que foi corretamente fechada sem merge por ter ficado RED contra produção real, e enumera o próximo passo READY em ordem de prioridade.
