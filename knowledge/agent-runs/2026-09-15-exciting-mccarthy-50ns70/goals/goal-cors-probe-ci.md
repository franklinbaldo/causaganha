---
type: AgentGoal
id: "2026-09-15-exciting-mccarthy-50ns70-goal-cors-probe-ci"
run_id: "2026-09-15-exciting-mccarthy-50ns70"
goal: "Make scripts/benchmarks/archive_cors_probe.mjs (issue #1482's real-browser CORS confirmation) actually runnable outside this sandbox by declaring `playwright` as a real dependency instead of resolving it through a Claude-Code-sandbox-only path, give the probe a pass/fail exit code driven by an explicit, unit-tested assertion of the expected CORS contract (metadata endpoint reachable cross-origin, download endpoint blocked), and wire it into a new scheduled GitHub Actions workflow (mirroring ia-practicality-probe.yml's pattern) so a future change in archive.org's CORS behavior -- in either direction -- is caught automatically instead of silently."
rationale: "Issue #1482's own 'Suggested next step' #3 explicitly asks for 'a real (non-mocked) browser-level regression test so this can't silently break again', and PR #1489/commit 37c0f14 (2026-09-14) explicitly deferred 'a dedicated live-network Playwright job wired into CI' as separate follow-up. Investigating that follow-up surfaced a sharper, concrete blocker underneath it: archive_cors_probe.mjs (and its sibling wasm_query_bench.mjs from #1471/#1480) resolve `require.resolve('playwright', { paths: ['/opt/node22/lib/node_modules'] })` -- a path that exists only inside this specific Claude Code sandbox image, confirmed by `ls /opt/` and by grepping every package.json in the repo for 'playwright' (zero hits). So the CI-wiring follow-up is not merely undone, it is currently impossible: the script as committed would fail with a module-resolution error on any GitHub-hosted runner. This is real, unclaimed, read-only-to-the-domain work (touches only web/ tooling config, a benchmark script, and a new workflow file; no IA write credentials needed, unlike the rest of the #1468/#1471/#1472 epic which this round reconfirmed is still blocked in this sandbox) that turns a one-off manual probe with a hardcoded environment assumption into a real, repo-portable regression check -- directly closing #1482's remaining acceptance item."
success_signal: "A new pure function (e.g. evaluateCorsProbeResult) that turns a probe result JSON into a pass/fail verdict has a Vitest unit test suite that is RED before the function exists and GREEN after, covering: expected-blocked (current reality, exit 0/pass), download-endpoint-now-succeeds (a real behavior change worth flagging, exit 1/fail with a clear message so a human notices archive.org may have fixed CORS), and metadata-endpoint-broken (an actual regression, exit 1/fail). `playwright` becomes a declared, installable dependency (web/package.json devDependency, installed via `npm ci` in the web/ CI job) and archive_cors_probe.mjs resolves it without the sandbox-only hardcoded path when run from a real checkout. A new `.github/workflows/archive-cors-probe.yml` (schedule + workflow_dispatch, read-only permissions, matching ia-practicality-probe.yml's shape) installs Chromium via Playwright and runs the probe against live archive.org, failing the job on an unexpected verdict. `cd web && npx vitest run` and the full `uv run pytest -q` stay green; `uv run ruff check` / `ruff format --check` and the web lint stay clean. A PR is opened."
status: "achieved"
---

# Goal: tornar o probe de CORS do #1482 executável em CI real

`archive_cors_probe.mjs` resolve `playwright` por um caminho hardcoded que só existe neste sandbox do Claude Code -- o follow-up que a própria issue #1482 pediu (ligar o probe ao CI) está hoje bloqueado por isso, não apenas pendente. Este goal declara `playwright` como dependência real, extrai a lógica de veredito (bloqueado/ok/regressão) em uma função pura testada via TDD, e liga tudo a um novo workflow agendado no padrão de `ia-practicality-probe.yml`.
