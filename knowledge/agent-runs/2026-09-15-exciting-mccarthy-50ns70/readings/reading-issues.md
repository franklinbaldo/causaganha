---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-50ns70-reading-issues"
run_id: "2026-09-15-exciting-mccarthy-50ns70"
subject: "open_issues"
reference: "mcp__github__list_issues(state=OPEN) + issue_read on #1482, #1470, #1471"
finding: "22 issues open. Most recently touched: #1482 (web/duckdb: archive.org download endpoint sends no CORS header, so DuckDBExplorer's read_parquet() is unreadable cross-origin for the actual Parquet download, only the /metadata endpoint works), updated 2026-09-15T00:37:40Z by PR #1489/commit 37c0f14, which added a real-browser (Playwright) confirmation of the block and explicitly left the issue's own suggestion #3 -- 'a dedicated live-network Playwright job wired into CI' -- as separate follow-up, still open. Verified by reading scripts/benchmarks/archive_cors_probe.mjs and scripts/benchmarks/wasm_query_bench.mjs directly: both resolve their `playwright` import via `require.resolve('playwright', { paths: ['/opt/node22/lib/node_modules'] })` -- a path that only exists inside this specific Claude Code sandbox image (confirmed via `ls /opt/`), not on GitHub-hosted runners, and `playwright` is not declared as a dependency anywhere in the repo (`web/package.json` has no playwright entry; no root package.json exists; grepped .github/workflows/*.yml for both script names -- zero references, neither is wired into any workflow). So the issue's own suggested next step is currently not just undone but actually blocked: the probe script as committed cannot run in real CI at all. #1470/#1471/#1472 (Parquet/CNJ reorder epic, part of parent #1468): #1470's audit criteria are fully implemented as of PR #1487 (2026-09-14); #1471/#1472's remaining step is publishing a reordered candidate to Internet Archive, which needs IA_ACCESS_KEY/IA_SECRET_KEY -- `env | grep -i 'IA_\\|ARCHIVE'` returns nothing in this sandbox, same blocker every AgentRun round has hit since at least 2026-09-11. The other 19 open issues (#950, #1022, #985, #951, #1093, and the #884/#886/#887/#1047/#1050-1057 segmenter cluster) are unchanged since before 2026-09-07 and were already surveyed as not newly actionable by recent rounds (segmenter ones need GPU/annotation infrastructure this sandbox does not have)."
---

# Leitura de issues abertas

22 issues abertas. A mais recentemente tocada e mais acionável é #1482: o download direto de Parquet no `archive.org` não envia `Access-Control-Allow-Origin`, então `DuckDBExplorer.svelte` não consegue de fato ler o arquivo no navegador (só a classificação de erro foi implementada, em PR #1484). A rodada anterior (`37c0f14`) confirmou o bloqueio com um probe Playwright real, mas deixou a etapa 3 da própria issue -- ligar esse probe ao CI como teste de regressão -- como follow-up. Inspecionando o script (`scripts/benchmarks/archive_cors_probe.mjs`) e seu irmão (`wasm_query_bench.mjs`), ambos resolvem `playwright` por um caminho hardcoded (`/opt/node22/lib/node_modules`) que só existe neste sandbox específico do Claude Code -- `playwright` não é dependência declarada em nenhum `package.json` do repositório. Ou seja, o follow-up sugerido pela própria issue está hoje bloqueado por um problema mais concreto: o script não roda fora deste sandbox. #1470/#1471/#1472 seguem com a etapa de publicação no Internet Archive bloqueada por falta de credenciais `IA_ACCESS_KEY`/`IA_SECRET_KEY`, confirmado de novo nesta rodada.
