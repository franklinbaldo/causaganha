---
type: AgentReading
id: "2026-09-15-exciting-mccarthy-50ns70-reading-claude-md"
run_id: "2026-09-15-exciting-mccarthy-50ns70"
subject: "claude_md"
reference: "CLAUDE.md"
finding: "Read in full against HEAD c25a2c8. Two runtime surfaces: Python backend (src/causaganha, src/djen_backup) and web frontend (web/, Astro 7 + Svelte 5). djen-backup sync engine: sync-manifest.parquet on IA is the single source of truth; djen_raw is the raw HTTP transport code, never a verdict on availability (a 200 with body 'Sem comunicações' and no download URL is absent, same as 404); never treat 403 as absent (CloudFront/WAF rate limiting). CSS token boundary: Panda CSS via the cobogo preset is the one design system; three legacy Svelte islands keep --papel-*/--s-* aliases because Panda's `include` never scans .svelte. Style: ruff strict, no blind except Exception outside the ADR-0011 per-item worker-loop bulkhead, TRY300/TRY301/TRY401 enforced, Python 3.12+ with `from __future__ import annotations`. Before committing: `uv run ruff check`, `uv run ruff format --check`, `uv run pytest -q`. Separately load-bearing for this round's own instructions: `.claude/hourly-loop.md` (co-introduced with `.claude/agent-run-scaffold.md` in bc627c7/#1429) states explicitly that the hourly loop now runs exclusively on Wisk and that new AgentRuns must not be created going forward -- a conflict with this scheduled prompt's literal instructions, already flagged without resolution by three prior AgentRun rounds (njkncp/vd5dfq on 2026-09-11, bueov4 and to0ars on 2026-09-14, the latter of which sent a proactive notification escalating it). This round complies with the scheduled prompt as written (consistent with those prior rounds' decision) rather than re-escalating an already-escalated, still-unresolved scheduling question a third time, and spends its budget on domain work instead."
---

# Leitura de CLAUDE.md

Leitura completa contra o HEAD atual (`c25a2c8`). Regras de domínio e estilo inalteradas em relação às rodadas anteriores. Reconfirmado o conflito já conhecido entre `.claude/hourly-loop.md` (política do repositório: não criar novos `AgentRun` no loop horário, usar exclusivamente o Wisk) e o prompt agendado desta sessão (que instrui exatamente isso). Como esse conflito já foi escalado duas vezes (rodadas `bueov4` e `to0ars`, ambas em 2026-09-14) sem mudança observável no agendamento, esta rodada opta por seguir o prompt como escrito -- igual às rodadas anteriores -- em vez de repetir uma notificação já enviada, e direciona o esforço da rodada para avanço real de domínio.
