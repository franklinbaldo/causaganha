---
type: AgentReading
id: "2026-09-05-exciting-mccarthy-fnt3vx-reading-issues"
run_id: "2026-09-05-exciting-mccarthy-fnt3vx"
subject: "open_issues"
reference: "franklinbaldo/causaganha open issues (list_issues, state=OPEN, 28 total) + issue_read on #1107, #1136, #1093, #950, #1047, #924, #1022, #1042, #1048 + get_comments on #1136 and #1048, checked 2026-09-05T20:5x-21:1x"
finding: "#1107 (contract(processo) MCP/Web parity) is already CLOSED as completed via merged PR #1159 — earlier rounds' next_move chains treating it as still-open/READY are stale as of this round. #1136 (padronizar estados de loading/empty/unavailable/error) is READY but its own newest comment gates it explicitly on #1139's PR landing first ('READY depois da PR de hierarquia de /publicacoes'), and #1139's PR (#1160) is open-but-unmerged by a concurrent process — building on main now would mean redoing the work once #1160 reorders /publicacoes. #1093 (web(teor): busca direta de decisões) is explicitly 'NÃO é prioridade imediata', gated on #950. #950 (MCP remote HTTP endpoint) is a real infra/deploy decision (hosting, auth posture, rate limiting) unsuited to an unattended session without explicit user sign-off on a live deployment. #1042 (prove update-catalog end-to-end) requires observing a live GitHub Actions run with real IA-upload side effects — every prior round correctly deferred this as non-autonomous-session-shaped, and this round concurs. #924 is an unverified model-authored review (explicitly flagged 'não é auditoria' in its own body); spot-checking its claims live found most already fixed by earlier rounds (item 3.1 JURIS/DataJud→catalog: `RECONCILE_EXPECTED_SOURCES` on `main` already includes `juris,datajud`; item 3.3 dead code: `scripts/db/`, `deployment/cron`, `deployment/systemd`, `notebooks/train_privacy_filter.*`, `AUDIT.md` are all already gone; item 3.4 canary pending_real alarm: already implemented, `PENDING_REAL_THRESHOLD`/`PENDING_REAL_MAX_AGE_HOURS_THRESHOLD` both live in `scripts/canary_check.py` and documented in `docs/SERVICE_OBJECTIVES.md`) — but one 3.3 claim survives verification: `experiments/archive/test_all_improvements.py` and `experiments/archive/test_djen_api.py` still import genuinely removed modules (`causaganha.analysis.embedding_service_v2`, `causaganha.api.client`, etc. — confirmed via direct `uv run python3 -c 'import ...'`, 5/6 imports fail with ModuleNotFoundError). #1047 (segmenter evidence-first roadmap, parent of #1048) is on track; #1048 (make OPF training semantics/checkpoint selection correct) has 3 of its 4 checklist items already closed by merged PRs #1101/#1104/#1126 per its own comment thread, leaving only 'reassess/supersede PR #1035 explicitly' open — and PR #1035 (closed, not merged, mergeable_state=dirty) is confirmed superseded: diffing it against current main's `scripts/run_segmenter_training.py` shows PR #1035 would reintroduce the pre-#1048 per-epoch subprocess design (explicitly the design #1048's own docstring says diverges from OPF's canonical semantics), while every one of its useful CLI-flag additions (`--learning-rate`/`--weight-decay`/`--grad-accum-steps`/`--max-grad-norm`) already exists on main under the canonical single-continuous-invocation design."
---

# Leitura de issues abertas

Descoberta central: várias issues marcadas como pendentes em rodadas/relatórios anteriores já estão de fato resolvidas (verificado ao vivo, não apenas lido do texto da issue) — #1107 fechada, e três dos cinco achados de #924 já corrigidos. As lacunas reais e não reivindicadas por trabalho concorrente encontradas são: (1) dois arquivos em `experiments/archive/` com imports comprovadamente quebrados (módulos removidos), e (2) o último item do checklist de #1048, que só falta documentar explicitamente porque a PR #1035 já está superada pelo desenho canônico atual.
