---
type: AgentDecision
id: "2026-09-09-exciting-mccarthy-p7xocl-decision-scope-limited-to-append-manifest"
run_id: "2026-09-09-exciting-mccarthy-p7xocl"
goal_id: "2026-09-09-exciting-mccarthy-p7xocl-goal-catalog-scripts-csv-escaping"
question: "e6f4j2's next_move named three scripts (scripts/generate_catalog.py x2 functions, scripts/pipeline/consolidate.py, scripts/append_manifest.py) as sharing the same hand-rolled str.split(',') pattern fixed in five other modules today. Should all three be converted to csv.reader in this PR, matching the sibling-module precedent, or only the ones with a demonstrable corruption path?"
choice: "Fixed only scripts/append_manifest.py's get_new_uploads(). Declined to touch scripts/generate_catalog.py's get_items_from_sync_manifest()/generate_collect_progress() or scripts/pipeline/consolidate.py's load_sync_manifest()."
rationale: "Traced every field each function actually reads from the 6-column wire format (tribunal,date,ia_status,djen_status,djen_raw,updated_at). get_items_from_sync_manifest and generate_collect_progress only ever read indices 0-2 (tribunal/date/ia_status); load_sync_manifest reads 0-3 (adds djen_status). All four of those fields are closed, comma-free values today and forever under the current writers: tribunal/date are structural (never contain commas), ia_status and djen_status are internal enums assigned only by src/djen_backup/manifest.py's own state machine (never externally supplied, never freeform). djen_raw (index 4) is the one field with a real precedent for carrying non-enum text (the '200:<detail>' format from PR #1344, and this family's own tests treat a comma there as a realistic future value) -- but none of these three functions read index 4 or beyond, except append_manifest.py's get_new_uploads(), which reads updated_at at index 5, downstream of djen_raw. A comma in djen_raw shifts every column after it, which is exactly what corrupts get_new_uploads' updated_at (proven by this round's RED test) but leaves the other three functions' index 0-3 reads untouched since the shift only starts at index 4. Converting the other two files' readers to csv.reader anyway, without a field that can carry a comma reaching them, would be validation for a scenario that provably can't happen given the current schema -- padding the PR with a cosmetic change instead of a real fix. This closes e6f4j2's next_move question definitively (audited, not just deferred): two of the three named scripts don't need this fix; the third did and now has it."
---

# Decisão: fix restrito a append_manifest.py

Das três funções apontadas pelo `next_move` da rodada anterior, apenas `get_new_uploads()` (em `scripts/append_manifest.py`) lê uma coluna (`updated_at`, índice 5) posterior à única coluna que pode carregar vírgula (`djen_raw`, índice 4). As outras duas funções só leem índices 0-3, que são enums/datas fechados e nunca carregam vírgula sob os escritores atuais -- corrigi-las seria validação para um cenário que não pode ocorrer.
