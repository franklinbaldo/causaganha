---
type: AgentGoal
id: "2026-09-09-exciting-mccarthy-p7xocl-goal-catalog-scripts-csv-escaping"
run_id: "2026-09-09-exciting-mccarthy-p7xocl"
goal: "Make scripts/generate_catalog.py's get_items_from_sync_manifest()/generate_collect_progress(), scripts/pipeline/consolidate.py's load_sync_manifest(), and scripts/append_manifest.py's get_new_uploads() round-trip a comma-bearing field of data/sync-manifest.csv without silent corruption, by switching from bare str.split(',') to csv.reader, mirroring the fix already landed today in src/djen_backup/manifest.py, src/djen_backup/segments.py, src/datajud/manifest.py, src/tjro_juris/manifest.py and src/stj_acordaos/manifest.py."
rationale: "The immediately preceding round (e6f4j2)'s own next_move named these three scripts as the last remaining hand-rolled CSV readers of this bug class, but flagged their live-path status as unverified. This round confirmed all three are live: .github/workflows/update-catalog.yml invokes append_manifest.py and generate_catalog.py directly, and consolidate-parquet.yml invokes scripts/pipeline/consolidate.py. All three read data/sync-manifest.csv, which .github/actions/download-state regenerates every run via DuckDB's `COPY (...) TO 'data/sync-manifest.csv' (FORMAT CSV, HEADER)` -- a real RFC4180 CSV writer that quotes any field containing a comma. A bare str.split(',') does not understand that quoting, so a comma in any exported field would silently misparse into extra columns. Today's field writers (src/djen_backup/engine.py's _classify_djen_status) only ever produce a closed set of comma-free tokens ('200', str(status_code), 'timeout', 'network', 'no_publications'), so this is a real but currently-latent bug -- not yet triggered by live data -- exactly the same shape as every other fix in this family today, and the last one named by that family's own next_move chain."
success_signal: "Three new regression tests (one per script) prove a comma-bearing field on a DuckDB-quoted CSV row silently corrupts today's str.split(',') parsing -- RED before the fix -- and parses correctly after switching to csv.reader -- GREEN after. All pre-existing tests in tests/test_catalog_parsing.py stay green unchanged (proving csv.reader parses every existing comma-free fixture identically to the old split). Full Python suite, ruff check, ruff format --check stay green, and a PR is opened, driven to green CI, and merged."
status: "achieved"
---

# Goal: escapar CSV nos três scripts de catálogo/consolidação restantes

Fechar, nos três últimos leitores de `data/sync-manifest.csv` ainda não corrigidos (`scripts/generate_catalog.py`, `scripts/pipeline/consolidate.py`, `scripts/append_manifest.py`), o mesmo buraco de parsing de CSV sem aspas que a família de rodadas de hoje já corrigiu em cinco outros módulos.
