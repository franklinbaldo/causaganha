CREATE TABLE "StjAcordao" (
    id VARCHAR PRIMARY KEY,
    classe VARCHAR,
    relator VARCHAR,
    tema VARCHAR,
    tese VARCHAR,
    ementa VARCHAR,
    data_decisao VARCHAR,
    data_publicacao VARCHAR
);
