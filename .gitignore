__pycache__/
**/__pycache__/
*.pyc
.env
uv.lock

# Data files
data/*
data/*.duckdb
data/benchmark/

# Keep benchmark files in git
!data/benchmark/
!data/benchmark/**

# Keep gold segmenter splits in git (source of truth; Drive/IA are caches)
!data/segmenter_splits/
!data/segmenter_splits/**

# Keep segmenter samples in git (annotation pool; published DJEN texts)
!data/segmenter_samples/
!data/segmenter_samples/**

# Keep the RFC 0012 segmenter_dataset store in git (canonical CSV tables for
# documents/annotations/reviews/releases — RFC 0012 §8)
!data/segmenter/
!data/segmenter/**



# Processing artifacts

*.egg-info/

# Coverage reports
coverage.xml
.coverage
htmlcov/
docs/_build/

# Cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
site/

# Test artifacts
tests/mock_data/
djen_proxy

# Temporary files from agent execution
prs.json
files_*.json

# Gemini CLI
.gemini/
gha-creds-*.json

# Generated web files
web/public/dashboard-data.json
web/public/perf-metrics.json
web/public/tribunal_quality_scores.json
web/public/ia-snapshot.json
web/public/cache/backfill.json
web/public/cache/backfill_state.json

# Generated catalog/inventory data (canonical copy lives on Internet Archive)
catalog/completed-items.json
catalog/catalog.duckdb
catalog/catalog.sql
catalog/manifest.parquet
catalog/backfill-needed.parquet
catalog/consolidate-progress.json
catalog/consolidate-progress.jsonl
catalog/omission_stats.json
src/data/zip-inventory.txt

# Generated dashboard cache (fetched from IA at build/runtime)
web/public/cache/

# Generated query contract outputs (rendered from .qmd at build time)
web/public/data/

# DuckDB spill/temp directory (local runtime only)
.tmp/

# Generated/runtime files
.pending_alert_backfill.json
analysis_results.txt

# Runtime artifacts (generated locally, not for version control)
causaganha.db
*.png
!web/public/apple-touch-icon.png
*.log
data_*.json
ia_data.json
run_stats.json
logs/

# segmenter v7 gold-build scratch (PII OCR + intermediate labels; never commit)
data/segmenter_work/

# Claude Code subagent worktrees (transient)
.claude/worktrees/

# Cloudflare Wrangler local state and secrets
.wrangler/
.dev.vars*
deployment/relay-cf/node_modules/
deployment/relay-cf/dist/
